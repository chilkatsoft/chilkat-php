<?php

/* CkSsh.php -- Chilkat PHP extension proxy class CkSsh.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Connect to SSH servers, authenticate, run commands, open channels, and manage secure sessions.
 *
 * Chilkat.Ssh is Chilkat's primary SSH client class for applications that need secure remote command execution, shell sessions, channel-based communication, port forwarding, and detailed control over SSH transport behavior. It supports password and public-key authentication, keyboard-interactive authentication, stdout and stderr handling, proxies, host key inspection, algorithm selection, connection reuse, and detailed diagnostics for authentication, transport, channel, and disconnect issues.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html
 */
class CkSsh {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkSsh') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkSsh();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkSsh_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkSsh_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkSsh_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Set to _TRUE_ to request that the currently running Chilkat operation abort. This applies to synchronous and asynchronous methods that may block on network or lengthy processing. A synchronous call can be interrupted by setting this property from another thread. When the abort is observed, the current method returns failure, but the SSH connection and channel are not automatically closed. Bytes already buffered remain available, the remote process can continue running, and a later receive call can continue on the same channel. The same object and connection can also be used to open new channels afterward. An aborted receive does not set DisconnectCode or DisconnectReason, and object-level configuration settings remain unchanged. An aborted Connect leaves the object disconnected but reusable for a later connection attempt. When the abort is observed, Chilkat resets the property to _FALSE_. If no operation is running, it is reset when the next method begins.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkSsh_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to request that the currently running Chilkat operation abort. This applies to synchronous and asynchronous methods that may block on network or lengthy processing. A synchronous call can be interrupted by setting this property from another thread. When the abort is observed, the current method returns failure, but the SSH connection and channel are not automatically closed. Bytes already buffered remain available, the remote process can continue running, and a later receive call can continue on the same channel. The same object and connection can also be used to open new channels afterward. An aborted receive does not set DisconnectCode or DisconnectReason, and object-level configuration settings remain unchanged. An aborted Connect leaves the object disconnected but reusable for a later connection attempt. When the abort is observed, Chilkat resets the property to _FALSE_. If no operation is running, it is reset when the next method begins.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkSsh_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * Contains the result code from the most recent call to an SSH authentication method, including AuthenticatePw, AuthenticatePk, AuthenticatePwPk, and their SecureString variants. The initial value is 0, and a successful authentication resets it to 0. A failed attempt sets one of the following values. ValueMeaning 1Transport failure. The connection is unavailable, communication was lost, or a read operation failed or timed out. Calling an authentication method before Connect also produces this value. 2Invalid key for public-key authentication. For example, the SshKey is empty, public-only, malformed, unsupported, or otherwise unsuitable. 3No mutually supported authentication method was available. 4SSH authentication protocol error. An unexpected or invalid message was received. 5The password or private key was rejected by the server. 6The SSH session is already authenticated. 7The server requires a password change. See PasswordChangeRequested. This property is not cleared by unrelated operations such as SendIgnore or Disconnect. Read it immediately after an authentication method returns. If the server closes the connection because of an authentication-attempt limit, the result may be 1; inspect DisconnectCode, DisconnectReason, and LastErrorText.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthFailReason
	 * @return int
	 */
	function get_AuthFailReason() {
		return CkSsh_get_AuthFailReason($this->_cPtr);
	}

	/**
	 * Controls whether recognized caret notation is converted to ASCII control characters in strings passed to SendReqExec and ChannelSendString. The default is _FALSE_. When set to _TRUE_, recognized uppercase sequences such as ^C, ^M, ^?, and ^^ are translated before the text is sent. An unrecognized sequence, such as lowercase ^a, is sent literally. Caret  Dec  Hex  Name  Description ^@       0   00   NUL   Null ^A       1   01   SOH   Start of Heading ^B       2   02   STX   Start of Text ^C       3   03   ETX   End of Text ^D       4   04   EOT   End of Transmission ^E       5   05   ENQ   Enquiry ^F       6   06   ACK   Acknowledge ^G       7   07   BEL   Bell ^H       8   08   BS    Backspace ^I       9   09   HT    Horizontal Tab ^J      10   0A   LF    Line Feed ^K      11   0B   VT    Vertical Tab ^L      12   0C   FF    Form Feed ^M      13   0D   CR    Carriage Return ^N      14   0E   SO    Shift Out ^O      15   0F   SI    Shift In ^P      16   10   DLE   Data Link Escape ^Q      17   11   DC1   Device Control One (XON) ^R      18   12   DC2   Device Control Two ^S      19   13   DC3   Device Control Three (XOFF) ^T      20   14   DC4   Device Control Four ^U      21   15   NAK   Negative Acknowledge ^V      22   16   SYN   Synchronous Idle ^W      23   17   ETB   End of Transmission Block ^X      24   18   CAN   Cancel ^Y      25   19   EM    End of Medium ^Z      26   1A   SUB   Substitute ^[      27   1B   ESC   Escape ^\      28   1C   FS    File Separator ^]      29   1D   GS    Group Separator ^^      30   1E   RS    Record Separator ^_      31   1F   US    Unit Separator ^?     127   7F   DEL   Delete
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#CaretControl
	 * @return bool
	 */
	function get_CaretControl() {
		return CkSsh_get_CaretControl($this->_cPtr);
	}

	/**
	 * Controls whether recognized caret notation is converted to ASCII control characters in strings passed to SendReqExec and ChannelSendString. The default is _FALSE_. When set to _TRUE_, recognized uppercase sequences such as ^C, ^M, ^?, and ^^ are translated before the text is sent. An unrecognized sequence, such as lowercase ^a, is sent literally. Caret  Dec  Hex  Name  Description ^@       0   00   NUL   Null ^A       1   01   SOH   Start of Heading ^B       2   02   STX   Start of Text ^C       3   03   ETX   End of Text ^D       4   04   EOT   End of Transmission ^E       5   05   ENQ   Enquiry ^F       6   06   ACK   Acknowledge ^G       7   07   BEL   Bell ^H       8   08   BS    Backspace ^I       9   09   HT    Horizontal Tab ^J      10   0A   LF    Line Feed ^K      11   0B   VT    Vertical Tab ^L      12   0C   FF    Form Feed ^M      13   0D   CR    Carriage Return ^N      14   0E   SO    Shift Out ^O      15   0F   SI    Shift In ^P      16   10   DLE   Data Link Escape ^Q      17   11   DC1   Device Control One (XON) ^R      18   12   DC2   Device Control Two ^S      19   13   DC3   Device Control Three (XOFF) ^T      20   14   DC4   Device Control Four ^U      21   15   NAK   Negative Acknowledge ^V      22   16   SYN   Synchronous Idle ^W      23   17   ETB   End of Transmission Block ^X      24   18   CAN   Cancel ^Y      25   19   EM    End of Medium ^Z      26   1A   SUB   Substitute ^[      27   1B   ESC   Escape ^\      28   1C   FS    File Separator ^]      29   1D   GS    Group Separator ^^      30   1E   RS    Record Separator ^_      31   1F   US    Unit Separator ^?     127   7F   DEL   Delete
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#CaretControl
	 * @param bool $newVal
	 */
	function put_CaretControl($newVal) {
		CkSsh_put_CaretControl($this->_cPtr,$newVal);
	}

	/**
	 * Contains the SSH channel-open failure code when the server rejects a request to open a channel. The values are defined by RFC 4254. CodeSymbolic name 1SSH_OPEN_ADMINISTRATIVELY_PROHIBITED 2SSH_OPEN_CONNECT_FAILED 3SSH_OPEN_UNKNOWN_CHANNEL_TYPE 4SSH_OPEN_RESOURCE_SHORTAGE The initial value is 0. A local failure, such as attempting to open a channel before connecting, can also leave this property at 0 because no SSH channel-open failure message was received. Read this property immediately after a channel-open method returns -1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelOpenFailCode
	 * @return int
	 */
	function get_ChannelOpenFailCode() {
		return CkSsh_get_ChannelOpenFailCode($this->_cPtr);
	}

	/**
	 * Contains the descriptive text associated with ChannelOpenFailCode when the server rejects a channel-open request. The text is supplied by the server and may be empty or generic. The initial value is an empty string. A rejected channel-open request normally leaves the underlying SSH connection open. Local failures that occur before a request reaches the server may leave both this property and ChannelOpenFailCode unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelOpenFailReason
	 * @param CkString $str
	 */
	function get_ChannelOpenFailReason($str) {
		CkSsh_get_ChannelOpenFailReason($this->_cPtr,$str);
	}

	/**
	 * Contains the descriptive text associated with ChannelOpenFailCode when the server rejects a channel-open request. The text is supplied by the server and may be empty or generic. The initial value is an empty string. A rejected channel-open request normally leaves the underlying SSH connection open. Local failures that occur before a request reaches the server may leave both this property and ChannelOpenFailCode unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelOpenFailReason
	 * @return ?string
	 */
	function channelOpenFailReason() {
		return CkSsh_channelOpenFailReason($this->_cPtr);
	}

	/**
	 * Specifies the SSH client-identification string sent when a connection is established. The default is SSH-2.0-Chilkat_ followed by the Chilkat version, for example SSH-2.0-Chilkat_11.6.0. The value should begin with SSH-2.0-. A server may disconnect if the identification string is not valid.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIdentifier
	 * @param CkString $str
	 */
	function get_ClientIdentifier($str) {
		CkSsh_get_ClientIdentifier($this->_cPtr,$str);
	}

	/**
	 * Specifies the SSH client-identification string sent when a connection is established. The default is SSH-2.0-Chilkat_ followed by the Chilkat version, for example SSH-2.0-Chilkat_11.6.0. The value should begin with SSH-2.0-. A server may disconnect if the identification string is not valid.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIdentifier
	 * @return ?string
	 */
	function clientIdentifier() {
		return CkSsh_clientIdentifier($this->_cPtr);
	}

	/**
	 * Specifies the SSH client-identification string sent when a connection is established. The default is SSH-2.0-Chilkat_ followed by the Chilkat version, for example SSH-2.0-Chilkat_11.6.0. The value should begin with SSH-2.0-. A server may disconnect if the identification string is not valid.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIdentifier
	 * @param ?string $newVal
	 */
	function put_ClientIdentifier($newVal) {
		CkSsh_put_ClientIdentifier($this->_cPtr,$newVal);
	}

	/**
	 * This property is normally left unset. Set it only when the computer has multiple network interfaces or multiple local IP addresses and the application needs to use a specific one. The value may be a numeric IPv4 or IPv6 address. Do not specify a domain name. When this property is empty, Chilkat and the operating system automatically select the appropriate local address. The specified address must be available on the local computer and must be compatible with the address family used for the connection. If the address is invalid or unavailable, the connection fails. This setting applies to all connection paths, including direct SSH connections, HTTP and SOCKS proxy connections, ConnectThroughSsh, and multi-hop SSH connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIpAddress
	 * @param CkString $str
	 */
	function get_ClientIpAddress($str) {
		CkSsh_get_ClientIpAddress($this->_cPtr,$str);
	}

	/**
	 * This property is normally left unset. Set it only when the computer has multiple network interfaces or multiple local IP addresses and the application needs to use a specific one. The value may be a numeric IPv4 or IPv6 address. Do not specify a domain name. When this property is empty, Chilkat and the operating system automatically select the appropriate local address. The specified address must be available on the local computer and must be compatible with the address family used for the connection. If the address is invalid or unavailable, the connection fails. This setting applies to all connection paths, including direct SSH connections, HTTP and SOCKS proxy connections, ConnectThroughSsh, and multi-hop SSH connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIpAddress
	 * @return ?string
	 */
	function clientIpAddress() {
		return CkSsh_clientIpAddress($this->_cPtr);
	}

	/**
	 * This property is normally left unset. Set it only when the computer has multiple network interfaces or multiple local IP addresses and the application needs to use a specific one. The value may be a numeric IPv4 or IPv6 address. Do not specify a domain name. When this property is empty, Chilkat and the operating system automatically select the appropriate local address. The specified address must be available on the local computer and must be compatible with the address family used for the connection. If the address is invalid or unavailable, the connection fails. This setting applies to all connection paths, including direct SSH connections, HTTP and SOCKS proxy connections, ConnectThroughSsh, and multi-hop SSH connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientIpAddress
	 * @param ?string $newVal
	 */
	function put_ClientIpAddress($newVal) {
		CkSsh_put_ClientIpAddress($this->_cPtr,$newVal);
	}

	/**
	 * This property is normally left at its default value of 0. In this case, the operating system automatically chooses an unused local port from its ephemeral-port range. Applications should set a specific local port only when a remote system or network policy requires the connection to originate from that port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientPort
	 * @return int
	 */
	function get_ClientPort() {
		return CkSsh_get_ClientPort($this->_cPtr);
	}

	/**
	 * This property is normally left at its default value of 0. In this case, the operating system automatically chooses an unused local port from its ephemeral-port range. Applications should set a specific local port only when a remote system or network policy requires the connection to originate from that port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClientPort
	 * @param int $newVal
	 */
	function put_ClientPort($newVal) {
		CkSsh_put_ClientPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum number of milliseconds allowed for the remote endpoint to accept the TCP connection. The default is 30000 (30 seconds). A value of 0 removes the Chilkat-imposed connect timeout; in practice, the attempt ends when the operating system reports that the connection failed. Negative values are treated as 0. This timeout applies whether the destination is specified by a DNS hostname or an IP address, and to direct connections and connections made through HTTP proxies, SOCKS proxies, and ConnectThroughSsh. For ConnectThroughSsh, the inner Ssh object’s value is used. Scope: This property covers only TCP connection establishment. Proxy authentication, the SSH identification exchange, key exchange, and other server communication are controlled by ReadTimeoutMs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ConnectTimeoutMs
	 * @return int
	 */
	function get_ConnectTimeoutMs() {
		return CkSsh_get_ConnectTimeoutMs($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of milliseconds allowed for the remote endpoint to accept the TCP connection. The default is 30000 (30 seconds). A value of 0 removes the Chilkat-imposed connect timeout; in practice, the attempt ends when the operating system reports that the connection failed. Negative values are treated as 0. This timeout applies whether the destination is specified by a DNS hostname or an IP address, and to direct connections and connections made through HTTP proxies, SOCKS proxies, and ConnectThroughSsh. For ConnectThroughSsh, the inner Ssh object’s value is used. Scope: This property covers only TCP connection establishment. Proxy authentication, the SSH identification exchange, key exchange, and other server communication are controlled by ReadTimeoutMs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ConnectTimeoutMs
	 * @param int $newVal
	 */
	function put_ConnectTimeoutMs($newVal) {
		CkSsh_put_ConnectTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkSsh_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkSsh_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkSsh_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Contains the reason code from the most recent SSH DISCONNECT message received from the server. The values are defined by RFC 4253. The initial value is 0. It also remains 0 after a normal local Disconnect, an ordinary TCP connection loss that did not include an SSH DISCONNECT message, a failed connection attempt, authentication rejection, receive timeout, receive abort, or rejected channel-open request. When the server sends an SSH DISCONNECT, this property contains the server-provided code and remains available after CheckConnection. A subsequent call to Connect clears the value to 0, whether that new attempt succeeds or fails. CodeSymbolic name 1SSH_DISCONNECT_HOST_NOT_ALLOWED_TO_CONNECT 2SSH_DISCONNECT_PROTOCOL_ERROR 3SSH_DISCONNECT_KEY_EXCHANGE_FAILED 4SSH_DISCONNECT_RESERVED 5SSH_DISCONNECT_MAC_ERROR 6SSH_DISCONNECT_COMPRESSION_ERROR 7SSH_DISCONNECT_SERVICE_NOT_AVAILABLE 8SSH_DISCONNECT_PROTOCOL_VERSION_NOT_SUPPORTED 9SSH_DISCONNECT_HOST_KEY_NOT_VERIFIABLE 10SSH_DISCONNECT_CONNECTION_LOST 11SSH_DISCONNECT_BY_APPLICATION 12SSH_DISCONNECT_TOO_MANY_CONNECTIONS 13SSH_DISCONNECT_AUTH_CANCELLED_BY_USER 14SSH_DISCONNECT_NO_MORE_AUTH_METHODS_AVAILABLE 15SSH_DISCONNECT_ILLEGAL_USER_NAME
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DisconnectCode
	 * @return int
	 */
	function get_DisconnectCode() {
		return CkSsh_get_DisconnectCode($this->_cPtr);
	}

	/**
	 * Contains the descriptive text sent with the most recent server SSH DISCONNECT message. See DisconnectCode for the corresponding RFC 4253 reason code. The initial value is an empty string. It remains empty after a normal local Disconnect, an ordinary TCP connection loss without an SSH DISCONNECT message, a failed connection attempt, authentication rejection, receive timeout, receive abort, or rejected channel-open request. When a server-sent SSH DISCONNECT is received, the text remains available after CheckConnection. A subsequent call to Connect clears it, whether the new attempt succeeds or fails. The returned string is empty rather than null when no reason is available.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DisconnectReason
	 * @param CkString $str
	 */
	function get_DisconnectReason($str) {
		CkSsh_get_DisconnectReason($this->_cPtr,$str);
	}

	/**
	 * Contains the descriptive text sent with the most recent server SSH DISCONNECT message. See DisconnectCode for the corresponding RFC 4253 reason code. The initial value is an empty string. It remains empty after a normal local Disconnect, an ordinary TCP connection loss without an SSH DISCONNECT message, a failed connection attempt, authentication rejection, receive timeout, receive abort, or rejected channel-open request. When a server-sent SSH DISCONNECT is received, the text remains available after CheckConnection. A subsequent call to Connect clears it, whether the new attempt succeeds or fails. The returned string is empty rather than null when no reason is available.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#DisconnectReason
	 * @return ?string
	 */
	function disconnectReason() {
		return CkSsh_disconnectReason($this->_cPtr);
	}

	/**
	 * Controls whether SSH compression may be negotiated. The default is _TRUE_, meaning compression is used when the server supports it. Some older servers advertise compression but fail when it is enabled. If the server disconnects during or immediately after connection/authentication, try setting this property to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#EnableCompression
	 * @return bool
	 */
	function get_EnableCompression() {
		return CkSsh_get_EnableCompression($this->_cPtr);
	}

	/**
	 * Controls whether SSH compression may be negotiated. The default is _TRUE_, meaning compression is used when the server supports it. Some older servers advertise compression but fail when it is enabled. If the server disconnects during or immediately after connection/authentication, try setting this property to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#EnableCompression
	 * @param bool $newVal
	 */
	function put_EnableCompression($newVal) {
		CkSsh_put_EnableCompression($this->_cPtr,$newVal);
	}

	/**
	 * Enables automatic resolution of secret specification strings from secure operating-system storage. The default is _FALSE_. When _TRUE_, supported password properties and authentication methods may receive a value beginning with !! instead of a literal credential. Chilkat resolves the secret from Windows Credential Manager on Windows or Apple Keychain on macOS. Secret specification format: !![appName|]service[|domain]|usernameThis feature applies to HttpProxyPassword, SocksPassword, AuthenticatePw, and AuthenticatePwPk.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#EnableSecrets
	 * @return bool
	 */
	function get_EnableSecrets() {
		return CkSsh_get_EnableSecrets($this->_cPtr);
	}

	/**
	 * Enables automatic resolution of secret specification strings from secure operating-system storage. The default is _FALSE_. When _TRUE_, supported password properties and authentication methods may receive a value beginning with !! instead of a literal credential. Chilkat resolves the secret from Windows Credential Manager on Windows or Apple Keychain on macOS. Secret specification format: !![appName|]service[|domain]|usernameThis feature applies to HttpProxyPassword, SocksPassword, AuthenticatePw, and AuthenticatePwPk.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#EnableSecrets
	 * @param bool $newVal
	 */
	function put_EnableSecrets($newVal) {
		CkSsh_put_EnableSecrets($this->_cPtr,$newVal);
	}

	/**
	 * Restricts SSH cipher negotiation to one cipher or a comma-separated preference list. Leave this property empty to let Chilkat choose the first mutually supported cipher from its normal preference order. Supported cipher names chacha20-poly1305@openssh.com aes128-ctr, aes192-ctr, aes256-ctr aes128-cbc, aes192-cbc, aes256-cbc aes128-gcm@openssh.com, aes256-gcm@openssh.com twofish128-cbc, twofish256-cbc blowfish-cbc, 3des-cbc arcfour128, arcfour256 Cipher names must use the exact lowercase spelling shown above. If none of the specified ciphers is supported by the server, Connect fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ForceCipher
	 * @param CkString $str
	 */
	function get_ForceCipher($str) {
		CkSsh_get_ForceCipher($this->_cPtr,$str);
	}

	/**
	 * Restricts SSH cipher negotiation to one cipher or a comma-separated preference list. Leave this property empty to let Chilkat choose the first mutually supported cipher from its normal preference order. Supported cipher names chacha20-poly1305@openssh.com aes128-ctr, aes192-ctr, aes256-ctr aes128-cbc, aes192-cbc, aes256-cbc aes128-gcm@openssh.com, aes256-gcm@openssh.com twofish128-cbc, twofish256-cbc blowfish-cbc, 3des-cbc arcfour128, arcfour256 Cipher names must use the exact lowercase spelling shown above. If none of the specified ciphers is supported by the server, Connect fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ForceCipher
	 * @return ?string
	 */
	function forceCipher() {
		return CkSsh_forceCipher($this->_cPtr);
	}

	/**
	 * Restricts SSH cipher negotiation to one cipher or a comma-separated preference list. Leave this property empty to let Chilkat choose the first mutually supported cipher from its normal preference order. Supported cipher names chacha20-poly1305@openssh.com aes128-ctr, aes192-ctr, aes256-ctr aes128-cbc, aes192-cbc, aes256-cbc aes128-gcm@openssh.com, aes256-gcm@openssh.com twofish128-cbc, twofish256-cbc blowfish-cbc, 3des-cbc arcfour128, arcfour256 Cipher names must use the exact lowercase spelling shown above. If none of the specified ciphers is supported by the server, Connect fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ForceCipher
	 * @param ?string $newVal
	 */
	function put_ForceCipher($newVal) {
		CkSsh_put_ForceCipher($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck event callbacks during operations that support progress events. The callback gives the application an opportunity to abort an operation before it completes. The default is 0, which disables periodic AbortCheck callbacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkSsh_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck event callbacks during operations that support progress events. The callback gives the application an opportunity to abort an operation before it completes. The default is 0, which disables periodic AbortCheck callbacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkSsh_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the preferred host-key algorithm used during SSH connection establishment. The default is DSS; set it to RSA only when required for compatibility with a particular server. Normally this property should be left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HostKeyAlg
	 * @param CkString $str
	 */
	function get_HostKeyAlg($str) {
		CkSsh_get_HostKeyAlg($this->_cPtr,$str);
	}

	/**
	 * Specifies the preferred host-key algorithm used during SSH connection establishment. The default is DSS; set it to RSA only when required for compatibility with a particular server. Normally this property should be left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HostKeyAlg
	 * @return ?string
	 */
	function hostKeyAlg() {
		return CkSsh_hostKeyAlg($this->_cPtr);
	}

	/**
	 * Specifies the preferred host-key algorithm used during SSH connection establishment. The default is DSS; set it to RSA only when required for compatibility with a particular server. Normally this property should be left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HostKeyAlg
	 * @param ?string $newVal
	 */
	function put_HostKeyAlg($newVal) {
		CkSsh_put_HostKeyAlg($this->_cPtr,$newVal);
	}

	/**
	 * Contains the connected server’s host-key fingerprint in Chilkat’s legacy MD5 format: ssh-ed25519 256 c8:73:22:c7:82:aa:09:f1:1c:4e:99:8a:a0:62:a7:87The value consists of the SSH host-key type, key size, and the MD5 digest represented as colon-separated hexadecimal bytes. It is empty before a successful connection and after a normal Disconnect, a server-sent SSH DISCONNECT, or a failed connection attempt. After an abrupt TCP loss, a previously obtained fingerprint can temporarily remain even when IsConnected is _FALSE_. Therefore, a nonempty value is not proof that the connection is currently usable. Check IsConnected before relying on per-session state. Server identity: Compare the fingerprint with a value obtained from a trusted source before sending credentials. For new applications, use GetHostKeyFP with SHA256.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HostKeyFingerprint
	 * @param CkString $str
	 */
	function get_HostKeyFingerprint($str) {
		CkSsh_get_HostKeyFingerprint($this->_cPtr,$str);
	}

	/**
	 * Contains the connected server’s host-key fingerprint in Chilkat’s legacy MD5 format: ssh-ed25519 256 c8:73:22:c7:82:aa:09:f1:1c:4e:99:8a:a0:62:a7:87The value consists of the SSH host-key type, key size, and the MD5 digest represented as colon-separated hexadecimal bytes. It is empty before a successful connection and after a normal Disconnect, a server-sent SSH DISCONNECT, or a failed connection attempt. After an abrupt TCP loss, a previously obtained fingerprint can temporarily remain even when IsConnected is _FALSE_. Therefore, a nonempty value is not proof that the connection is currently usable. Check IsConnected before relying on per-session state. Server identity: Compare the fingerprint with a value obtained from a trusted source before sending credentials. For new applications, use GetHostKeyFP with SHA256.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HostKeyFingerprint
	 * @return ?string
	 */
	function hostKeyFingerprint() {
		return CkSsh_hostKeyFingerprint($this->_cPtr);
	}

	/**
	 * Specifies the authentication method used by an HTTP proxy. Matching is case-insensitive. ValueBehavior emptyAutomatically negotiate a supported authentication method with the proxy. BasicUse HTTP Basic authentication. NTLMUse NTLM authentication. Basic and NTLM are the supported methods. Chilkat automatically handles a 407 Proxy Authentication Required response and performs the required authentication exchange. NTLMv2 is used unless NTLMv1 is explicitly selected by setting Global.DefaultNtlmVersion to 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyAuthMethod
	 * @param CkString $str
	 */
	function get_HttpProxyAuthMethod($str) {
		CkSsh_get_HttpProxyAuthMethod($this->_cPtr,$str);
	}

	/**
	 * Specifies the authentication method used by an HTTP proxy. Matching is case-insensitive. ValueBehavior emptyAutomatically negotiate a supported authentication method with the proxy. BasicUse HTTP Basic authentication. NTLMUse NTLM authentication. Basic and NTLM are the supported methods. Chilkat automatically handles a 407 Proxy Authentication Required response and performs the required authentication exchange. NTLMv2 is used unless NTLMv1 is explicitly selected by setting Global.DefaultNtlmVersion to 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyAuthMethod
	 * @return ?string
	 */
	function httpProxyAuthMethod() {
		return CkSsh_httpProxyAuthMethod($this->_cPtr);
	}

	/**
	 * Specifies the authentication method used by an HTTP proxy. Matching is case-insensitive. ValueBehavior emptyAutomatically negotiate a supported authentication method with the proxy. BasicUse HTTP Basic authentication. NTLMUse NTLM authentication. Basic and NTLM are the supported methods. Chilkat automatically handles a 407 Proxy Authentication Required response and performs the required authentication exchange. NTLMv2 is used unless NTLMv1 is explicitly selected by setting Global.DefaultNtlmVersion to 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyAuthMethod
	 * @param ?string $newVal
	 */
	function put_HttpProxyAuthMethod($newVal) {
		CkSsh_put_HttpProxyAuthMethod($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the optional Windows domain used for NTLM authentication with an HTTP proxy. The value is the legacy Windows NetBIOS domain name. It is not a DNS domain name and is not the workstation name. This property is ignored unless NTLM proxy authentication is used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyDomain
	 * @param CkString $str
	 */
	function get_HttpProxyDomain($str) {
		CkSsh_get_HttpProxyDomain($this->_cPtr,$str);
	}

	/**
	 * Specifies the optional Windows domain used for NTLM authentication with an HTTP proxy. The value is the legacy Windows NetBIOS domain name. It is not a DNS domain name and is not the workstation name. This property is ignored unless NTLM proxy authentication is used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyDomain
	 * @return ?string
	 */
	function httpProxyDomain() {
		return CkSsh_httpProxyDomain($this->_cPtr);
	}

	/**
	 * Specifies the optional Windows domain used for NTLM authentication with an HTTP proxy. The value is the legacy Windows NetBIOS domain name. It is not a DNS domain name and is not the workstation name. This property is ignored unless NTLM proxy authentication is used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyDomain
	 * @param ?string $newVal
	 */
	function put_HttpProxyDomain($newVal) {
		CkSsh_put_HttpProxyDomain($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy. Chilkat uses the HTTP CONNECT method to establish the SSH tunnel through the proxy. The TCP connection to the proxy is not itself protected by TLS for this SSH use case. The HTTP proxy is used only when SocksVersion is 0, this property is nonempty, and HttpProxyPort is nonzero. A configured SOCKS proxy takes precedence. These settings apply to both Connect and ConnectThroughSsh.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyHostname
	 * @param CkString $str
	 */
	function get_HttpProxyHostname($str) {
		CkSsh_get_HttpProxyHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy. Chilkat uses the HTTP CONNECT method to establish the SSH tunnel through the proxy. The TCP connection to the proxy is not itself protected by TLS for this SSH use case. The HTTP proxy is used only when SocksVersion is 0, this property is nonempty, and HttpProxyPort is nonzero. A configured SOCKS proxy takes precedence. These settings apply to both Connect and ConnectThroughSsh.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyHostname
	 * @return ?string
	 */
	function httpProxyHostname() {
		return CkSsh_httpProxyHostname($this->_cPtr);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy. Chilkat uses the HTTP CONNECT method to establish the SSH tunnel through the proxy. The TCP connection to the proxy is not itself protected by TLS for this SSH use case. The HTTP proxy is used only when SocksVersion is 0, this property is nonempty, and HttpProxyPort is nonzero. A configured SOCKS proxy takes precedence. These settings apply to both Connect and ConnectThroughSsh.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyHostname
	 * @param ?string $newVal
	 */
	function put_HttpProxyHostname($newVal) {
		CkSsh_put_HttpProxyHostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyPassword
	 * @param CkString $str
	 */
	function get_HttpProxyPassword($str) {
		CkSsh_get_HttpProxyPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyPassword
	 * @return ?string
	 */
	function httpProxyPassword() {
		return CkSsh_httpProxyPassword($this->_cPtr);
	}

	/**
	 * Specifies the password used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyPassword
	 * @param ?string $newVal
	 */
	function put_HttpProxyPassword($newVal) {
		CkSsh_put_HttpProxyPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the HTTP proxy port. An HTTP proxy is used only when SocksVersion is 0, HttpProxyHostname is nonempty, and this property is nonzero. A configured SOCKS proxy takes precedence.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyPort
	 * @return int
	 */
	function get_HttpProxyPort() {
		return CkSsh_get_HttpProxyPort($this->_cPtr);
	}

	/**
	 * Specifies the HTTP proxy port. An HTTP proxy is used only when SocksVersion is 0, HttpProxyHostname is nonempty, and this property is nonzero. A configured SOCKS proxy takes precedence.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyPort
	 * @param int $newVal
	 */
	function put_HttpProxyPort($newVal) {
		CkSsh_put_HttpProxyPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the username used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyUsername
	 * @param CkString $str
	 */
	function get_HttpProxyUsername($str) {
		CkSsh_get_HttpProxyUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the username used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyUsername
	 * @return ?string
	 */
	function httpProxyUsername() {
		return CkSsh_httpProxyUsername($this->_cPtr);
	}

	/**
	 * Specifies the username used to authenticate with an HTTP proxy. If the proxy requires authentication and this property is empty, the connection fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#HttpProxyUsername
	 * @param ?string $newVal
	 */
	function put_HttpProxyUsername($newVal) {
		CkSsh_put_HttpProxyUsername($this->_cPtr,$newVal);
	}

	/**
	 * Specifies an inactivity interval, in milliseconds, for SSH receive waits that use this property. The default is 0, meaning no inactivity limit. This is not simply an application-output timeout. Receiving any SSH packet, including transport or channel-control traffic that contains no stdout or stderr bytes, counts as communication and restarts the interval. A remote command can therefore remain silent longer than this value without necessarily causing a timeout. Timeout handling is method-specific. Methods having an explicit timeout argument, such as ChannelPoll and WaitForChannelMessage, use that argument for the corresponding wait. In v11.6.0 testing, ChannelReadAndPoll used IdleTimeoutMs for its initial read when ReadTimeoutMs was 0. This property governs receiving. It is not a send-progress timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#IdleTimeoutMs
	 * @return int
	 */
	function get_IdleTimeoutMs() {
		return CkSsh_get_IdleTimeoutMs($this->_cPtr);
	}

	/**
	 * Specifies an inactivity interval, in milliseconds, for SSH receive waits that use this property. The default is 0, meaning no inactivity limit. This is not simply an application-output timeout. Receiving any SSH packet, including transport or channel-control traffic that contains no stdout or stderr bytes, counts as communication and restarts the interval. A remote command can therefore remain silent longer than this value without necessarily causing a timeout. Timeout handling is method-specific. Methods having an explicit timeout argument, such as ChannelPoll and WaitForChannelMessage, use that argument for the corresponding wait. In v11.6.0 testing, ChannelReadAndPoll used IdleTimeoutMs for its initial read when ReadTimeoutMs was 0. This property governs receiving. It is not a send-progress timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#IdleTimeoutMs
	 * @param int $newVal
	 */
	function put_IdleTimeoutMs($newVal) {
		CkSsh_put_IdleTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Returns _TRUE_ when Chilkat currently considers the SSH transport connected. It becomes _TRUE_ after Connect establishes the secure transport. It is _FALSE_ before connecting, after Disconnect, after a failed connection attempt, after a server-sent SSH DISCONNECT, or when socket closure is detected. An authentication failure, channel-open rejection, receive timeout, or aborted receive does not by itself set this property to _FALSE_. If the underlying transport remains usable, the application can retry the operation on the same connection. Reading this property can reflect a remote close that the operating system has already reported. In testing, it became _FALSE_ before CheckConnection was called. However, _TRUE_ does not prove that the remote server is responsive or that a silent network failure has not occurred. To verify that SSH traffic can still be exchanged, call SendIgnore and check its return value. After abrupt transport loss or a server protocol disconnect, retained per-channel state can be stale even though this property is _FALSE_. Treat IsConnected = _FALSE_ as authoritative for network I/O. Bytes received before the loss can remain available in a retained channel buffer.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#IsConnected
	 * @return bool
	 */
	function get_IsConnected() {
		return CkSsh_get_IsConnected($this->_cPtr);
	}

	/**
	 * Controls whether SSH protocol traffic is accumulated in SessionLog. The default is _FALSE_. This object-level setting persists across disconnects, failed operations, and subsequent connections until changed by the application. The log for a completed session remains available after disconnect or transport loss. A later successful connection begins a new session log. Session logging is intended for diagnostics. The log grows as a session proceeds, so disable it when no longer needed. Chilkat does not write literal authentication passwords or private-key passphrases to the session log, but diagnostic logs should still be handled as potentially sensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#KeepSessionLog
	 * @return bool
	 */
	function get_KeepSessionLog() {
		return CkSsh_get_KeepSessionLog($this->_cPtr);
	}

	/**
	 * Controls whether SSH protocol traffic is accumulated in SessionLog. The default is _FALSE_. This object-level setting persists across disconnects, failed operations, and subsequent connections until changed by the application. The log for a completed session remains available after disconnect or transport loss. A later successful connection begins a new session log. Session logging is intended for diagnostics. The log grows as a session proceeds, so disable it when no longer needed. Chilkat does not write literal authentication passwords or private-key passphrases to the session log, but diagnostic logs should still be handled as potentially sensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#KeepSessionLog
	 * @param bool $newVal
	 */
	function put_KeepSessionLog($newVal) {
		CkSsh_put_KeepSessionLog($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkSsh_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkSsh_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkSsh_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkSsh_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkSsh_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkSsh_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkSsh_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkSsh_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum SSH channel packet size advertised in the SSH_MSG_CHANNEL_OPEN message. The default is 8192. For high-volume transfers, a value such as 32768 may improve performance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#MaxPacketSize
	 * @return int
	 */
	function get_MaxPacketSize() {
		return CkSsh_get_MaxPacketSize($this->_cPtr);
	}

	/**
	 * Specifies the maximum SSH channel packet size advertised in the SSH_MSG_CHANNEL_OPEN message. The default is 8192. For high-volume transfers, a value such as 32768 may improve performance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#MaxPacketSize
	 * @param int $newVal
	 */
	function put_MaxPacketSize($newVal) {
		CkSsh_put_MaxPacketSize($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of channel records in the active SSH channel collection. A channel is normally added after its open request is accepted and removed when final remote-close processing occurs. After Disconnect or transport loss, this property returns 0 because former channels are no longer active or enumerable. Buffered data for a former channel can nevertheless remain retrievable by its channel number until it is released or a new connection clears it. A channel closed locally with ChannelSendClose can have ChannelIsOpen return _FALSE_ while it is still counted until final cleanup. Conversely, a remotely closed channel can cease to be counted while its results are only temporarily retained. Result-lifetime warning: Querying this property is an SSH operation that can process pending final cleanup. After ChannelReceiveToClose or another completion receive returns, retrieve stdout, stderr, and exit status before reading NumOpenChannels; otherwise the completed channel record can be discarded. Use GetChannelNumber and GetChannelType with zero-based indexes from 0 through NumOpenChannels - 1. Do not assume every enumerated channel is still open for application I/O.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#NumOpenChannels
	 * @return int
	 */
	function get_NumOpenChannels() {
		return CkSsh_get_NumOpenChannels($this->_cPtr);
	}

	/**
	 * Indicates that the server rejected password authentication because it requires the user to change the password. This property is set by AuthenticatePw and AuthenticatePwPk. When _TRUE_, call the authentication method again and pass both passwords in this form: |oldPassword|newPassword|
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#PasswordChangeRequested
	 * @return bool
	 */
	function get_PasswordChangeRequested() {
		return CkSsh_get_PasswordChangeRequested($this->_cPtr);
	}

	/**
	 * Controls which address family is selected when DNS resolution for a hostname returns both an IPv4 address and an IPv6 address.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#PreferIpv6
	 * @return bool
	 */
	function get_PreferIpv6() {
		return CkSsh_get_PreferIpv6($this->_cPtr);
	}

	/**
	 * Controls which address family is selected when DNS resolution for a hostname returns both an IPv4 address and an IPv6 address.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#PreferIpv6
	 * @param bool $newVal
	 */
	function put_PreferIpv6($newVal) {
		CkSsh_put_PreferIpv6($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a receive timeout, in milliseconds, for SSH methods that use this property. The default is 0, meaning no ReadTimeoutMs limit. The exact scope is method-specific: MethodObserved timeout scope ChannelReadLimits the wait for channel input. ChannelReadAndPollLimits the initial wait when nonzero. After the first data arrives, pollTimeoutMs controls the additional quiet-period wait. ChannelReceiveToCloseLimits the receive operation while waiting for channel closure. ChannelReceiveUntilMatch and ChannelReceiveUntilMatchNLimits the wait for a matching pattern. ChannelPoll and WaitForChannelMessageUse their explicit pollTimeoutMs arguments instead. A receive timeout does not discard bytes already received. The connection and channel can remain usable, and a later receive call can continue. Always inspect and preserve buffered stdout and stderr after a timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReadTimeoutMs
	 * @return int
	 */
	function get_ReadTimeoutMs() {
		return CkSsh_get_ReadTimeoutMs($this->_cPtr);
	}

	/**
	 * Specifies a receive timeout, in milliseconds, for SSH methods that use this property. The default is 0, meaning no ReadTimeoutMs limit. The exact scope is method-specific: MethodObserved timeout scope ChannelReadLimits the wait for channel input. ChannelReadAndPollLimits the initial wait when nonzero. After the first data arrives, pollTimeoutMs controls the additional quiet-period wait. ChannelReceiveToCloseLimits the receive operation while waiting for channel closure. ChannelReceiveUntilMatch and ChannelReceiveUntilMatchNLimits the wait for a matching pattern. ChannelPoll and WaitForChannelMessageUse their explicit pollTimeoutMs arguments instead. A receive timeout does not discard bytes already received. The connection and channel can remain usable, and a later receive call can continue. Always inspect and preserve buffered stdout and stderr after a timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReadTimeoutMs
	 * @param int $newVal
	 */
	function put_ReadTimeoutMs($newVal) {
		CkSsh_put_ReadTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the character encoding used for command text sent by SendReqExec, QuickCommand, and QuickCmdSend. The default is ANSI. On Windows, ANSI uses the Windows ANSI code page returned by GetACP, not the OEM code page. Its exact meaning on non-Windows platforms is environment-dependent. For portable behavior, specify an explicit charset such as utf-8 or windows-1252. This property applies to the command string itself. It does not override the explicit charset argument passed to ChannelSendString. Use a supported charset name. In v11.6.0, an unrecognized charset name was observed to fall back to UTF-8 rather than fail; applications should not rely on that fallback.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReqExecCharset
	 * @param CkString $str
	 */
	function get_ReqExecCharset($str) {
		CkSsh_get_ReqExecCharset($this->_cPtr,$str);
	}

	/**
	 * Specifies the character encoding used for command text sent by SendReqExec, QuickCommand, and QuickCmdSend. The default is ANSI. On Windows, ANSI uses the Windows ANSI code page returned by GetACP, not the OEM code page. Its exact meaning on non-Windows platforms is environment-dependent. For portable behavior, specify an explicit charset such as utf-8 or windows-1252. This property applies to the command string itself. It does not override the explicit charset argument passed to ChannelSendString. Use a supported charset name. In v11.6.0, an unrecognized charset name was observed to fall back to UTF-8 rather than fail; applications should not rely on that fallback.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReqExecCharset
	 * @return ?string
	 */
	function reqExecCharset() {
		return CkSsh_reqExecCharset($this->_cPtr);
	}

	/**
	 * Specifies the character encoding used for command text sent by SendReqExec, QuickCommand, and QuickCmdSend. The default is ANSI. On Windows, ANSI uses the Windows ANSI code page returned by GetACP, not the OEM code page. Its exact meaning on non-Windows platforms is environment-dependent. For portable behavior, specify an explicit charset such as utf-8 or windows-1252. This property applies to the command string itself. It does not override the explicit charset argument passed to ChannelSendString. Use a supported charset name. In v11.6.0, an unrecognized charset name was observed to fall back to UTF-8 rather than fail; applications should not rely on that fallback.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReqExecCharset
	 * @param ?string $newVal
	 */
	function put_ReqExecCharset($newVal) {
		CkSsh_put_ReqExecCharset($this->_cPtr,$newVal);
	}

	/**
	 * Contains the SSH server-identification string received during connection establishment. For example: SSH-2.0-OpenSSH_8.9p1 Ubuntu-3ubuntu0.16The initial value is an empty string. After a successful connection, the value remains available after a normal Disconnect, a server-sent SSH DISCONNECT, or an abrupt transport loss. A subsequent failed connection attempt clears this property rather than retaining the identifier from an earlier session. A successful reconnect replaces it with the newly received identifier. The empty result is an empty string, not null.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ServerIdentifier
	 * @param CkString $str
	 */
	function get_ServerIdentifier($str) {
		CkSsh_get_ServerIdentifier($this->_cPtr,$str);
	}

	/**
	 * Contains the SSH server-identification string received during connection establishment. For example: SSH-2.0-OpenSSH_8.9p1 Ubuntu-3ubuntu0.16The initial value is an empty string. After a successful connection, the value remains available after a normal Disconnect, a server-sent SSH DISCONNECT, or an abrupt transport loss. A subsequent failed connection attempt clears this property rather than retaining the identifier from an earlier session. A successful reconnect replaces it with the newly received identifier. The empty result is an empty string, not null.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ServerIdentifier
	 * @return ?string
	 */
	function serverIdentifier() {
		return CkSsh_serverIdentifier($this->_cPtr);
	}

	/**
	 * Contains the in-memory SSH protocol log for the current or most recently completed successful session. Enable logging by setting KeepSessionLog to _TRUE_. The initial value is an empty string. A normal disconnect, server disconnect, or transport loss leaves the existing session log available. A later successful Connect starts a new session log rather than appending indefinitely to the previous session. A failed TCP connection attempt does not necessarily replace an existing session log; inspect LastErrorText for the failed call. Authentication passwords and private-key passphrases are not included in the log. The log may contain server identifiers, negotiated algorithms, authentication method names, and other connection diagnostics. This property contains log text; it is not a file path. Treat diagnostic output as potentially sensitive and avoid retaining it longer than necessary.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SessionLog
	 * @param CkString $str
	 */
	function get_SessionLog($str) {
		CkSsh_get_SessionLog($this->_cPtr,$str);
	}

	/**
	 * Contains the in-memory SSH protocol log for the current or most recently completed successful session. Enable logging by setting KeepSessionLog to _TRUE_. The initial value is an empty string. A normal disconnect, server disconnect, or transport loss leaves the existing session log available. A later successful Connect starts a new session log rather than appending indefinitely to the previous session. A failed TCP connection attempt does not necessarily replace an existing session log; inspect LastErrorText for the failed call. Authentication passwords and private-key passphrases are not included in the log. The log may contain server identifiers, negotiated algorithms, authentication method names, and other connection diagnostics. This property contains log text; it is not a file path. Treat diagnostic output as potentially sensitive and avoid retaining it longer than necessary.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SessionLog
	 * @return ?string
	 */
	function sessionLog() {
		return CkSsh_sessionLog($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS proxy hostname or IPv4 address. This property is used only when SocksVersion is 4 or 5. An IPv6 address cannot be used for the SOCKS proxy. SOCKS4a is not supported. With SOCKS5, the destination hostname is sent to the proxy and resolved remotely by the proxy. IPv6 destination addresses are not supported through SOCKS5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksHostname
	 * @param CkString $str
	 */
	function get_SocksHostname($str) {
		CkSsh_get_SocksHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the SOCKS proxy hostname or IPv4 address. This property is used only when SocksVersion is 4 or 5. An IPv6 address cannot be used for the SOCKS proxy. SOCKS4a is not supported. With SOCKS5, the destination hostname is sent to the proxy and resolved remotely by the proxy. IPv6 destination addresses are not supported through SOCKS5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksHostname
	 * @return ?string
	 */
	function socksHostname() {
		return CkSsh_socksHostname($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS proxy hostname or IPv4 address. This property is used only when SocksVersion is 4 or 5. An IPv6 address cannot be used for the SOCKS proxy. SOCKS4a is not supported. With SOCKS5, the destination hostname is sent to the proxy and resolved remotely by the proxy. IPv6 destination addresses are not supported through SOCKS5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksHostname
	 * @param ?string $newVal
	 */
	function put_SocksHostname($newVal) {
		CkSsh_put_SocksHostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the SOCKS5 password when username/password authentication is required. SOCKS4 does not define password authentication, so this property is ignored for SOCKS4. SOCKS5 supports either no authentication or username/password authentication. Leave both SocksUsername and this property empty to request no authentication. The connection fails if the proxy requires an unsupported authentication method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksPassword
	 * @param CkString $str
	 */
	function get_SocksPassword($str) {
		CkSsh_get_SocksPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the SOCKS5 password when username/password authentication is required. SOCKS4 does not define password authentication, so this property is ignored for SOCKS4. SOCKS5 supports either no authentication or username/password authentication. Leave both SocksUsername and this property empty to request no authentication. The connection fails if the proxy requires an unsupported authentication method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksPassword
	 * @return ?string
	 */
	function socksPassword() {
		return CkSsh_socksPassword($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS5 password when username/password authentication is required. SOCKS4 does not define password authentication, so this property is ignored for SOCKS4. SOCKS5 supports either no authentication or username/password authentication. Leave both SocksUsername and this property empty to request no authentication. The connection fails if the proxy requires an unsupported authentication method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksPassword
	 * @param ?string $newVal
	 */
	function put_SocksPassword($newVal) {
		CkSsh_put_SocksPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the SOCKS proxy port. The default is 1080. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksPort
	 * @return int
	 */
	function get_SocksPort() {
		return CkSsh_get_SocksPort($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS proxy port. The default is 1080. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksPort
	 * @param int $newVal
	 */
	function put_SocksPort($newVal) {
		CkSsh_put_SocksPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the SOCKS username. For SOCKS4, it is sent as the SOCKS user ID. For SOCKS5, it is used with SocksPassword for username/password authentication. For SOCKS5, leave both properties empty to request no authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksUsername
	 * @param CkString $str
	 */
	function get_SocksUsername($str) {
		CkSsh_get_SocksUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the SOCKS username. For SOCKS4, it is sent as the SOCKS user ID. For SOCKS5, it is used with SocksPassword for username/password authentication. For SOCKS5, leave both properties empty to request no authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksUsername
	 * @return ?string
	 */
	function socksUsername() {
		return CkSsh_socksUsername($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS username. For SOCKS4, it is sent as the SOCKS user ID. For SOCKS5, it is used with SocksPassword for username/password authentication. For SOCKS5, leave both properties empty to request no authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksUsername
	 * @param ?string $newVal
	 */
	function put_SocksUsername($newVal) {
		CkSsh_put_SocksUsername($this->_cPtr,$newVal);
	}

	/**
	 * Selects whether and how a SOCKS proxy is used. ValueBehavior 0Do not use a SOCKS proxy. This is the default. Chilkat may still use an HTTP proxy if both HttpProxyHostname and HttpProxyPort are set. 4Connect through a SOCKS4 proxy. SOCKS4a is not supported. 5Connect through a SOCKS5 proxy. Proxy selection: A SOCKS proxy takes precedence over an HTTP proxy. When this property is 4 or 5, Chilkat uses the configured SOCKS proxy. When it is 0, Chilkat uses an HTTP proxy only if HttpProxyHostname is nonempty and HttpProxyPort is nonzero. Otherwise, Chilkat connects directly to the SSH server. Assigning a value other than 4 or 5 sets the property to 0. These proxy settings apply to both Connect and ConnectThroughSsh.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksVersion
	 * @return int
	 */
	function get_SocksVersion() {
		return CkSsh_get_SocksVersion($this->_cPtr);
	}

	/**
	 * Selects whether and how a SOCKS proxy is used. ValueBehavior 0Do not use a SOCKS proxy. This is the default. Chilkat may still use an HTTP proxy if both HttpProxyHostname and HttpProxyPort are set. 4Connect through a SOCKS4 proxy. SOCKS4a is not supported. 5Connect through a SOCKS5 proxy. Proxy selection: A SOCKS proxy takes precedence over an HTTP proxy. When this property is 4 or 5, Chilkat uses the configured SOCKS proxy. When it is 0, Chilkat uses an HTTP proxy only if HttpProxyHostname is nonempty and HttpProxyPort is nonzero. Otherwise, Chilkat connects directly to the SSH server. Assigning a value other than 4 or 5 sets the property to 0. These proxy settings apply to both Connect and ConnectThroughSsh.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SocksVersion
	 * @param int $newVal
	 */
	function put_SocksVersion($newVal) {
		CkSsh_put_SocksVersion($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the socket receive-buffer size. The default is 4194304 bytes. Normally this property should remain unchanged. When download throughput is unexpectedly low, testing a larger value may help. Values should generally be multiples of 4096. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SoRcvBuf
	 * @return int
	 */
	function get_SoRcvBuf() {
		return CkSsh_get_SoRcvBuf($this->_cPtr);
	}

	/**
	 * Specifies the socket receive-buffer size. The default is 4194304 bytes. Normally this property should remain unchanged. When download throughput is unexpectedly low, testing a larger value may help. Values should generally be multiples of 4096. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SoRcvBuf
	 * @param int $newVal
	 */
	function put_SoRcvBuf($newVal) {
		CkSsh_put_SoRcvBuf($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the socket send-buffer size. The default is 262144 bytes. Normally this property should remain unchanged. When upload throughput is unexpectedly low, testing values such as 524288 or 1048576 may help. Values should generally be multiples of 4096. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SoSndBuf
	 * @return int
	 */
	function get_SoSndBuf() {
		return CkSsh_get_SoSndBuf($this->_cPtr);
	}

	/**
	 * Specifies the socket send-buffer size. The default is 262144 bytes. Normally this property should remain unchanged. When upload throughput is unexpectedly low, testing values such as 524288 or 1048576 may help. Values should generally be multiples of 4096. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SoSndBuf
	 * @param int $newVal
	 */
	function put_SoSndBuf($newVal) {
		CkSsh_put_SoSndBuf($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether SSH extended-data received as stderr is merged into the normal channel receive buffer. The default is _TRUE_. When _TRUE_, retrieve combined stdout and stderr with GetReceivedData, GetReceivedDataN, GetReceivedText, or GetReceivedTextS. When _FALSE_, retrieve SSH stderr separately with GetReceivedStderr or GetReceivedStderrText. The setting is applied when each incoming stderr packet is processed. Changing it while a channel is active affects only stderr received afterward. Already separated stderr is not moved into stdout, and already merged stderr cannot later be separated. Read-method return values count bytes in both the normal and separate stderr buffers. GetReceivedNumBytes counts only the normal receive buffer. This setting also affects QuickCommand. When _FALSE_, its returned string contains stdout but does not include separately received stderr. PTY channels: When a pseudo-terminal is allocated, the remote terminal normally combines the process's stdout and stderr into a single PTY byte stream before SSH transports it. In that case both appear in the normal receive buffer and the stderr buffer is empty regardless of this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StderrToStdout
	 * @return bool
	 */
	function get_StderrToStdout() {
		return CkSsh_get_StderrToStdout($this->_cPtr);
	}

	/**
	 * Controls whether SSH extended-data received as stderr is merged into the normal channel receive buffer. The default is _TRUE_. When _TRUE_, retrieve combined stdout and stderr with GetReceivedData, GetReceivedDataN, GetReceivedText, or GetReceivedTextS. When _FALSE_, retrieve SSH stderr separately with GetReceivedStderr or GetReceivedStderrText. The setting is applied when each incoming stderr packet is processed. Changing it while a channel is active affects only stderr received afterward. Already separated stderr is not moved into stdout, and already merged stderr cannot later be separated. Read-method return values count bytes in both the normal and separate stderr buffers. GetReceivedNumBytes counts only the normal receive buffer. This setting also affects QuickCommand. When _FALSE_, its returned string contains stdout but does not include separately received stderr. PTY channels: When a pseudo-terminal is allocated, the remote terminal normally combines the process's stdout and stderr into a single PTY byte stream before SSH transports it. In that case both appear in the normal receive buffer and the stderr buffer is empty regardless of this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StderrToStdout
	 * @param bool $newVal
	 */
	function put_StderrToStdout($newVal) {
		CkSsh_put_StderrToStdout($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether ANSI SGR color and text-style sequences are removed from normal received text returned by GetReceivedText and GetReceivedTextS. The default is _TRUE_. It removes ESC-prefixed CSI sequences ending in m, including empty, semicolon-separated, and colon-separated parameter forms such as ESC[m, ESC[1;31m, ESC[38;5;196m, and ESC[38:5:196m. It does not remove non-SGR CSI sequences such as ESC[2J, OSC sequences, 8-bit C1 CSI sequences beginning with byte 0x9B, or an incomplete trailing SGR sequence. For GetReceivedTextS, stripping applies to both the literal substring search and the returned value; the corresponding original raw escape-sequence bytes are consumed. It does not affect raw-byte getters, PeekReceivedText, GetReceivedStderrText, pattern matching in ChannelReceiveUntilMatch, or the string returned by QuickCommand. An SGR sequence split across SSH packets can still be removed if all of its bytes remain buffered until the text is retrieved.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StripColorCodes
	 * @return bool
	 */
	function get_StripColorCodes() {
		return CkSsh_get_StripColorCodes($this->_cPtr);
	}

	/**
	 * Controls whether ANSI SGR color and text-style sequences are removed from normal received text returned by GetReceivedText and GetReceivedTextS. The default is _TRUE_. It removes ESC-prefixed CSI sequences ending in m, including empty, semicolon-separated, and colon-separated parameter forms such as ESC[m, ESC[1;31m, ESC[38;5;196m, and ESC[38:5:196m. It does not remove non-SGR CSI sequences such as ESC[2J, OSC sequences, 8-bit C1 CSI sequences beginning with byte 0x9B, or an incomplete trailing SGR sequence. For GetReceivedTextS, stripping applies to both the literal substring search and the returned value; the corresponding original raw escape-sequence bytes are consumed. It does not affect raw-byte getters, PeekReceivedText, GetReceivedStderrText, pattern matching in ChannelReceiveUntilMatch, or the string returned by QuickCommand. An SGR sequence split across SSH packets can still be removed if all of its bytes remain buffered until the text is retrieved.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StripColorCodes
	 * @param bool $newVal
	 */
	function put_StripColorCodes($newVal) {
		CkSsh_put_StripColorCodes($this->_cPtr,$newVal);
	}

	/**
	 * Controls the TCP_NODELAY setting for the underlying TCP connection. The default is _TRUE_, which disables the Nagle algorithm and generally improves responsiveness when many small messages are exchanged. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#TcpNoDelay
	 * @return bool
	 */
	function get_TcpNoDelay() {
		return CkSsh_get_TcpNoDelay($this->_cPtr);
	}

	/**
	 * Controls the TCP_NODELAY setting for the underlying TCP connection. The default is _TRUE_, which disables the Nagle algorithm and generally improves responsiveness when many small messages are exchanged. When a proxy is used, this setting applies to the TCP connection made to the proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#TcpNoDelay
	 * @param bool $newVal
	 */
	function put_TcpNoDelay($newVal) {
		CkSsh_put_TcpNoDelay($this->_cPtr,$newVal);
	}

	/**
	 * Provides comma-separated compatibility and security options for uncommon SSH scenarios. The default is an empty string, which is appropriate for most applications. KeywordEffect ForceUserAuthRsaSha1Forces ssh-rsa/SHA-1 for RSA user authentication. Set before connecting when a server advertises RSA-SHA2 but still requires the legacy SHA-1 user-auth signature. no-weak-mac-algsRemoves hmac-sha1-96, hmac-sha1, hmac-md5, and hmac-ripemd160 from the offered MAC algorithms. ProtectFromVpnOn Android, bypasses an installed or active VPN. +ssh-hmac-etmRe-enables Encrypt-then-MAC algorithms that are disabled by default as a Terrapin mitigation. +chacha20-poly1305@openssh.comRe-enables chacha20-poly1305@openssh.com, which is disabled by default as a Terrapin mitigation. Set connection-negotiation options before calling Connect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkSsh_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides comma-separated compatibility and security options for uncommon SSH scenarios. The default is an empty string, which is appropriate for most applications. KeywordEffect ForceUserAuthRsaSha1Forces ssh-rsa/SHA-1 for RSA user authentication. Set before connecting when a server advertises RSA-SHA2 but still requires the legacy SHA-1 user-auth signature. no-weak-mac-algsRemoves hmac-sha1-96, hmac-sha1, hmac-md5, and hmac-ripemd160 from the offered MAC algorithms. ProtectFromVpnOn Android, bypasses an installed or active VPN. +ssh-hmac-etmRe-enables Encrypt-then-MAC algorithms that are disabled by default as a Terrapin mitigation. +chacha20-poly1305@openssh.comRe-enables chacha20-poly1305@openssh.com, which is disabled by default as a Terrapin mitigation. Set connection-negotiation options before calling Connect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkSsh_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides comma-separated compatibility and security options for uncommon SSH scenarios. The default is an empty string, which is appropriate for most applications. KeywordEffect ForceUserAuthRsaSha1Forces ssh-rsa/SHA-1 for RSA user authentication. Set before connecting when a server advertises RSA-SHA2 but still requires the legacy SHA-1 user-auth signature. no-weak-mac-algsRemoves hmac-sha1-96, hmac-sha1, hmac-md5, and hmac-ripemd160 from the offered MAC algorithms. ProtectFromVpnOn Android, bypasses an installed or active VPN. +ssh-hmac-etmRe-enables Encrypt-then-MAC algorithms that are disabled by default as a Terrapin mitigation. +chacha20-poly1305@openssh.comRe-enables chacha20-poly1305@openssh.com, which is disabled by default as a Terrapin mitigation. Set connection-negotiation options before calling Connect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkSsh_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * Contains a user-authentication banner received from the server during authentication. Check this property after StartKeyboardAuth or another authentication attempt and display it to the user when appropriate. This is per-authentication runtime state, not a persistent connection setting. A value assigned by the application can remain before authentication begins, including across a failed connection attempt, but an authentication attempt clears or replaces the value. In testing, both successful and rejected password authentication cleared a previously assigned value when the server sent no banner. When no banner is available, the property returns an empty string rather than null.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UserAuthBanner
	 * @param CkString $str
	 */
	function get_UserAuthBanner($str) {
		CkSsh_get_UserAuthBanner($this->_cPtr,$str);
	}

	/**
	 * Contains a user-authentication banner received from the server during authentication. Check this property after StartKeyboardAuth or another authentication attempt and display it to the user when appropriate. This is per-authentication runtime state, not a persistent connection setting. A value assigned by the application can remain before authentication begins, including across a failed connection attempt, but an authentication attempt clears or replaces the value. In testing, both successful and rejected password authentication cleared a previously assigned value when the server sent no banner. When no banner is available, the property returns an empty string rather than null.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UserAuthBanner
	 * @return ?string
	 */
	function userAuthBanner() {
		return CkSsh_userAuthBanner($this->_cPtr);
	}

	/**
	 * Contains a user-authentication banner received from the server during authentication. Check this property after StartKeyboardAuth or another authentication attempt and display it to the user when appropriate. This is per-authentication runtime state, not a persistent connection setting. A value assigned by the application can remain before authentication begins, including across a failed connection attempt, but an authentication attempt clears or replaces the value. In testing, both successful and rejected password authentication cleared a previously assigned value when the server sent no banner. When no banner is available, the property returns an empty string rather than null.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#UserAuthBanner
	 * @param ?string $newVal
	 */
	function put_UserAuthBanner($newVal) {
		CkSsh_put_UserAuthBanner($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkSsh_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkSsh_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkSsh_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkSsh_version($this->_cPtr);
	}

	/**
	 * Authenticates an active, unauthenticated SSH connection using public-key authentication. ARG1 identifies the server account, and ARG2 must contain the corresponding private key. The public key must already be authorized for that account on the server. An empty or public-only SshKey is rejected locally with AuthFailReason equal to 2; this local validation failure does not close the SSH connection. If the server rejects the key but keeps the connection open, the application may retry on the same connection with another private key or a different authentication method. The server may instead disconnect after an authentication-attempt limit is reached. Reconnect before retrying when IsConnected is _FALSE_. Calling this method before Connect fails with reason 1. Calling it after the session is already authenticated fails with reason 6. After a failure, inspect AuthFailReason, LastErrorText, and, if the connection was closed, DisconnectReason.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePk
	 * @param ?string $username
	 * @param CkSshKey $privateKey
	 * @return bool
	 */
	function AuthenticatePk($username,$privateKey) {
		return CkSsh_AuthenticatePk($this->_cPtr,$username,$privateKey);
	}

	/**
	 * Creates an asynchronous task to call the AuthenticatePk method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePkAsync
	 * @param ?string $username
	 * @param CkSshKey $privateKey
	 * @return ?CkTask
	 */
	function AuthenticatePkAsync($username,$privateKey) {
		return chilkat::_obj(CkSsh_AuthenticatePkAsync($this->_cPtr,$username,$privateKey),'CkTask');
	}

	/**
	 * Authenticates an active, unauthenticated SSH connection using login and password. Call Connect first. Calling this method without a connection fails with AuthFailReason equal to 1. Calling it after the session is already authenticated fails with reason 6. If authentication fails and the server keeps the SSH connection open, IsConnected remains _TRUE_ and the application may retry on the same connection with corrected credentials or a different authentication method. A later correct password succeeded on the same connection in testing. A server can disconnect after too many failed attempts; reconnect before retrying when IsConnected is _FALSE_. An authentication attempt clears or replaces UserAuthBanner. Object-level configuration settings remain unchanged. If the server requests a password change, see PasswordChangeRequested. After a failure, inspect AuthFailReason and LastErrorText. DisconnectCode and DisconnectReason remain zero and empty unless the server actually sends an SSH DISCONNECT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePw
	 * @param ?string $login
	 * @param ?string $password
	 * @return bool
	 */
	function AuthenticatePw($login,$password) {
		return CkSsh_AuthenticatePw($this->_cPtr,$login,$password);
	}

	/**
	 * Creates an asynchronous task to call the AuthenticatePw method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePwAsync
	 * @param ?string $login
	 * @param ?string $password
	 * @return ?CkTask
	 */
	function AuthenticatePwAsync($login,$password) {
		return chilkat::_obj(CkSsh_AuthenticatePwAsync($this->_cPtr,$login,$password),'CkTask');
	}

	/**
	 * Authenticates with a server that requires both a password and a private key. ARG1 is the username, ARG2 is the password, and ARG3 is the private key. Most servers require either password or public-key authentication rather than both. After a failure, inspect AuthFailReason and LastErrorText.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePwPk
	 * @param ?string $username
	 * @param ?string $password
	 * @param CkSshKey $privateKey
	 * @return bool
	 */
	function AuthenticatePwPk($username,$password,$privateKey) {
		return CkSsh_AuthenticatePwPk($this->_cPtr,$username,$password,$privateKey);
	}

	/**
	 * Creates an asynchronous task to call the AuthenticatePwPk method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticatePwPkAsync
	 * @param ?string $username
	 * @param ?string $password
	 * @param CkSshKey $privateKey
	 * @return ?CkTask
	 */
	function AuthenticatePwPkAsync($username,$password,$privateKey) {
		return chilkat::_obj(CkSsh_AuthenticatePwPkAsync($this->_cPtr,$username,$password,$privateKey),'CkTask');
	}

	/**
	 * Performs the same authentication as AuthenticatePw, but receives the login and password in SecureString objects. The connection-state, retry, timeout, and AuthFailReason behavior is the same as for AuthenticatePw.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticateSecPw
	 * @param CkSecureString $login
	 * @param CkSecureString $password
	 * @return bool
	 */
	function AuthenticateSecPw($login,$password) {
		return CkSsh_AuthenticateSecPw($this->_cPtr,$login,$password);
	}

	/**
	 * Creates an asynchronous task to call the AuthenticateSecPw method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticateSecPwAsync
	 * @param CkSecureString $login
	 * @param CkSecureString $password
	 * @return ?CkTask
	 */
	function AuthenticateSecPwAsync($login,$password) {
		return chilkat::_obj(CkSsh_AuthenticateSecPwAsync($this->_cPtr,$login,$password),'CkTask');
	}

	/**
	 * Performs the same combined password/private-key authentication as AuthenticatePwPk, but receives the username and password in SecureString objects.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticateSecPwPk
	 * @param CkSecureString $username
	 * @param CkSecureString $password
	 * @param CkSshKey $privateKey
	 * @return bool
	 */
	function AuthenticateSecPwPk($username,$password,$privateKey) {
		return CkSsh_AuthenticateSecPwPk($this->_cPtr,$username,$password,$privateKey);
	}

	/**
	 * Creates an asynchronous task to call the AuthenticateSecPwPk method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#AuthenticateSecPwPkAsync
	 * @param CkSecureString $username
	 * @param CkSecureString $password
	 * @param CkSshKey $privateKey
	 * @return ?CkTask
	 */
	function AuthenticateSecPwPkAsync($username,$password,$privateKey) {
		return chilkat::_obj(CkSsh_AuthenticateSecPwPkAsync($this->_cPtr,$username,$password,$privateKey),'CkTask');
	}

	/**
	 * Returns _TRUE_ when channelNum identifies a channel that Chilkat currently considers open for application I/O; otherwise returns _FALSE_. It returns _FALSE_ for invalid or nonexistent channel numbers, after a normal local Disconnect, after remote closure is processed, and immediately after ChannelSendClose closes the channel locally. NumOpenChannels can temporarily continue counting a locally closed channel until final remote-close processing occurs. Connection loss: After abrupt transport loss or a server-sent SSH DISCONNECT, a retained channel record can temporarily cause this method to return _TRUE_ even though IsConnected is _FALSE_. Treat IsConnected = _FALSE_ as authoritative. Buffered bytes from before the loss can still be retrievable.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelIsOpen
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelIsOpen($channelNum) {
		return CkSsh_ChannelIsOpen($this->_cPtr,$channelNum);
	}

	/**
	 * Polls channelNum for incoming SSH channel messages, waiting at most pollTimeoutMs milliseconds. Pass 0 for a nonblocking poll. The explicit pollTimeoutMs controls this wait; shorter values of ReadTimeoutMs or IdleTimeoutMs do not shorten it. Data received is appended to the normal receive buffer or separate stderr buffer according to StderrToStdout. Return valueMeaning -1An error occurred. Inspect LastErrorText, IsConnected, and ChannelIsOpen. -2No additional applicable channel message arrived before the applicable timeout. Previously buffered stdout or stderr can still be available. 0EOF or channel closure was processed. Buffered data can still remain and should be drained. > 0The total number of bytes currently buffered for the channel, counting both the normal receive buffer and a separate stderr buffer. It is not the number of bytes newly received by this call. The return value is not a newly received byte count. Inspect GetReceivedNumBytes, retrieve separate stderr when applicable, and check EOF, CLOSE, and exit-status flags. Processing a final channel message can remove a completed channel record, so drain buffered output before polling again.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelPoll
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @return int
	 */
	function ChannelPoll($channelNum,$pollTimeoutMs) {
		return CkSsh_ChannelPoll($this->_cPtr,$channelNum,$pollTimeoutMs);
	}

	/**
	 * Creates an asynchronous task to call the ChannelPoll method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelPollAsync
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @return ?CkTask
	 */
	function ChannelPollAsync($channelNum,$pollTimeoutMs) {
		return chilkat::_obj(CkSsh_ChannelPollAsync($this->_cPtr,$channelNum,$pollTimeoutMs),'CkTask');
	}

	/**
	 * Reads incoming SSH channel messages for channelNum. A nonzero ReadTimeoutMs limits the wait for channel input. Received data is appended to the normal receive buffer or separate stderr buffer according to StderrToStdout. Return valueMeaning -1An error occurred. Inspect LastErrorText, IsConnected, and ChannelIsOpen. -2No additional applicable channel message arrived before the applicable timeout. Previously buffered stdout or stderr can still be available. 0EOF or channel closure was processed. Buffered data can still remain and should be drained. > 0The total number of bytes currently buffered for the channel, counting both the normal receive buffer and a separate stderr buffer. It is not the number of bytes newly received by this call. A timeout returns -2 rather than closing the channel. A later read can continue on the same channel. Because all channels share one SSH transport, this method can also process and buffer packets for other open channels while waiting for channelNum.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelRead
	 * @param int $channelNum
	 * @return int
	 */
	function ChannelRead($channelNum) {
		return CkSsh_ChannelRead($this->_cPtr,$channelNum);
	}

	/**
	 * Creates an asynchronous task to call the ChannelRead method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReadAsync
	 * @param int $channelNum
	 * @return ?CkTask
	 */
	function ChannelReadAsync($channelNum) {
		return chilkat::_obj(CkSsh_ChannelReadAsync($this->_cPtr,$channelNum),'CkTask');
	}

	/**
	 * Reads channelNum in two phases: Waits for the first applicable channel message. A nonzero ReadTimeoutMs limits this initial wait. When ReadTimeoutMs is 0, IdleTimeoutMs can provide the initial-read timeout. After data begins arriving, continues processing until no additional data arrives for pollTimeoutMs milliseconds. The second phase is governed by pollTimeoutMs; a shorter ReadTimeoutMs does not shorten that quiet-period wait. This pattern is useful for shell commands: allow a longer wait for the first output, then stop shortly after the output stream becomes idle. Return valueMeaning -1An error occurred. Inspect LastErrorText, IsConnected, and ChannelIsOpen. -2No additional applicable channel message arrived before the applicable timeout. Previously buffered stdout or stderr can still be available. 0EOF or channel closure was processed. Buffered data can still remain and should be drained. > 0The total number of bytes currently buffered for the channel, counting both the normal receive buffer and a separate stderr buffer. It is not the number of bytes newly received by this call. The method can return 0 or -2 while bytes remain buffered. Drain the normal and stderr buffers based on their actual contents, not solely on the method return value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReadAndPoll
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @return int
	 */
	function ChannelReadAndPoll($channelNum,$pollTimeoutMs) {
		return CkSsh_ChannelReadAndPoll($this->_cPtr,$channelNum,$pollTimeoutMs);
	}

	/**
	 * Creates an asynchronous task to call the ChannelReadAndPoll method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReadAndPollAsync
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @return ?CkTask
	 */
	function ChannelReadAndPollAsync($channelNum,$pollTimeoutMs) {
		return chilkat::_obj(CkSsh_ChannelReadAndPollAsync($this->_cPtr,$channelNum,$pollTimeoutMs),'CkTask');
	}

	/**
	 * Works like ChannelReadAndPoll, but also returns after newly processed data causes the total buffered channel data to reach or exceed maxNumBytes. maxNumBytes is a return threshold, not a hard receive-buffer limit. An entire SSH packet can be buffered, so the amount returned can exceed the requested threshold. Data already buffered before the call does not by itself cause an immediate threshold return. A value of 0 or a negative value does not impose a useful threshold. For streaming large output, use a positive threshold, drain the buffer after each call, and perform a final drain when EOF or CLOSE is observed. Return valueMeaning -1An error occurred. Inspect LastErrorText, IsConnected, and ChannelIsOpen. -2No additional applicable channel message arrived before the timeout. Previously buffered stdout or stderr can still be available. 0EOF or channel closure was processed. Buffered data can still remain and should be drained. > 0The total number of bytes currently buffered for the channel, counting both the normal receive buffer and a separate stderr buffer. It is not the number of bytes newly received by this call.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReadAndPoll2
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @param int $maxNumBytes
	 * @return int
	 */
	function ChannelReadAndPoll2($channelNum,$pollTimeoutMs,$maxNumBytes) {
		return CkSsh_ChannelReadAndPoll2($this->_cPtr,$channelNum,$pollTimeoutMs,$maxNumBytes);
	}

	/**
	 * Creates an asynchronous task to call the ChannelReadAndPoll2 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReadAndPoll2Async
	 * @param int $channelNum
	 * @param int $pollTimeoutMs
	 * @param int $maxNumBytes
	 * @return ?CkTask
	 */
	function ChannelReadAndPoll2Async($channelNum,$pollTimeoutMs,$maxNumBytes) {
		return chilkat::_obj(CkSsh_ChannelReadAndPoll2Async($this->_cPtr,$channelNum,$pollTimeoutMs,$maxNumBytes),'CkTask');
	}

	/**
	 * Returns _TRUE_ if an SSH CLOSE message has been received for channelNum. Remote CLOSE is distinct from EOF and closes the channel in both directions. When it is processed, ChannelIsOpen becomes _FALSE_ and the channel is normally removed from NumOpenChannels. Returns _FALSE_ for an invalid, released, locally disconnected, or no-longer-retained channel number. A transport can be lost without receiving a channel CLOSE; check IsConnected separately. Retrieve buffered output, stderr, and exit status promptly after the receive method completes and before calling ChannelRelease.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceivedClose
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelReceivedClose($channelNum) {
		return CkSsh_ChannelReceivedClose($this->_cPtr,$channelNum);
	}

	/**
	 * Returns _TRUE_ if an SSH EOF message has been received for channelNum. EOF means the server will send no more ordinary channel data. It does not close the channel. The server can send EOF before it sends CLOSE. During that interval, this method is _TRUE_, ChannelReceivedClose is _FALSE_, and the channel can remain open and counted by NumOpenChannels. Exit status and CLOSE can arrive later. Returns _FALSE_ for an invalid, released, disconnected, or no-longer-retained channel number. After transport failure, channel flags can be stale or unavailable; check IsConnected first.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceivedEof
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelReceivedEof($channelNum) {
		return CkSsh_ChannelReceivedEof($this->_cPtr,$channelNum);
	}

	/**
	 * Returns _TRUE_ if the server supplied an exit-status value for channelNum. When _TRUE_, call GetChannelExitStatus. Exit status is independent of EOF and CLOSE. It can arrive after EOF and before or with CLOSE. The server is not required to send an exit status for every channel type or command. Returns _FALSE_ for an invalid, released, disconnected, or no-longer-retained channel number. A connection failure does not synthesize an exit status. Check this flag and retrieve the value immediately after the receive method completes. A later unrelated SSH operation, including channel enumeration, can finalize and remove the completed channel record.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceivedExitStatus
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelReceivedExitStatus($channelNum) {
		return CkSsh_ChannelReceivedExitStatus($this->_cPtr,$channelNum);
	}

	/**
	 * Receives data on channelNum until the server sends and Chilkat processes channel CLOSE. The server can send EOF earlier; EOF alone does not complete this method. Both normal data and stderr are processed. Stderr is merged or kept separate according to StderrToStdout. A nonzero ReadTimeoutMs limits the receive operation. IdleTimeoutMs is not a simple stdout/stderr inactivity timer because any SSH communication can restart it. If the method times out or is canceled, bytes received before the failure remain buffered, the SSH connection and channel can remain open, and a later call can continue receiving on the same channel. These outcomes do not set DisconnectCode or DisconnectReason. If the SSH transport is lost, the method fails and IsConnected becomes _FALSE_, but bytes received before the loss can still remain retrievable. This method buffers all received output in memory. It successfully accumulated at least 8 MiB in testing, but no upper limit was established. For potentially large or unbounded output, use incremental reads and drain the buffers as data arrives. Retrieve completed-channel results immediately. After this method returns, get stdout, stderr, and exit status before calling unrelated SSH methods or querying NumOpenChannels. A later SSH operation can finalize and discard the completed channel record. A _TRUE_ return means the SSH receive operation completed. It does not mean the remote command exited successfully.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveToClose
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelReceiveToClose($channelNum) {
		return CkSsh_ChannelReceiveToClose($this->_cPtr,$channelNum);
	}

	/**
	 * Creates an asynchronous task to call the ChannelReceiveToClose method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveToCloseAsync
	 * @param int $channelNum
	 * @return ?CkTask
	 */
	function ChannelReceiveToCloseAsync($channelNum) {
		return chilkat::_obj(CkSsh_ChannelReceiveToCloseAsync($this->_cPtr,$channelNum),'CkTask');
	}

	/**
	 * Receives text on channelNum until the accumulated text matches matchPattern. Existing buffered text is checked before waiting for additional network data. The supported pattern syntax consists of literal characters plus *, which matches zero or more characters and can span line breaks. It can appear anywhere in the pattern. The ? wildcard and bracket character classes are not supported, and backslash does not escape a literal *. A pattern containing only * waits until at least some text has arrived rather than matching an empty buffer immediately. charset specifies the character encoding, and caseSensitive controls comparison. The method can read beyond the matched text because an entire SSH packet may already be available. Matching considers stderr even when StderrToStdout is _FALSE_. StripColorCodes is not applied before matching. A nonzero ReadTimeoutMs limits the wait. IdleTimeoutMs is not a substitute for this limit because unrelated SSH traffic can restart the inactivity interval. Usage notes:Use distinctive literal sentinels when exact command boundaries are required. After success, GetReceivedTextS can remove and return text through a known literal terminator. Avoid an empty matchPattern; it does not define a useful boundary.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveUntilMatch
	 * @param int $channelNum
	 * @param ?string $matchPattern
	 * @param ?string $charset
	 * @param bool $caseSensitive
	 * @return bool
	 */
	function ChannelReceiveUntilMatch($channelNum,$matchPattern,$charset,$caseSensitive) {
		return CkSsh_ChannelReceiveUntilMatch($this->_cPtr,$channelNum,$matchPattern,$charset,$caseSensitive);
	}

	/**
	 * Creates an asynchronous task to call the ChannelReceiveUntilMatch method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveUntilMatchAsync
	 * @param int $channelNum
	 * @param ?string $matchPattern
	 * @param ?string $charset
	 * @param bool $caseSensitive
	 * @return ?CkTask
	 */
	function ChannelReceiveUntilMatchAsync($channelNum,$matchPattern,$charset,$caseSensitive) {
		return chilkat::_obj(CkSsh_ChannelReceiveUntilMatchAsync($this->_cPtr,$channelNum,$matchPattern,$charset,$caseSensitive),'CkTask');
	}

	/**
	 * Works like ChannelReceiveUntilMatch, but returns when any pattern in matchPatterns is matched. The array must contain at least one element. The first matching content encountered in the received stream determines when the method returns; array order does not override an earlier token in the stream. Existing buffered text is checked before waiting. Empty elements are ignored, and duplicate patterns are allowed. The method does not return the index or value of the pattern that matched, and GetLastJsonData does not identify it. After success, inspect the buffered text or use distinct nonoverlapping sentinels that the application can identify reliably. The same pattern grammar, stderr behavior, and ReadTimeoutMs behavior described for ChannelReceiveUntilMatch apply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveUntilMatchN
	 * @param int $channelNum
	 * @param CkStringArray $matchPatterns
	 * @param ?string $charset
	 * @param bool $caseSensitive
	 * @return bool
	 */
	function ChannelReceiveUntilMatchN($channelNum,$matchPatterns,$charset,$caseSensitive) {
		return CkSsh_ChannelReceiveUntilMatchN($this->_cPtr,$channelNum,$matchPatterns,$charset,$caseSensitive);
	}

	/**
	 * Creates an asynchronous task to call the ChannelReceiveUntilMatchN method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelReceiveUntilMatchNAsync
	 * @param int $channelNum
	 * @param CkStringArray $matchPatterns
	 * @param ?string $charset
	 * @param bool $caseSensitive
	 * @return ?CkTask
	 */
	function ChannelReceiveUntilMatchNAsync($channelNum,$matchPatterns,$charset,$caseSensitive) {
		return chilkat::_obj(CkSsh_ChannelReceiveUntilMatchNAsync($this->_cPtr,$channelNum,$matchPatterns,$charset,$caseSensitive),'CkTask');
	}

	/**
	 * Releases Chilkat’s retained local resources for channelNum after all required output and exit information has been retrieved. It does not send EOF or CLOSE and is not a channel-closing method. Calling it while a channel is still protocol-open has no effect. It can also be called after Disconnect to discard buffered data retained for a former channel. After release, methods such as GetReceivedNumBytes report that the channel is unavailable. Calling this method with an invalid channel number is a harmless no-op. Repeated calls are harmless. Do not release a completed channel until stdout, stderr, and exit status have been retrieved. Also avoid unrelated SSH calls between completion and result retrieval because they can finalize and remove the completed channel record. ChannelReceiveToClose(channelNum) GetReceivedText(...) GetReceivedStderrText(...) if (ChannelReceivedExitStatus(channelNum))     GetChannelExitStatus(channelNum) ChannelRelease(channelNum)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelRelease
	 * @param int $channelNum
	 */
	function ChannelRelease($channelNum) {
		CkSsh_ChannelRelease($this->_cPtr,$channelNum);
	}

	/**
	 * Sends an SSH CLOSE message for channelNum and immediately marks the channel locally closed. The method does not wait for remote EOF, remote CLOSE, or an exit status, and it does not guarantee that the remote process has terminated. After a successful call, ChannelIsOpen returns _FALSE_, but NumOpenChannels can continue counting the channel until final remote-close processing occurs. A repeated call can succeed while the local channel record remains; after the remote CLOSE removes the channel, another call fails because the channel no longer exists. For normal exec completion, prefer receiving through the remote CLOSE so all output and exit information can be collected. Use this method when the application intentionally abandons or closes the channel early.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendClose
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelSendClose($channelNum) {
		return CkSsh_ChannelSendClose($this->_cPtr,$channelNum);
	}

	/**
	 * Creates an asynchronous task to call the ChannelSendClose method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendCloseAsync
	 * @param int $channelNum
	 * @return ?CkTask
	 */
	function ChannelSendCloseAsync($channelNum) {
		return chilkat::_obj(CkSsh_ChannelSendCloseAsync($this->_cPtr,$channelNum),'CkTask');
	}

	/**
	 * Sends the exact bytes in byteData on channelNum. Binary values, including embedded zero bytes, are preserved. A zero-length byte array is valid and sends no data. Chilkat splits large inputs into SSH packets and waits for channel-window adjustments as needed. A multi-megabyte input can be passed in one call. Partial delivery on failure: A _FALSE_ return does not mean that zero bytes were delivered. If the remote side closes the channel during a large send, an indeterminate prefix can already have been transmitted. The method does not report the partial byte count. Do not automatically resend the complete payload unless the application protocol can safely detect and recover from partial or duplicate data. Do not call this method after ChannelSendEof. A successful return after EOF does not guarantee delivery.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendData
	 * @param int $channelNum
	 * @param CkByteData $byteData
	 * @return bool
	 */
	function ChannelSendData($channelNum,$byteData) {
		return CkSsh_ChannelSendData($this->_cPtr,$channelNum,$byteData);
	}

	/**
	 * Creates an asynchronous task to call the ChannelSendData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendDataAsync
	 * @param int $channelNum
	 * @param CkByteData $byteData
	 * @return ?CkTask
	 */
	function ChannelSendDataAsync($channelNum,$byteData) {
		return chilkat::_obj(CkSsh_ChannelSendDataAsync($this->_cPtr,$channelNum,$byteData),'CkTask');
	}

	/**
	 * Sends an SSH EOF message on channelNum. This closes only the client-to-server data direction. The application may continue receiving stdout, stderr, exit status, EOF, and CLOSE from the server. After EOF is sent, do not call ChannelSendData or ChannelSendString again for that channel. A later send call can return _TRUE_ even though the data is not delivered to the remote process. Repeated calls to ChannelSendEof are harmless but unnecessary. For an exec request that reads standard input, EOF is the normal way to indicate that all input has been supplied.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendEof
	 * @param int $channelNum
	 * @return bool
	 */
	function ChannelSendEof($channelNum) {
		return CkSsh_ChannelSendEof($this->_cPtr,$channelNum);
	}

	/**
	 * Creates an asynchronous task to call the ChannelSendEof method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendEofAsync
	 * @param int $channelNum
	 * @return ?CkTask
	 */
	function ChannelSendEofAsync($channelNum) {
		return chilkat::_obj(CkSsh_ChannelSendEofAsync($this->_cPtr,$channelNum),'CkTask');
	}

	/**
	 * Encodes textData using charset, then sends the resulting bytes on channelNum. No line ending or terminator is added automatically. An empty string is valid and sends zero bytes. When CaretControl is _TRUE_, recognized caret sequences are translated to control characters before sending. On Windows, ANSI uses the Windows ANSI code page returned by GetACP, not the OEM code page. Its meaning on other platforms is environment-dependent. Use an explicit charset such as utf-8 or windows-1252 for portable behavior. Use a supported charset name. In v11.6.0, an unrecognized charset name was observed to fall back to UTF-8 rather than fail; applications should not rely on that fallback. The method handles SSH packet sizing and channel-window flow control internally. It does not expose a partial-byte count if an error occurs. Do not call this method after ChannelSendEof. A successful return after EOF does not guarantee that the text was delivered. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendString
	 * @param int $channelNum
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return bool
	 */
	function ChannelSendString($channelNum,$textData,$charset) {
		return CkSsh_ChannelSendString($this->_cPtr,$channelNum,$textData,$charset);
	}

	/**
	 * Creates an asynchronous task to call the ChannelSendString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ChannelSendStringAsync
	 * @param int $channelNum
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function ChannelSendStringAsync($channelNum,$textData,$charset) {
		return chilkat::_obj(CkSsh_ChannelSendStringAsync($this->_cPtr,$channelNum,$textData,$charset),'CkTask');
	}

	/**
	 * Returns _TRUE_ when the underlying SSH transport currently appears connected, and _FALSE_ when the object is unconnected, has been disconnected, a connection attempt failed, or transport loss has already been detected. In testing, this method returned immediately and matched IsConnected. It should not be treated as an end-to-end SSH round trip or proof that the remote server is responsive. Use SendIgnore when an active SSH exchange is required. This method does not authenticate the session. A failed authentication, receive timeout, receive abort, or rejected channel-open request can leave it returning _TRUE_ because those failures do not necessarily close the transport. A _FALSE_ result can simply mean that no connection exists. The method does not necessarily replace LastErrorText, so an existing error string may describe an earlier operation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#CheckConnection
	 * @return bool
	 */
	function CheckConnection() {
		return CkSsh_CheckConnection($this->_cPtr);
	}

	/**
	 * Clears all TTY mode settings previously added with SetTtyMode. The next SendReqPty request will not include those cleared modes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ClearTtyModes
	 */
	function ClearTtyModes() {
		CkSsh_ClearTtyModes($this->_cPtr);
	}

	/**
	 * Establishes an SSH connection to domainName on the TCP port specified by port. The domainName may be a DNS hostname or a numeric IPv4 or IPv6 address. A null or empty domainName is treated as a zero-length hostname and causes the call to fail. Proxy selection: If SocksVersion is 4 or 5, Chilkat connects through the configured SOCKS proxy. Otherwise, if HttpProxyHostname is nonempty and HttpProxyPort is nonzero, Chilkat connects through the HTTP proxy. If neither condition applies, Chilkat connects directly to the SSH server.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#Connect
	 * @param ?string $domainName
	 * @param int $port
	 * @return bool
	 */
	function Connect($domainName,$port) {
		return CkSsh_Connect($this->_cPtr,$domainName,$port);
	}

	/**
	 * Creates an asynchronous task to call the Connect method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ConnectAsync
	 * @param ?string $domainName
	 * @param int $port
	 * @return ?CkTask
	 */
	function ConnectAsync($domainName,$port) {
		return chilkat::_obj(CkSsh_ConnectAsync($this->_cPtr,$domainName,$port),'CkTask');
	}

	/**
	 * Connects to another SSH server through the already connected and authenticated Ssh object in ARG1. ARG2 and ARG3 identify the destination host and port. application  →  first SSH server  →  destination SSH serverAfter this method succeeds, authenticate separately with the destination server by calling one of its Authenticate* methods. The proxy properties, ClientIpAddress, ClientPort, and socket options on the inner object apply to this connection. SOCKS takes precedence over HTTP proxy settings, using the same selection rules as Connect. The inner object’s ConnectTimeoutMs controls the timeout for establishing this connection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ConnectThroughSsh
	 * @param CkSsh $ssh
	 * @param ?string $hostname
	 * @param int $port
	 * @return bool
	 */
	function ConnectThroughSsh($ssh,$hostname,$port) {
		return CkSsh_ConnectThroughSsh($this->_cPtr,$ssh,$hostname,$port);
	}

	/**
	 * Creates an asynchronous task to call the ConnectThroughSsh method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ConnectThroughSshAsync
	 * @param CkSsh $ssh
	 * @param ?string $hostname
	 * @param int $port
	 * @return ?CkTask
	 */
	function ConnectThroughSshAsync($ssh,$hostname,$port) {
		return chilkat::_obj(CkSsh_ConnectThroughSshAsync($this->_cPtr,$ssh,$hostname,$port),'CkTask');
	}

	/**
	 * Continues keyboard-interactive authentication by submitting the response in ARG1. For a single prompt, ARG1 may be the response text. When the preceding information request contains multiple prompts, supply an XML response: <response>   <response1>response to first prompt</response1>   <response2>response to second prompt</response2>   ... </response>The returned XML has one of three forms: authentication success, authentication failure, or another information request containing additional prompts. <success>success message</success>
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ContinueKeyboardAuth
	 * @param ?string $response
	 * @return ?string
	 */
	function continueKeyboardAuth($response) {
		return CkSsh_continueKeyboardAuth($this->_cPtr,$response);
	}

	/**
	 * Creates an asynchronous task to call the ContinueKeyboardAuth method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ContinueKeyboardAuthAsync
	 * @param ?string $response
	 * @return ?CkTask
	 */
	function ContinueKeyboardAuthAsync($response) {
		return chilkat::_obj(CkSsh_ContinueKeyboardAuthAsync($this->_cPtr,$response),'CkTask');
	}

	/**
	 * Closes the current SSH transport. It is harmless to call when no connection exists and may be called repeatedly. After this method returns, IsConnected is _FALSE_, NumOpenChannels is 0, and former channels are no longer open or enumerated. However, bytes that were already buffered for a former channel can remain retrievable by its channel number after the disconnect. Retrieve the data and call ChannelRelease, or allow the next Connect call to clear retained old-channel data. A local disconnect does not set DisconnectCode or DisconnectReason; they remain 0 and an empty string. ServerIdentifier remains available, while HostKeyFingerprint is cleared. The Ssh object remains reusable. Writable configuration settings are not reset and remain in effect for subsequent calls to Connect. Every new connection begins unauthenticated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#Disconnect
	 */
	function Disconnect() {
		CkSsh_Disconnect($this->_cPtr);
	}

	/**
	 * Queries a connected, unauthenticated SSH server for its advertised user-authentication methods. The result is a lowercase, comma-separated list without spaces, such as publickey,password,keyboard-interactive. Connection side effect: Call this method after Connect and before authenticating. It intentionally disconnects after the query, so call Connect again before authenticating. Calling it after successful authentication fails and also leaves the object disconnected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetAuthMethods
	 * @return ?string
	 */
	function getAuthMethods() {
		return CkSsh_getAuthMethods($this->_cPtr);
	}

	/**
	 * Queries a connected, unauthenticated SSH server for its advertised user-authentication methods. The result is a lowercase, comma-separated list without spaces, such as publickey,password,keyboard-interactive. Connection side effect: Call this method after Connect and before authenticating. It intentionally disconnects after the query, so call Connect again before authenticating. Calling it after successful authentication fails and also leaves the object disconnected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetAuthMethods
	 * @return ?string
	 */
	function authMethods() {
		return CkSsh_authMethods($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the GetAuthMethods method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetAuthMethodsAsync
	 * @return ?CkTask
	 */
	function GetAuthMethodsAsync() {
		return chilkat::_obj(CkSsh_GetAuthMethodsAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Returns the remote process exit status received for channelNum. Call this method only after ChannelReceivedExitStatus returns _TRUE_. Retrieve the value immediately after the receive operation and before calling unrelated SSH methods, querying NumOpenChannels, or releasing the channel. After the completed channel record is removed, a returned numeric 0 is not a valid substitute for checking method success. An exit status of 0 conventionally indicates success on Unix-like systems; nonzero meanings are determined by the remote program and operating system. A nonzero exit status is not a Chilkat API failure and does not cause SendReqExec or ChannelReceiveToClose to fail.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetChannelExitStatus
	 * @param int $channelNum
	 * @return int
	 */
	function GetChannelExitStatus($channelNum) {
		return CkSsh_GetChannelExitStatus($this->_cPtr,$channelNum);
	}

	/**
	 * Returns the channel number at zero-based enumeration index in the active channel collection. The index is an enumeration position, not a channel number. Use NumOpenChannels to determine the valid range from 0 through NumOpenChannels - 1. Returns -1 when index is negative or otherwise out of range, including index 0 when the collection is empty. Channel numbers are opaque identifiers; do not assume they start at 0, reset after reconnect, are sequential, or are reused predictably. The active collection can temporarily include a channel that was closed locally and is awaiting final remote-close processing. Former channels retained only for buffered-result retrieval after disconnect are not enumerated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetChannelNumber
	 * @param int $index
	 * @return int
	 */
	function GetChannelNumber($index) {
		return CkSsh_GetChannelNumber($this->_cPtr,$index);
	}

	/**
	 * Returns the type of the channel at zero-based enumeration index in the active channel collection. The index is an enumeration position, not a channel number. Possible values include session, x11, forwarded-tcpip, and direct-tcpip. If index is negative or otherwise out of range, the string-returning method reports failure, returns null, and sets LastMethodSuccess to _FALSE_. This includes index 0 when NumOpenChannels is 0. The active collection can temporarily include a channel that was closed locally but is still awaiting final remote-close processing. Former channels retained only for buffered-result retrieval after disconnect are not enumerated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetChannelType
	 * @param int $index
	 * @return ?string
	 */
	function getChannelType($index) {
		return CkSsh_getChannelType($this->_cPtr,$index);
	}

	/**
	 * Returns the type of the channel at zero-based enumeration index in the active channel collection. The index is an enumeration position, not a channel number. Possible values include session, x11, forwarded-tcpip, and direct-tcpip. If index is negative or otherwise out of range, the string-returning method reports failure, returns null, and sets LastMethodSuccess to _FALSE_. This includes index 0 when NumOpenChannels is 0. The active collection can temporarily include a channel that was closed locally but is still awaiting final remote-close processing. Former channels retained only for buffered-result retrieval after disconnect are not enumerated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetChannelType
	 * @param int $index
	 * @return ?string
	 */
	function channelType($index) {
		return CkSsh_channelType($this->_cPtr,$index);
	}

	/**
	 * Returns the active connection’s server host-key fingerprint. ARG1 specifies the hash algorithm. Hash-algorithm names are case-insensitive. Common choices include SHA256, SHA384, SHA512, SHA1, MD5, and the supported SHA-3 algorithms. The digest is Base64 encoded without trailing padding characters. ARG2 controls whether the host-key type is included, and ARG3 controls whether the canonical hash name is included. ARG2ARG3Example format _TRUE__TRUE_ssh-ed25519 SHA256:S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _TRUE__FALSE_ssh-ed25519 S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _FALSE__TRUE_SHA256:S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _FALSE__FALSE_S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 An active SSH connection is required. The method fails when called before connecting or after disconnecting. For MD5, this method still returns an unpadded Base64 digest; it does not return the colon-separated hexadecimal form used by HostKeyFingerprint. Important: If ARG1 is not recognized, Chilkat falls back to SHA1. Applications should therefore pass a known algorithm name explicitly. Compare the returned fingerprint with a value obtained from a trusted source before sending credentials.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetHostKeyFP
	 * @param ?string $hashAlg
	 * @param bool $includeKeyType
	 * @param bool $includeHashName
	 * @return ?string
	 */
	function getHostKeyFP($hashAlg,$includeKeyType,$includeHashName) {
		return CkSsh_getHostKeyFP($this->_cPtr,$hashAlg,$includeKeyType,$includeHashName);
	}

	/**
	 * Returns the active connection’s server host-key fingerprint. ARG1 specifies the hash algorithm. Hash-algorithm names are case-insensitive. Common choices include SHA256, SHA384, SHA512, SHA1, MD5, and the supported SHA-3 algorithms. The digest is Base64 encoded without trailing padding characters. ARG2 controls whether the host-key type is included, and ARG3 controls whether the canonical hash name is included. ARG2ARG3Example format _TRUE__TRUE_ssh-ed25519 SHA256:S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _TRUE__FALSE_ssh-ed25519 S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _FALSE__TRUE_SHA256:S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 _FALSE__FALSE_S1GreiGjlHBf1QlPPaHs2qxor4nDYl9bhFcPRN4FwF0 An active SSH connection is required. The method fails when called before connecting or after disconnecting. For MD5, this method still returns an unpadded Base64 digest; it does not return the colon-separated hexadecimal form used by HostKeyFingerprint. Important: If ARG1 is not recognized, Chilkat falls back to SHA1. Applications should therefore pass a known algorithm name explicitly. Compare the returned fingerprint with a value obtained from a trusted source before sending credentials.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetHostKeyFP
	 * @param ?string $hashAlg
	 * @param bool $includeKeyType
	 * @param bool $includeHashName
	 * @return ?string
	 */
	function hostKeyFP($hashAlg,$includeKeyType,$includeHashName) {
		return CkSsh_hostKeyFP($this->_cPtr,$hashAlg,$includeKeyType,$includeHashName);
	}

	/**
	 * Copies structured diagnostic information from the most recently called method into the JsonObject in ARG1. Many methods do not produce additional JSON details; in those cases the object may contain little or no information. Authentication passwords and private-key passphrases are not included in this diagnostic JSON.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetLastJsonData
	 * @param CkJsonObject $json
	 */
	function GetLastJsonData($json) {
		CkSsh_GetLastJsonData($this->_cPtr,$json);
	}

	/**
	 * Appends all bytes currently accumulated in the normal receive buffer for channelNum to bd, then clears the channel receive buffer. Existing bytes in bd are preserved and the received bytes are appended. If the method fails, such as for an invalid channel, bd is left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedBd
	 * @param int $channelNum
	 * @param CkBinData $bd
	 * @return bool
	 */
	function GetReceivedBd($channelNum,$bd) {
		return CkSsh_GetReceivedBd($this->_cPtr,$channelNum,$bd);
	}

	/**
	 * Returns all bytes currently accumulated in the normal receive buffer for channelNum, then clears that buffer. For a valid retained channel with no buffered data, the method succeeds and returns a zero-length byte array. For an invalid or no-longer-retained channel, it fails. The separate stderr buffer is not affected. Data received before a timeout, cancellation, or abrupt transport loss can still be returned. After normal channel completion, call this method before unrelated SSH operations that can finalize and remove the completed channel record.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedData
	 * @param int $channelNum
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function GetReceivedData($channelNum,$outBytes) {
		return CkSsh_GetReceivedData($this->_cPtr,$channelNum,$outBytes);
	}

	/**
	 * Returns and removes up to maxNumBytes bytes from the beginning of the normal receive buffer for channelNum. Any remaining bytes stay buffered. If maxNumBytes is 0, the method succeeds, returns zero bytes, and consumes nothing. If it is negative or greater than the available amount, all currently buffered bytes are returned and removed. An empty valid buffer returns success with a zero-length result.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedDataN
	 * @param int $channelNum
	 * @param int $maxNumBytes
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function GetReceivedDataN($channelNum,$maxNumBytes,$outBytes) {
		return CkSsh_GetReceivedDataN($this->_cPtr,$channelNum,$maxNumBytes,$outBytes);
	}

	/**
	 * Returns the number of bytes currently available in the normal receive buffer for channelNum. It does not include bytes in the separate stderr buffer when StderrToStdout is _FALSE_. Returns -1 when the channel number is invalid or its retained resources have been released. After a normal Disconnect or abrupt transport loss, bytes received earlier can remain available and this method can still return their count even though the channel is no longer active or enumerable. A subsequent Connect clears retained data from the previous session. Buffered bytes can also remain after a timeout or cancellation. After normal channel completion, a later unrelated SSH operation can finalize and discard the completed channel record. Query and drain the buffer promptly. Retrieve normal bytes with GetReceivedData, GetReceivedDataN, GetReceivedBd, or a text getter. Retrieve separate stderr with GetReceivedStderr or GetReceivedStderrText.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedNumBytes
	 * @param int $channelNum
	 * @return int
	 */
	function GetReceivedNumBytes($channelNum) {
		return CkSsh_GetReceivedNumBytes($this->_cPtr,$channelNum);
	}

	/**
	 * Decodes and appends to ARG3 all text currently accumulated in the normal receive buffer for channelNum, using charset. The receive buffer is cleared after the text is returned. For a valid retained channel with no buffered data, an empty string is returned successfully. For an invalid or no-longer-retained channel, the string-returning API reports failure. After channel completion, retrieve text immediately before unrelated SSH operations can discard the completed channel record. When StripColorCodes is _TRUE_, ANSI SGR color/style sequences are removed from the returned normal text. Other terminal-control sequences can remain. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedSb
	 * @param int $channelNum
	 * @param ?string $charset
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function GetReceivedSb($channelNum,$charset,$sb) {
		return CkSsh_GetReceivedSb($this->_cPtr,$channelNum,$charset,$sb);
	}

	/**
	 * Returns all bytes currently accumulated in the separate stderr buffer for channelNum, then clears that buffer. For a valid retained channel with no separate stderr, the method succeeds and returns a zero-length byte array. For an invalid or no-longer-retained channel, it fails. Retrieve stderr immediately after completion, before another SSH operation can discard the completed channel record. When StderrToStdout is _TRUE_ - the default - stderr is merged into the normal receive buffer instead. A PTY normally combines stdout and stderr before SSH transports them, regardless of this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedStderr
	 * @param int $channelNum
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function GetReceivedStderr($channelNum,$outBytes) {
		return CkSsh_GetReceivedStderr($this->_cPtr,$channelNum,$outBytes);
	}

	/**
	 * Decodes and returns all text currently accumulated in the separate stderr buffer for channelNum, using charset. The stderr buffer is cleared after the text is returned. For a valid retained channel with no separate stderr, an empty string is returned successfully. For an invalid or no-longer-retained channel, the string-returning API reports failure. Retrieve stderr immediately after completion, before another SSH operation can discard the completed channel record. StripColorCodes is not applied to stderr text. Text getters do not retain an incomplete multibyte sequence for a later call; use raw bytes and a stateful application decoder for streaming multibyte data. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedStderrText
	 * @param int $channelNum
	 * @param ?string $charset
	 * @return ?string
	 */
	function getReceivedStderrText($channelNum,$charset) {
		return CkSsh_getReceivedStderrText($this->_cPtr,$channelNum,$charset);
	}

	/**
	 * Decodes and returns all text currently accumulated in the separate stderr buffer for channelNum, using charset. The stderr buffer is cleared after the text is returned. For a valid retained channel with no separate stderr, an empty string is returned successfully. For an invalid or no-longer-retained channel, the string-returning API reports failure. Retrieve stderr immediately after completion, before another SSH operation can discard the completed channel record. StripColorCodes is not applied to stderr text. Text getters do not retain an incomplete multibyte sequence for a later call; use raw bytes and a stateful application decoder for streaming multibyte data. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedStderrText
	 * @param int $channelNum
	 * @param ?string $charset
	 * @return ?string
	 */
	function receivedStderrText($channelNum,$charset) {
		return CkSsh_receivedStderrText($this->_cPtr,$channelNum,$charset);
	}

	/**
	 * Decodes and returns all text currently accumulated in the normal receive buffer for channelNum, using charset. The receive buffer is cleared after the text is returned. For a valid retained channel with no buffered data, an empty string is returned successfully. For an invalid or no-longer-retained channel, the string-returning API reports failure. After channel completion, retrieve text immediately before unrelated SSH operations can discard the completed channel record. When StripColorCodes is _TRUE_, ANSI SGR color/style sequences are removed from the returned normal text. Other terminal-control sequences can remain. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedText
	 * @param int $channelNum
	 * @param ?string $charset
	 * @return ?string
	 */
	function getReceivedText($channelNum,$charset) {
		return CkSsh_getReceivedText($this->_cPtr,$channelNum,$charset);
	}

	/**
	 * Decodes and returns all text currently accumulated in the normal receive buffer for channelNum, using charset. The receive buffer is cleared after the text is returned. For a valid retained channel with no buffered data, an empty string is returned successfully. For an invalid or no-longer-retained channel, the string-returning API reports failure. After channel completion, retrieve text immediately before unrelated SSH operations can discard the completed channel record. When StripColorCodes is _TRUE_, ANSI SGR color/style sequences are removed from the returned normal text. Other terminal-control sequences can remain. See Supported Charsets.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedText
	 * @param int $channelNum
	 * @param ?string $charset
	 * @return ?string
	 */
	function receivedText($channelNum,$charset) {
		return CkSsh_receivedText($this->_cPtr,$channelNum,$charset);
	}

	/**
	 * Decodes text in the normal receive buffer for channelNum and performs a literal, case-sensitive search for substr using charset. If found, the method returns and removes everything through and including the first occurrence. If not found, it returns an empty string successfully and leaves the buffer unchanged. An empty substr is invalid and causes failure without consuming the buffer. When StripColorCodes is _TRUE_, SGR sequences are removed for both the substring search and the returned value. The method consumes the corresponding original raw bytes, including the stripped escape sequences. As with other destructive text getters, incomplete multibyte sequences are not retained as decoder state for a later call. Retrieve completed-channel text before unrelated SSH operations can remove the retained channel record.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedTextS
	 * @param int $channelNum
	 * @param ?string $substr
	 * @param ?string $charset
	 * @return ?string
	 */
	function getReceivedTextS($channelNum,$substr,$charset) {
		return CkSsh_getReceivedTextS($this->_cPtr,$channelNum,$substr,$charset);
	}

	/**
	 * Decodes text in the normal receive buffer for channelNum and performs a literal, case-sensitive search for substr using charset. If found, the method returns and removes everything through and including the first occurrence. If not found, it returns an empty string successfully and leaves the buffer unchanged. An empty substr is invalid and causes failure without consuming the buffer. When StripColorCodes is _TRUE_, SGR sequences are removed for both the substring search and the returned value. The method consumes the corresponding original raw bytes, including the stripped escape sequences. As with other destructive text getters, incomplete multibyte sequences are not retained as decoder state for a later call. Retrieve completed-channel text before unrelated SSH operations can remove the retained channel record.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#GetReceivedTextS
	 * @param int $channelNum
	 * @param ?string $substr
	 * @param ?string $charset
	 * @return ?string
	 */
	function receivedTextS($channelNum,$substr,$charset) {
		return CkSsh_receivedTextS($this->_cPtr,$channelNum,$substr,$charset);
	}

	/**
	 * This method is deprecated. Call GetLastJsonData instead. Returns structured diagnostic information about the most recently called method when such information is available. Authentication passwords and private-key passphrases are not included.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LastJsonData
	 * @return ?CkJsonObject
	 * @deprecated
	 */
	function LastJsonData() {
		return chilkat::_obj(CkSsh_LastJsonData($this->_cPtr),'CkJsonObject');
	}

	/**
	 * Loads into this object the caller state associated with the asynchronous method represented by ARG1. This is used by generated asynchronous wrappers to recover the object that initiated the task.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkSsh_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Requests a custom SSH channel whose application-defined channel type is supplied in channelType. The SSH server must implement that channel type. On success, returns the channel number used by the normal channel send, receive, EOF, CLOSE, and release methods. Returns -1 if the server rejects the channel. In that case, ChannelOpenFailCode and ChannelOpenFailReason contain the server-provided failure information. The underlying SSH connection normally remains open, CheckConnection remains _TRUE_, and other channels can be opened and used afterward. A channel-open rejection does not set DisconnectCode or DisconnectReason unless the server also sends an SSH DISCONNECT. Object-level configuration settings are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenCustomChannel
	 * @param ?string $channelType
	 * @return int
	 */
	function OpenCustomChannel($channelType) {
		return CkSsh_OpenCustomChannel($this->_cPtr,$channelType);
	}

	/**
	 * Creates an asynchronous task to call the OpenCustomChannel method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenCustomChannelAsync
	 * @param ?string $channelType
	 * @return ?CkTask
	 */
	function OpenCustomChannelAsync($channelType) {
		return chilkat::_obj(CkSsh_OpenCustomChannelAsync($this->_cPtr,$channelType),'CkTask');
	}

	/**
	 * Opens a direct-tcpip channel through the SSH server to targetHostname and targetPort. The target name is sent to and resolved by the SSH server, not by the client computer. It must therefore be resolvable and reachable from the server's network environment. A numeric IPv4 address may be supplied to avoid remote DNS-name resolution. Each successful call creates one independent raw TCP byte stream. Multiple channels to the same target may be open concurrently. Bytes sent with ChannelSend* methods are forwarded to the target, and bytes returned by the target are read with ChannelRead* or ChannelReceive* methods. For example, when the target is an SSH server, its SSH identification line is received as ordinary channel data. The channel is enumerated with type direct-tcpip. Returns the new channel number on success, or -1 if the SSH server rejects or cannot establish the target connection. On server rejection, inspect ChannelOpenFailCode and ChannelOpenFailReason. Name-resolution and target-connection failures normally leave the underlying SSH connection open.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenDirectTcpIpChannel
	 * @param ?string $targetHostname
	 * @param int $targetPort
	 * @return int
	 */
	function OpenDirectTcpIpChannel($targetHostname,$targetPort) {
		return CkSsh_OpenDirectTcpIpChannel($this->_cPtr,$targetHostname,$targetPort);
	}

	/**
	 * Creates an asynchronous task to call the OpenDirectTcpIpChannel method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenDirectTcpIpChannelAsync
	 * @param ?string $targetHostname
	 * @param int $targetPort
	 * @return ?CkTask
	 */
	function OpenDirectTcpIpChannelAsync($targetHostname,$targetPort) {
		return chilkat::_obj(CkSsh_OpenDirectTcpIpChannelAsync($this->_cPtr,$targetHostname,$targetPort),'CkTask');
	}

	/**
	 * Opens a new SSH session channel. The SSH transport must already be connected and user authentication must have completed successfully. Calling this method before Connect or before authentication returns -1. A failure caused by missing authentication does not by itself close the SSH transport. Opening a session channel does not start a shell, execute a command, or start a subsystem. After opening the channel, send the appropriate session request. 1. Connect 2. Authenticate 3. OpenSessionChannel 4. Optionally send preparatory requests such as SendReqPty or SendReqSetEnv 5. Send one primary request: SendReqExec, SendReqShell, or SendReqSubsystemReturns an opaque channel number on success, or -1 on failure. Do not assume channel numbers start at 0, are sequential, or are reused in a predictable way.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenSessionChannel
	 * @return int
	 */
	function OpenSessionChannel() {
		return CkSsh_OpenSessionChannel($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the OpenSessionChannel method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#OpenSessionChannelAsync
	 * @return ?CkTask
	 */
	function OpenSessionChannelAsync() {
		return chilkat::_obj(CkSsh_OpenSessionChannelAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Returns the text currently buffered in the normal receive buffer for channelNum, decoded using charset, without removing any bytes. For a valid empty buffer, it returns an empty string successfully. StripColorCodes is not applied; terminal escape sequences are returned as buffered. Because the operation is nondestructive, it is useful for checking whether a complete multibyte sequence or logical delimiter has arrived before calling a destructive text getter.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#PeekReceivedText
	 * @param int $channelNum
	 * @param ?string $charset
	 * @return ?string
	 */
	function peekReceivedText($channelNum,$charset) {
		return CkSsh_peekReceivedText($this->_cPtr,$channelNum,$charset);
	}

	/**
	 * Waits up to pollTimeoutMs milliseconds for any command previously started by QuickCmdSend to complete. A value of 0 performs a nonblocking check. Manually opened exec channels are not returned by this method. Return valueMeaning >= 0The channel number of a completed quick command. Each completed channel is returned only once. -1Pending quick commands exist, but none completed before the timeout. -2No quick commands remain to be checked, or an error occurred such as loss of the SSH connection. Examine LastErrorText and the connection state when the distinction matters. After a channel number is returned, retrieve its stdout, stderr, and exit status with the normal channel methods. The channel is not returned again on the next check, even before ChannelRelease is called.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCmdCheck
	 * @param int $pollTimeoutMs
	 * @return int
	 */
	function QuickCmdCheck($pollTimeoutMs) {
		return CkSsh_QuickCmdCheck($this->_cPtr,$pollTimeoutMs);
	}

	/**
	 * Creates an asynchronous task to call the QuickCmdCheck method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCmdCheckAsync
	 * @param int $pollTimeoutMs
	 * @return ?CkTask
	 */
	function QuickCmdCheckAsync($pollTimeoutMs) {
		return chilkat::_obj(CkSsh_QuickCmdCheckAsync($this->_cPtr,$pollTimeoutMs),'CkTask');
	}

	/**
	 * Starts a remote command and immediately returns its channel number, allowing multiple commands to run concurrently on the same authenticated SSH connection. Internally, this method opens a session channel and sends an exec request. command is encoded according to ReqExecCharset. The returned channel is visible through NumOpenChannels, GetChannelNumber, and GetChannelType, and remains owned by the application. Use QuickCmdCheck to detect completion, retrieve output and exit status with the normal channel methods, and then call ChannelRelease. Returns -1 on failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCmdSend
	 * @param ?string $command
	 * @return int
	 */
	function QuickCmdSend($command) {
		return CkSsh_QuickCmdSend($this->_cPtr,$command);
	}

	/**
	 * Creates an asynchronous task to call the QuickCmdSend method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCmdSendAsync
	 * @param ?string $command
	 * @return ?CkTask
	 */
	function QuickCmdSendAsync($command) {
		return chilkat::_obj(CkSsh_QuickCmdSendAsync($this->_cPtr,$command),'CkTask');
	}

	/**
	 * Executes one noninteractive remote command and returns the stdout collected as text. Internally, this method opens a session channel, sends an exec request, receives the command output through EOF, and decodes the stdout using charset. ReqExecCharset controls the encoding used to send command. The final SSH channel CLOSE can still be pending when this method returns. The internal channel may therefore remain temporarily visible in NumOpenChannels, often with EOF already received but CLOSE not yet processed. Simply sleeping does not process pending SSH messages. A later SSH operation, such as WaitForChannelMessage or another QuickCommand, processes the final close and removes the previous channel. Remote exit status is not reflected in the return value: A command that exits nonzero may still return its stdout normally. An empty string can mean a successful command produced no stdout, or that a failed command wrote only to stderr. When StderrToStdout is _FALSE_, separately received stderr is not included in the returned text. Exit status may or may not have arrived at the instant this method returns. Use QuickCmdSend/QuickCmdCheck or the explicit channel workflow when the application must reliably inspect stderr, provide stdin, or obtain the remote exit status. Do not depend on the temporary internal channel created by this convenience method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCommand
	 * @param ?string $command
	 * @param ?string $charset
	 * @return ?string
	 */
	function quickCommand($command,$charset) {
		return CkSsh_quickCommand($this->_cPtr,$command,$charset);
	}

	/**
	 * Creates an asynchronous task to call the QuickCommand method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickCommandAsync
	 * @param ?string $command
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function QuickCommandAsync($command,$charset) {
		return chilkat::_obj(CkSsh_QuickCommandAsync($this->_cPtr,$command,$charset),'CkTask');
	}

	/**
	 * Starts a remote shell using the simplified sequence OpenSessionChannel → SendReqPty → SendReqShell. The default PTY size is 80 columns by 24 rows. Returns the open shell channel number on success, or -1 on failure. The method returns after the shell request is accepted; it does not wait for or consume the initial login text or shell prompt. Use the returned channel with the normal send and receive methods, then allow the shell to close and call ChannelRelease after retrieving the required output and exit information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickShell
	 * @return int
	 */
	function QuickShell() {
		return CkSsh_QuickShell($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the QuickShell method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#QuickShellAsync
	 * @return ?CkTask
	 */
	function QuickShellAsync() {
		return chilkat::_obj(CkSsh_QuickShellAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Initiates SSH key re-exchange and waits for it to complete. Either client or server may initiate rekeying at any time. RFC 4253 recommends changing keys after approximately one gigabyte of transferred data or one hour of connection time, whichever occurs first. In normal use, servers initiate rekeying as needed and Chilkat handles it transparently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReKey
	 * @return bool
	 */
	function ReKey() {
		return CkSsh_ReKey($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ReKey method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#ReKeyAsync
	 * @return ?CkTask
	 */
	function ReKeyAsync() {
		return chilkat::_obj(CkSsh_ReKeyAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Sends an SSH IGNORE message. The server does not reply, but a successful send helps verify that the connection is still writable. No channel is required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendIgnore
	 * @return bool
	 */
	function SendIgnore() {
		return CkSsh_SendIgnore($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the SendIgnore method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendIgnoreAsync
	 * @return ?CkTask
	 */
	function SendIgnoreAsync() {
		return chilkat::_obj(CkSsh_SendIgnoreAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Requests execution of commandLine on the session channel identified by channelNum. The command text is sent using ReqExecCharset. Request acceptance is not command success: A _TRUE_ return means the SSH server accepted the exec request. The remote command can still fail, be missing, or exit with a nonzero status. Receive the channel to completion, then use ChannelReceivedExitStatus and GetChannelExitStatus to determine the remote process result. An exec channel is used for one command. Open a new session channel for each independent command. A second exec request on the same channel is normally rejected, while the first command continues running. After the request is accepted, data sent with ChannelSendData or ChannelSendString becomes the command’s standard input. Call ChannelSendEof when no more input will be sent; stdout and stderr may still be received afterward.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqExec
	 * @param int $channelNum
	 * @param ?string $commandLine
	 * @return bool
	 */
	function SendReqExec($channelNum,$commandLine) {
		return CkSsh_SendReqExec($this->_cPtr,$channelNum,$commandLine);
	}

	/**
	 * Creates an asynchronous task to call the SendReqExec method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqExecAsync
	 * @param int $channelNum
	 * @param ?string $commandLine
	 * @return ?CkTask
	 */
	function SendReqExecAsync($channelNum,$commandLine) {
		return chilkat::_obj(CkSsh_SendReqExecAsync($this->_cPtr,$channelNum,$commandLine),'CkTask');
	}

	/**
	 * Recommendation for automation: Do not request a pseudo-terminal unless the remote command or application specifically requires terminal behavior. Most automated SSH applications should operate without a PTY so that stdout and stderr remain clean, predictable, and easier to parse.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqPty
	 * @param int $channelNum
	 * @param ?string $termType
	 * @param int $widthInChars
	 * @param int $heightInChars
	 * @param int $widthInPixels
	 * @param int $heightInPixels
	 * @return bool
	 */
	function SendReqPty($channelNum,$termType,$widthInChars,$heightInChars,$widthInPixels,$heightInPixels) {
		return CkSsh_SendReqPty($this->_cPtr,$channelNum,$termType,$widthInChars,$heightInChars,$widthInPixels,$heightInPixels);
	}

	/**
	 * Creates an asynchronous task to call the SendReqPty method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqPtyAsync
	 * @param int $channelNum
	 * @param ?string $termType
	 * @param int $widthInChars
	 * @param int $heightInChars
	 * @param int $widthInPixels
	 * @param int $heightInPixels
	 * @return ?CkTask
	 */
	function SendReqPtyAsync($channelNum,$termType,$widthInChars,$heightInChars,$widthInPixels,$heightInPixels) {
		return chilkat::_obj(CkSsh_SendReqPtyAsync($this->_cPtr,$channelNum,$termType,$widthInChars,$heightInChars,$widthInPixels,$heightInPixels),'CkTask');
	}

	/**
	 * Requests that the server set environment variable name to value for the session channel identified by channelNum. Send environment requests before the primary exec, shell, or subsystem request. Multiple variables may be requested. If the server accepts repeated requests for the same name, the later value is used. An empty value is allowed. The server controls which names are accepted; for example, an OpenSSH configuration can accept selected locale variables while rejecting arbitrary names. A _TRUE_ return means the server accepted this environment request. A rejected request returns _FALSE_ but normally leaves the channel and SSH connection usable, so the application can continue with the primary request. A request sent after the primary request has started is normally rejected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSetEnv
	 * @param int $channelNum
	 * @param ?string $name
	 * @param ?string $value
	 * @return bool
	 */
	function SendReqSetEnv($channelNum,$name,$value) {
		return CkSsh_SendReqSetEnv($this->_cPtr,$channelNum,$name,$value);
	}

	/**
	 * Creates an asynchronous task to call the SendReqSetEnv method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSetEnvAsync
	 * @param int $channelNum
	 * @param ?string $name
	 * @param ?string $value
	 * @return ?CkTask
	 */
	function SendReqSetEnvAsync($channelNum,$name,$value) {
		return chilkat::_obj(CkSsh_SendReqSetEnvAsync($this->_cPtr,$channelNum,$name,$value),'CkTask');
	}

	/**
	 * Starts a remote shell on the open session channel identified by channelNum. A shell can be requested without a PTY when the server permits it. For an interactive terminal session, call SendReqPty first. A _TRUE_ return means the server accepted the shell request. This method does not wait for, detect, or remove a shell prompt. Login messages, prompts, command echo, and command output all arrive as ordinary channel data. Command boundaries are application-defined: SSH does not identify when an individual shell command finishes. Use a prompt pattern only when it is reliably known, or append a unique sentinel to each command and receive until that sentinel is found. Send commands with ChannelSendString using the line ending expected by the remote shell or device. Unix-like shells normally accept LF; other systems may require CRLF or another terminator. Multiple commands may be sent sequentially until the shell exits or the channel closes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqShell
	 * @param int $channelNum
	 * @return bool
	 */
	function SendReqShell($channelNum) {
		return CkSsh_SendReqShell($this->_cPtr,$channelNum);
	}

	/**
	 * Creates an asynchronous task to call the SendReqShell method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqShellAsync
	 * @param int $channelNum
	 * @return ?CkTask
	 */
	function SendReqShellAsync($channelNum) {
		return chilkat::_obj(CkSsh_SendReqShellAsync($this->_cPtr,$channelNum),'CkTask');
	}

	/**
	 * Requests delivery of signalName to the remote process running on channelNum. Send the request after the exec process, shell, or subsystem has started. Supported signal names are: ABRT  ALRM  FPE  HUP  ILL  INT  KILL PIPE  QUIT  SEGV TERM USR1 USR2These are Unix-style signals and are effective only when supported by the SSH server, remote operating system, and target process. A _TRUE_ return indicates that the SSH request succeeded; the process may handle, ignore, or otherwise respond to the signal according to remote behavior.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSignal
	 * @param int $channelNum
	 * @param ?string $signalName
	 * @return bool
	 */
	function SendReqSignal($channelNum,$signalName) {
		return CkSsh_SendReqSignal($this->_cPtr,$channelNum,$signalName);
	}

	/**
	 * Creates an asynchronous task to call the SendReqSignal method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSignalAsync
	 * @param int $channelNum
	 * @param ?string $signalName
	 * @return ?CkTask
	 */
	function SendReqSignalAsync($channelNum,$signalName) {
		return chilkat::_obj(CkSsh_SendReqSignalAsync($this->_cPtr,$channelNum,$signalName),'CkTask');
	}

	/**
	 * Requests the predefined subsystem named by subsystemName on session channel channelNum. A _TRUE_ return means the server accepted the subsystem request; subsequent protocol data is exchanged with the normal channel send and receive methods. If the server rejects the subsystem, this method returns _FALSE_ and the channel normally remains open and reusable for another primary request such as SendReqExec. Once a subsystem request is accepted, do not send an exec, shell, or second subsystem request on that channel. For example, the SSH sftp subsystem can be started this way, but applications should normally use the SFtp class instead of implementing the SFTP protocol directly over an Ssh channel.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSubsystem
	 * @param int $channelNum
	 * @param ?string $subsystemName
	 * @return bool
	 */
	function SendReqSubsystem($channelNum,$subsystemName) {
		return CkSsh_SendReqSubsystem($this->_cPtr,$channelNum,$subsystemName);
	}

	/**
	 * Creates an asynchronous task to call the SendReqSubsystem method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqSubsystemAsync
	 * @param int $channelNum
	 * @param ?string $subsystemName
	 * @return ?CkTask
	 */
	function SendReqSubsystemAsync($channelNum,$subsystemName) {
		return chilkat::_obj(CkSsh_SendReqSubsystemAsync($this->_cPtr,$channelNum,$subsystemName),'CkTask');
	}

	/**
	 * Notifies the server that the terminal dimensions changed for the PTY associated with channelNum. widthInChars and heightInRows specify the character dimensions; pixWidth and pixHeight specify the pixel dimensions. This request may be sent after the PTY is allocated, either before or after starting the shell or exec process. The tested OpenSSH servers applied the new character dimensions to an active shell.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqWindowChange
	 * @param int $channelNum
	 * @param int $widthInChars
	 * @param int $heightInRows
	 * @param int $pixWidth
	 * @param int $pixHeight
	 * @return bool
	 */
	function SendReqWindowChange($channelNum,$widthInChars,$heightInRows,$pixWidth,$pixHeight) {
		return CkSsh_SendReqWindowChange($this->_cPtr,$channelNum,$widthInChars,$heightInRows,$pixWidth,$pixHeight);
	}

	/**
	 * Creates an asynchronous task to call the SendReqWindowChange method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqWindowChangeAsync
	 * @param int $channelNum
	 * @param int $widthInChars
	 * @param int $heightInRows
	 * @param int $pixWidth
	 * @param int $pixHeight
	 * @return ?CkTask
	 */
	function SendReqWindowChangeAsync($channelNum,$widthInChars,$heightInRows,$pixWidth,$pixHeight) {
		return chilkat::_obj(CkSsh_SendReqWindowChangeAsync($this->_cPtr,$channelNum,$widthInChars,$heightInRows,$pixWidth,$pixHeight),'CkTask');
	}

	/**
	 * Sends an SSH X11-forwarding request on the session channel in ARG1. ARG2 controls whether forwarding is limited to a single connection; ARG3 through ARG5 provide the X11 authentication protocol, cookie, and screen number. This low-level method exposes the SSH connection-protocol request. The application is responsible for supplying appropriate X11 values and handling forwarded X11 channels.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqX11Forwarding
	 * @param int $channelNum
	 * @param bool $singleConnection
	 * @param ?string $authProt
	 * @param ?string $authCookie
	 * @param int $screenNum
	 * @return bool
	 */
	function SendReqX11Forwarding($channelNum,$singleConnection,$authProt,$authCookie,$screenNum) {
		return CkSsh_SendReqX11Forwarding($this->_cPtr,$channelNum,$singleConnection,$authProt,$authCookie,$screenNum);
	}

	/**
	 * Creates an asynchronous task to call the SendReqX11Forwarding method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqX11ForwardingAsync
	 * @param int $channelNum
	 * @param bool $singleConnection
	 * @param ?string $authProt
	 * @param ?string $authCookie
	 * @param int $screenNum
	 * @return ?CkTask
	 */
	function SendReqX11ForwardingAsync($channelNum,$singleConnection,$authProt,$authCookie,$screenNum) {
		return chilkat::_obj(CkSsh_SendReqX11ForwardingAsync($this->_cPtr,$channelNum,$singleConnection,$authProt,$authCookie,$screenNum),'CkTask');
	}

	/**
	 * Deprecated for practical use. This method should not be used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqXonXoff
	 * @param int $channelNum
	 * @param bool $clientCanDo
	 * @return bool
	 */
	function SendReqXonXoff($channelNum,$clientCanDo) {
		return CkSsh_SendReqXonXoff($this->_cPtr,$channelNum,$clientCanDo);
	}

	/**
	 * Creates an asynchronous task to call the SendReqXonXoff method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SendReqXonXoffAsync
	 * @param int $channelNum
	 * @param bool $clientCanDo
	 * @return ?CkTask
	 */
	function SendReqXonXoffAsync($channelNum,$clientCanDo) {
		return chilkat::_obj(CkSsh_SendReqXonXoffAsync($this->_cPtr,$channelNum,$clientCanDo),'CkTask');
	}

	/**
	 * Applies an explicit allow-list of SSH negotiation algorithms supplied in ARG1. Use this method before Connect when security policy or server compatibility requires precise control over accepted host-key, key-exchange, cipher, MAC, or compression algorithms.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SetAllowedAlgorithms
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetAllowedAlgorithms($json) {
		return CkSsh_SetAllowedAlgorithms($this->_cPtr,$json);
	}

	/**
	 * Adds or updates one TTY mode to be included in a later SendReqPty request. ttyName is the mode name and ttyValue is its integer value. Call this method before SendReqPty, once for each mode to set. For example, SetTtyMode("ECHO", 0) requests that command input not be echoed by the allocated terminal. Supported mode names include: VINTR VQUIT VERASE VKILL VEOF VEOL VEOL2 VSTART VSTOP VSUSP VDSUSP VREPRINT VWERASE VLNEXT VFLUSH VSWTCH VSTATUS VDISCARD IGNPAR PARMRK INPCK ISTRIP INLCR IGNCR ICRNL IUCLC IXON IXANY IXOFF IMAXBEL ISIG ICANON XCASE ECHO ECHOE ECHOK ECHONL NOFLSH TOSTOP IEXTEN ECHOCTL ECHOKE PENDIN OPOST OLCUC ONLCR OCRNL ONOCR ONLRET CS7 CS8 PARENB PARODD TTY_OP_ISPEED TTY_OP_OSPEEDMost applications do not need to set TTY modes explicitly. Use ClearTtyModes before a later PTY request when previously configured modes should no longer be included.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#SetTtyMode
	 * @param ?string $ttyName
	 * @param int $ttyValue
	 * @return bool
	 */
	function SetTtyMode($ttyName,$ttyValue) {
		return CkSsh_SetTtyMode($this->_cPtr,$ttyName,$ttyValue);
	}

	/**
	 * Begins keyboard-interactive authentication for the login in ARG1. The returned XML describes the server’s prompts and whether each response should be echoed. <infoRequest numPrompts="N">   <name>name</name>   <instruction>instructions</instruction>   <prompt1 echo="1_or_0">prompt text</prompt1>   ... </infoRequest>If authentication immediately succeeds or fails, the returned XML instead has one of these forms: <success>success message</success>
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StartKeyboardAuth
	 * @param ?string $login
	 * @return ?string
	 */
	function startKeyboardAuth($login) {
		return CkSsh_startKeyboardAuth($this->_cPtr,$login);
	}

	/**
	 * Creates an asynchronous task to call the StartKeyboardAuth method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#StartKeyboardAuthAsync
	 * @param ?string $login
	 * @return ?CkTask
	 */
	function StartKeyboardAuthAsync($login) {
		return chilkat::_obj(CkSsh_StartKeyboardAuthAsync($this->_cPtr,$login),'CkTask');
	}

	/**
	 * Waits for the next SSH channel message on any channel. The pollTimeoutMs argument controls this method: 0: nonblocking check. > 0: wait up to the specified number of milliseconds. < 0: wait without a method-level timeout. The explicit argument governs the wait; shorter values of ReadTimeoutMs or IdleTimeoutMs do not shorten it. Return valueMeaning >= 0The channel number associated with the message that was processed. -1No channel message arrived before the timeout. -2Another error occurred, such as a lost connection. The method processes one pending channel message. Ordinary data is appended to the appropriate channel buffer, and control messages can update EOF, CLOSE, and exit-status state. It does not consume bytes already buffered for the application. A channel is not returned again merely because unread bytes remain buffered, but it can be returned again for a later stderr, EOF, exit-status, or CLOSE message. Drain available stdout and stderr promptly after a channel number is returned. A later wait or poll that processes final closure can remove the completed channel record and make its buffered data and status unavailable.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#WaitForChannelMessage
	 * @param int $pollTimeoutMs
	 * @return int
	 */
	function WaitForChannelMessage($pollTimeoutMs) {
		return CkSsh_WaitForChannelMessage($this->_cPtr,$pollTimeoutMs);
	}

	/**
	 * Creates an asynchronous task to call the WaitForChannelMessage method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkSshRef.html#WaitForChannelMessageAsync
	 * @param int $pollTimeoutMs
	 * @return ?CkTask
	 */
	function WaitForChannelMessageAsync($pollTimeoutMs) {
		return chilkat::_obj(CkSsh_WaitForChannelMessageAsync($this->_cPtr,$pollTimeoutMs),'CkTask');
	}
}
