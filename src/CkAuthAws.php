<?php

/* CkAuthAws.php -- Chilkat PHP extension proxy class CkAuthAws.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Configure AWS request signing for REST calls and temporary pre-signed URLs.
 *
 * Chilkat.AuthAws holds the AWS authentication settings used by Chilkat.Rest when sending signed AWS requests. It supports AWS Signature Version 4 by default, can optionally use the older Signature Version 2 where applicable, accepts direct credential values or secure secret specifiers, and can generate temporary Signature V4 pre-signed URLs for AWS services.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html
 */
class CkAuthAws {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkAuthAws') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkAuthAws();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkAuthAws_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkAuthAws_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * The AWS access key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#AccessKey
	 * @param CkString $str
	 */
	function get_AccessKey($str) {
		CkAuthAws_get_AccessKey($this->_cPtr,$str);
	}

	/**
	 * The AWS access key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#AccessKey
	 * @return ?string
	 */
	function accessKey() {
		return CkAuthAws_accessKey($this->_cPtr);
	}

	/**
	 * The AWS access key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#AccessKey
	 * @param ?string $newVal
	 */
	function put_AccessKey($newVal) {
		CkAuthAws_put_AccessKey($this->_cPtr,$newVal);
	}

	/**
	 * If AWS Signature Version V2 is used, then this property must be set.  The rules for setting the canonicalized resource for the V2 signature method is described here: Constructing the CanonicalizedResource Element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#CanonicalizedResourceV2
	 * @param CkString $str
	 */
	function get_CanonicalizedResourceV2($str) {
		CkAuthAws_get_CanonicalizedResourceV2($this->_cPtr,$str);
	}

	/**
	 * If AWS Signature Version V2 is used, then this property must be set.  The rules for setting the canonicalized resource for the V2 signature method is described here: Constructing the CanonicalizedResource Element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#CanonicalizedResourceV2
	 * @return ?string
	 */
	function canonicalizedResourceV2() {
		return CkAuthAws_canonicalizedResourceV2($this->_cPtr);
	}

	/**
	 * If AWS Signature Version V2 is used, then this property must be set.  The rules for setting the canonicalized resource for the V2 signature method is described here: Constructing the CanonicalizedResource Element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#CanonicalizedResourceV2
	 * @param ?string $newVal
	 */
	function put_CanonicalizedResourceV2($newVal) {
		CkAuthAws_put_CanonicalizedResourceV2($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkAuthAws_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkAuthAws_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkAuthAws_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_ a secret specifier of the format "!!part1|part2|.." can be provided instead of the actual value for the AccessKey and SecretKey properties.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#EnableSecrets
	 * @return bool
	 */
	function get_EnableSecrets() {
		return CkAuthAws_get_EnableSecrets($this->_cPtr);
	}

	/**
	 * If _TRUE_ a secret specifier of the format "!!part1|part2|.." can be provided instead of the actual value for the AccessKey and SecretKey properties.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#EnableSecrets
	 * @param bool $newVal
	 */
	function put_EnableSecrets($newVal) {
		CkAuthAws_put_EnableSecrets($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkAuthAws_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkAuthAws_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkAuthAws_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkAuthAws_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkAuthAws_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkAuthAws_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkAuthAws_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkAuthAws_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the base64 encoding of the 16 bytes of the MD5 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed MD5  is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the MD5 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedMd5
	 * @param CkString $str
	 */
	function get_PrecomputedMd5($str) {
		CkAuthAws_get_PrecomputedMd5($this->_cPtr,$str);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the base64 encoding of the 16 bytes of the MD5 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed MD5  is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the MD5 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedMd5
	 * @return ?string
	 */
	function precomputedMd5() {
		return CkAuthAws_precomputedMd5($this->_cPtr);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the base64 encoding of the 16 bytes of the MD5 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed MD5  is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the MD5 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedMd5
	 * @param ?string $newVal
	 */
	function put_PrecomputedMd5($newVal) {
		CkAuthAws_put_PrecomputedMd5($this->_cPtr,$newVal);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the lowercase hex encoding of the 32-bytes of the SHA256 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed SHA-256 is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the SHA-256 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedSha256
	 * @param CkString $str
	 */
	function get_PrecomputedSha256($str) {
		CkAuthAws_get_PrecomputedSha256($this->_cPtr,$str);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the lowercase hex encoding of the 32-bytes of the SHA256 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed SHA-256 is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the SHA-256 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedSha256
	 * @return ?string
	 */
	function precomputedSha256() {
		return CkAuthAws_precomputedSha256($this->_cPtr);
	}

	/**
	 * This property can optionally be set for AWS requests that have a non-empty request body.  This should be the lowercase hex encoding of the 32-bytes of the SHA256 hash. The most common need for this is if doing an S3 upload from a stream.  (If the pre-computed SHA-256 is not provided, then Chilkat is forced to stream the entire file into memory so that it can calculate the SHA-256 for authentication.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#PrecomputedSha256
	 * @param ?string $newVal
	 */
	function put_PrecomputedSha256($newVal) {
		CkAuthAws_put_PrecomputedSha256($this->_cPtr,$newVal);
	}

	/**
	 * The AWS  region, such as us-east-1, us-west-2, eu-west-1, eu-central-1, etc.  The default is us-east-1.  It is only used when the SignatureVersion property is set to 4.  This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#Region
	 * @param CkString $str
	 */
	function get_Region($str) {
		CkAuthAws_get_Region($this->_cPtr,$str);
	}

	/**
	 * The AWS  region, such as us-east-1, us-west-2, eu-west-1, eu-central-1, etc.  The default is us-east-1.  It is only used when the SignatureVersion property is set to 4.  This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#Region
	 * @return ?string
	 */
	function region() {
		return CkAuthAws_region($this->_cPtr);
	}

	/**
	 * The AWS  region, such as us-east-1, us-west-2, eu-west-1, eu-central-1, etc.  The default is us-east-1.  It is only used when the SignatureVersion property is set to 4.  This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#Region
	 * @param ?string $newVal
	 */
	function put_Region($newVal) {
		CkAuthAws_put_Region($this->_cPtr,$newVal);
	}

	/**
	 * The AWS secret key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#SecretKey
	 * @param CkString $str
	 */
	function get_SecretKey($str) {
		CkAuthAws_get_SecretKey($this->_cPtr,$str);
	}

	/**
	 * The AWS secret key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#SecretKey
	 * @return ?string
	 */
	function secretKey() {
		return CkAuthAws_secretKey($this->_cPtr);
	}

	/**
	 * The AWS secret key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#SecretKey
	 * @param ?string $newVal
	 */
	function put_SecretKey($newVal) {
		CkAuthAws_put_SecretKey($this->_cPtr,$newVal);
	}

	/**
	 * The AWS service namespace, such as s3, ses, etc.  See http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html#genref-aws-service-namespaces This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#ServiceName
	 * @param CkString $str
	 */
	function get_ServiceName($str) {
		CkAuthAws_get_ServiceName($this->_cPtr,$str);
	}

	/**
	 * The AWS service namespace, such as s3, ses, etc.  See http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html#genref-aws-service-namespaces This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#ServiceName
	 * @return ?string
	 */
	function serviceName() {
		return CkAuthAws_serviceName($this->_cPtr);
	}

	/**
	 * The AWS service namespace, such as s3, ses, etc.  See http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html#genref-aws-service-namespaces This property is unused when the SignatureVersion property is set to 2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#ServiceName
	 * @param ?string $newVal
	 */
	function put_ServiceName($newVal) {
		CkAuthAws_put_ServiceName($this->_cPtr,$newVal);
	}

	/**
	 * The AWS Signature Version to be used in authentication.  The default value is 4.  This can optionally be set to the value 2 to use the older V2 signature version.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#SignatureVersion
	 * @return int
	 */
	function get_SignatureVersion() {
		return CkAuthAws_get_SignatureVersion($this->_cPtr);
	}

	/**
	 * The AWS Signature Version to be used in authentication.  The default value is 4.  This can optionally be set to the value 2 to use the older V2 signature version.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#SignatureVersion
	 * @param int $newVal
	 */
	function put_SignatureVersion($newVal) {
		CkAuthAws_put_SignatureVersion($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkAuthAws_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkAuthAws_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkAuthAws_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkAuthAws_version($this->_cPtr);
	}

	/**
	 * Generates a temporary pre-signed URL for an Amazon AWS service using AWS Signature V4.   Requires that the SecretKey, AccessKey, and Region properties be set to valid values prior to calling this method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAuthAwsRef.html#GenPresignedUrl
	 * @param ?string $httpVerb
	 * @param bool $useHttps
	 * @param ?string $domain
	 * @param ?string $path
	 * @param int $numSecondsValid
	 * @param ?string $awsService
	 * @return ?string
	 */
	function genPresignedUrl($httpVerb,$useHttps,$domain,$path,$numSecondsValid,$awsService) {
		return CkAuthAws_genPresignedUrl($this->_cPtr,$httpVerb,$useHttps,$domain,$path,$numSecondsValid,$awsService);
	}
}
