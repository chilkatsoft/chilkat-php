<?php

/* CkZipCrc.php -- Chilkat PHP extension proxy class CkZipCrc.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Calculate ZIP-compatible CRC32 values for files, bytes, strings, and streamed data.
 *
 * Chilkat.ZipCrc calculates the 32-bit CRC value used in ZIP file headers. It can compute CRC32 values for in-memory byte arrays, BinData, StringBuilder content, strings using a specified character encoding, and entire files. It also supports streaming CRC calculation, allowing applications to process large or incremental data in chunks without holding the full input in memory.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html
 */
class CkZipCrc {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkZipCrc') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkZipCrc();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkZipCrc_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkZipCrc_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkZipCrc_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkZipCrc_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkZipCrc_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkZipCrc_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkZipCrc_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkZipCrc_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkZipCrc_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkZipCrc_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkZipCrc_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkZipCrc_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkZipCrc_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkZipCrc_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkZipCrc_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkZipCrc_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkZipCrc_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkZipCrc_version($this->_cPtr);
	}

	/**
	 * Provides a way to calculate a CRC by streaming the data a chunk at a time. An application would start by calling BeginStream. Then it would add data by calling MoreData for each additional chunk. After the last chunk has been processed, the EndStream method is called to return the CRC.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#BeginStream
	 */
	function BeginStream() {
		CkZipCrc_BeginStream($this->_cPtr);
	}

	/**
	 * Calculates a 32-bit CRC for in-memory byte data.  This is the 32-bit CRC that would be found in a Zip file header if a file containing the ARG1 was added to a zip archive.  Returns the CRC32 of the data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#CalculateCrc
	 * @param CkByteData $data
	 * @return int
	 */
	function CalculateCrc($data) {
		return CkZipCrc_CalculateCrc($this->_cPtr,$data);
	}

	/**
	 * Calculates a CRC32 for the bytes contained in ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#CrcBd
	 * @param CkBinData $bd
	 * @return int
	 */
	function CrcBd($bd) {
		return CkZipCrc_CrcBd($this->_cPtr,$bd);
	}

	/**
	 * Calculates a CRC32 for the string contained in ARG1.  The ARG2 is the byte representation to be used for the ARG1 when calculating the CRC32.  It can be utf-8, utf-16, windows-1252, iso-8859-1, or any of the character encodings (charsets) listed at the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#CrcSb
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @return int
	 */
	function CrcSb($sb,$charset) {
		return CkZipCrc_CrcSb($this->_cPtr,$sb,$charset);
	}

	/**
	 * Calculates a CRC32 for a string.  The ARG2 is the byte representation to be used for the ARG1 when calculating the CRC32.  It can be utf-8, utf-16, windows-1252, iso-8859-1, or any of the character encodings (charsets) listed at the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#CrcString
	 * @param ?string $str
	 * @param ?string $charset
	 * @return int
	 */
	function CrcString($str,$charset) {
		return CkZipCrc_CrcString($this->_cPtr,$str,$charset);
	}

	/**
	 * Finalizes and returns the Zip CRC value calculated by calling BeginStream followed by multiple calls to  MoreData.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#EndStream
	 * @return int
	 */
	function EndStream() {
		return CkZipCrc_EndStream($this->_cPtr);
	}

	/**
	 * Calculates the CRC32 of a file.  The data contained in the file is streamed for the calculation to keep the memory footprint small and constant.   Returns the CRC32 of the file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#FileCrc
	 * @param ?string $path
	 * @return int
	 */
	function FileCrc($path) {
		return CkZipCrc_FileCrc($this->_cPtr,$path);
	}

	/**
	 * Creates an asynchronous task to call the FileCrc method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#FileCrcAsync
	 * @param ?string $path
	 * @return ?CkTask
	 */
	function FileCrcAsync($path) {
		return chilkat::_obj(CkZipCrc_FileCrcAsync($this->_cPtr,$path),'CkTask');
	}

	/**
	 * Loads the caller of the task's async method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkZipCrc_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Adds additional data to the CRC currently being calculated. (See BeginStream for more information.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#MoreData
	 * @param CkByteData $data
	 */
	function MoreData($data) {
		CkZipCrc_MoreData($this->_cPtr,$data);
	}

	/**
	 * Converts a 32-bit integer to a hex string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkZipCrcRef.html#ToHex
	 * @param int $crc
	 * @return ?string
	 */
	function toHex($crc) {
		return CkZipCrc_toHex($this->_cPtr,$crc);
	}
}
