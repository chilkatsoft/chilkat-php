<?php

/* CkJsonArray.php -- Chilkat PHP extension proxy class CkJsonArray.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Read, modify, search, and emit ordered JSON arrays.
 *
 * Chilkat.JsonArray is the JSON array companion to Chilkat.JsonObject. It represents an ordered list of JSON values and provides index-based methods for reading, inserting, replacing, deleting, swapping, searching, and emitting array data. It supports scalar values, nested objects, nested arrays, date/time parsing, exact numeric string handling, compact or pretty output, and search helpers for arrays of strings or arrays of objects.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html
 */
class CkJsonArray {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkJsonArray') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkJsonArray();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkJsonArray_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkJsonArray_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkJsonArray_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkJsonArray_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkJsonArray_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether emitted JSON is compact or pretty-formatted. If _TRUE_, output contains no unnecessary whitespace and is emitted on a single line. If _FALSE_, indentation and line breaks are added for readability. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#EmitCompact
	 * @return bool
	 */
	function get_EmitCompact() {
		return CkJsonArray_get_EmitCompact($this->_cPtr);
	}

	/**
	 * Controls whether emitted JSON is compact or pretty-formatted. If _TRUE_, output contains no unnecessary whitespace and is emitted on a single line. If _FALSE_, indentation and line breaks are added for readability. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#EmitCompact
	 * @param bool $newVal
	 */
	function put_EmitCompact($newVal) {
		CkJsonArray_put_EmitCompact($this->_cPtr,$newVal);
	}

	/**
	 * Controls line endings when pretty-formatted JSON is emitted. If _TRUE_, line breaks use CRLF ( ); if _FALSE_, they use LF ( ). The default is _TRUE_. Applies only to pretty output: When EmitCompact is _TRUE_, the JSON is emitted on one line and this setting has no effect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#EmitCrlf
	 * @return bool
	 */
	function get_EmitCrlf() {
		return CkJsonArray_get_EmitCrlf($this->_cPtr);
	}

	/**
	 * Controls line endings when pretty-formatted JSON is emitted. If _TRUE_, line breaks use CRLF ( ); if _FALSE_, they use LF ( ). The default is _TRUE_. Applies only to pretty output: When EmitCompact is _TRUE_, the JSON is emitted on one line and this setting has no effect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#EmitCrlf
	 * @param bool $newVal
	 */
	function put_EmitCrlf($newVal) {
		CkJsonArray_put_EmitCrlf($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the zero-based array index at which FindString and FindObject begin searching. The default value is 0. Iterating matches: After finding a match at index n, set this property to n + 1 before the next search to continue with later elements.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#FindStartIndex
	 * @return int
	 */
	function get_FindStartIndex() {
		return CkJsonArray_get_FindStartIndex($this->_cPtr);
	}

	/**
	 * Specifies the zero-based array index at which FindString and FindObject begin searching. The default value is 0. Iterating matches: After finding a match at index n, set this property to n + 1 before the next search to continue with later elements.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#FindStartIndex
	 * @param int $newVal
	 */
	function put_FindStartIndex($newVal) {
		CkJsonArray_put_FindStartIndex($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkJsonArray_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkJsonArray_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkJsonArray_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkJsonArray_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkJsonArray_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkJsonArray_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkJsonArray_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkJsonArray_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of values currently contained in the JSON array. Array indexes therefore range from 0 through Size - 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Size
	 * @return int
	 */
	function get_Size() {
		return CkJsonArray_get_Size($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkJsonArray_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkJsonArray_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkJsonArray_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkJsonArray_version($this->_cPtr);
	}

	/**
	 * Inserts a new, empty JSON array at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddArrayAt
	 * @param int $index
	 * @return bool
	 */
	function AddArrayAt($index) {
		return CkJsonArray_AddArrayAt($this->_cPtr,$index);
	}

	/**
	 * Inserts a new, empty JSON array at zero-based position index and updates jarr to reference the newly inserted array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Immediate editing: Because jarr references the inserted array, it can be populated directly without a separate ArrayAt lookup.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddArrayAt2
	 * @param int $index
	 * @param CkJsonArray $jarr
	 * @return bool
	 */
	function AddArrayAt2($index,$jarr) {
		return CkJsonArray_AddArrayAt2($this->_cPtr,$index,$jarr);
	}

	/**
	 * Inserts a new JSON boolean value at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddBoolAt
	 * @param int $index
	 * @param bool $value
	 * @return bool
	 */
	function AddBoolAt($index,$value) {
		return CkJsonArray_AddBoolAt($this->_cPtr,$index,$value);
	}

	/**
	 * Inserts a new JSON integer value at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddIntAt
	 * @param int $index
	 * @param int $value
	 * @return bool
	 */
	function AddIntAt($index,$value) {
		return CkJsonArray_AddIntAt($this->_cPtr,$index,$value);
	}

	/**
	 * Inserts a JSON null value at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddNullAt
	 * @param int $index
	 * @return bool
	 */
	function AddNullAt($index) {
		return CkJsonArray_AddNullAt($this->_cPtr,$index);
	}

	/**
	 * Inserts a JSON number at zero-based position index. The numericStr argument supplies the exact JSON numeric text to store. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Exact numeric text: Use this method when lexical form or precision matters, for example 12345678901234567890, 1.2500, or scientific notation. The supplied text must be valid JSON number syntax; JSON does not permit NaN or infinity.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddNumberAt
	 * @param int $index
	 * @param ?string $numericStr
	 * @return bool
	 */
	function AddNumberAt($index,$numericStr) {
		return CkJsonArray_AddNumberAt($this->_cPtr,$index,$numericStr);
	}

	/**
	 * Inserts a new, empty JSON object at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddObjectAt
	 * @param int $index
	 * @return bool
	 */
	function AddObjectAt($index) {
		return CkJsonArray_AddObjectAt($this->_cPtr,$index);
	}

	/**
	 * Inserts a new, empty JSON object at zero-based position index and updates json to reference the newly inserted object. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Immediate editing: Because json references the inserted object, it can be populated directly without a separate ObjectAt lookup.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddObjectAt2
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function AddObjectAt2($index,$json) {
		return CkJsonArray_AddObjectAt2($this->_cPtr,$index,$json);
	}

	/**
	 * Inserts an independent copy of jsonObj at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Copy semantics: The inserted object is copied rather than referenced. Later changes to the source jsonObj do not serve as edits to the inserted array value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddObjectCopyAt
	 * @param int $index
	 * @param CkJsonObject $jsonObj
	 * @return bool
	 */
	function AddObjectCopyAt($index,$jsonObj) {
		return CkJsonArray_AddObjectCopyAt($this->_cPtr,$index,$jsonObj);
	}

	/**
	 * Inserts a new JSON string value at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddStringAt
	 * @param int $index
	 * @param ?string $value
	 * @return bool
	 */
	function AddStringAt($index,$value) {
		return CkJsonArray_AddStringAt($this->_cPtr,$index,$value);
	}

	/**
	 * Inserts a new JSON unsigned integer value at zero-based position index. Existing elements at that position and after it are shifted one position toward the end of the array. 0Insert as the first array element. -1Append as the last array element. nInsert at zero-based position n. Insert vs. replace: The Add*At methods insert a new array element. Use the corresponding Set*At method when replacing an existing element.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AddUIntAt
	 * @param int $index
	 * @param int $value
	 * @return bool
	 */
	function AddUIntAt($index,$value) {
		return CkJsonArray_AddUIntAt($this->_cPtr,$index,$value);
	}

	/**
	 * Appends copies of all values from jarr to the end of this array, preserving their order. Source is unchanged: The source array is not emptied or moved. This method is useful for concatenating JSON arrays, including arrays belonging to different JSON documents.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#AppendArrayItems
	 * @param CkJsonArray $jarr
	 * @return bool
	 */
	function AppendArrayItems($jarr) {
		return CkJsonArray_AppendArrayItems($this->_cPtr,$jarr);
	}

	/**
	 * Returns the nested JSON array stored at zero-based position index. Reference, not copy: The returned JsonArray refers to the existing nested array in the same JSON document. Changes made through the returned object modify that nested array. Type requirement: The value at index must be an array. Use TypeAt when the value type is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#ArrayAt
	 * @param int $index
	 * @return ?CkJsonArray
	 */
	function ArrayAt($index) {
		return chilkat::_obj(CkJsonArray_ArrayAt($this->_cPtr,$index),'CkJsonArray');
	}

	/**
	 * Updates jarr so that it references the nested JSON array stored at zero-based position index. Reference, not copy: Changes made through jarr modify the existing nested array in the same JSON document.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#ArrayAt2
	 * @param int $index
	 * @param CkJsonArray $jarr
	 * @return bool
	 */
	function ArrayAt2($index,$jarr) {
		return CkJsonArray_ArrayAt2($this->_cPtr,$index,$jarr);
	}

	/**
	 * Returns the boolean value stored at zero-based position index. Type-aware access: Use TypeAt when the JSON type at index is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#BoolAt
	 * @param int $index
	 * @return bool
	 */
	function BoolAt($index) {
		return CkJsonArray_BoolAt($this->_cPtr,$index);
	}

	/**
	 * Removes all values from the array, leaving an empty JSON array ([]).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Clear
	 */
	function Clear() {
		CkJsonArray_Clear($this->_cPtr);
	}

	/**
	 * Parses the date/time value at zero-based position index and loads the result into dateTime. Recognized forms include ISO 8601 timestamps (for example, 2009-11-04T19:55:41Z), RFC 822-style date/time strings, and Unix timestamp integers. JSON has no native date type: Dates are application conventions represented as strings or numbers. These methods recognize common representations and convert them to Chilkat date/time objects.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DateAt
	 * @param int $index
	 * @param CkDateTime $dateTime
	 * @return bool
	 */
	function DateAt($index,$dateTime) {
		return CkJsonArray_DateAt($this->_cPtr,$index,$dateTime);
	}

	/**
	 * Deletes the value at zero-based position index. Elements following the deleted value shift down by one position.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DeleteAt
	 * @param int $index
	 * @return bool
	 */
	function DeleteAt($index) {
		return CkJsonArray_DeleteAt($this->_cPtr,$index);
	}

	/**
	 * Parses the date/time value at zero-based position index and loads the result into dt. If bLocal is _TRUE_, the populated fields represent local time; if _FALSE_, they represent UTC/GMT. Recognized forms include ISO 8601 timestamps, RFC 822-style date/time strings, and Unix timestamp integers. JSON has no native date type: Dates are application conventions represented as strings or numbers. These methods recognize common representations and convert them to Chilkat date/time objects.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#DtAt
	 * @param int $index
	 * @param bool $bLocal
	 * @param CkDtObj $dt
	 * @return bool
	 */
	function DtAt($index,$bLocal,$dt) {
		return CkJsonArray_DtAt($this->_cPtr,$index,$bLocal,$dt);
	}

	/**
	 * Serializes this JSON array and returns the resulting JSON text. Formatting: Set EmitCompact to choose compact versus pretty output. When pretty output is enabled, EmitCrlf selects CRLF or LF line endings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Emit
	 * @return ?string
	 */
	function emit() {
		return CkJsonArray_emit($this->_cPtr);
	}

	/**
	 * Serializes this JSON array into the supplied StringBuilder. Formatting: Set EmitCompact to choose compact versus pretty output. When pretty output is enabled, EmitCrlf selects CRLF or LF line endings. Large JSON: Using a StringBuilder can avoid returning a very large JSON document as a language-native string before passing it to another Chilkat method or writing it to a file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#EmitSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function EmitSb($sb) {
		return CkJsonArray_EmitSb($this->_cPtr,$sb);
	}

	/**
	 * Searches array elements that are JSON objects and returns the zero-based index of the first object whose direct member name has a value matching value. Returns -1 if no match is found. The value pattern may contain *, which matches zero or more characters. Set caseSensitive to control whether letter case must match. Member name, not JSON path: name identifies a direct member of each object being tested; it is not a JSON path. Search start: Searching begins at FindStartIndex, whose default is 0. To continue searching after a match, set FindStartIndex to the previous returned index plus 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#FindObject
	 * @param ?string $name
	 * @param ?string $value
	 * @param bool $caseSensitive
	 * @return int
	 */
	function FindObject($name,$value,$caseSensitive) {
		return CkJsonArray_FindObject($this->_cPtr,$name,$value,$caseSensitive);
	}

	/**
	 * Searches for the first string array element matching value and returns its zero-based index, or -1 if no match is found. The pattern may contain *, which matches zero or more characters. Set caseSensitive to control whether letter case must match. Search start: Searching begins at FindStartIndex, whose default is 0. To continue searching after a match, set FindStartIndex to the previous returned index plus 1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#FindString
	 * @param ?string $value
	 * @param bool $caseSensitive
	 * @return int
	 */
	function FindString($value,$caseSensitive) {
		return CkJsonArray_FindString($this->_cPtr,$value,$caseSensitive);
	}

	/**
	 * Returns the integer value stored at zero-based position index. Type-aware access: Use TypeAt when the JSON type at index is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#IntAt
	 * @param int $index
	 * @return int
	 */
	function IntAt($index) {
		return CkJsonArray_IntAt($this->_cPtr,$index);
	}

	/**
	 * Returns _TRUE_ if the value at zero-based position index is the JSON value null; otherwise returns _FALSE_. JSON null: null is a distinct JSON value. It is not the same as an empty string, zero, false, an empty object, or an empty array.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#IsNullAt
	 * @param int $index
	 * @return bool
	 */
	function IsNullAt($index) {
		return CkJsonArray_IsNullAt($this->_cPtr,$index);
	}

	/**
	 * Parses the JSON array text in jsonArray and loads it into this object. The input must represent a JSON array, beginning with [ and ending with ]. Important — loading detaches the array: Calling this method makes the JsonArray its own independent JSON document. If the object previously referenced an array nested inside another JSON document, that relationship is discarded. For this reason, call Load/LoadSb on a new JsonArray instance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Load
	 * @param ?string $jsonArray
	 * @return bool
	 */
	function Load($jsonArray) {
		return CkJsonArray_Load($this->_cPtr,$jsonArray);
	}

	/**
	 * Parses the JSON array contained in sb and loads it into this object. The input must represent a JSON array, beginning with [ and ending with ]. Important — loading detaches the array: Calling this method makes the JsonArray its own independent JSON document. If the object previously referenced an array nested inside another JSON document, that relationship is discarded. For this reason, call Load/LoadSb on a new JsonArray instance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#LoadSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function LoadSb($sb) {
		return CkJsonArray_LoadSb($this->_cPtr,$sb);
	}

	/**
	 * Returns the JSON object stored at zero-based position index. Reference, not copy: The returned JsonObject refers to the existing object in the same JSON document. Changes made through the returned object modify that nested object. Type requirement: The value at index must be an object. Use TypeAt when the value type is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#ObjectAt
	 * @param int $index
	 * @return ?CkJsonObject
	 */
	function ObjectAt($index) {
		return chilkat::_obj(CkJsonArray_ObjectAt($this->_cPtr,$index),'CkJsonObject');
	}

	/**
	 * Updates jsonObj so that it references the JSON object stored at zero-based position index. Reference, not copy: Changes made through jsonObj modify the existing object in the same JSON document.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#ObjectAt2
	 * @param int $index
	 * @param CkJsonObject $jsonObj
	 * @return bool
	 */
	function ObjectAt2($index,$jsonObj) {
		return CkJsonArray_ObjectAt2($this->_cPtr,$index,$jsonObj);
	}

	/**
	 * Replaces the value at zero-based position index with the specified JSON boolean value. The array length and the positions of other elements are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetBoolAt
	 * @param int $index
	 * @param bool $value
	 * @return bool
	 */
	function SetBoolAt($index,$value) {
		return CkJsonArray_SetBoolAt($this->_cPtr,$index,$value);
	}

	/**
	 * Replaces the value at zero-based position index with the specified JSON integer value. The array length and the positions of other elements are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetIntAt
	 * @param int $index
	 * @param int $value
	 * @return bool
	 */
	function SetIntAt($index,$value) {
		return CkJsonArray_SetIntAt($this->_cPtr,$index,$value);
	}

	/**
	 * Replaces the value at zero-based position index with the JSON value null. The array length and the positions of other elements are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetNullAt
	 * @param int $index
	 * @return bool
	 */
	function SetNullAt($index) {
		return CkJsonArray_SetNullAt($this->_cPtr,$index);
	}

	/**
	 * Replaces the value at zero-based position index with a JSON number. The value argument supplies the exact JSON numeric text to store. Exact numeric text: Use this method when lexical form or precision matters. The supplied text must be valid JSON number syntax; JSON does not permit NaN or infinity.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetNumberAt
	 * @param int $index
	 * @param ?string $value
	 * @return bool
	 */
	function SetNumberAt($index,$value) {
		return CkJsonArray_SetNumberAt($this->_cPtr,$index,$value);
	}

	/**
	 * Replaces the value at zero-based position index with the specified JSON string value. The array length and the positions of other elements are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetStringAt
	 * @param int $index
	 * @param ?string $value
	 * @return bool
	 */
	function SetStringAt($index,$value) {
		return CkJsonArray_SetStringAt($this->_cPtr,$index,$value);
	}

	/**
	 * Replaces the value at zero-based position index with the specified JSON unsigned integer value. The array length and the positions of other elements are unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#SetUIntAt
	 * @param int $index
	 * @param int $value
	 * @return bool
	 */
	function SetUIntAt($index,$value) {
		return CkJsonArray_SetUIntAt($this->_cPtr,$index,$value);
	}

	/**
	 * Returns the string value stored at zero-based position index. Type-aware access: Use TypeAt when the JSON type at index is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#StringAt
	 * @param int $index
	 * @return ?string
	 */
	function stringAt($index) {
		return CkJsonArray_stringAt($this->_cPtr,$index);
	}

	/**
	 * Exchanges the values at zero-based positions index1 and index2. The array length does not change.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#Swap
	 * @param int $index1
	 * @param int $index2
	 * @return bool
	 */
	function Swap($index1,$index2) {
		return CkJsonArray_Swap($this->_cPtr,$index1,$index2);
	}

	/**
	 * Returns an integer identifying the JSON type of the value at zero-based position index. Returns -1 if no value exists at that index. 1string 2number 3object 4array 5boolean 6null Why use TypeAt: JSON arrays may contain mixed types. Checking the type first avoids choosing a type-specific accessor such as ObjectAt, ArrayAt, or IntAt for the wrong kind of value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#TypeAt
	 * @param int $index
	 * @return int
	 */
	function TypeAt($index) {
		return CkJsonArray_TypeAt($this->_cPtr,$index);
	}

	/**
	 * Returns the unsigned integer value stored at zero-based position index. Type-aware access: Use TypeAt when the JSON type at index is not already known.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJsonArrayRef.html#UIntAt
	 * @param int $index
	 * @return int
	 */
	function UIntAt($index) {
		return CkJsonArray_UIntAt($this->_cPtr,$index);
	}
}
