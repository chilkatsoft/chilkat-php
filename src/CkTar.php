<?php

/* CkTar.php -- Chilkat PHP extension proxy class CkTar.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Create, list, verify, filter, and extract TAR archives and compressed TAR formats.
 *
 * Chilkat.Tar creates, verifies, lists, and extracts TAR archives, including compressed TAR formats such as .tar.gz, .tgz, .tar.bz2, and .tar.Z. It supports adding individual files and directory trees, controlling archive paths, preserving or overriding file permissions, filtering included or extracted files, extracting from memory, extracting selected entries to memory or BinData, producing XML listings, and creating Debian .deb package archives from control and data tarballs.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html
 */
class CkTar {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkTar') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkTar();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkTar_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkTar_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html
	 * @param ?CkTarProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkTar_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Controls whether extraction methods also capture an XML inventory of the archive. When set to _TRUE_, methods such as Untar, UntarGz, UntarBz2, and UntarZ store the listing in XmlListing. The XML has the same structure as the string returned by ListXml. The default is _FALSE_. Inspect without extracting: Set CaptureXmlListing and SuppressOutput to _TRUE_ before an untar operation to scan the archive and obtain its XML listing without writing archive entries to disk.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#CaptureXmlListing
	 * @return bool
	 */
	function get_CaptureXmlListing() {
		return CkTar_get_CaptureXmlListing($this->_cPtr);
	}

	/**
	 * Controls whether extraction methods also capture an XML inventory of the archive. When set to _TRUE_, methods such as Untar, UntarGz, UntarBz2, and UntarZ store the listing in XmlListing. The XML has the same structure as the string returned by ListXml. The default is _FALSE_. Inspect without extracting: Set CaptureXmlListing and SuppressOutput to _TRUE_ before an untar operation to scan the archive and obtain its XML listing without writing archive entries to disk.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#CaptureXmlListing
	 * @param bool $newVal
	 */
	function put_CaptureXmlListing($newVal) {
		CkTar_put_CaptureXmlListing($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the character encoding used to decode filenames stored in a TAR archive during extraction. The default is utf-8, which is normally appropriate for modern archives. This property affects archive entry names only. It does not convert or otherwise alter the contents of extracted files. TAR archives written by the WriteTar* methods store filenames as UTF-8. Filename interoperability: The original TAR header format does not always identify the character encoding of a filename. Change this property only when an older archive is known to use a different encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Charset
	 * @param CkString $str
	 */
	function get_Charset($str) {
		CkTar_get_Charset($this->_cPtr,$str);
	}

	/**
	 * Specifies the character encoding used to decode filenames stored in a TAR archive during extraction. The default is utf-8, which is normally appropriate for modern archives. This property affects archive entry names only. It does not convert or otherwise alter the contents of extracted files. TAR archives written by the WriteTar* methods store filenames as UTF-8. Filename interoperability: The original TAR header format does not always identify the character encoding of a filename. Change this property only when an older archive is known to use a different encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Charset
	 * @return ?string
	 */
	function charset() {
		return CkTar_charset($this->_cPtr);
	}

	/**
	 * Specifies the character encoding used to decode filenames stored in a TAR archive during extraction. The default is utf-8, which is normally appropriate for modern archives. This property affects archive entry names only. It does not convert or otherwise alter the contents of extracted files. TAR archives written by the WriteTar* methods store filenames as UTF-8. Filename interoperability: The original TAR header format does not always identify the character encoding of a filename. Change this property only when an older archive is known to use a different encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Charset
	 * @param ?string $newVal
	 */
	function put_Charset($newVal) {
		CkTar_put_Charset($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkTar_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkTar_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkTar_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the UNIX permission bits written to the TAR header for directory entries created by the WriteTar* methods. The default is octal 0755, which represents rwxr-xr-x. Archive metadata: This value records permissions in the archive. Whether the exact permissions are restored during extraction depends on the destination operating system, filesystem, process privileges, and local security policy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DirMode
	 * @return int
	 */
	function get_DirMode() {
		return CkTar_get_DirMode($this->_cPtr);
	}

	/**
	 * Specifies the UNIX permission bits written to the TAR header for directory entries created by the WriteTar* methods. The default is octal 0755, which represents rwxr-xr-x. Archive metadata: This value records permissions in the archive. Whether the exact permissions are restored during extraction depends on the destination operating system, filesystem, process privileges, and local security policy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DirMode
	 * @param int $newVal
	 */
	function put_DirMode($newVal) {
		CkTar_put_DirMode($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a path prefix to prepend to entries when a TAR archive is created. For example, setting this property to subdir1 causes an entry such as report.txt to be stored as subdir1/report.txt. The prefix changes only the path stored in the archive; it does not rename or move the source file on disk. AddFile2 exception: AddFile2 explicitly supplies the complete path within the archive, so DirPrefix is not applied to files added by that method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DirPrefix
	 * @param CkString $str
	 */
	function get_DirPrefix($str) {
		CkTar_get_DirPrefix($this->_cPtr,$str);
	}

	/**
	 * Specifies a path prefix to prepend to entries when a TAR archive is created. For example, setting this property to subdir1 causes an entry such as report.txt to be stored as subdir1/report.txt. The prefix changes only the path stored in the archive; it does not rename or move the source file on disk. AddFile2 exception: AddFile2 explicitly supplies the complete path within the archive, so DirPrefix is not applied to files added by that method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DirPrefix
	 * @return ?string
	 */
	function dirPrefix() {
		return CkTar_dirPrefix($this->_cPtr);
	}

	/**
	 * Specifies a path prefix to prepend to entries when a TAR archive is created. For example, setting this property to subdir1 causes an entry such as report.txt to be stored as subdir1/report.txt. The prefix changes only the path stored in the archive; it does not rename or move the source file on disk. AddFile2 exception: AddFile2 explicitly supplies the complete path within the archive, so DirPrefix is not applied to files added by that method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#DirPrefix
	 * @param ?string $newVal
	 */
	function put_DirPrefix($newVal) {
		CkTar_put_DirPrefix($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the UNIX permission bits written to the TAR header for ordinary file entries created by the WriteTar* methods. The default is octal 0644, which represents rw-r--r--. Shell scripts: Files recognized as shell scripts use ScriptFileMode instead, allowing executable permissions to be recorded separately.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#FileMode
	 * @return int
	 */
	function get_FileMode() {
		return CkTar_get_FileMode($this->_cPtr);
	}

	/**
	 * Specifies the UNIX permission bits written to the TAR header for ordinary file entries created by the WriteTar* methods. The default is octal 0644, which represents rw-r--r--. Shell scripts: Files recognized as shell scripts use ScriptFileMode instead, allowing executable permissions to be recorded separately.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#FileMode
	 * @param int $newVal
	 */
	function put_FileMode($newVal) {
		CkTar_put_FileMode($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the numeric group identifier (GID) written to TAR headers when an archive is created. The default value is 1000. Ownership metadata: A stored GID is archive metadata. Restoring ownership may require elevated privileges and may be ignored or mapped differently on non-UNIX systems.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GroupId
	 * @return int
	 */
	function get_GroupId() {
		return CkTar_get_GroupId($this->_cPtr);
	}

	/**
	 * Specifies the numeric group identifier (GID) written to TAR headers when an archive is created. The default value is 1000. Ownership metadata: A stored GID is archive metadata. Restoring ownership may require elevated privileges and may be ignored or mapped differently on non-UNIX systems.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GroupId
	 * @param int $newVal
	 */
	function put_GroupId($newVal) {
		CkTar_put_GroupId($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the group name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a group name and the numeric GroupId. Extraction tools and operating systems may give different precedence to these fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GroupName
	 * @param CkString $str
	 */
	function get_GroupName($str) {
		CkTar_get_GroupName($this->_cPtr,$str);
	}

	/**
	 * Specifies the group name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a group name and the numeric GroupId. Extraction tools and operating systems may give different precedence to these fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GroupName
	 * @return ?string
	 */
	function groupName() {
		return CkTar_groupName($this->_cPtr);
	}

	/**
	 * Specifies the group name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a group name and the numeric GroupId. Extraction tools and operating systems may give different precedence to these fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GroupName
	 * @param ?string $newVal
	 */
	function put_GroupName($newVal) {
		CkTar_put_GroupName($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the approximate interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables periodic AbortCheck callbacks. Cancellation is cooperative: A callback can request that an operation stop, but the request is observed only when the running method reaches a cancellation check. Short operations may finish before a callback occurs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkTar_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Specifies the approximate interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables periodic AbortCheck callbacks. Cancellation is cooperative: A callback can request that an operation stop, but the request is observed only when the running method reaches a cancellation check. Short operations may finish before a callback occurs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkTar_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkTar_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkTar_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkTar_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkTar_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkTar_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkTar_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkTar_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkTar_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether the patterns in MustMatch and MustNotMatch are matched case-sensitively. The default is _FALSE_. Cross-platform behavior: Case sensitivity in the archive filter is independent of whether the local filesystem itself treats filenames as case-sensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MatchCaseSensitive
	 * @return bool
	 */
	function get_MatchCaseSensitive() {
		return CkTar_get_MatchCaseSensitive($this->_cPtr);
	}

	/**
	 * Controls whether the patterns in MustMatch and MustNotMatch are matched case-sensitively. The default is _FALSE_. Cross-platform behavior: Case sensitivity in the archive filter is independent of whether the local filesystem itself treats filenames as case-sensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MatchCaseSensitive
	 * @param bool $newVal
	 */
	function put_MatchCaseSensitive($newVal) {
		CkTar_put_MatchCaseSensitive($this->_cPtr,$newVal);
	}

	/**
	 * Specifies an inclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path is included or extracted only when it matches this pattern. The default is an empty string, which permits all paths. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.txt selects paths ending in .txt. Case handling is controlled by MatchCaseSensitive. Combined filters: If both MustMatch and MustNotMatch are set, a path must satisfy the inclusion pattern and must not satisfy the exclusion pattern.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustMatch
	 * @param CkString $str
	 */
	function get_MustMatch($str) {
		CkTar_get_MustMatch($this->_cPtr,$str);
	}

	/**
	 * Specifies an inclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path is included or extracted only when it matches this pattern. The default is an empty string, which permits all paths. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.txt selects paths ending in .txt. Case handling is controlled by MatchCaseSensitive. Combined filters: If both MustMatch and MustNotMatch are set, a path must satisfy the inclusion pattern and must not satisfy the exclusion pattern.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustMatch
	 * @return ?string
	 */
	function mustMatch() {
		return CkTar_mustMatch($this->_cPtr);
	}

	/**
	 * Specifies an inclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path is included or extracted only when it matches this pattern. The default is an empty string, which permits all paths. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.txt selects paths ending in .txt. Case handling is controlled by MatchCaseSensitive. Combined filters: If both MustMatch and MustNotMatch are set, a path must satisfy the inclusion pattern and must not satisfy the exclusion pattern.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustMatch
	 * @param ?string $newVal
	 */
	function put_MustMatch($newVal) {
		CkTar_put_MustMatch($this->_cPtr,$newVal);
	}

	/**
	 * Specifies an exclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path that matches this pattern is skipped. The default is an empty string, which excludes nothing. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.obj skips paths ending in .obj. Case handling is controlled by MatchCaseSensitive. Exclusion takes effect after inclusion: When both filters are set, matching MustMatch does not override a match in MustNotMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustNotMatch
	 * @param CkString $str
	 */
	function get_MustNotMatch($str) {
		CkTar_get_MustNotMatch($this->_cPtr,$str);
	}

	/**
	 * Specifies an exclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path that matches this pattern is skipped. The default is an empty string, which excludes nothing. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.obj skips paths ending in .obj. Case handling is controlled by MatchCaseSensitive. Exclusion takes effect after inclusion: When both filters are set, matching MustMatch does not override a match in MustNotMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustNotMatch
	 * @return ?string
	 */
	function mustNotMatch() {
		return CkTar_mustNotMatch($this->_cPtr);
	}

	/**
	 * Specifies an exclusion pattern applied when creating or extracting TAR archives, including compressed TAR formats. A candidate path that matches this pattern is skipped. The default is an empty string, which excludes nothing. The pattern may contain zero or more asterisks (*), where each asterisk matches zero or more characters. For example, *.obj skips paths ending in .obj. Case handling is controlled by MatchCaseSensitive. Exclusion takes effect after inclusion: When both filters are set, matching MustMatch does not override a match in MustNotMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#MustNotMatch
	 * @param ?string $newVal
	 */
	function put_MustNotMatch($newVal) {
		CkTar_put_MustNotMatch($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether absolute archive paths are converted to relative paths during extraction. When _TRUE_, a leading / or \ is removed before the entry is written. The default is _TRUE_. Recommended security setting: Keep this property enabled when extracting archives you did not create. Also extract untrusted archives into a dedicated directory and inspect their entry names before relying on the extracted content.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#NoAbsolutePaths
	 * @return bool
	 */
	function get_NoAbsolutePaths() {
		return CkTar_get_NoAbsolutePaths($this->_cPtr);
	}

	/**
	 * Controls whether absolute archive paths are converted to relative paths during extraction. When _TRUE_, a leading / or \ is removed before the entry is written. The default is _TRUE_. Recommended security setting: Keep this property enabled when extracting archives you did not create. Also extract untrusted archives into a dedicated directory and inspect their entry names before relying on the extracted content.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#NoAbsolutePaths
	 * @param bool $newVal
	 */
	function put_NoAbsolutePaths($newVal) {
		CkTar_put_NoAbsolutePaths($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of directory roots currently registered for archive creation by calls to AddDirRoot and AddDirRoot2. Individual files added with AddFile or AddFile2 are not included in this count. Use zero-based indexes from 0 through NumDirRoots - 1 with GetDirRoot.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#NumDirRoots
	 * @return int
	 */
	function get_NumDirRoots() {
		return CkTar_get_NumDirRoots($this->_cPtr);
	}

	/**
	 * Specifies the value reported by PercentDone when an eligible operation is 100% complete. The default is 100. Increasing the scale allows finer-grained integer progress values; for example, a scale of 1000 allows 453 to represent 45.3%. The value is clamped to the range 10 through 100000. This property is available only in environments that support event callbacks, and only methods for which measurable progress is available emit percentage updates. Scale, not frequency: A larger scale increases the numeric resolution of progress values but does not guarantee a callback for every possible value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#PercentDoneScale
	 * @return int
	 */
	function get_PercentDoneScale() {
		return CkTar_get_PercentDoneScale($this->_cPtr);
	}

	/**
	 * Specifies the value reported by PercentDone when an eligible operation is 100% complete. The default is 100. Increasing the scale allows finer-grained integer progress values; for example, a scale of 1000 allows 453 to represent 45.3%. The value is clamped to the range 10 through 100000. This property is available only in environments that support event callbacks, and only methods for which measurable progress is available emit percentage updates. Scale, not frequency: A larger scale increases the numeric resolution of progress values but does not guarantee a callback for every possible value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#PercentDoneScale
	 * @param int $newVal
	 */
	function put_PercentDoneScale($newVal) {
		CkTar_put_PercentDoneScale($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the UNIX permission bits written to TAR headers for shell-script files recognized by extensions such as .sh, .csh, .bash, and .bsh. The default is octal 0755, which records executable permissions. Other files: Non-script files use FileMode; directories use DirMode.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#ScriptFileMode
	 * @return int
	 */
	function get_ScriptFileMode() {
		return CkTar_get_ScriptFileMode($this->_cPtr);
	}

	/**
	 * Specifies the UNIX permission bits written to TAR headers for shell-script files recognized by extensions such as .sh, .csh, .bash, and .bsh. The default is octal 0755, which records executable permissions. Other files: Non-script files use FileMode; directories use DirMode.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#ScriptFileMode
	 * @param int $newVal
	 */
	function put_ScriptFileMode($newVal) {
		CkTar_put_ScriptFileMode($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether untar operations write extracted files and directories to the filesystem. When _TRUE_, methods such as Untar, UntarGz, UntarBz2, and UntarZ scan the archive without producing extracted output. The default is _FALSE_. Archive inventory workflow: Combine this property with CaptureXmlListing to obtain an XML listing without extracting the archive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#SuppressOutput
	 * @return bool
	 */
	function get_SuppressOutput() {
		return CkTar_get_SuppressOutput($this->_cPtr);
	}

	/**
	 * Controls whether untar operations write extracted files and directories to the filesystem. When _TRUE_, methods such as Untar, UntarGz, UntarBz2, and UntarZ scan the archive without producing extracted output. The default is _FALSE_. Archive inventory workflow: Combine this property with CaptureXmlListing to obtain an XML listing without extracting the archive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#SuppressOutput
	 * @param bool $newVal
	 */
	function put_SuppressOutput($newVal) {
		CkTar_put_SuppressOutput($this->_cPtr,$newVal);
	}

	/**
	 * Deprecated. Use MatchCaseSensitive instead. Until this property is removed, reading or setting it has the same effect as reading or setting MatchCaseSensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarCaseSensitive
	 * @return bool
	 */
	function get_UntarCaseSensitive() {
		return CkTar_get_UntarCaseSensitive($this->_cPtr);
	}

	/**
	 * Deprecated. Use MatchCaseSensitive instead. Until this property is removed, reading or setting it has the same effect as reading or setting MatchCaseSensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarCaseSensitive
	 * @param bool $newVal
	 */
	function put_UntarCaseSensitive($newVal) {
		CkTar_put_UntarCaseSensitive($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether untar methods add per-entry extraction details to LastErrorText, LastErrorXml, and LastErrorHtml. The default is _FALSE_. Diagnostic output is not necessarily an error: When this property is enabled, the LastError properties can contain useful extraction trace information even after a successful operation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarDebugLog
	 * @return bool
	 */
	function get_UntarDebugLog() {
		return CkTar_get_UntarDebugLog($this->_cPtr);
	}

	/**
	 * Controls whether untar methods add per-entry extraction details to LastErrorText, LastErrorXml, and LastErrorHtml. The default is _FALSE_. Diagnostic output is not necessarily an error: When this property is enabled, the LastError properties can contain useful extraction trace information even after a successful operation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarDebugLog
	 * @param bool $newVal
	 */
	function put_UntarDebugLog($newVal) {
		CkTar_put_UntarDebugLog($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether directory components from archive entry names are discarded during extraction. When _TRUE_, each file is written directly into UntarFromDir rather than recreating the archived directory tree. The default is _FALSE_. Filename collisions: Different archive entries can have the same final filename after their directory paths are removed. Use this option only when flattening the archive is intentional and potential collisions are acceptable.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarDiscardPaths
	 * @return bool
	 */
	function get_UntarDiscardPaths() {
		return CkTar_get_UntarDiscardPaths($this->_cPtr);
	}

	/**
	 * Controls whether directory components from archive entry names are discarded during extraction. When _TRUE_, each file is written directly into UntarFromDir rather than recreating the archived directory tree. The default is _FALSE_. Filename collisions: Different archive entries can have the same final filename after their directory paths are removed. Use this option only when flattening the archive is intentional and potential collisions are acceptable.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarDiscardPaths
	 * @param bool $newVal
	 */
	function put_UntarDiscardPaths($newVal) {
		CkTar_put_UntarDiscardPaths($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the destination directory for extraction. The default is ., meaning the current working directory of the calling process. When UntarDiscardPaths is _FALSE_, archived subdirectories are recreated beneath this directory. When it is _TRUE_, extracted files are placed directly in this directory. Safer extraction: For untrusted archives, choose a new or otherwise isolated destination directory rather than extracting directly into an application or system directory.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFromDir
	 * @param CkString $str
	 */
	function get_UntarFromDir($str) {
		CkTar_get_UntarFromDir($this->_cPtr,$str);
	}

	/**
	 * Specifies the destination directory for extraction. The default is ., meaning the current working directory of the calling process. When UntarDiscardPaths is _FALSE_, archived subdirectories are recreated beneath this directory. When it is _TRUE_, extracted files are placed directly in this directory. Safer extraction: For untrusted archives, choose a new or otherwise isolated destination directory rather than extracting directly into an application or system directory.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFromDir
	 * @return ?string
	 */
	function untarFromDir() {
		return CkTar_untarFromDir($this->_cPtr);
	}

	/**
	 * Specifies the destination directory for extraction. The default is ., meaning the current working directory of the calling process. When UntarDiscardPaths is _FALSE_, archived subdirectories are recreated beneath this directory. When it is _TRUE_, extracted files are placed directly in this directory. Safer extraction: For untrusted archives, choose a new or otherwise isolated destination directory rather than extracting directly into an application or system directory.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFromDir
	 * @param ?string $newVal
	 */
	function put_UntarFromDir($newVal) {
		CkTar_put_UntarFromDir($this->_cPtr,$newVal);
	}

	/**
	 * Limits the number of matching entries extracted by an untar operation. The default is 0, which means no maximum. For example, set MustMatch to select a particular path and set UntarMaxCount to 1 to stop after the first matching entry is extracted. Archive order matters: When a limit is used, extraction stops after the configured number of matches in archive order.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarMaxCount
	 * @return int
	 */
	function get_UntarMaxCount() {
		return CkTar_get_UntarMaxCount($this->_cPtr);
	}

	/**
	 * Limits the number of matching entries extracted by an untar operation. The default is 0, which means no maximum. For example, set MustMatch to select a particular path and set UntarMaxCount to 1 to stop after the first matching entry is extracted. Archive order matters: When a limit is used, extraction stops after the configured number of matches in archive order.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarMaxCount
	 * @param int $newVal
	 */
	function put_UntarMaxCount($newVal) {
		CkTar_put_UntarMaxCount($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the numeric user identifier (UID) written to TAR headers when an archive is created. The default value is 1000. Ownership metadata: A stored UID does not by itself change the owner of a local source file. Restoring ownership during extraction may require elevated privileges and may not apply on non-UNIX systems.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UserId
	 * @return int
	 */
	function get_UserId() {
		return CkTar_get_UserId($this->_cPtr);
	}

	/**
	 * Specifies the numeric user identifier (UID) written to TAR headers when an archive is created. The default value is 1000. Ownership metadata: A stored UID does not by itself change the owner of a local source file. Restoring ownership during extraction may require elevated privileges and may not apply on non-UNIX systems.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UserId
	 * @param int $newVal
	 */
	function put_UserId($newVal) {
		CkTar_put_UserId($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the user name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a user name and the numeric UserId. Their interpretation depends on the extracting tool and destination system.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UserName
	 * @param CkString $str
	 */
	function get_UserName($str) {
		CkTar_get_UserName($this->_cPtr,$str);
	}

	/**
	 * Specifies the user name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a user name and the numeric UserId. Their interpretation depends on the extracting tool and destination system.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UserName
	 * @return ?string
	 */
	function userName() {
		return CkTar_userName($this->_cPtr);
	}

	/**
	 * Specifies the user name written to TAR headers when an archive is created. By default, Chilkat uses the username of the account running the application. Name versus numeric ID: TAR headers can contain both a user name and the numeric UserId. Their interpretation depends on the extracting tool and destination system.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UserName
	 * @param ?string $newVal
	 */
	function put_UserName($newVal) {
		CkTar_put_UserName($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkTar_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkTar_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkTar_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkTar_version($this->_cPtr);
	}

	/**
	 * Specifies the TAR header format used by the WriteTar* methods. The default is gnu. Supported values are: ValueUse gnuGNU TAR format and extensions. This is the Chilkat default and supports archive metadata beyond classic ustar limits. paxPOSIX pax extended format. It is designed to represent long names and additional metadata through extensible records. ustarPOSIX ustar format. It is broadly interoperable but has stricter limits on path lengths and metadata fields. Choosing a format: Use ustar when compatibility with simple TAR readers is the priority and the archive fits its limits. Use pax for portable extended metadata, or retain gnu when GNU TAR compatibility is expected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteFormat
	 * @param CkString $str
	 */
	function get_WriteFormat($str) {
		CkTar_get_WriteFormat($this->_cPtr,$str);
	}

	/**
	 * Specifies the TAR header format used by the WriteTar* methods. The default is gnu. Supported values are: ValueUse gnuGNU TAR format and extensions. This is the Chilkat default and supports archive metadata beyond classic ustar limits. paxPOSIX pax extended format. It is designed to represent long names and additional metadata through extensible records. ustarPOSIX ustar format. It is broadly interoperable but has stricter limits on path lengths and metadata fields. Choosing a format: Use ustar when compatibility with simple TAR readers is the priority and the archive fits its limits. Use pax for portable extended metadata, or retain gnu when GNU TAR compatibility is expected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteFormat
	 * @return ?string
	 */
	function writeFormat() {
		return CkTar_writeFormat($this->_cPtr);
	}

	/**
	 * Specifies the TAR header format used by the WriteTar* methods. The default is gnu. Supported values are: ValueUse gnuGNU TAR format and extensions. This is the Chilkat default and supports archive metadata beyond classic ustar limits. paxPOSIX pax extended format. It is designed to represent long names and additional metadata through extensible records. ustarPOSIX ustar format. It is broadly interoperable but has stricter limits on path lengths and metadata fields. Choosing a format: Use ustar when compatibility with simple TAR readers is the priority and the archive fits its limits. Use pax for portable extended metadata, or retain gnu when GNU TAR compatibility is expected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteFormat
	 * @param ?string $newVal
	 */
	function put_WriteFormat($newVal) {
		CkTar_put_WriteFormat($this->_cPtr,$newVal);
	}

	/**
	 * Contains the XML listing captured by the most recent untar operation for which CaptureXmlListing was _TRUE_. The XML structure is the same as the output from ListXml. Read after the operation: Use the untar method's return value to confirm that the archive was processed successfully before consuming this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#XmlListing
	 * @param CkString $str
	 */
	function get_XmlListing($str) {
		CkTar_get_XmlListing($this->_cPtr,$str);
	}

	/**
	 * Contains the XML listing captured by the most recent untar operation for which CaptureXmlListing was _TRUE_. The XML structure is the same as the output from ListXml. Read after the operation: Use the untar method's return value to confirm that the archive was processed successfully before consuming this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#XmlListing
	 * @return ?string
	 */
	function xmlListing() {
		return CkTar_xmlListing($this->_cPtr);
	}

	/**
	 * Contains the XML listing captured by the most recent untar operation for which CaptureXmlListing was _TRUE_. The XML structure is the same as the output from ListXml. Read after the operation: Use the untar method's return value to confirm that the archive was processed successfully before consuming this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#XmlListing
	 * @param ?string $newVal
	 */
	function put_XmlListing($newVal) {
		CkTar_put_XmlListing($this->_cPtr,$newVal);
	}

	/**
	 * Adds the directory tree at ARG1 to the set of sources used by the next WriteTar* operation. Call this method multiple times to include multiple directory trees in one archive. Use AddDirRoot2 when the tree should appear beneath an explicit prefix inside the archive. Queued archive sources: Directory roots and files are retained by the object until ClearDirRootsAndFiles is called.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#AddDirRoot
	 * @param ?string $dirPath
	 * @return bool
	 */
	function AddDirRoot($dirPath) {
		return CkTar_AddDirRoot($this->_cPtr,$dirPath);
	}

	/**
	 * Adds the directory tree at ARG2 to the archive-creation source list and stores its entries beneath the archive prefix supplied in ARG1. Call this method multiple times, or combine it with AddDirRoot, before calling a WriteTar* method. ARG1 should be a relative archive path such as abc/123 and should not end with /. If DirPrefix is also set, DirPrefix is applied first. Path composition example: With DirPrefix = "package" and rootPrefix = "assets", files from this root are stored beneath package/assets/.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#AddDirRoot2
	 * @param ?string $rootPrefix
	 * @param ?string $rootPath
	 * @return bool
	 */
	function AddDirRoot2($rootPrefix,$rootPath) {
		return CkTar_AddDirRoot2($this->_cPtr,$rootPrefix,$rootPath);
	}

	/**
	 * Adds the local file at ARG1 to the set of sources used by the next WriteTar* operation. Call AddFile, AddFile2, and the directory-root methods as needed before writing a single archive. Use AddFile2 when the path stored inside the TAR must be specified independently of the source file path. Queued archive sources: Added files and directory roots remain associated with the object until ClearDirRootsAndFiles is called.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#AddFile
	 * @param ?string $path
	 * @return bool
	 */
	function AddFile($path) {
		return CkTar_AddFile($this->_cPtr,$path);
	}

	/**
	 * Adds the local file at ARG1 to the archive-creation source list and stores it using the exact archive path supplied in ARG2. This allows the source path and the path visible inside the TAR to differ. Call this method as many times as needed, optionally together with the other file and directory-root methods, before calling a single WriteTar* method. DirPrefix is not applied: Because ARG2 already specifies the path within the TAR, DirPrefix does not modify entries added by this method. Use forward slashes and a relative path for portable archive names.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#AddFile2
	 * @param ?string $filePath
	 * @param ?string $pathWithinTar
	 * @return bool
	 */
	function AddFile2($filePath,$pathWithinTar) {
		return CkTar_AddFile2($this->_cPtr,$filePath,$pathWithinTar);
	}

	/**
	 * Removes all files and directory roots previously registered by AddDirRoot, AddDirRoot2, AddFile, and AddFile2. Call this method before building a different archive with the same Tar object when the earlier source list should not be reused.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#ClearDirRootsAndFiles
	 * @return bool
	 */
	function ClearDirRootsAndFiles() {
		return CkTar_ClearDirRootsAndFiles($this->_cPtr);
	}

	/**
	 * Creates a Debian binary package at ARG3 from the prepared control archive at ARG1 and data archive at ARG2. The inputs are typically named control.tar.gz and data.tar.gz, or use another compression form accepted by the target Debian tooling. Debian package structure: A modern .deb file is an ar archive containing a debian-binary format marker, a control.tar.* member for package metadata and maintainer scripts, and a data.tar.* member for the installed filesystem payload. Prepared inputs required: This method packages the supplied control and data tarballs. The caller is responsible for making their directory layout, control metadata, permissions, and package contents valid for Debian package-management tools.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#CreateDeb
	 * @param ?string $controlPath
	 * @param ?string $dataPath
	 * @param ?string $debPath
	 * @return bool
	 */
	function CreateDeb($controlPath,$dataPath,$debPath) {
		return CkTar_CreateDeb($this->_cPtr,$controlPath,$dataPath,$debPath);
	}

	/**
	 * Returns a directory root previously added with AddDirRoot or AddDirRoot2. ARG1 is a zero-based index. Valid indexes range from 0 through NumDirRoots - 1. For example, after two roots are added, GetDirRoot(0) returns the first and GetDirRoot(1) returns the second.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GetDirRoot
	 * @param int $index
	 * @return ?string
	 */
	function getDirRoot($index) {
		return CkTar_getDirRoot($this->_cPtr,$index);
	}

	/**
	 * Returns a directory root previously added with AddDirRoot or AddDirRoot2. ARG1 is a zero-based index. Valid indexes range from 0 through NumDirRoots - 1. For example, after two roots are added, GetDirRoot(0) returns the first and GetDirRoot(1) returns the second.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#GetDirRoot
	 * @param int $index
	 * @return ?string
	 */
	function dirRoot($index) {
		return CkTar_dirRoot($this->_cPtr,$index);
	}

	/**
	 * Scans the uncompressed TAR archive at ARG1 and returns an XML description of its files, directories, and archive metadata. The archive is not extracted. Compressed tarballs: For a compressed TAR archive, an untar method can capture the same listing format by setting CaptureXmlListing. Set SuppressOutput as well when no files should be written.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#ListXml
	 * @param ?string $tarPath
	 * @return ?string
	 */
	function listXml($tarPath) {
		return CkTar_listXml($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the ListXml method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#ListXmlAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function ListXmlAsync($tarPath) {
		return chilkat::_obj(CkTar_ListXmlAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Loads the caller object associated with ARG1, an asynchronous Chilkat Task, into this Tar object. Specialized async support: This method supports Chilkat task-caller plumbing and is not normally needed in ordinary archive code. Use it only when a Chilkat example or support instruction specifically requires it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkTar_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Extracts the uncompressed TAR archive at ARG1 into the directory specified by UntarFromDir. Archived directory trees are recreated unless UntarDiscardPaths is _TRUE_. Returns the number of files and directories extracted, or -1 if the operation fails. Extraction is also affected by the matching, path-safety, count-limit, listing, and output-suppression properties of this object. TAR is a container format: An ordinary .tar archive is not compressed. Use UntarGz, UntarBz2, or UntarZ for the corresponding compressed tarball formats.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#Untar
	 * @param ?string $tarPath
	 * @return int
	 */
	function Untar($tarPath) {
		return CkTar_Untar($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the Untar method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function UntarAsync($tarPath) {
		return chilkat::_obj(CkTar_UntarAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Decompresses and extracts the .tar.bz2 archive at ARG1 into UntarFromDir. This format is also commonly named .tbz, .tbz2, or .tb2. Returns _TRUE_ on success and _FALSE_ on failure. The same extraction filters, path controls, listing options, and output settings used by Untar also apply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarBz2
	 * @param ?string $tarPath
	 * @return bool
	 */
	function UntarBz2($tarPath) {
		return CkTar_UntarBz2($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the UntarBz2 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarBz2Async
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function UntarBz2Async($tarPath) {
		return chilkat::_obj(CkTar_UntarBz2Async($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Scans the uncompressed TAR archive at ARG1 and copies the contents of the first file whose archive path matches ARG2 into the BinData object supplied in ARG3. Matching is evaluated in archive order. No archive entry is written to the local filesystem. Returns _TRUE_ when a matching file is successfully extracted to ARG3 and _FALSE_ otherwise.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFirstMatchingToBd
	 * @param ?string $tarPath
	 * @param ?string $matchPattern
	 * @param CkBinData $bd
	 * @return bool
	 */
	function UntarFirstMatchingToBd($tarPath,$matchPattern,$bd) {
		return CkTar_UntarFirstMatchingToBd($this->_cPtr,$tarPath,$matchPattern,$bd);
	}

	/**
	 * Reads an in-memory, uncompressed TAR archive from ARG1 and returns the contents of the first file whose archive path matches ARG2. Matching is evaluated in archive order. No file is written to the local filesystem. Use the normal method-success indicator and LastErrorText to distinguish a successful result from a missing match or processing failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFirstMatchingToMemory
	 * @param CkByteData $tarFileBytes
	 * @param ?string $matchPattern
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function UntarFirstMatchingToMemory($tarFileBytes,$matchPattern,$outBytes) {
		return CkTar_UntarFirstMatchingToMemory($this->_cPtr,$tarFileBytes,$matchPattern,$outBytes);
	}

	/**
	 * Extracts an uncompressed TAR archive supplied as bytes in ARG1. Files and directories are written beneath UntarFromDir, subject to the same filters and path options as Untar. Returns the number of files and directories extracted, or -1 on failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFromMemory
	 * @param CkByteData $tarFileBytes
	 * @return int
	 */
	function UntarFromMemory($tarFileBytes) {
		return CkTar_UntarFromMemory($this->_cPtr,$tarFileBytes);
	}

	/**
	 * Creates an asynchronous task to call the UntarFromMemory method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarFromMemoryAsync
	 * @param CkByteData $tarFileBytes
	 * @return ?CkTask
	 */
	function UntarFromMemoryAsync($tarFileBytes) {
		return chilkat::_obj(CkTar_UntarFromMemoryAsync($this->_cPtr,$tarFileBytes),'CkTask');
	}

	/**
	 * Decompresses and extracts the .tar.gz archive at ARG1 into UntarFromDir. The .tgz extension is a common short form for the same gzip-compressed TAR structure. Returns _TRUE_ on success and _FALSE_ on failure. The same extraction filters, path controls, listing options, and output settings used by Untar also apply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarGz
	 * @param ?string $tarPath
	 * @return bool
	 */
	function UntarGz($tarPath) {
		return CkTar_UntarGz($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the UntarGz method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarGzAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function UntarGzAsync($tarPath) {
		return chilkat::_obj(CkTar_UntarGzAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Decompresses and extracts the .tar.Z archive at ARG1 into UntarFromDir. The .Z suffix denotes the historical UNIX compress format; .taz is a common alternate extension. Returns _TRUE_ on success and _FALSE_ on failure. The same extraction filters, path controls, listing options, and output settings used by Untar also apply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarZ
	 * @param ?string $tarPath
	 * @return bool
	 */
	function UntarZ($tarPath) {
		return CkTar_UntarZ($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the UntarZ method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#UntarZAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function UntarZAsync($tarPath) {
		return chilkat::_obj(CkTar_UntarZAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Opens the uncompressed TAR archive at ARG1 and scans its headers through the end of the archive. Returns _TRUE_ when the archive structure is accepted and no TAR-format errors are found; otherwise returns _FALSE_. What verification does not establish: Structural verification does not prove that archived files are safe, trustworthy, or appropriate to extract. It is not malware scanning and does not authenticate the archive publisher.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#VerifyTar
	 * @param ?string $tarPath
	 * @return bool
	 */
	function VerifyTar($tarPath) {
		return CkTar_VerifyTar($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the VerifyTar method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#VerifyTarAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function VerifyTarAsync($tarPath) {
		return chilkat::_obj(CkTar_VerifyTarAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Creates an uncompressed TAR archive at ARG1 from the files and directory trees previously added with AddFile, AddFile2, AddDirRoot, and AddDirRoot2. Archive paths, permissions, ownership metadata, filters, and header format are controlled by the corresponding properties. Returns _TRUE_ on success and _FALSE_ on failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTar
	 * @param ?string $tarPath
	 * @return bool
	 */
	function WriteTar($tarPath) {
		return CkTar_WriteTar($this->_cPtr,$tarPath);
	}

	/**
	 * Creates an asynchronous task to call the WriteTar method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTarAsync
	 * @param ?string $tarPath
	 * @return ?CkTask
	 */
	function WriteTarAsync($tarPath) {
		return chilkat::_obj(CkTar_WriteTarAsync($this->_cPtr,$tarPath),'CkTask');
	}

	/**
	 * Creates a bzip2-compressed TAR archive at ARG1 from the files and directory trees previously added to this object. Common filename extensions include .tar.bz2, .tbz, .tbz2, and .tb2. Returns _TRUE_ on success and _FALSE_ on failure. Two-layer format: The result is a TAR byte stream compressed with bzip2. TAR supplies the multi-file archive structure; bzip2 supplies compression.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTarBz2
	 * @param ?string $bz2Path
	 * @return bool
	 */
	function WriteTarBz2($bz2Path) {
		return CkTar_WriteTarBz2($this->_cPtr,$bz2Path);
	}

	/**
	 * Creates an asynchronous task to call the WriteTarBz2 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTarBz2Async
	 * @param ?string $bz2Path
	 * @return ?CkTask
	 */
	function WriteTarBz2Async($bz2Path) {
		return chilkat::_obj(CkTar_WriteTarBz2Async($this->_cPtr,$bz2Path),'CkTask');
	}

	/**
	 * Creates a gzip-compressed TAR archive at ARG1 from the files and directory trees previously added to this object. The usual extensions are .tar.gz and .tgz. Returns _TRUE_ on success and _FALSE_ on failure. Two-layer format: The result is a TAR byte stream compressed with gzip. TAR supplies the multi-file archive structure; gzip supplies compression.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTarGz
	 * @param ?string $gzPath
	 * @return bool
	 */
	function WriteTarGz($gzPath) {
		return CkTar_WriteTarGz($this->_cPtr,$gzPath);
	}

	/**
	 * Creates an asynchronous task to call the WriteTarGz method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkTarRef.html#WriteTarGzAsync
	 * @param ?string $gzPath
	 * @return ?CkTask
	 */
	function WriteTarGzAsync($gzPath) {
		return chilkat::_obj(CkTar_WriteTarGzAsync($this->_cPtr,$gzPath),'CkTask');
	}
}
