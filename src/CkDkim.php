<?php

/* CkDkim.php -- Chilkat PHP extension proxy class CkDkim.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Sign and verify MIME email with DKIM signatures.
 *
 * Chilkat.Dkim is the Chilkat class for applying and verifying DKIM signatures on MIME email data. It supports DKIM signing with rsa-sha256 or rsa-sha1, configurable canonicalization, selectable signed headers, signature counting, DNS public-key lookup, public-key caching, public-key prefetching, and detailed verification reporting through VerifyInfo.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html
 */
class CkDkim {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkDkim') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkDkim();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkDkim_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkDkim_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkDkim_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Set to _TRUE_ to request cancellation of a currently running operation. A synchronous operation can be cancelled from another thread by setting this property on the same object. Methods that complete quickly or do not periodically check for cancellation may finish before the request is observed. Cancellation behavior: Treat this as a cancellation request rather than a guarantee of immediate termination. After the method returns, use the method's normal return value and diagnostic properties to determine the outcome.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkDkim_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to request cancellation of a currently running operation. A synchronous operation can be cancelled from another thread by setting this property on the same object. Methods that complete quickly or do not periodically check for cancellation may finish before the request is observed. Cancellation behavior: Treat this as a cancellation request rather than a guarantee of immediate termination. After the method returns, use the method's normal return value and diagnostic properties to determine the outcome.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkDkim_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkDkim_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkDkim_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkDkim_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the signing algorithm written to the a= tag of a newly created DKIM-Signature header. The default is rsa-sha256.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimAlg
	 * @param CkString $str
	 */
	function get_DkimAlg($str) {
		CkDkim_get_DkimAlg($this->_cPtr,$str);
	}

	/**
	 * Specifies the signing algorithm written to the a= tag of a newly created DKIM-Signature header. The default is rsa-sha256.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimAlg
	 * @return ?string
	 */
	function dkimAlg() {
		return CkDkim_dkimAlg($this->_cPtr);
	}

	/**
	 * Specifies the signing algorithm written to the a= tag of a newly created DKIM-Signature header. The default is rsa-sha256.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimAlg
	 * @param ?string $newVal
	 */
	function put_DkimAlg($newVal) {
		CkDkim_put_DkimAlg($this->_cPtr,$newVal);
	}

	/**
	 * Controls the maximum number of bytes from the canonicalized message body used when computing the DKIM body hash. The default is 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimBodyLengthCount
	 * @return int
	 */
	function get_DkimBodyLengthCount() {
		return CkDkim_get_DkimBodyLengthCount($this->_cPtr);
	}

	/**
	 * Controls the maximum number of bytes from the canonicalized message body used when computing the DKIM body hash. The default is 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimBodyLengthCount
	 * @param int $newVal
	 */
	function put_DkimBodyLengthCount($newVal) {
		CkDkim_put_DkimBodyLengthCount($this->_cPtr,$newVal);
	}

	/**
	 * Specifies one DKIM canonicalization algorithm to use for both the signed header fields and the message body. The default is relaxed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimCanon
	 * @param CkString $str
	 */
	function get_DkimCanon($str) {
		CkDkim_get_DkimCanon($this->_cPtr,$str);
	}

	/**
	 * Specifies one DKIM canonicalization algorithm to use for both the signed header fields and the message body. The default is relaxed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimCanon
	 * @return ?string
	 */
	function dkimCanon() {
		return CkDkim_dkimCanon($this->_cPtr);
	}

	/**
	 * Specifies one DKIM canonicalization algorithm to use for both the signed header fields and the message body. The default is relaxed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimCanon
	 * @param ?string $newVal
	 */
	function put_DkimCanon($newVal) {
		CkDkim_put_DkimCanon($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the signing domain written verbatim to the d= tag of the generated DKIM-Signature header. The corresponding public key is normally published beneath the DNS name formed from DkimSelector and this domain. Set this property to a nonempty, syntactically valid domain before calling DkimSign. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; uppercase letters, surrounding whitespace, a trailing dot, and Unicode characters are emitted as supplied.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimDomain
	 * @param CkString $str
	 */
	function get_DkimDomain($str) {
		CkDkim_get_DkimDomain($this->_cPtr,$str);
	}

	/**
	 * Specifies the signing domain written verbatim to the d= tag of the generated DKIM-Signature header. The corresponding public key is normally published beneath the DNS name formed from DkimSelector and this domain. Set this property to a nonempty, syntactically valid domain before calling DkimSign. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; uppercase letters, surrounding whitespace, a trailing dot, and Unicode characters are emitted as supplied.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimDomain
	 * @return ?string
	 */
	function dkimDomain() {
		return CkDkim_dkimDomain($this->_cPtr);
	}

	/**
	 * Specifies the signing domain written verbatim to the d= tag of the generated DKIM-Signature header. The corresponding public key is normally published beneath the DNS name formed from DkimSelector and this domain. Set this property to a nonempty, syntactically valid domain before calling DkimSign. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; uppercase letters, surrounding whitespace, a trailing dot, and Unicode characters are emitted as supplied.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimDomain
	 * @param ?string $newVal
	 */
	function put_DkimDomain($newVal) {
		CkDkim_put_DkimDomain($this->_cPtr,$newVal);
	}

	/**
	 * A colon-separated list of header field names to include in the DKIM signature. The list is written to the signature's h= tag in the same order. The default is: mime-version:date:message-id:subject:from:to:content-type Header-name matching is case-insensitive. For each occurrence of a name in this list, Chilkat selects the next matching header field from the bottom of the message header block upward, as required by DKIM. For example, if the message contains two Subject fields, listing subject once signs the bottommost field, while listing it twice signs both fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimHeaders
	 * @param CkString $str
	 */
	function get_DkimHeaders($str) {
		CkDkim_get_DkimHeaders($this->_cPtr,$str);
	}

	/**
	 * A colon-separated list of header field names to include in the DKIM signature. The list is written to the signature's h= tag in the same order. The default is: mime-version:date:message-id:subject:from:to:content-type Header-name matching is case-insensitive. For each occurrence of a name in this list, Chilkat selects the next matching header field from the bottom of the message header block upward, as required by DKIM. For example, if the message contains two Subject fields, listing subject once signs the bottommost field, while listing it twice signs both fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimHeaders
	 * @return ?string
	 */
	function dkimHeaders() {
		return CkDkim_dkimHeaders($this->_cPtr);
	}

	/**
	 * A colon-separated list of header field names to include in the DKIM signature. The list is written to the signature's h= tag in the same order. The default is: mime-version:date:message-id:subject:from:to:content-type Header-name matching is case-insensitive. For each occurrence of a name in this list, Chilkat selects the next matching header field from the bottom of the message header block upward, as required by DKIM. For example, if the message contains two Subject fields, listing subject once signs the bottommost field, while listing it twice signs both fields.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimHeaders
	 * @param ?string $newVal
	 */
	function put_DkimHeaders($newVal) {
		CkDkim_put_DkimHeaders($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the selector written verbatim to the s= tag of a generated DKIM signature. The selector and DkimDomain form the DNS name used to locate the public key: selector._domainkey.signing-domain For example, selector brisbane and domain chilkatsoft.com produce brisbane._domainkey.chilkatsoft.com. Supply only the selector. Do not include ._domainkey. or the signing domain. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; for example, a complete DNS name is emitted as supplied rather than reduced to a selector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimSelector
	 * @param CkString $str
	 */
	function get_DkimSelector($str) {
		CkDkim_get_DkimSelector($this->_cPtr,$str);
	}

	/**
	 * Specifies the selector written verbatim to the s= tag of a generated DKIM signature. The selector and DkimDomain form the DNS name used to locate the public key: selector._domainkey.signing-domain For example, selector brisbane and domain chilkatsoft.com produce brisbane._domainkey.chilkatsoft.com. Supply only the selector. Do not include ._domainkey. or the signing domain. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; for example, a complete DNS name is emitted as supplied rather than reduced to a selector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimSelector
	 * @return ?string
	 */
	function dkimSelector() {
		return CkDkim_dkimSelector($this->_cPtr);
	}

	/**
	 * Specifies the selector written verbatim to the s= tag of a generated DKIM signature. The selector and DkimDomain form the DNS name used to locate the public key: selector._domainkey.signing-domain For example, selector brisbane and domain chilkatsoft.com produce brisbane._domainkey.chilkatsoft.com. Supply only the selector. Do not include ._domainkey. or the signing domain. If this property is empty, DkimSign returns _FALSE_ without modifying the MIME data. A nonempty value is not otherwise validated or normalized during signing; for example, a complete DNS name is emitted as supplied rather than reduced to a selector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimSelector
	 * @param ?string $newVal
	 */
	function put_DkimSelector($newVal) {
		CkDkim_put_DkimSelector($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the algorithm used when creating a legacy DomainKey-Signature header. The only supported value, and the default, is rsa-sha1. Legacy protocol: DomainKeys is the predecessor to DKIM and is obsolete for modern email authentication. New implementations should use DKIM with rsa-sha256 unless compatibility with an older system specifically requires DomainKeys.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyAlg
	 * @param CkString $str
	 * @deprecated
	 */
	function get_DomainKeyAlg($str) {
		CkDkim_get_DomainKeyAlg($this->_cPtr,$str);
	}

	/**
	 * Specifies the algorithm used when creating a legacy DomainKey-Signature header. The only supported value, and the default, is rsa-sha1. Legacy protocol: DomainKeys is the predecessor to DKIM and is obsolete for modern email authentication. New implementations should use DKIM with rsa-sha256 unless compatibility with an older system specifically requires DomainKeys.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyAlg
	 * @return ?string
	 * @deprecated
	 */
	function domainKeyAlg() {
		return CkDkim_domainKeyAlg($this->_cPtr);
	}

	/**
	 * Specifies the algorithm used when creating a legacy DomainKey-Signature header. The only supported value, and the default, is rsa-sha1. Legacy protocol: DomainKeys is the predecessor to DKIM and is obsolete for modern email authentication. New implementations should use DKIM with rsa-sha256 unless compatibility with an older system specifically requires DomainKeys.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyAlg
	 * @param ?string $newVal
	 * @deprecated
	 */
	function put_DomainKeyAlg($newVal) {
		CkDkim_put_DomainKeyAlg($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the canonicalization algorithm used when creating a legacy DomainKey-Signature header. The default is nofws.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyCanon
	 * @param CkString $str
	 * @deprecated
	 */
	function get_DomainKeyCanon($str) {
		CkDkim_get_DomainKeyCanon($this->_cPtr,$str);
	}

	/**
	 * Specifies the canonicalization algorithm used when creating a legacy DomainKey-Signature header. The default is nofws.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyCanon
	 * @return ?string
	 * @deprecated
	 */
	function domainKeyCanon() {
		return CkDkim_domainKeyCanon($this->_cPtr);
	}

	/**
	 * Specifies the canonicalization algorithm used when creating a legacy DomainKey-Signature header. The default is nofws.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyCanon
	 * @param ?string $newVal
	 * @deprecated
	 */
	function put_DomainKeyCanon($newVal) {
		CkDkim_put_DomainKeyCanon($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the signing domain written to a legacy DomainKey-Signature header. Together with DomainKeySelector, it identifies the DNS location of the DomainKeys public key. Legacy protocol: DomainKeys is obsolete. Use DkimDomain for new DKIM implementations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyDomain
	 * @param CkString $str
	 * @deprecated
	 */
	function get_DomainKeyDomain($str) {
		CkDkim_get_DomainKeyDomain($this->_cPtr,$str);
	}

	/**
	 * Specifies the signing domain written to a legacy DomainKey-Signature header. Together with DomainKeySelector, it identifies the DNS location of the DomainKeys public key. Legacy protocol: DomainKeys is obsolete. Use DkimDomain for new DKIM implementations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyDomain
	 * @return ?string
	 * @deprecated
	 */
	function domainKeyDomain() {
		return CkDkim_domainKeyDomain($this->_cPtr);
	}

	/**
	 * Specifies the signing domain written to a legacy DomainKey-Signature header. Together with DomainKeySelector, it identifies the DNS location of the DomainKeys public key. Legacy protocol: DomainKeys is obsolete. Use DkimDomain for new DKIM implementations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyDomain
	 * @param ?string $newVal
	 * @deprecated
	 */
	function put_DomainKeyDomain($newVal) {
		CkDkim_put_DomainKeyDomain($this->_cPtr,$newVal);
	}

	/**
	 * A colon-separated list of header field names to include in a legacy DomainKeys signature. The default is: mime-version:date:message-id:subject:from:to:content-type Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimHeaders.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyHeaders
	 * @param CkString $str
	 * @deprecated
	 */
	function get_DomainKeyHeaders($str) {
		CkDkim_get_DomainKeyHeaders($this->_cPtr,$str);
	}

	/**
	 * A colon-separated list of header field names to include in a legacy DomainKeys signature. The default is: mime-version:date:message-id:subject:from:to:content-type Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimHeaders.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyHeaders
	 * @return ?string
	 * @deprecated
	 */
	function domainKeyHeaders() {
		return CkDkim_domainKeyHeaders($this->_cPtr);
	}

	/**
	 * A colon-separated list of header field names to include in a legacy DomainKeys signature. The default is: mime-version:date:message-id:subject:from:to:content-type Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimHeaders.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyHeaders
	 * @param ?string $newVal
	 * @deprecated
	 */
	function put_DomainKeyHeaders($newVal) {
		CkDkim_put_DomainKeyHeaders($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the selector used to locate the public key for a legacy DomainKeys signature. The DNS name is formed as: selector._domainkey.signing-domain For example, selector reykjavik and domain example-code.com produce reykjavik._domainkey.example-code.com. Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimSelector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeySelector
	 * @param CkString $str
	 * @deprecated
	 */
	function get_DomainKeySelector($str) {
		CkDkim_get_DomainKeySelector($this->_cPtr,$str);
	}

	/**
	 * Specifies the selector used to locate the public key for a legacy DomainKeys signature. The DNS name is formed as: selector._domainkey.signing-domain For example, selector reykjavik and domain example-code.com produce reykjavik._domainkey.example-code.com. Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimSelector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeySelector
	 * @return ?string
	 * @deprecated
	 */
	function domainKeySelector() {
		return CkDkim_domainKeySelector($this->_cPtr);
	}

	/**
	 * Specifies the selector used to locate the public key for a legacy DomainKeys signature. The DNS name is formed as: selector._domainkey.signing-domain For example, selector reykjavik and domain example-code.com produce reykjavik._domainkey.example-code.com. Legacy protocol: This property applies only to DomainKeys. For modern DKIM signing, use DkimSelector.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeySelector
	 * @param ?string $newVal
	 * @deprecated
	 */
	function put_DomainKeySelector($newVal) {
		CkDkim_put_DomainKeySelector($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck callbacks during methods that support periodic cancellation checks. Set to 0 to disable these callbacks. The default is 0. PrefetchPublicKey invokes AbortCheck while performing its DNS lookup. Setting this property to 1 and requesting cancellation from the first callback causes PrefetchPublicKey to return _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkDkim_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck callbacks during methods that support periodic cancellation checks. Set to 0 to disable these callbacks. The default is 0. PrefetchPublicKey invokes AbortCheck while performing its DNS lookup. Setting this property to 1 and requesting cancellation from the first callback causes PrefetchPublicKey to return _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkDkim_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkDkim_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkDkim_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkDkim_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkDkim_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkDkim_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkDkim_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkDkim_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkDkim_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkDkim_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkDkim_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Contains JSON describing the most recent call to DkimVerify. The property is initially an empty string and is replaced or cleared by each verification attempt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#VerifyInfo
	 * @param CkString $str
	 */
	function get_VerifyInfo($str) {
		CkDkim_get_VerifyInfo($this->_cPtr,$str);
	}

	/**
	 * Contains JSON describing the most recent call to DkimVerify. The property is initially an empty string and is replaced or cleared by each verification attempt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#VerifyInfo
	 * @return ?string
	 */
	function verifyInfo() {
		return CkDkim_verifyInfo($this->_cPtr);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkDkim_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkDkim_version($this->_cPtr);
	}

	/**
	 * Creates a DKIM-Signature header and prepends it to the complete MIME message stored in mimeData. The BinData is modified in place. Before calling this method:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimSign
	 * @param CkBinData $mimeData
	 * @return bool
	 */
	function DkimSign($mimeData) {
		return CkDkim_DkimSign($this->_cPtr,$mimeData);
	}

	/**
	 * Verifies the DKIM-Signature header at zero-based sigIndex in the complete MIME message stored in mimeData. Signatures are indexed in top-to-bottom header order. Use NumDkimSigs to obtain the count. Chilkat first looks for a public key cached under the exact selector/domain strings. If no exact match exists, it synchronously retrieves the TXT record at selector._domainkey.domain. The method does not modify mimeData.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DkimVerify
	 * @param int $sigIndex
	 * @param CkBinData $mimeData
	 * @return bool
	 */
	function DkimVerify($sigIndex,$mimeData) {
		return CkDkim_DkimVerify($this->_cPtr,$sigIndex,$mimeData);
	}

	/**
	 * Creates a legacy DomainKey-Signature header and prepends it to the MIME message stored in mimeData. The BinData is modified in place. Before calling this method, set DomainKeyDomain and DomainKeySelector, and load the matching private key by calling SetDomainKeyPrivateKey.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeySign
	 * @param CkBinData $mimeData
	 * @return bool
	 * @deprecated
	 */
	function DomainKeySign($mimeData) {
		return CkDkim_DomainKeySign($this->_cPtr,$mimeData);
	}

	/**
	 * Verifies the legacy DomainKeys signature at zero-based index sigIndex in the MIME message stored in mimeData. Use NumDomainKeySigs to determine how many DomainKey-Signature headers are present. After the call, inspect VerifyInfo for details about the signature and verification result.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#DomainKeyVerify
	 * @param int $sigIndex
	 * @param CkBinData $mimeData
	 * @return bool
	 * @deprecated
	 */
	function DomainKeyVerify($sigIndex,$mimeData) {
		return CkDkim_DomainKeyVerify($this->_cPtr,$sigIndex,$mimeData);
	}

	/**
	 * Loads and caches an RSA public key in this Dkim object for the exact selector and domain strings. A later DkimVerify uses the cached key when the signature contains the same selector and domain; otherwise verification falls back to DNS. publicKey contains a textual key representation. Supported formats include:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LoadPublicKey
	 * @param ?string $selector
	 * @param ?string $domain
	 * @param ?string $publicKey
	 * @return bool
	 */
	function LoadPublicKey($selector,$domain,$publicKey) {
		return CkDkim_LoadPublicKey($this->_cPtr,$selector,$domain,$publicKey);
	}

	/**
	 * Loads an RSA public key from the local file at publicKeyFilepath and caches it in this Dkim object for the exact selector and domain strings. The file is read during this call. Supported formats include PEM X.509 SubjectPublicKeyInfo, PEM PKCS#1 RSA public key, binary DER PKCS#1 RSA public key, XML RSA key data, and RSA JWK JSON. The selector/domain cache key is case-sensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LoadPublicKeyFile
	 * @param ?string $selector
	 * @param ?string $domain
	 * @param ?string $publicKeyFilepath
	 * @return bool
	 */
	function LoadPublicKeyFile($selector,$domain,$publicKeyFilepath) {
		return CkDkim_LoadPublicKeyFile($this->_cPtr,$selector,$domain,$publicKeyFilepath);
	}

	/**
	 * Loads the original caller object associated with the asynchronous task. This is an internal support method used by Chilkat's asynchronous task infrastructure and is not normally needed in application code.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkDkim_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Returns the number of header fields named DKIM-Signature in mimeData. Header-name matching is case-insensitive. The method counts matching fields without validating the signature syntax; for example, a malformed DKIM-Signature: nonsense field is still counted. The indexes used by DkimVerify are zero-based and follow top-to-bottom header order. The method returns 0 when no matching header is found, including for an empty message. It does not modify mimeData.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#NumDkimSigs
	 * @param CkBinData $mimeData
	 * @return int
	 */
	function NumDkimSigs($mimeData) {
		return CkDkim_NumDkimSigs($this->_cPtr,$mimeData);
	}

	/**
	 * Returns the number of legacy DomainKey-Signature header fields found in the MIME message stored in mimeData. Signature indexes passed to DomainKeyVerify are zero-based and range from 0 through one less than this count. Legacy protocol: This count does not include modern DKIM-Signature headers. Use NumDkimSigs for DKIM.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#NumDomainKeySigs
	 * @param CkBinData $mimeData
	 * @return int
	 * @deprecated
	 */
	function NumDomainKeySigs($mimeData) {
		return CkDkim_NumDomainKeySigs($this->_cPtr,$mimeData);
	}

	/**
	 * Retrieves the DKIM public key from DNS and caches it in this Dkim object for the exact selector and domain strings. The TXT lookup is made for: selector._domainkey.domain The method returns _TRUE_ when the DNS lookup and public-key loading succeed. Repeated calls for a cached pair can complete without another observable lookup. A successful prefetch replaces an existing manually loaded key for the same selector/domain pair.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#PrefetchPublicKey
	 * @param ?string $selector
	 * @param ?string $domain
	 * @return bool
	 */
	function PrefetchPublicKey($selector,$domain) {
		return CkDkim_PrefetchPublicKey($this->_cPtr,$selector,$domain);
	}

	/**
	 * Creates an asynchronous task to call the PrefetchPublicKey method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#PrefetchPublicKeyAsync
	 * @param ?string $selector
	 * @param ?string $domain
	 * @return ?CkTask
	 */
	function PrefetchPublicKeyAsync($selector,$domain) {
		return chilkat::_obj(CkDkim_PrefetchPublicKeyAsync($this->_cPtr,$selector,$domain),'CkTask');
	}

	/**
	 * Copies the RSA key from privateKey into this object for subsequent calls to DkimSign. The source PrivateKey object may be destroyed after this method returns successfully, and the configured key can be reused for multiple signatures. A later successful call replaces the previously configured signing key. A failed call with an empty or unrecognized key clears the usable signing-key state; a subsequent DkimSign fails until a valid key is set again.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#SetDkimPrivateKey
	 * @param CkPrivateKey $privateKey
	 * @return bool
	 */
	function SetDkimPrivateKey($privateKey) {
		return CkDkim_SetDkimPrivateKey($this->_cPtr,$privateKey);
	}

	/**
	 * Associates the RSA privateKey with this object for subsequent calls to DomainKeySign. Legacy protocol: This method is for obsolete DomainKeys signing. New applications should use SetDkimPrivateKey and DkimSign.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkDkimRef.html#SetDomainKeyPrivateKey
	 * @param CkPrivateKey $privateKey
	 * @return bool
	 * @deprecated
	 */
	function SetDomainKeyPrivateKey($privateKey) {
		return CkDkim_SetDomainKeyPrivateKey($this->_cPtr,$privateKey);
	}
}
