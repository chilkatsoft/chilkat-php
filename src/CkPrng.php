<?php

/* CkPrng.php -- Chilkat PHP extension proxy class CkPrng.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Generate random bytes, encoded values, passwords, and application identifiers.
 *
 * Use Chilkat.Prng when an application needs random bytes or random text values for cryptographic or application-level purposes. The class can seed itself automatically from the system entropy source, accept additional entropy from the application, and produce random data as raw bytes, encoded strings, passwords, integers, booleans, GUID-style values, or higher-level identifiers such as Firebase Push IDs.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html
 */
class CkPrng {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkPrng') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkPrng();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkPrng_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkPrng_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkPrng_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkPrng_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkPrng_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkPrng_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkPrng_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkPrng_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkPrng_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkPrng_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkPrng_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkPrng_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkPrng_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Selects the pseudo-random number generator used by this object. The only supported value is fortuna, which is also the default. Chilkat's Fortuna implementation uses AES-256 for generation and SHA-256 in its entropy-processing and reseeding design. Compatibility note: Assigning any value other than fortuna is ignored. Applications should normally leave this property unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#PrngName
	 * @param CkString $str
	 */
	function get_PrngName($str) {
		CkPrng_get_PrngName($this->_cPtr,$str);
	}

	/**
	 * Selects the pseudo-random number generator used by this object. The only supported value is fortuna, which is also the default. Chilkat's Fortuna implementation uses AES-256 for generation and SHA-256 in its entropy-processing and reseeding design. Compatibility note: Assigning any value other than fortuna is ignored. Applications should normally leave this property unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#PrngName
	 * @return ?string
	 */
	function prngName() {
		return CkPrng_prngName($this->_cPtr);
	}

	/**
	 * Selects the pseudo-random number generator used by this object. The only supported value is fortuna, which is also the default. Chilkat's Fortuna implementation uses AES-256 for generation and SHA-256 in its entropy-processing and reseeding design. Compatibility note: Assigning any value other than fortuna is ignored. Applications should normally leave this property unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#PrngName
	 * @param ?string $newVal
	 */
	function put_PrngName($newVal) {
		CkPrng_put_PrngName($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkPrng_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkPrng_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkPrng_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkPrng_version($this->_cPtr);
	}

	/**
	 * Adds caller-supplied seed material to the PRNG. ARG1 contains the bytes in the text encoding named by ARG2, such as hex, base64, or ascii. For Fortuna, the decoded bytes are incorporated into the internal entropy pools and become available to later reseeding. This method is optional and is not the normal way an application obtains entropy for the PRNG. It may be called more than once and at any point during the lifetime of the object. It returns _TRUE_ when the input is decoded and accepted, or _FALSE_ if the encoding is invalid or another error occurs. Automatic system seeding: If no entropy has been added before the first request for random output, Chilkat automatically obtains 32 bytes from the platform's cryptographic system entropy source to seed the PRNG. This is the normal and recommended mode of operation. Call AddEntropy only when the application has additional trustworthy entropy to contribute or deliberately needs deterministic seeding for testing. Repeatable tests: A fresh Prng object can be seeded with the same deterministic values to reproduce the same output sequence, provided the same entropy calls and random-generation calls occur in the same order. Predictable seed material is appropriate only for tests and debugging. Do not use it for real keys, nonces, passwords, or tokens.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#AddEntropy
	 * @param ?string $entropy
	 * @param ?string $encoding
	 * @return bool
	 */
	function AddEntropy($entropy,$encoding) {
		return CkPrng_AddEntropy($this->_cPtr,$entropy,$encoding);
	}

	/**
	 * Adds the bytes in ARG1 as caller-supplied seed material for the PRNG. For Fortuna, the bytes are incorporated into the internal entropy pools and become available to later reseeding. This method is optional and is not normally needed to initialize the PRNG. It may be called repeatedly. It returns _TRUE_ when the bytes are accepted, or _FALSE_ if an error occurs. Automatic system seeding: If no entropy has been added before the first request for random output, Chilkat automatically obtains 32 bytes from the platform's cryptographic system entropy source to seed the PRNG. This is the normal and recommended mode of operation. Call AddEntropyBytes only when the application has additional trustworthy entropy to contribute or deliberately needs deterministic seeding for testing. Repeatable tests: Predictable bytes can be used to reproduce an output sequence on a fresh Prng object when the same calls are made in the same order. Use predictable seed material only for tests and debugging, never for production cryptographic values.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#AddEntropyBytes
	 * @param CkByteData $entropy
	 * @return bool
	 */
	function AddEntropyBytes($entropy) {
		return CkPrng_AddEntropyBytes($this->_cPtr,$entropy);
	}

	/**
	 * Exports a Base64-encoded value derived from the PRNG's accumulated entropy pools. The internal pools are rehashed for export so the returned value is not a direct disclosure of the generator state. The returned value can be persisted and supplied to ImportEntropy when a later process starts. For language bindings in which this method directly returns a string, check LastMethodSuccess before using the result. Seed-file guidance: Imported entropy can improve startup behavior, but it should not be the only seed used indefinitely. After importing, also allow Chilkat to obtain fresh operating-system entropy. Protect persisted entropy from unauthorized modification and accidental reuse across cloned systems.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#ExportEntropy
	 * @return ?string
	 */
	function exportEntropy() {
		return CkPrng_exportEntropy($this->_cPtr);
	}

	/**
	 * Generates a Firebase-compatible push ID. A push ID is a 20-character identifier built from a 48-bit millisecond timestamp followed by 72 bits of randomness, encoded with Firebase's ordered 64-character alphabet. The timestamp prefix makes push IDs sort approximately by creation time, while the random portion makes collisions highly unlikely across independent clients. For language bindings in which this method directly returns a string, check LastMethodSuccess before using the result. Not a secret: A push ID is an identifier, not an authentication token. Its prefix reveals approximate creation time, and uniqueness is probabilistic rather than an absolute guarantee.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#FirebasePushId
	 * @return ?string
	 */
	function firebasePushId() {
		return CkPrng_firebasePushId($this->_cPtr);
	}

	/**
	 * Generates ARG1 random bytes and returns those bytes as text using the encoding named by ARG2, such as hex, base64, or base64url. ARG1 is the number of binary bytes before text encoding; it is not the number of output characters. If no entropy has been added before the first request for random output, Chilkat automatically obtains 32 bytes from the platform's cryptographic system entropy source to seed the PRNG. For language bindings in which this method directly returns a string, check LastMethodSuccess before using the result. Encoding does not add entropy: Hex, Base64, and other encodings represent the same random bytes in different textual forms. Choose numBytes from the security requirement of the key, nonce, salt, or token—not from the desired text length.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GenRandom
	 * @param int $numBytes
	 * @param ?string $encoding
	 * @return ?string
	 */
	function genRandom($numBytes,$encoding) {
		return CkPrng_genRandom($this->_cPtr,$numBytes,$encoding);
	}

	/**
	 * Generates ARG1 random bytes and appends them to the existing contents of the BinData object passed in ARG2. The destination is not cleared before the bytes are added. If no entropy has been added before the first request for random output, Chilkat automatically obtains 32 bytes from the platform's cryptographic system entropy source to seed the PRNG. Returns _TRUE_ on success or _FALSE_ on failure. Appending behavior: Call BinData.Clear first when the destination should contain only the newly generated bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GenRandomBd
	 * @param int $numBytes
	 * @param CkBinData $bd
	 * @return bool
	 */
	function GenRandomBd($numBytes,$bd) {
		return CkPrng_GenRandomBd($this->_cPtr,$numBytes,$bd);
	}

	/**
	 * Generates and returns ARG1 random bytes. If no entropy has been added before the first request for random output, Chilkat automatically obtains 32 bytes from the platform's cryptographic system entropy source to seed the PRNG. In language bindings where byte-returning methods use LastMethodSuccess, verify that property before using the returned bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GenRandomBytes
	 * @param int $numBytes
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function GenRandomBytes($numBytes,$outBytes) {
		return CkPrng_GenRandomBytes($this->_cPtr,$numBytes,$outBytes);
	}

	/**
	 * Requests ARG1 bytes from the platform's cryptographic system entropy source and returns them using the text encoding named by ARG2. This obtains system-provided random seed material directly; it is different from asking the Fortuna generator to expand its current state with GenRandom. For seeding this PRNG, 32 bytes normally provide the full 256-bit seed input expected by the implementation. Applications may request another amount when a protocol specifically requires raw bytes from the system source. For language bindings in which this method directly returns a string, check LastMethodSuccess before using the result. Usually unnecessary: The simplest secure use is to call a random-generation method directly. If no entropy was added explicitly, Chilkat automatically obtains 32 bytes of system entropy on the first generation request.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GetEntropy
	 * @param int $numBytes
	 * @param ?string $encoding
	 * @return ?string
	 */
	function getEntropy($numBytes,$encoding) {
		return CkPrng_getEntropy($this->_cPtr,$numBytes,$encoding);
	}

	/**
	 * Requests ARG1 bytes from the platform's cryptographic system entropy source and returns them using the text encoding named by ARG2. This obtains system-provided random seed material directly; it is different from asking the Fortuna generator to expand its current state with GenRandom. For seeding this PRNG, 32 bytes normally provide the full 256-bit seed input expected by the implementation. Applications may request another amount when a protocol specifically requires raw bytes from the system source. For language bindings in which this method directly returns a string, check LastMethodSuccess before using the result. Usually unnecessary: The simplest secure use is to call a random-generation method directly. If no entropy was added explicitly, Chilkat automatically obtains 32 bytes of system entropy on the first generation request.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GetEntropy
	 * @param int $numBytes
	 * @param ?string $encoding
	 * @return ?string
	 */
	function entropy($numBytes,$encoding) {
		return CkPrng_entropy($this->_cPtr,$numBytes,$encoding);
	}

	/**
	 * Requests and returns ARG1 bytes from the platform's cryptographic system entropy source. This obtains system-provided random seed material directly rather than expanding the current Fortuna state. For seeding this PRNG, 32 bytes normally provide the full 256-bit seed input expected by the implementation. In language bindings where byte-returning methods use LastMethodSuccess, verify that property before using the returned bytes. Usually unnecessary: Random-generation methods automatically seed the object from the operating system on first use when the application has not supplied entropy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#GetEntropyBytes
	 * @param int $numBytes
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function GetEntropyBytes($numBytes,$outBytes) {
		return CkPrng_GetEntropyBytes($this->_cPtr,$numBytes,$outBytes);
	}

	/**
	 * Imports the Base64-encoded entropy value previously returned by ExportEntropy. The imported material is supplied to the PRNG for use in its entropy and reseeding process. Returns _TRUE_ if the value is valid and imported successfully. Returns _FALSE_ if it cannot be decoded or imported; check LastErrorText for details. Recommended startup sequence: Import persisted entropy before the first random-generation call, then mix in fresh system entropy or allow the first generation call to perform Chilkat's automatic system seeding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#ImportEntropy
	 * @param ?string $entropy
	 * @return bool
	 */
	function ImportEntropy($entropy) {
		return CkPrng_ImportEntropy($this->_cPtr,$entropy);
	}

	/**
	 * Returns a pseudorandom integer in the inclusive range from ARG1 through ARG2. For example, RandomInt(4, 8) can return 4, 5, 6, 7, or 8. The caller should ensure that ARG1 is less than or equal to ARG2. Cryptographic material: Use GenRandom, GenRandomBytes, or GenRandomBd when a protocol requires an exact number of random bytes. Do not assemble keys or nonces from decimal random integers unless the protocol explicitly defines that representation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#RandomInt
	 * @param int $low
	 * @param int $high
	 * @return int
	 */
	function RandomInt($low,$high) {
		return CkPrng_RandomInt($this->_cPtr,$low,$high);
	}

	/**
	 * Generates a password of exactly ARG1 characters while applying the requested composition rules: ArgumentEffect ARG2When _TRUE_, require at least one digit from 0-9. ARG3When _TRUE_, require at least one lowercase and at least one uppercase US-ASCII letter. ARG4When nonempty, require at least one character from this supplied set, such as !@#$%. ARG5Exclude every listed character from the generated password. This is useful for ambiguous characters such as iIlL1oO0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#RandomPassword
	 * @param int $length
	 * @param bool $mustIncludeDigit
	 * @param bool $upperAndLowercase
	 * @param ?string $mustHaveOneOf
	 * @param ?string $excludeChars
	 * @return ?string
	 */
	function randomPassword($length,$mustIncludeDigit,$upperAndLowercase,$mustHaveOneOf,$excludeChars) {
		return CkPrng_randomPassword($this->_cPtr,$length,$mustIncludeDigit,$upperAndLowercase,$mustHaveOneOf,$excludeChars);
	}

	/**
	 * Generates a string of exactly ARG1 characters. The remaining arguments select the character categories placed in the source alphabet: ArgumentWhen _TRUE_ ARG2Include digits 0-9. ARG3Include lowercase ASCII letters a-z. ARG4Include uppercase ASCII letters A-Z.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkPrngRef.html#RandomString
	 * @param int $length
	 * @param bool $bDigits
	 * @param bool $bLower
	 * @param bool $bUpper
	 * @return ?string
	 */
	function randomString($length,$bDigits,$bLower,$bUpper) {
		return CkPrng_randomString($this->_cPtr,$length,$bDigits,$bLower,$bUpper);
	}
}
