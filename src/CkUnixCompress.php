<?php

/* CkUnixCompress.php -- Chilkat PHP extension proxy class CkUnixCompress.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Compress, uncompress, and unpack legacy UNIX .Z files.
 *
 * Chilkat.UnixCompress provides UNIX compress and uncompress functionality for legacy .Z files. It supports file-based, memory-based, and string-based workflows, uses LZW compression, can unpack .tar.Z archives, and includes abort and progress-related support for longer-running operations.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html
 */
class CkUnixCompress {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkUnixCompress') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkUnixCompress();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkUnixCompress_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkUnixCompress_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkUnixCompress_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkUnixCompress_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkUnixCompress_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkUnixCompress_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkUnixCompress_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkUnixCompress_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkUnixCompress_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkUnixCompress_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkUnixCompress_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkUnixCompress_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkUnixCompress_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkUnixCompress_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkUnixCompress_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkUnixCompress_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkUnixCompress_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkUnixCompress_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkUnixCompress_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkUnixCompress_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkUnixCompress_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkUnixCompress_version($this->_cPtr);
	}

	/**
	 * Compresses a file to create a UNIX compressed file (.Z).  This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressFile
	 * @param ?string $inFilename
	 * @param ?string $destPath
	 * @return bool
	 */
	function CompressFile($inFilename,$destPath) {
		return CkUnixCompress_CompressFile($this->_cPtr,$inFilename,$destPath);
	}

	/**
	 * Creates an asynchronous task to call the CompressFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressFileAsync
	 * @param ?string $inFilename
	 * @param ?string $destPath
	 * @return ?CkTask
	 */
	function CompressFileAsync($inFilename,$destPath) {
		return chilkat::_obj(CkUnixCompress_CompressFileAsync($this->_cPtr,$inFilename,$destPath),'CkTask');
	}

	/**
	 * Unix compresses a file to an in-memory image of a .Z file.  (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressFileToMem
	 * @param ?string $inFilename
	 * @param CkByteData $outData
	 * @return bool
	 */
	function CompressFileToMem($inFilename,$outData) {
		return CkUnixCompress_CompressFileToMem($this->_cPtr,$inFilename,$outData);
	}

	/**
	 * Creates an asynchronous task to call the CompressFileToMem method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressFileToMemAsync
	 * @param ?string $inFilename
	 * @return ?CkTask
	 */
	function CompressFileToMemAsync($inFilename) {
		return chilkat::_obj(CkUnixCompress_CompressFileToMemAsync($this->_cPtr,$inFilename),'CkTask');
	}

	/**
	 * Compresses in-memory data to an in-memory image of a .Z file.   (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressMemory
	 * @param CkByteData $inData
	 * @param CkByteData $outData
	 * @return bool
	 */
	function CompressMemory($inData,$outData) {
		return CkUnixCompress_CompressMemory($this->_cPtr,$inData,$outData);
	}

	/**
	 * Unix compresses and creates a .Z file from in-memory data.   (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressMemToFile
	 * @param CkByteData $inData
	 * @param ?string $destPath
	 * @return bool
	 */
	function CompressMemToFile($inData,$destPath) {
		return CkUnixCompress_CompressMemToFile($this->_cPtr,$inData,$destPath);
	}

	/**
	 * Compresses a string to an in-memory image of a .Z file.   Prior to compression, the string is converted to the character encoding specified by ARG2.   The ARG2 can be any one of a large number of character encodings, such as utf-8, iso-8859-1, Windows-1252, ucs-2, etc.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressString
	 * @param ?string $inStr
	 * @param ?string $charset
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function CompressString($inStr,$charset,$outBytes) {
		return CkUnixCompress_CompressString($this->_cPtr,$inStr,$charset,$outBytes);
	}

	/**
	 * Unix compresses and creates a .Z file from string data.   The ARG2 specifies the character encoding used for the byte representation of the characters when compressed.  The ARG2 can be any one of a large number of character encodings, such as utf-8, iso-8859-1, Windows-1252, ucs-2, etc.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#CompressStringToFile
	 * @param ?string $inStr
	 * @param ?string $charset
	 * @param ?string $destPath
	 * @return bool
	 */
	function CompressStringToFile($inStr,$charset,$destPath) {
		return CkUnixCompress_CompressStringToFile($this->_cPtr,$inStr,$charset,$destPath);
	}

	/**
	 * Loads the caller of the task's async method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkUnixCompress_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Uncompresses a .Z file.  (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFile
	 * @param ?string $inFilename
	 * @param ?string $destPath
	 * @return bool
	 */
	function UncompressFile($inFilename,$destPath) {
		return CkUnixCompress_UncompressFile($this->_cPtr,$inFilename,$destPath);
	}

	/**
	 * Creates an asynchronous task to call the UncompressFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFileAsync
	 * @param ?string $inFilename
	 * @param ?string $destPath
	 * @return ?CkTask
	 */
	function UncompressFileAsync($inFilename,$destPath) {
		return chilkat::_obj(CkUnixCompress_UncompressFileAsync($this->_cPtr,$inFilename,$destPath),'CkTask');
	}

	/**
	 * Uncompresses a .Z file directly to memory.  (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFileToMem
	 * @param ?string $inFilename
	 * @param CkByteData $outData
	 * @return bool
	 */
	function UncompressFileToMem($inFilename,$outData) {
		return CkUnixCompress_UncompressFileToMem($this->_cPtr,$inFilename,$outData);
	}

	/**
	 * Creates an asynchronous task to call the UncompressFileToMem method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFileToMemAsync
	 * @param ?string $inFilename
	 * @return ?CkTask
	 */
	function UncompressFileToMemAsync($inFilename) {
		return chilkat::_obj(CkUnixCompress_UncompressFileToMemAsync($this->_cPtr,$inFilename),'CkTask');
	}

	/**
	 * Uncompresses a .Z file that contains a text file.  The contents of the text file are returned as a string.  The character encoding of the text file is specified by ARG2.  Typical charsets are iso-8859-1, utf-8, windows-1252, shift_JIS, big5, etc.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFileToString
	 * @param ?string $zFilename
	 * @param ?string $charset
	 * @return ?string
	 */
	function uncompressFileToString($zFilename,$charset) {
		return CkUnixCompress_uncompressFileToString($this->_cPtr,$zFilename,$charset);
	}

	/**
	 * Creates an asynchronous task to call the UncompressFileToString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressFileToStringAsync
	 * @param ?string $zFilename
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function UncompressFileToStringAsync($zFilename,$charset) {
		return chilkat::_obj(CkUnixCompress_UncompressFileToStringAsync($this->_cPtr,$zFilename,$charset),'CkTask');
	}

	/**
	 * Uncompresses from an in-memory image of a .Z file directly into memory.   (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressMemory
	 * @param CkByteData $inData
	 * @param CkByteData $outData
	 * @return bool
	 */
	function UncompressMemory($inData,$outData) {
		return CkUnixCompress_UncompressMemory($this->_cPtr,$inData,$outData);
	}

	/**
	 * Uncompresses from an in-memory image of a .Z file to a file.   (This compression uses the LZW (Lempel-Ziv-Welch) compression algorithm.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressMemToFile
	 * @param CkByteData $inData
	 * @param ?string $destPath
	 * @return bool
	 */
	function UncompressMemToFile($inData,$destPath) {
		return CkUnixCompress_UncompressMemToFile($this->_cPtr,$inData,$destPath);
	}

	/**
	 * Uncompresses from an in-memory image of a .Z file directly to a string.   The ARG2 specifies the character encoding used to interpret the bytes resulting from the decompression.  The ARG2 can be any one of a large number of character encodings, such as utf-8, iso-8859-1, Windows-1252, ucs-2, etc.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UncompressString
	 * @param CkByteData $inCompressedData
	 * @param ?string $charset
	 * @return ?string
	 */
	function uncompressString($inCompressedData,$charset) {
		return CkUnixCompress_uncompressString($this->_cPtr,$inCompressedData,$charset);
	}

	/**
	 * Unpacks a .tar.Z file.  The decompress and untar occur in streaming mode.  There are no temporary files and the memory footprint is constant (and small) while untarring.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UnTarZ
	 * @param ?string $zFilename
	 * @param ?string $destDir
	 * @param bool $bNoAbsolute
	 * @return bool
	 */
	function UnTarZ($zFilename,$destDir,$bNoAbsolute) {
		return CkUnixCompress_UnTarZ($this->_cPtr,$zFilename,$destDir,$bNoAbsolute);
	}

	/**
	 * Creates an asynchronous task to call the UnTarZ method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUnixCompressRef.html#UnTarZAsync
	 * @param ?string $zFilename
	 * @param ?string $destDir
	 * @param bool $bNoAbsolute
	 * @return ?CkTask
	 */
	function UnTarZAsync($zFilename,$destDir,$bNoAbsolute) {
		return chilkat::_obj(CkUnixCompress_UnTarZAsync($this->_cPtr,$zFilename,$destDir,$bNoAbsolute),'CkTask');
	}
}
