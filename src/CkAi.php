<?php

/* CkAi.php -- Chilkat PHP extension proxy class CkAi.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Use one consistent API for multiple AI providers.
 *
 * Chilkat.Ai provides a unified interface for sending prompts, multimodal input, conversations, streaming requests, tool calls, and file references to many AI providers. Instead of writing separate REST integrations for each vendor, applications can use one Chilkat API while Chilkat handles provider-specific differences in authentication, request formatting, response parsing, streaming events, and tool-calling workflows.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html
 */
class CkAi {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkAi') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkAi();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkAi_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkAi_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkAi_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * This is the API key used for authentication.  Use your API key that matches your Provider .
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKey
	 * @param CkString $str
	 */
	function get_ApiKey($str) {
		CkAi_get_ApiKey($this->_cPtr,$str);
	}

	/**
	 * This is the API key used for authentication.  Use your API key that matches your Provider .
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKey
	 * @return ?string
	 */
	function apiKey() {
		return CkAi_apiKey($this->_cPtr);
	}

	/**
	 * This is the API key used for authentication.  Use your API key that matches your Provider .
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKey
	 * @param ?string $newVal
	 */
	function put_ApiKey($newVal) {
		CkAi_put_ApiKey($this->_cPtr,$newVal);
	}

	/**
	 * For custom Provider's, use this field name for the API key in the HTTP header, but only if the API key is to be placed in a custom header field.   If the API key is to be provided in an "Authorization: Bearer" header, then leave this property empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKeyHeader
	 * @param CkString $str
	 */
	function get_ApiKeyHeader($str) {
		CkAi_get_ApiKeyHeader($this->_cPtr,$str);
	}

	/**
	 * For custom Provider's, use this field name for the API key in the HTTP header, but only if the API key is to be placed in a custom header field.   If the API key is to be provided in an "Authorization: Bearer" header, then leave this property empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKeyHeader
	 * @return ?string
	 */
	function apiKeyHeader() {
		return CkAi_apiKeyHeader($this->_cPtr);
	}

	/**
	 * For custom Provider's, use this field name for the API key in the HTTP header, but only if the API key is to be placed in a custom header field.   If the API key is to be provided in an "Authorization: Bearer" header, then leave this property empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiKeyHeader
	 * @param ?string $newVal
	 */
	function put_ApiKeyHeader($newVal) {
		CkAi_put_ApiKeyHeader($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the API specification to be used.  Possible choices are ChatCompletions, Responses, Gemini, Anthropic.  The default value is Responses because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiSpec
	 * @param CkString $str
	 */
	function get_ApiSpec($str) {
		CkAi_get_ApiSpec($this->_cPtr,$str);
	}

	/**
	 * Specifies the API specification to be used.  Possible choices are ChatCompletions, Responses, Gemini, Anthropic.  The default value is Responses because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiSpec
	 * @return ?string
	 */
	function apiSpec() {
		return CkAi_apiSpec($this->_cPtr);
	}

	/**
	 * Specifies the API specification to be used.  Possible choices are ChatCompletions, Responses, Gemini, Anthropic.  The default value is Responses because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ApiSpec
	 * @param ?string $newVal
	 */
	function put_ApiSpec($newVal) {
		CkAi_put_ApiSpec($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the provider's base URL.  For example, http://localhost:11434/v1 or https://api.together.xyz/v1.  The default value is https://api.openai.com/v1 because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#BaseUrl
	 * @param CkString $str
	 */
	function get_BaseUrl($str) {
		CkAi_get_BaseUrl($this->_cPtr,$str);
	}

	/**
	 * Specifies the provider's base URL.  For example, http://localhost:11434/v1 or https://api.together.xyz/v1.  The default value is https://api.openai.com/v1 because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#BaseUrl
	 * @return ?string
	 */
	function baseUrl() {
		return CkAi_baseUrl($this->_cPtr);
	}

	/**
	 * Specifies the provider's base URL.  For example, http://localhost:11434/v1 or https://api.together.xyz/v1.  The default value is https://api.openai.com/v1 because the default value of the Provider property is openai. Setting the Provider property to a value other than custom will also automatically set this property to the correct and needed value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#BaseUrl
	 * @param ?string $newVal
	 */
	function put_BaseUrl($newVal) {
		CkAi_put_BaseUrl($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkAi_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkAi_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkAi_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_ a secret specifier of the format "!!part1|part2|.." can be provided instead of the actual value for the ApiKey property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#EnableSecrets
	 * @return bool
	 */
	function get_EnableSecrets() {
		return CkAi_get_EnableSecrets($this->_cPtr);
	}

	/**
	 * If _TRUE_ a secret specifier of the format "!!part1|part2|.." can be provided instead of the actual value for the ApiKey property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#EnableSecrets
	 * @param bool $newVal
	 */
	function put_EnableSecrets($newVal) {
		CkAi_put_EnableSecrets($this->_cPtr,$newVal);
	}

	/**
	 * For multimodal inputs like image data, image URLs, file data, and file URLs, the full data or URL is sent initially. If the property is set to _TRUE_, the full data/URL is re-sent in subsequent interactions. If set to _FALSE_, only the image or file summary is sent thereafter.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#FullTranscript
	 * @return bool
	 */
	function get_FullTranscript() {
		return CkAi_get_FullTranscript($this->_cPtr);
	}

	/**
	 * For multimodal inputs like image data, image URLs, file data, and file URLs, the full data or URL is sent initially. If the property is set to _TRUE_, the full data/URL is re-sent in subsequent interactions. If set to _FALSE_, only the image or file summary is sent thereafter.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#FullTranscript
	 * @param bool $newVal
	 */
	function put_FullTranscript($newVal) {
		CkAi_put_FullTranscript($this->_cPtr,$newVal);
	}

	/**
	 * _TRUE_ if the response output has function calls.  Otherwise _FALSE_.  This only applies to manual function tool calling, not automatic JS tool calls.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#HasFunctionCalls
	 * @return bool
	 */
	function get_HasFunctionCalls() {
		return CkAi_get_HasFunctionCalls($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkAi_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkAi_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * This applies to providers like OpenAI that can maintain conversation state on the server. If set to _TRUE_, only the conversation ID is sent with each query. If set to _FALSE_, the entire conversation transcript is sent with each query, making each query stateless.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#HostedConvo
	 * @return bool
	 */
	function get_HostedConvo() {
		return CkAi_get_HostedConvo($this->_cPtr);
	}

	/**
	 * This applies to providers like OpenAI that can maintain conversation state on the server. If set to _TRUE_, only the conversation ID is sent with each query. If set to _FALSE_, the entire conversation transcript is sent with each query, making each query stateless.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#HostedConvo
	 * @param bool $newVal
	 */
	function put_HostedConvo($newVal) {
		CkAi_put_HostedConvo($this->_cPtr,$newVal);
	}

	/**
	 * The maximum amount of time to wait for additional incoming data when receiving a response.   The default value  is 90000 (90 seconds) in v11.3.0 and earlier.  Starting in v11.4.0, the default value is 180000 (180 seconds). This is not a total timeout, but the maximum time allowed when data transmission has paused.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#IdleTimeoutMs
	 * @return int
	 */
	function get_IdleTimeoutMs() {
		return CkAi_get_IdleTimeoutMs($this->_cPtr);
	}

	/**
	 * The maximum amount of time to wait for additional incoming data when receiving a response.   The default value  is 90000 (90 seconds) in v11.3.0 and earlier.  Starting in v11.4.0, the default value is 180000 (180 seconds). This is not a total timeout, but the maximum time allowed when data transmission has paused.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#IdleTimeoutMs
	 * @param int $newVal
	 */
	function put_IdleTimeoutMs($newVal) {
		CkAi_put_IdleTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * The maximum time in seconds allowed for an automatic JavaScript tool function.  The default value is 60 seconds to allow for most use cases, but to prevent an tool call that never returns.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#JsToolTimeout
	 * @return int
	 */
	function get_JsToolTimeout() {
		return CkAi_get_JsToolTimeout($this->_cPtr);
	}

	/**
	 * The maximum time in seconds allowed for an automatic JavaScript tool function.  The default value is 60 seconds to allow for most use cases, but to prevent an tool call that never returns.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#JsToolTimeout
	 * @param int $newVal
	 */
	function put_JsToolTimeout($newVal) {
		CkAi_put_JsToolTimeout($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkAi_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkAi_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkAi_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkAi_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkAi_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkAi_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkAi_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkAi_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Read-only.  _TRUE_ if the model refused to produce the requested structured output during the most recent Ask.  When a refusal occurs, GetOutputJson returns _FALSE_ and the provider's explanation is available in RefusalText .  This property is reset at the start of each Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#LastResponseRefused
	 * @return bool
	 */
	function get_LastResponseRefused() {
		return CkAi_get_LastResponseRefused($this->_cPtr);
	}

	/**
	 * The model to be used for AI conversations.  For example, GPT-4o or GPT-5.  The list of available models can be obtained by calling GetModels.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Model
	 * @param CkString $str
	 */
	function get_Model($str) {
		CkAi_get_Model($this->_cPtr,$str);
	}

	/**
	 * The model to be used for AI conversations.  For example, GPT-4o or GPT-5.  The list of available models can be obtained by calling GetModels.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Model
	 * @return ?string
	 */
	function model() {
		return CkAi_model($this->_cPtr);
	}

	/**
	 * The model to be used for AI conversations.  For example, GPT-4o or GPT-5.  The list of available models can be obtained by calling GetModels.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Model
	 * @param ?string $newVal
	 */
	function put_Model($newVal) {
		CkAi_put_Model($this->_cPtr,$newVal);
	}

	/**
	 * This is the AI provider.  Possible values are: openai, google, claude, deepseek, perplexity, mistral, and xai.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Provider
	 * @param CkString $str
	 */
	function get_Provider($str) {
		CkAi_get_Provider($this->_cPtr,$str);
	}

	/**
	 * This is the AI provider.  Possible values are: openai, google, claude, deepseek, perplexity, mistral, and xai.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Provider
	 * @return ?string
	 */
	function provider() {
		return CkAi_provider($this->_cPtr);
	}

	/**
	 * This is the AI provider.  Possible values are: openai, google, claude, deepseek, perplexity, mistral, and xai.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Provider
	 * @param ?string $newVal
	 */
	function put_Provider($newVal) {
		CkAi_put_Provider($this->_cPtr,$newVal);
	}

	/**
	 * Read-only.  When LastResponseRefused is _TRUE_, this is the model's message explaining why it declined to produce the requested structured output.  Otherwise it is the empty string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#RefusalText
	 * @param CkString $str
	 */
	function get_RefusalText($str) {
		CkAi_get_RefusalText($this->_cPtr,$str);
	}

	/**
	 * Read-only.  When LastResponseRefused is _TRUE_, this is the model's message explaining why it declined to produce the requested structured output.  Otherwise it is the empty string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#RefusalText
	 * @return ?string
	 */
	function refusalText() {
		return CkAi_refusalText($this->_cPtr);
	}

	/**
	 * The HTTP response status code for the last Ask is as follows:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ResponseStatusCode
	 * @return int
	 */
	function get_ResponseStatusCode() {
		return CkAi_get_ResponseStatusCode($this->_cPtr);
	}

	/**
	 * The currently selected conversation can be set to your desired conversation name. Once set, all future queries will be part of that conversation, updating its state with each input and output. By default, no conversation is selected, indicated by an empty string, leading to stateless queries. The first conversation created or imported will be automatically selected. If you attempt to select a non-existent conversation, the selection will not change. You can reset this property to an empty string to revert to stateless querying.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SelectedConvo
	 * @param CkString $str
	 */
	function get_SelectedConvo($str) {
		CkAi_get_SelectedConvo($this->_cPtr,$str);
	}

	/**
	 * The currently selected conversation can be set to your desired conversation name. Once set, all future queries will be part of that conversation, updating its state with each input and output. By default, no conversation is selected, indicated by an empty string, leading to stateless queries. The first conversation created or imported will be automatically selected. If you attempt to select a non-existent conversation, the selection will not change. You can reset this property to an empty string to revert to stateless querying.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SelectedConvo
	 * @return ?string
	 */
	function selectedConvo() {
		return CkAi_selectedConvo($this->_cPtr);
	}

	/**
	 * The currently selected conversation can be set to your desired conversation name. Once set, all future queries will be part of that conversation, updating its state with each input and output. By default, no conversation is selected, indicated by an empty string, leading to stateless queries. The first conversation created or imported will be automatically selected. If you attempt to select a non-existent conversation, the selection will not change. You can reset this property to an empty string to revert to stateless querying.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SelectedConvo
	 * @param ?string $newVal
	 */
	function put_SelectedConvo($newVal) {
		CkAi_put_SelectedConvo($this->_cPtr,$newVal);
	}

	/**
	 * When set to _TRUE_, the Ask method operates in streaming mode, returning immediately after the request is sent. You can then use the PollAi and NextAiEvent methods to receive streaming events. By default, this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Streaming
	 * @return bool
	 */
	function get_Streaming() {
		return CkAi_get_Streaming($this->_cPtr);
	}

	/**
	 * When set to _TRUE_, the Ask method operates in streaming mode, returning immediately after the request is sent. You can then use the PollAi and NextAiEvent methods to receive streaming events. By default, this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Streaming
	 * @param bool $newVal
	 */
	function put_Streaming($newVal) {
		CkAi_put_Streaming($this->_cPtr,$newVal);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs. This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkAi_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs. This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkAi_uncommonOptions($this->_cPtr);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs. This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkAi_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkAi_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkAi_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkAi_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkAi_version($this->_cPtr);
	}

	/**
	 * Sends the current potentially multimodal input within the context of the selected conversation and receives the model's output.  Prior to v11.4.0 the ARG1 is limited to text. Starting in v11.4.0, the ARG1 can be set to image for AI providers that support conversational image output.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#Ask
	 * @param ?string $outputType
	 * @return bool
	 */
	function Ask($outputType) {
		return CkAi_Ask($this->_cPtr,$outputType);
	}

	/**
	 * Creates an asynchronous task to call the Ask method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#AskAsync
	 * @param ?string $outputType
	 * @return ?CkTask
	 */
	function AskAsync($outputType) {
		return chilkat::_obj(CkAi_AskAsync($this->_cPtr,$outputType),'CkTask');
	}

	/**
	 * Deletes the conversation by its ARG1.  If ARG1 equals * then all conversations are deleted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#DeleteConvo
	 * @param ?string $name
	 * @return bool
	 */
	function DeleteConvo($name) {
		return CkAi_DeleteConvo($this->_cPtr,$name);
	}

	/**
	 * Exports the conversation specified by ARG1 to ARG2, replacing ARG2's contents with the exported transcript. You can later import a conversation using ImportConvo .  The currently selected conversation can be exported by passing * for the ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ExportConvo
	 * @param ?string $name
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function ExportConvo($name,$json) {
		return CkAi_ExportConvo($this->_cPtr,$name,$json);
	}

	/**
	 * This method returns the function calls in ARG1 if the output contains any.  Returns _TRUE_ if one or more function calls were returned, otherwise returns _FALSE_.  This only applies to manual function tool calling, not automatic JS tool calls.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetFunctionCalls
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetFunctionCalls($json) {
		return CkAi_GetFunctionCalls($this->_cPtr,$json);
	}

	/**
	 * Provides details about the last method called on this object instance. While many methods do not generate information, some allow you to retrieve details by accessing the last JSON data immediately after the method call returns.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetLastJsonData
	 * @param CkJsonObject $json
	 */
	function GetLastJsonData($json) {
		CkAi_GetLastJsonData($this->_cPtr,$json);
	}

	/**
	 * Returns a list of models available to your account, such as GPT-4o and GPT-5. The models are listed in ARG1, sorted in ascending, case-insensitive order. To access the raw JSON response after calling this method, use GetLastJsonData .
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetModels
	 * @param CkStringTable $st
	 * @return bool
	 */
	function GetModels($st) {
		return CkAi_GetModels($this->_cPtr,$st);
	}

	/**
	 * Creates an asynchronous task to call the GetModels method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetModelsAsync
	 * @param CkStringTable $st
	 * @return ?CkTask
	 */
	function GetModelsAsync($st) {
		return chilkat::_obj(CkAi_GetModelsAsync($this->_cPtr,$st),'CkTask');
	}

	/**
	 * Returns in ARG1 the binary output from the last call to Ask, such as for an AI generated image.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetOutputBd
	 * @param CkBinData $bd
	 * @return bool
	 */
	function GetOutputBd($bd) {
		return CkAi_GetOutputBd($this->_cPtr,$bd);
	}

	/**
	 * Returns the model's structured JSON output from the most recent Ask, parsed into ARG1.  Use this after setting a schema with SetOutputSchema .  Returns _TRUE_ and populates ARG1 when the output is valid JSON.  Returns _FALSE_ if the model refused to produce the output (in which case LastResponseRefused is _TRUE_ and RefusalText contains the explanation), if there is no output, or if the output is not valid JSON.  The raw JSON text is also available from GetOutputText .
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetOutputJson
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetOutputJson($json) {
		return CkAi_GetOutputJson($this->_cPtr,$json);
	}

	/**
	 * Returns the text output from the last call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetOutputText
	 * @return ?string
	 */
	function getOutputText() {
		return CkAi_getOutputText($this->_cPtr);
	}

	/**
	 * Returns the text output from the last call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetOutputText
	 * @return ?string
	 */
	function outputText() {
		return CkAi_outputText($this->_cPtr);
	}

	/**
	 * Returns the text output, appended to ARG1, from the last call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetOutputTextSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function GetOutputTextSb($sb) {
		return CkAi_GetOutputTextSb($this->_cPtr,$sb);
	}

	/**
	 * Returns the web search sources the model used in the most recent Ask, if web search was enabled via the web_search param of SetAskParams .  Returns _TRUE_ if the last Ask performed one or more web searches, in which case the normalized results are placed in ARG1.  Returns _FALSE_ if no web search occurred.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#GetSearchResults
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetSearchResults($json) {
		return CkAi_GetSearchResults($this->_cPtr,$json);
	}

	/**
	 * Imports the conversation from ARG2 and assigns it the name in ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ImportConvo
	 * @param ?string $name
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function ImportConvo($name,$json) {
		return CkAi_ImportConvo($this->_cPtr,$name,$json);
	}

	/**
	 * Adds file data, such as a PDF, to the potentially multimodal input that will be sent in the next call to Ask.  The ARG2 contains the file data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddFileData
	 * @param ?string $filename
	 * @param CkBinData $bd
	 * @param ?string $summary
	 * @return bool
	 */
	function InputAddFileData($filename,$bd,$summary) {
		return CkAi_InputAddFileData($this->_cPtr,$filename,$bd,$summary);
	}

	/**
	 * Adds a reference to a previously uploaded file.  The ARG1 is the id returned from the provider when originally uploading the file.  The ARG2, such as "application/pdf", "application/json", "text/xml", etc. is needed for some AI providers such as Google.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddFileId
	 * @param ?string $file_id
	 * @param ?string $mime_type
	 * @return bool
	 */
	function InputAddFileId($file_id,$mime_type) {
		return CkAi_InputAddFileId($this->_cPtr,$file_id,$mime_type);
	}

	/**
	 * Adds a file located at a ARG1 to the potentially multimodal input that will be sent in the next call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddFileUrl
	 * @param ?string $url
	 * @param ?string $summary
	 * @return bool
	 */
	function InputAddFileUrl($url,$summary) {
		return CkAi_InputAddFileUrl($this->_cPtr,$url,$summary);
	}

	/**
	 * Provide a function call result to the model. This only applies to manual function tool calling, not automatic JS tool calls.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddFnResult
	 * @param ?string $call_id
	 * @param ?string $fnResult
	 * @return bool
	 */
	function InputAddFnResult($call_id,$fnResult) {
		return CkAi_InputAddFnResult($this->_cPtr,$call_id,$fnResult);
	}

	/**
	 * Adds an image to the potentially multimodal input that will be sent in the next call to Ask.  The ARG1 contains the images data.  The image data must be jpeg, png, or webp format.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddImageData
	 * @param CkBinData $bd
	 * @param ?string $summary
	 * @return bool
	 */
	function InputAddImageData($bd,$summary) {
		return CkAi_InputAddImageData($this->_cPtr,$bd,$summary);
	}

	/**
	 * Adds a reference to a previously uploaded image file.  The ARG1 is the id returned from the provider when originally uploading the file.  The ARG2, such as "image/jpeg" is needed for some AI providers such as Google.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddImageFileId
	 * @param ?string $file_id
	 * @param ?string $mime_type
	 * @return bool
	 */
	function InputAddImageFileId($file_id,$mime_type) {
		return CkAi_InputAddImageFileId($this->_cPtr,$file_id,$mime_type);
	}

	/**
	 * Adds an image located at a ARG1 to the potentially multimodal input that will be sent in the next call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddImageUrl
	 * @param ?string $url
	 * @param ?string $summary
	 * @return bool
	 */
	function InputAddImageUrl($url,$summary) {
		return CkAi_InputAddImageUrl($this->_cPtr,$url,$summary);
	}

	/**
	 * Adds text to the potentially multimodal input that will be sent in the next call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddText
	 * @param ?string $text
	 * @return bool
	 */
	function InputAddText($text) {
		return CkAi_InputAddText($this->_cPtr,$text);
	}

	/**
	 * Adds text to the potentially multimodal input that will be sent in the next call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputAddTextSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function InputAddTextSb($sb) {
		return CkAi_InputAddTextSb($this->_cPtr,$sb);
	}

	/**
	 * Clears the inputs to be sent in the next call to Ask.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#InputClear
	 */
	function InputClear() {
		CkAi_InputClear($this->_cPtr);
	}

	/**
	 * Returns the full list of conversation names in ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#ListConvos
	 * @param CkStringTable $st
	 * @return bool
	 */
	function ListConvos($st) {
		return CkAi_ListConvos($this->_cPtr,$st);
	}

	/**
	 * Creates and initializes a transcript for a new conversation.  The ARG1 is a unique name for the conversation. You can reference the conversation by this name, such as for the ExportConvo method. The ARG2 sets the broad behavior, tone, and role of the assistant.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#NewConvo
	 * @param ?string $name
	 * @param ?string $sysMsg
	 * @param ?string $devMsg
	 * @return bool
	 */
	function NewConvo($name,$sysMsg,$devMsg) {
		return CkAi_NewConvo($this->_cPtr,$name,$sysMsg,$devMsg);
	}

	/**
	 * Retrieves the next available AI streaming mode response update. Use the PollAi method to check if a response update is immediately accessible. The event name is returned in ARG2. If the event name is delta, the text is returned in ARG3.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#NextAiEvent
	 * @param int $maxWaitMs
	 * @param CkStringBuilder $sbName
	 * @param CkStringBuilder $sbDelta
	 * @return bool
	 */
	function NextAiEvent($maxWaitMs,$sbName,$sbDelta) {
		return CkAi_NextAiEvent($this->_cPtr,$maxWaitMs,$sbName,$sbDelta);
	}

	/**
	 * Returns immediately with one of the following values:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#PollAi
	 * @param bool $abort
	 * @return int
	 */
	function PollAi($abort) {
		return CkAi_PollAi($this->_cPtr,$abort);
	}

	/**
	 * Adds JavaScript tool functions to the AI.  This function can be called multiple times to add tool functions from multiple JavaScript sources. The JavaScript is passed in ARG1.  If ARG2 is _TRUE_, then the ARG1 contains  JavaScript to be run for the purpose of adding supporting functions and does not contain a toolRegistry.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#RegisterJsTools
	 * @param CkStringBuilder $sb
	 * @param bool $evalOnly
	 * @param bool $allowAllKeyword
	 * @return bool
	 */
	function RegisterJsTools($sb,$evalOnly,$allowAllKeyword) {
		return CkAi_RegisterJsTools($this->_cPtr,$sb,$evalOnly,$allowAllKeyword);
	}

	/**
	 * Sets the tools JSON for AI requests.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#RegisterManualTools
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function RegisterManualTools($json) {
		return CkAi_RegisterManualTools($this->_cPtr,$json);
	}

	/**
	 * Sets additional Ask params such as:   effort (see below)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SetAskParams
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetAskParams($json) {
		return CkAi_SetAskParams($this->_cPtr,$json);
	}

	/**
	 * This method copies connection settings, including HTTP proxy, SOCKS proxy, network interface IP address, and other related settings from ARG1. It's optional; if not used, default settings apply. Call this method before sending REST API requests to an AI provider.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SetConnectionSettings
	 * @param CkSocket $sock
	 * @return bool
	 */
	function SetConnectionSettings($sock) {
		return CkAi_SetConnectionSettings($this->_cPtr,$sock);
	}

	/**
	 * Requests schema-constrained JSON output (structured output) for subsequent calls to Ask.  ARG1 is a short schema name (letters, digits, underscores, and hyphens) used as an identifier by some providers.  ARG2 is the desired output shape as a JSON Schema.  ARG3 (strict) requests strict conformance where the provider supports it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SetOutputSchema
	 * @param ?string $name
	 * @param CkJsonObject $schema
	 * @param bool $strict
	 * @return bool
	 */
	function SetOutputSchema($name,$schema,$strict) {
		return CkAi_SetOutputSchema($this->_cPtr,$name,$schema,$strict);
	}

	/**
	 * Sets additional provider params needed for some pre-defined providers.  As of this current version, the possible params are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SetProviderParams
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetProviderParams($json) {
		return CkAi_SetProviderParams($this->_cPtr,$json);
	}

	/**
	 * This method is provided for convenience.  It causes the calling thread to sleep for a number of milliseconds.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#SleepMs
	 * @param int $millisec
	 */
	function SleepMs($millisec) {
		CkAi_SleepMs($this->_cPtr,$millisec);
	}

	/**
	 * Handles a js_function_call event within a streaming Ask response loop by automatically executing the requested tool and adding its result to the conversation.  The tool may be a JavaScript tool (registered with RegisterJsTools) or an MCP tool (registered with UseMcp).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#StreamingJsToolCall
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function StreamingJsToolCall($sb) {
		return CkAi_StreamingJsToolCall($this->_cPtr,$sb);
	}

	/**
	 * Creates an asynchronous task to call the StreamingJsToolCall method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#StreamingJsToolCallAsync
	 * @param CkStringBuilder $sb
	 * @return ?CkTask
	 */
	function StreamingJsToolCallAsync($sb) {
		return chilkat::_obj(CkAi_StreamingJsToolCallAsync($this->_cPtr,$sb),'CkTask');
	}

	/**
	 * Uploads a file and returns the file_id (or uri) that can be used to reference the pre-uploaded file using InputAddFileId before an Ask.  Files can be uploaded to the following providers: openai, gemini, claude, and grok.  The ARG2 specifies the MIME type, such as "application/pdf" or "text/plain".   If ARG2 is the emptys string, Chilkat will automatically choose the MIME type based on the filename extension.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFile
	 * @param ?string $path
	 * @param ?string $mimeType
	 * @return ?string
	 */
	function uploadFile($path,$mimeType) {
		return CkAi_uploadFile($this->_cPtr,$path,$mimeType);
	}

	/**
	 * Creates an asynchronous task to call the UploadFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFileAsync
	 * @param ?string $path
	 * @param ?string $mimeType
	 * @return ?CkTask
	 */
	function UploadFileAsync($path,$mimeType) {
		return chilkat::_obj(CkAi_UploadFileAsync($this->_cPtr,$path,$mimeType),'CkTask');
	}

	/**
	 * Uploads the contents of ARG1 and returns the file_id (or uri) that can be used to reference the pre-uploaded file using InputAddFileId before an Ask.  Files can be uploaded to the following providers: openai, gemini, claude, and grok.  The ARG2 is mostly informational.  The ARG3 specifies the MIME type, such as "application/pdf" or "text/plain".   If ARG3 is the emptys string, Chilkat will automatically choose the MIME type based on the extension of the filename passed in ARG2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFileBd
	 * @param CkBinData $bd
	 * @param ?string $filename
	 * @param ?string $mimeType
	 * @return ?string
	 */
	function uploadFileBd($bd,$filename,$mimeType) {
		return CkAi_uploadFileBd($this->_cPtr,$bd,$filename,$mimeType);
	}

	/**
	 * Creates an asynchronous task to call the UploadFileBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFileBdAsync
	 * @param CkBinData $bd
	 * @param ?string $filename
	 * @param ?string $mimeType
	 * @return ?CkTask
	 */
	function UploadFileBdAsync($bd,$filename,$mimeType) {
		return chilkat::_obj(CkAi_UploadFileBdAsync($this->_cPtr,$bd,$filename,$mimeType),'CkTask');
	}

	/**
	 * Uploads the contents of ARG1 and returns the file_id (or uri) that can be used to reference the pre-uploaded file using InputAddFileId before an Ask.  Files can be uploaded to the following providers: openai, gemini, claude, and grok.  The ARG2 is mostly informational.  The ARG3 specifies the MIME type, such as "application/pdf" or "text/plain".   If ARG3 is the emptys string, Chilkat will automatically choose the MIME type based on the extension of the filename passed in ARG2.  It is the utf-8 byte representation of the string that is uploaded.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFileSb
	 * @param CkStringBuilder $sb
	 * @param ?string $filename
	 * @param ?string $mimeType
	 * @return ?string
	 */
	function uploadFileSb($sb,$filename,$mimeType) {
		return CkAi_uploadFileSb($this->_cPtr,$sb,$filename,$mimeType);
	}

	/**
	 * Creates an asynchronous task to call the UploadFileSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UploadFileSbAsync
	 * @param CkStringBuilder $sb
	 * @param ?string $filename
	 * @param ?string $mimeType
	 * @return ?CkTask
	 */
	function UploadFileSbAsync($sb,$filename,$mimeType) {
		return chilkat::_obj(CkAi_UploadFileSbAsync($this->_cPtr,$sb,$filename,$mimeType),'CkTask');
	}

	/**
	 * Registers the tools provided by the connected MCP (Model Context Protocol) server ARG1 so the AI model can call them during a conversation.  This lists the server's tools (a network operation) and namespaces each tool name using the prefix in ARG2 (to avoid collisions between servers and with JavaScript tools).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UseMcp
	 * @param CkMcp $cMcp
	 * @param ?string $namePrefix
	 * @return bool
	 */
	function UseMcp($cMcp,$namePrefix) {
		return CkAi_UseMcp($this->_cPtr,$cMcp,$namePrefix);
	}

	/**
	 * Creates an asynchronous task to call the UseMcp method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkAiRef.html#UseMcpAsync
	 * @param CkMcp $cMcp
	 * @param ?string $namePrefix
	 * @return ?CkTask
	 */
	function UseMcpAsync($cMcp,$namePrefix) {
		return chilkat::_obj(CkAi_UseMcpAsync($this->_cPtr,$cMcp,$namePrefix),'CkTask');
	}
}
