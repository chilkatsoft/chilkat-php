<?php

/* CkUpload.php -- Chilkat PHP extension proxy class CkUpload.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Upload files with HTTP POST, form fields, headers, progress tracking, and TLS control.
 *
 * Chilkat.Upload sends files to a web server using HTTP POST, typically as a multipart/form-data upload. It supports one or more file references, custom form parameters, custom HTTP headers, blocking and asynchronous uploads, progress monitoring, upload throttling, Basic HTTP authentication, proxy settings, HTTPS/TLS configuration, timeout control, and inspection of the HTTP response returned by the server.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html
 */
class CkUpload {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkUpload') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkUpload();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkUpload_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkUpload_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkUpload_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkUpload_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkUpload_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * If non-zero, limits (throttles) the upload bandwidth to approximately this maximum number of bytes per second.  The default value of this property is 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#BandwidthThrottleUp
	 * @return int
	 */
	function get_BandwidthThrottleUp() {
		return CkUpload_get_BandwidthThrottleUp($this->_cPtr);
	}

	/**
	 * If non-zero, limits (throttles) the upload bandwidth to approximately this maximum number of bytes per second.  The default value of this property is 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#BandwidthThrottleUp
	 * @param int $newVal
	 */
	function put_BandwidthThrottleUp($newVal) {
		CkUpload_put_BandwidthThrottleUp($this->_cPtr,$newVal);
	}

	/**
	 * The chunk size (in bytes) used by the underlying TCP/IP sockets for uploading files.  The default value is 65535.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ChunkSize
	 * @return int
	 */
	function get_ChunkSize() {
		return CkUpload_get_ChunkSize($this->_cPtr);
	}

	/**
	 * The chunk size (in bytes) used by the underlying TCP/IP sockets for uploading files.  The default value is 65535.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ChunkSize
	 * @param int $newVal
	 */
	function put_ChunkSize($newVal) {
		CkUpload_put_ChunkSize($this->_cPtr,$newVal);
	}

	/**
	 * The IP address to use for computers with multiple network interfaces or IP addresses. For computers with a single network interface (i.e. most computers), this property should not be set.  For multihoming computers, the default IP address is automatically used if this property is not set. The IP address is a string such as in dotted notation using numbers, not domain names, such as 165.164.55.124.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ClientIpAddress
	 * @param CkString $str
	 */
	function get_ClientIpAddress($str) {
		CkUpload_get_ClientIpAddress($this->_cPtr,$str);
	}

	/**
	 * The IP address to use for computers with multiple network interfaces or IP addresses. For computers with a single network interface (i.e. most computers), this property should not be set.  For multihoming computers, the default IP address is automatically used if this property is not set. The IP address is a string such as in dotted notation using numbers, not domain names, such as 165.164.55.124.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ClientIpAddress
	 * @return ?string
	 */
	function clientIpAddress() {
		return CkUpload_clientIpAddress($this->_cPtr);
	}

	/**
	 * The IP address to use for computers with multiple network interfaces or IP addresses. For computers with a single network interface (i.e. most computers), this property should not be set.  For multihoming computers, the default IP address is automatically used if this property is not set. The IP address is a string such as in dotted notation using numbers, not domain names, such as 165.164.55.124.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ClientIpAddress
	 * @param ?string $newVal
	 */
	function put_ClientIpAddress($newVal) {
		CkUpload_put_ClientIpAddress($this->_cPtr,$newVal);
	}

	/**
	 * The current filename being uploaded while an asynchronous upload is in progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#CurrentFilename
	 * @param CkString $str
	 */
	function get_CurrentFilename($str) {
		CkUpload_get_CurrentFilename($this->_cPtr,$str);
	}

	/**
	 * The current filename being uploaded while an asynchronous upload is in progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#CurrentFilename
	 * @return ?string
	 */
	function currentFilename() {
		return CkUpload_currentFilename($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkUpload_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkUpload_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkUpload_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, the request header will included an Expect: 100-continue header field.  This indicates that the server should respond with an intermediate response of 100 Continue or 417 Expectation Failed response based on the information available in the request header.  This helps avoid situations such as limits on upload sizes.  It allows the server to reject the upload, and then the client can abort prior to uploading the data. The default value of this property is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Expect100Continue
	 * @return bool
	 */
	function get_Expect100Continue() {
		return CkUpload_get_Expect100Continue($this->_cPtr);
	}

	/**
	 * When _TRUE_, the request header will included an Expect: 100-continue header field.  This indicates that the server should respond with an intermediate response of 100 Continue or 417 Expectation Failed response based on the information available in the request header.  This helps avoid situations such as limits on upload sizes.  It allows the server to reject the upload, and then the client can abort prior to uploading the data. The default value of this property is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Expect100Continue
	 * @param bool $newVal
	 */
	function put_Expect100Continue($newVal) {
		CkUpload_put_Expect100Continue($this->_cPtr,$newVal);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkUpload_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkUpload_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * The hostname of the HTTP server that is the target of the upload.  Do not include http:// in the hostname.  It can be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Hostname
	 * @param CkString $str
	 */
	function get_Hostname($str) {
		CkUpload_get_Hostname($this->_cPtr,$str);
	}

	/**
	 * The hostname of the HTTP server that is the target of the upload.  Do not include http:// in the hostname.  It can be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Hostname
	 * @return ?string
	 */
	function hostname() {
		return CkUpload_hostname($this->_cPtr);
	}

	/**
	 * The hostname of the HTTP server that is the target of the upload.  Do not include http:// in the hostname.  It can be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Hostname
	 * @param ?string $newVal
	 */
	function put_Hostname($newVal) {
		CkUpload_put_Hostname($this->_cPtr,$newVal);
	}

	/**
	 * A timeout in milliseconds.  The default value is 30000.  If the upload hangs (i.e. progress halts) for more than this time, the component will abort the upload.  (It will timeout.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#IdleTimeoutMs
	 * @return int
	 */
	function get_IdleTimeoutMs() {
		return CkUpload_get_IdleTimeoutMs($this->_cPtr);
	}

	/**
	 * A timeout in milliseconds.  The default value is 30000.  If the upload hangs (i.e. progress halts) for more than this time, the component will abort the upload.  (It will timeout.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#IdleTimeoutMs
	 * @param int $newVal
	 */
	function put_IdleTimeoutMs($newVal) {
		CkUpload_put_IdleTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkUpload_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkUpload_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkUpload_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkUpload_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkUpload_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkUpload_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkUpload_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkUpload_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * The HTTP login for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Login
	 * @param CkString $str
	 */
	function get_Login($str) {
		CkUpload_get_Login($this->_cPtr,$str);
	}

	/**
	 * The HTTP login for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Login
	 * @return ?string
	 */
	function login() {
		return CkUpload_login($this->_cPtr);
	}

	/**
	 * The HTTP login for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Login
	 * @param ?string $newVal
	 */
	function put_Login($newVal) {
		CkUpload_put_Login($this->_cPtr,$newVal);
	}

	/**
	 * After an upload has completed, this property contains the number of bytes sent.  During asynchronous uploads, this property contains the current number of bytes sent while the upload is in progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#NumBytesSent
	 * @return int
	 */
	function get_NumBytesSent() {
		return CkUpload_get_NumBytesSent($this->_cPtr);
	}

	/**
	 * The HTTP password for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Password
	 * @param CkString $str
	 */
	function get_Password($str) {
		CkUpload_get_Password($this->_cPtr,$str);
	}

	/**
	 * The HTTP password for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Password
	 * @return ?string
	 */
	function password() {
		return CkUpload_password($this->_cPtr);
	}

	/**
	 * The HTTP password for sites requiring authentication.  Chilkat Upload supports Basic HTTP authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Password
	 * @param ?string $newVal
	 */
	function put_Password($newVal) {
		CkUpload_put_Password($this->_cPtr,$newVal);
	}

	/**
	 * The path part of the upload URL.  Some examples:desc>
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Path
	 * @param CkString $str
	 */
	function get_Path($str) {
		CkUpload_get_Path($this->_cPtr,$str);
	}

	/**
	 * The path part of the upload URL.  Some examples:desc>
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Path
	 * @return ?string
	 */
	function path() {
		return CkUpload_path($this->_cPtr);
	}

	/**
	 * The path part of the upload URL.  Some examples:desc>
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Path
	 * @param ?string $newVal
	 */
	function put_Path($newVal) {
		CkUpload_put_Path($this->_cPtr,$newVal);
	}

	/**
	 * This property is only valid in programming environment and languages that allow for event callbacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#PercentDoneScale
	 * @return int
	 */
	function get_PercentDoneScale() {
		return CkUpload_get_PercentDoneScale($this->_cPtr);
	}

	/**
	 * This property is only valid in programming environment and languages that allow for event callbacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#PercentDoneScale
	 * @param int $newVal
	 */
	function put_PercentDoneScale($newVal) {
		CkUpload_put_PercentDoneScale($this->_cPtr,$newVal);
	}

	/**
	 * Contains the current percentage completion (0 to 100) while an asynchronous upload is in progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#PercentUploaded
	 * @return int
	 */
	function get_PercentUploaded() {
		return CkUpload_get_PercentUploaded($this->_cPtr);
	}

	/**
	 * The port number of the upload target (i.e. consumer) URL.  The default value is 80.  If SSL is used, this should be set to 443 (typically).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Port
	 * @return int
	 */
	function get_Port() {
		return CkUpload_get_Port($this->_cPtr);
	}

	/**
	 * The port number of the upload target (i.e. consumer) URL.  The default value is 80.  If SSL is used, this should be set to 443 (typically).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Port
	 * @param int $newVal
	 */
	function put_Port($newVal) {
		CkUpload_put_Port($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_, then use IPv6 over IPv4 when both are supported for a particular domain.   The default value of this property is _FALSE_, which will choose IPv4 over IPv6.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#PreferIpv6
	 * @return bool
	 */
	function get_PreferIpv6() {
		return CkUpload_get_PreferIpv6($this->_cPtr);
	}

	/**
	 * If _TRUE_, then use IPv6 over IPv4 when both are supported for a particular domain.   The default value of this property is _FALSE_, which will choose IPv4 over IPv6.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#PreferIpv6
	 * @param bool $newVal
	 */
	function put_PreferIpv6($newVal) {
		CkUpload_put_PreferIpv6($this->_cPtr,$newVal);
	}

	/**
	 * The domain name of a proxy host if an HTTP proxy is used.  Do not include the http://.  The domain name may be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyDomain
	 * @param CkString $str
	 */
	function get_ProxyDomain($str) {
		CkUpload_get_ProxyDomain($this->_cPtr,$str);
	}

	/**
	 * The domain name of a proxy host if an HTTP proxy is used.  Do not include the http://.  The domain name may be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyDomain
	 * @return ?string
	 */
	function proxyDomain() {
		return CkUpload_proxyDomain($this->_cPtr);
	}

	/**
	 * The domain name of a proxy host if an HTTP proxy is used.  Do not include the http://.  The domain name may be a hostname, such as www.chilkatsoft.com, or an IP address, such as 168.144.70.227.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyDomain
	 * @param ?string $newVal
	 */
	function put_ProxyDomain($newVal) {
		CkUpload_put_ProxyDomain($this->_cPtr,$newVal);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy login.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyLogin
	 * @param CkString $str
	 */
	function get_ProxyLogin($str) {
		CkUpload_get_ProxyLogin($this->_cPtr,$str);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy login.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyLogin
	 * @return ?string
	 */
	function proxyLogin() {
		return CkUpload_proxyLogin($this->_cPtr);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy login.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyLogin
	 * @param ?string $newVal
	 */
	function put_ProxyLogin($newVal) {
		CkUpload_put_ProxyLogin($this->_cPtr,$newVal);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy password.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyPassword
	 * @param CkString $str
	 */
	function get_ProxyPassword($str) {
		CkUpload_get_ProxyPassword($this->_cPtr,$str);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy password.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyPassword
	 * @return ?string
	 */
	function proxyPassword() {
		return CkUpload_proxyPassword($this->_cPtr);
	}

	/**
	 * If an HTTP proxy is used and it requires authentication, this property specifies the HTTP proxy password.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyPassword
	 * @param ?string $newVal
	 */
	function put_ProxyPassword($newVal) {
		CkUpload_put_ProxyPassword($this->_cPtr,$newVal);
	}

	/**
	 * The port number of a proxy server if an HTTP proxy is used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyPort
	 * @return int
	 */
	function get_ProxyPort() {
		return CkUpload_get_ProxyPort($this->_cPtr);
	}

	/**
	 * The port number of a proxy server if an HTTP proxy is used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ProxyPort
	 * @param int $newVal
	 */
	function put_ProxyPort($newVal) {
		CkUpload_put_ProxyPort($this->_cPtr,$newVal);
	}

	/**
	 * Returns the response body as a string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ResponseBodyStr
	 * @param CkString $str
	 */
	function get_ResponseBodyStr($str) {
		CkUpload_get_ResponseBodyStr($this->_cPtr,$str);
	}

	/**
	 * Returns the response body as a string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ResponseBodyStr
	 * @return ?string
	 */
	function responseBodyStr() {
		return CkUpload_responseBodyStr($this->_cPtr);
	}

	/**
	 * An HTTP upload is nothing more than an HTTP POST that contains the content of the files being uploaded.  Just as with any HTTP POST or GET, the server should send an HTTP response that consists of header and body.desc>This property contains the header part of the HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ResponseHeader
	 * @param CkString $str
	 */
	function get_ResponseHeader($str) {
		CkUpload_get_ResponseHeader($this->_cPtr,$str);
	}

	/**
	 * An HTTP upload is nothing more than an HTTP POST that contains the content of the files being uploaded.  Just as with any HTTP POST or GET, the server should send an HTTP response that consists of header and body.desc>This property contains the header part of the HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ResponseHeader
	 * @return ?string
	 */
	function responseHeader() {
		return CkUpload_responseHeader($this->_cPtr);
	}

	/**
	 * The HTTP response status code of the HTTP response.  A list of HTTP status codes can be found here: HTTP Response Status Codes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ResponseStatus
	 * @return int
	 */
	function get_ResponseStatus() {
		return CkUpload_get_ResponseStatus($this->_cPtr);
	}

	/**
	 * Set this to _TRUE_ if the upload is to HTTPS.  For example, if the target of the upload is: https://www.myuploadtarget.com/consumeUpload.asp desc>then set:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Ssl
	 * @return bool
	 */
	function get_Ssl() {
		return CkUpload_get_Ssl($this->_cPtr);
	}

	/**
	 * Set this to _TRUE_ if the upload is to HTTPS.  For example, if the target of the upload is: https://www.myuploadtarget.com/consumeUpload.asp desc>then set:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Ssl
	 * @param bool $newVal
	 */
	function put_Ssl($newVal) {
		CkUpload_put_Ssl($this->_cPtr,$newVal);
	}

	/**
	 * Provides a means for setting a list of ciphers that are allowed for SSL/TLS connections.  The default (empty string) indicates that all implemented ciphers are possible.  The TLS ciphers supported in Chilkat v9.5.0.55 and later are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslAllowedCiphers
	 * @param CkString $str
	 */
	function get_SslAllowedCiphers($str) {
		CkUpload_get_SslAllowedCiphers($this->_cPtr,$str);
	}

	/**
	 * Provides a means for setting a list of ciphers that are allowed for SSL/TLS connections.  The default (empty string) indicates that all implemented ciphers are possible.  The TLS ciphers supported in Chilkat v9.5.0.55 and later are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslAllowedCiphers
	 * @return ?string
	 */
	function sslAllowedCiphers() {
		return CkUpload_sslAllowedCiphers($this->_cPtr);
	}

	/**
	 * Provides a means for setting a list of ciphers that are allowed for SSL/TLS connections.  The default (empty string) indicates that all implemented ciphers are possible.  The TLS ciphers supported in Chilkat v9.5.0.55 and later are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslAllowedCiphers
	 * @param ?string $newVal
	 */
	function put_SslAllowedCiphers($newVal) {
		CkUpload_put_SslAllowedCiphers($this->_cPtr,$newVal);
	}

	/**
	 * Selects the secure protocol to be used for secure (SSL/TLS) connections.  Possible values are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslProtocol
	 * @param CkString $str
	 */
	function get_SslProtocol($str) {
		CkUpload_get_SslProtocol($this->_cPtr,$str);
	}

	/**
	 * Selects the secure protocol to be used for secure (SSL/TLS) connections.  Possible values are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslProtocol
	 * @return ?string
	 */
	function sslProtocol() {
		return CkUpload_sslProtocol($this->_cPtr);
	}

	/**
	 * Selects the secure protocol to be used for secure (SSL/TLS) connections.  Possible values are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SslProtocol
	 * @param ?string $newVal
	 */
	function put_SslProtocol($newVal) {
		CkUpload_put_SslProtocol($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a set of pins for Public Key Pinning for TLS connections.  This property lists the expected SPKI fingerprints for the server certificates.  If the server's certificate (sent during the TLS handshake) does not match any of the SPKI fingerprints, then the TLS handshake is aborted and the connection fails. The format of this string property is as follows:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#TlsPinSet
	 * @param CkString $str
	 */
	function get_TlsPinSet($str) {
		CkUpload_get_TlsPinSet($this->_cPtr,$str);
	}

	/**
	 * Specifies a set of pins for Public Key Pinning for TLS connections.  This property lists the expected SPKI fingerprints for the server certificates.  If the server's certificate (sent during the TLS handshake) does not match any of the SPKI fingerprints, then the TLS handshake is aborted and the connection fails. The format of this string property is as follows:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#TlsPinSet
	 * @return ?string
	 */
	function tlsPinSet() {
		return CkUpload_tlsPinSet($this->_cPtr);
	}

	/**
	 * Specifies a set of pins for Public Key Pinning for TLS connections.  This property lists the expected SPKI fingerprints for the server certificates.  If the server's certificate (sent during the TLS handshake) does not match any of the SPKI fingerprints, then the TLS handshake is aborted and the connection fails. The format of this string property is as follows:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#TlsPinSet
	 * @param ?string $newVal
	 */
	function put_TlsPinSet($newVal) {
		CkUpload_put_TlsPinSet($this->_cPtr,$newVal);
	}

	/**
	 * The total size of the upload (in bytes).  This property will become set at the beginning of an asynchronous upload.  A program may monitor asynchronous uploads by tracking both NumBytesSent and PercentUploaded. desc> This property is also set during synchronous uploads.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#TotalUploadSize
	 * @return int
	 */
	function get_TotalUploadSize() {
		return CkUpload_get_TotalUploadSize($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ when an asynchronous upload is started.  When the asynchronous upload is complete, this property becomes equal to _FALSE_.   A program will typically begin an asynchronous upload by calling BeginUpload, and then periodically checking the value of this property to determine when the upload is complete.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#UploadInProgress
	 * @return bool
	 */
	function get_UploadInProgress() {
		return CkUpload_get_UploadInProgress($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ (success) or _FALSE_ (failed) after an asynchronous upload completes or aborts due to failure.  When a program does an asynchronous upload, it will wait until UploadInProgress becomes _FALSE_.  It will then check the value of this property to determine if the upload was successful or not.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#UploadSuccess
	 * @return bool
	 */
	function get_UploadSuccess() {
		return CkUpload_get_UploadSuccess($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkUpload_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkUpload_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkUpload_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkUpload_version($this->_cPtr);
	}

	/**
	 * May be called during an asynchronous upload to abort.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AbortUpload
	 */
	function AbortUpload() {
		CkUpload_AbortUpload($this->_cPtr);
	}

	/**
	 * Adds a custom HTTP header to the HTTP upload.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AddCustomHeader
	 * @param ?string $name
	 * @param ?string $value
	 */
	function AddCustomHeader($name,$value) {
		CkUpload_AddCustomHeader($this->_cPtr,$name,$value);
	}

	/**
	 * Adds a file to the list of files to be uploaded in the next call to BlockingUpload, BeginUpload, or UploadToMemory.  To upload more than one file, call this method once for each file to be uploaded.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AddFileReference
	 * @param ?string $name
	 * @param ?string $filename
	 */
	function AddFileReference($name,$filename) {
		CkUpload_AddFileReference($this->_cPtr,$name,$filename);
	}

	/**
	 * Adds a custom HTTP request parameter to the upload.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#AddParam
	 * @param ?string $name
	 * @param ?string $value
	 */
	function AddParam($name,$value) {
		CkUpload_AddParam($this->_cPtr,$name,$value);
	}

	/**
	 * Starts an asynchronous upload.  Only one asynchronous upload may be in progress at a time.  To achieve multiple asynchronous uploads, use multiple instances of the Chilkat Upload object.  Each object instance is capable of managing a single asynchronous upload.desc> When this method is called, a background thread is started and the asynchronous upload runs in the background.  The upload may be aborted at any time by calling AbortUpload.   The upload is completed (or failed) when UploadInProgress becomes _FALSE_.  At that point, the UploadSuccess property may be checked to determine success (_TRUE_) or failure (_FALSE_).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#BeginUpload
	 * @return bool
	 */
	function BeginUpload() {
		return CkUpload_BeginUpload($this->_cPtr);
	}

	/**
	 * Uploads files to a web server using HTTP.  The files to be uploaded are indicated by calling AddFileReference once for each file (prior to calling this method).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#BlockingUpload
	 * @return bool
	 */
	function BlockingUpload() {
		return CkUpload_BlockingUpload($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the BlockingUpload method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#BlockingUploadAsync
	 * @return ?CkTask
	 */
	function BlockingUploadAsync() {
		return chilkat::_obj(CkUpload_BlockingUploadAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Clears the internal list of files created by calls to AddFileReference.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ClearFileReferences
	 */
	function ClearFileReferences() {
		CkUpload_ClearFileReferences($this->_cPtr);
	}

	/**
	 * Clears the internal list of params created by calls to AddParam.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#ClearParams
	 */
	function ClearParams() {
		CkUpload_ClearParams($this->_cPtr);
	}

	/**
	 * Loads the caller of the task's async method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkUpload_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * A convenience method for putting the calling process to sleep for N milliseconds.  It is provided here because some programming languages do not provide this capability, and sleeping for short increments of time is helpful when doing asynchronous uploads.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#SleepMs
	 * @param int $millisec
	 */
	function SleepMs($millisec) {
		CkUpload_SleepMs($this->_cPtr,$millisec);
	}

	/**
	 * Writes the complete HTTP POST to memory.  The POST contains the HTTP header, any custom params added by calling AddParam, and each file to be uploaded.  This is helpful in debugging.  It allows you to see the exact HTTP POST sent to the server if BlockingUpload or BeginUpload is called.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkUploadRef.html#UploadToMemory
	 * @param CkByteData $outData
	 * @return bool
	 */
	function UploadToMemory($outData) {
		return CkUpload_UploadToMemory($this->_cPtr,$outData);
	}
}
