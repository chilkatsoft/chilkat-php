<?php

/* CkEcc.php -- Chilkat PHP extension proxy class CkEcc.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Generate EC keys, create ECDSA signatures, verify signatures, and compute shared secrets.
 *
 * Chilkat.Ecc is the Chilkat class for direct elliptic-curve cryptographic operations. It can generate EC private keys, sign hashes or data using ECDSA, verify ECDSA signatures, sign through ECDSA certificates, and compute encoded shared secrets from compatible EC key pairs. It is used when an application needs explicit control over elliptic-curve keys, signatures, signature encoding, or key-agreement output.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html
 */
class CkEcc {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkEcc') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkEcc();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkEcc_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkEcc_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Controls the binary layout of ECDSA signatures created by this object. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#AsnFormat
	 * @return bool
	 */
	function get_AsnFormat() {
		return CkEcc_get_AsnFormat($this->_cPtr);
	}

	/**
	 * Controls the binary layout of ECDSA signatures created by this object. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#AsnFormat
	 * @param bool $newVal
	 */
	function put_AsnFormat($newVal) {
		CkEcc_put_AsnFormat($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkEcc_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkEcc_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkEcc_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkEcc_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkEcc_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkEcc_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkEcc_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkEcc_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkEcc_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkEcc_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkEcc_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkEcc_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkEcc_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkEcc_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkEcc_version($this->_cPtr);
	}

	/**
	 * Deprecated. Use GenKey for new code.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#GenEccKey
	 * @param ?string $curveName
	 * @param CkPrng $prng
	 * @return ?CkPrivateKey
	 * @deprecated
	 */
	function GenEccKey($curveName,$prng) {
		return chilkat::_obj(CkEcc_GenEccKey($this->_cPtr,$curveName,$prng),'CkPrivateKey');
	}

	/**
	 * Deprecated. Use GenKey2 for new code.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#GenEccKey2
	 * @param ?string $curveName
	 * @param ?string $encodedK
	 * @param ?string $encoding
	 * @return ?CkPrivateKey
	 * @deprecated
	 */
	function GenEccKey2($curveName,$encodedK,$encoding) {
		return chilkat::_obj(CkEcc_GenEccKey2($this->_cPtr,$curveName,$encodedK,$encoding),'CkPrivateKey');
	}

	/**
	 * Generates a new elliptic-curve private key and stores it in ARG3. ARG1 selects the named curve, and ARG2 supplies the cryptographically secure random data used to choose the private scalar. Returns _TRUE_ on success and _FALSE_ on failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#GenKey
	 * @param ?string $curveName
	 * @param CkPrng $prng
	 * @param CkPrivateKey $privKey
	 * @return bool
	 */
	function GenKey($curveName,$prng,$privKey) {
		return CkEcc_GenKey($this->_cPtr,$curveName,$prng,$privKey);
	}

	/**
	 * Constructs an elliptic-curve private key in ARG4 from a caller-supplied private scalar. ARG1 selects the named curve, ARG2 contains the scalar, and ARG3 names its representation, such as hex, base64, or decimal. Returns _TRUE_ on success and _FALSE_ on failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#GenKey2
	 * @param ?string $curveName
	 * @param ?string $encodedK
	 * @param ?string $encoding
	 * @param CkPrivateKey $privKey
	 * @return bool
	 */
	function GenKey2($curveName,$encodedK,$encoding,$privKey) {
		return CkEcc_GenKey2($this->_cPtr,$curveName,$encodedK,$encoding,$privKey);
	}

	/**
	 * Computes an elliptic-curve Diffie-Hellman shared secret from the private key in ARG1 and the peer's public key in ARG2. ARG3 specifies how the resulting bytes are encoded in the returned string, such as hex or base64.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#SharedSecretENC
	 * @param CkPrivateKey $privKey
	 * @param CkPublicKey $pubKey
	 * @param ?string $encoding
	 * @return ?string
	 */
	function sharedSecretENC($privKey,$pubKey,$encoding) {
		return CkEcc_sharedSecretENC($this->_cPtr,$privKey,$pubKey,$encoding);
	}

	/**
	 * Hashes the bytes currently contained in ARG1 and creates an ECDSA signature with the EC private key in ARG4. ARG2 selects the hash algorithm and may be sha256, sha384, or sha512. ARG3 selects the textual encoding of the returned signature, such as hex or base64. ARG5 supplies the random source used during ECDSA signature generation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#SignBd
	 * @param CkBinData $bdData
	 * @param ?string $hashAlg
	 * @param ?string $encoding
	 * @param CkPrivateKey $privKey
	 * @param CkPrng $prng
	 * @return ?string
	 */
	function signBd($bdData,$hashAlg,$encoding,$privKey,$prng) {
		return CkEcc_signBd($this->_cPtr,$bdData,$hashAlg,$encoding,$privKey,$prng);
	}

	/**
	 * Hashes the bytes in ARG1 and creates an ECDSA signature using the private key associated with the certificate in ARG4. ARG2 selects sha256, sha384, or sha512; ARG3 selects the textual encoding of the returned signature.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#SignBdUsingCert
	 * @param CkBinData $bdData
	 * @param ?string $hashAlg
	 * @param ?string $encoding
	 * @param CkCert $cert
	 * @return ?string
	 */
	function signBdUsingCert($bdData,$hashAlg,$encoding,$cert) {
		return CkEcc_signBdUsingCert($this->_cPtr,$bdData,$hashAlg,$encoding,$cert);
	}

	/**
	 * Creates an ECDSA signature over a digest that has already been computed by the application. ARG1 contains the digest encoded using the binary encoding named by ARG2, such as hex or base64. ARG3 supplies the EC private key, and ARG4 supplies the random source used during signature generation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#SignHashENC
	 * @param ?string $encodedHash
	 * @param ?string $encoding
	 * @param CkPrivateKey $privkey
	 * @param CkPrng $prng
	 * @return ?string
	 */
	function signHashENC($encodedHash,$encoding,$privkey,$prng) {
		return CkEcc_signHashENC($this->_cPtr,$encodedHash,$encoding,$privkey,$prng);
	}

	/**
	 * Creates an ECDSA signature over a digest that has already been computed. ARG1 contains the digest encoded using the binary encoding named by ARG2. ARG3 supplies a certificate whose public key is EC and whose associated private key is accessible for signing.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#SignHashUsingCert
	 * @param ?string $encodedHash
	 * @param ?string $encoding
	 * @param CkCert $cert
	 * @return ?string
	 */
	function signHashUsingCert($encodedHash,$encoding,$cert) {
		return CkEcc_signHashUsingCert($this->_cPtr,$encodedHash,$encoding,$cert);
	}

	/**
	 * Hashes the bytes in ARG1 using the algorithm named by ARG2 and verifies the ECDSA signature in ARG3 with the EC public key in ARG5. ARG4 identifies the textual encoding of the signature, such as hex or base64. Supported hash algorithms are sha256, sha384, and sha512.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#VerifyBd
	 * @param CkBinData $bdData
	 * @param ?string $hashAlg
	 * @param ?string $encodedSig
	 * @param ?string $encoding
	 * @param CkPublicKey $pubkey
	 * @return int
	 */
	function VerifyBd($bdData,$hashAlg,$encodedSig,$encoding,$pubkey) {
		return CkEcc_VerifyBd($this->_cPtr,$bdData,$hashAlg,$encodedSig,$encoding,$pubkey);
	}

	/**
	 * Verifies an ECDSA signature over a digest that was computed by the application. ARG1 is the encoded digest, ARG2 is the encoded signature, ARG3 names the binary encoding used by both strings, and ARG4 supplies the matching EC public key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkEccRef.html#VerifyHashENC
	 * @param ?string $encodedHash
	 * @param ?string $encodedSig
	 * @param ?string $encoding
	 * @param CkPublicKey $pubkey
	 * @return int
	 */
	function VerifyHashENC($encodedHash,$encodedSig,$encoding,$pubkey) {
		return CkEcc_VerifyHashENC($this->_cPtr,$encodedHash,$encodedSig,$encoding,$pubkey);
	}
}
