<?php

/* CkRest.php -- Chilkat PHP extension proxy class CkRest.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Build, send, authenticate, stream, and inspect REST API requests with precise control.
 *
 * Chilkat.Rest is a flexible REST client class for applications that need detailed control over request construction, connection reuse, authentication, headers, query parameters, multipart bodies, streaming uploads and downloads, response parsing, and low-level diagnostics. It is especially useful for APIs where the application must carefully manage provider-specific signing, request paths, body formats, and server responses.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html
 */
class CkRest {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkRest') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkRest();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkRest_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkRest_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkRest_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Controls whether long request-header fields may be folded onto continuation lines. The default is _FALSE_. When set to _FALSE_, each request header is emitted on a single physical line. Modern HTTP compatibility: Obsolete HTTP line folding is rejected by some servers and signing systems. Set this property to _FALSE_ when the service requires unfolded header lines, as is common for canonicalized request signatures.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AllowHeaderFolding
	 * @return bool
	 */
	function get_AllowHeaderFolding() {
		return CkRest_get_AllowHeaderFolding($this->_cPtr);
	}

	/**
	 * Controls whether long request-header fields may be folded onto continuation lines. The default is _FALSE_. When set to _FALSE_, each request header is emitted on a single physical line. Modern HTTP compatibility: Obsolete HTTP line folding is rejected by some servers and signing systems. Set this property to _FALSE_ when the service requires unfolded header lines, as is common for canonicalized request signatures.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AllowHeaderFolding
	 * @param bool $newVal
	 */
	function put_AllowHeaderFolding($newVal) {
		CkRest_put_AllowHeaderFolding($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether non-ASCII request-header values may be converted to RFC 2047 encoded-word form using Q or Base64 encoding. The default is _TRUE_. Encoded words have forms such as =?utf-8?Q?...?= and =?utf-8?B?...?=. When this property is _FALSE_, Chilkat does not apply this conversion. Compatibility feature: RFC 2047 encoded words originated in Internet message headers. Use this behavior only when the target HTTP service expects it; many APIs instead require ASCII header values or a service-specific encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AllowHeaderQB
	 * @return bool
	 */
	function get_AllowHeaderQB() {
		return CkRest_get_AllowHeaderQB($this->_cPtr);
	}

	/**
	 * Controls whether non-ASCII request-header values may be converted to RFC 2047 encoded-word form using Q or Base64 encoding. The default is _TRUE_. Encoded words have forms such as =?utf-8?Q?...?= and =?utf-8?B?...?=. When this property is _FALSE_, Chilkat does not apply this conversion. Compatibility feature: RFC 2047 encoded words originated in Internet message headers. Use this behavior only when the target HTTP service expects it; many APIs instead require ASCII header values or a service-specific encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AllowHeaderQB
	 * @param bool $newVal
	 */
	function put_AllowHeaderQB($newVal) {
		CkRest_put_AllowHeaderQB($this->_cPtr,$newVal);
	}

	/**
	 * Gets or sets the value sent in the HTTP Authorization request header. Supply only the field value, such as Bearer eyJ..., not the text Authorization:. Authentication helpers: For generated authentication, prefer the appropriate SetAuth* method. A configured authentication provider may generate or replace the Authorization header when the request is sent. Provider precedence: When a SetAuth* provider is active, its generated Authorization header takes precedence over this property and over a manually added Authorization header. ClearAuth clears both the provider and this value; clearing top-level headers also clears this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Authorization
	 * @param CkString $str
	 */
	function get_Authorization($str) {
		CkRest_get_Authorization($this->_cPtr,$str);
	}

	/**
	 * Gets or sets the value sent in the HTTP Authorization request header. Supply only the field value, such as Bearer eyJ..., not the text Authorization:. Authentication helpers: For generated authentication, prefer the appropriate SetAuth* method. A configured authentication provider may generate or replace the Authorization header when the request is sent. Provider precedence: When a SetAuth* provider is active, its generated Authorization header takes precedence over this property and over a manually added Authorization header. ClearAuth clears both the provider and this value; clearing top-level headers also clears this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Authorization
	 * @return ?string
	 */
	function authorization() {
		return CkRest_authorization($this->_cPtr);
	}

	/**
	 * Gets or sets the value sent in the HTTP Authorization request header. Supply only the field value, such as Bearer eyJ..., not the text Authorization:. Authentication helpers: For generated authentication, prefer the appropriate SetAuth* method. A configured authentication provider may generate or replace the Authorization header when the request is sent. Provider precedence: When a SetAuth* provider is active, its generated Authorization header takes precedence over this property and over a manually added Authorization header. ClearAuth clears both the provider and this value; clearing top-level headers also clears this property.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Authorization
	 * @param ?string $newVal
	 */
	function put_Authorization($newVal) {
		CkRest_put_Authorization($this->_cPtr,$newVal);
	}

	/**
	 * After Connect fails, returns a code identifying the connection stage or TLS handshake stage that failed. CodeMeaning 0Success 1Empty hostname 2DNS lookup failed 3DNS timeout 4Aborted by the application 5Internal failure 6Connection timed out 7Connection rejected or otherwise failed 99Chilkat is not unlocked or the trial expired TLS-related codes: CodeMeaning 100TLS internal error 101Failed to send ClientHello 102Unexpected handshake message 103Failed to read ServerHello 104No server certificate 105Unexpected TLS protocol version 106Server certificate verification failed 107Unacceptable TLS protocol version 109Failed to read handshake messages 110Failed to send the client-certificate handshake message 111Failed to send ClientKeyExchange 112Client certificate private key is inaccessible 113Failed to send CertificateVerify 114Failed to send ChangeCipherSpec 115Failed to send Finished 116The server Finished message is invalid Detailed diagnostics: Use the code as a category and inspect LastErrorText for the full failure context. Reset after success: A successful Connect resets this property to 0. HTTP response properties are separate and are not cleared by a later failed connection attempt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ConnectFailReason
	 * @return int
	 */
	function get_ConnectFailReason() {
		return CkRest_get_ConnectFailReason($this->_cPtr);
	}

	/**
	 * Gets or sets the maximum number of milliseconds to wait for the remote server to accept the TCP connection. This timeout covers connection establishment, not the later sending or receiving of HTTP data. Related timeout: Use IdleTimeoutMs to control how long an established operation may remain without send or receive progress. The default is 30000 milliseconds.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ConnectTimeoutMs
	 * @return int
	 */
	function get_ConnectTimeoutMs() {
		return CkRest_get_ConnectTimeoutMs($this->_cPtr);
	}

	/**
	 * Gets or sets the maximum number of milliseconds to wait for the remote server to accept the TCP connection. This timeout covers connection establishment, not the later sending or receiving of HTTP data. Related timeout: Use IdleTimeoutMs to control how long an established operation may remain without send or receive progress. The default is 30000 milliseconds.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ConnectTimeoutMs
	 * @param int $newVal
	 */
	function put_ConnectTimeoutMs($newVal) {
		CkRest_put_ConnectTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkRest_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkRest_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkRest_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, calls to the Send* and FullRequest* methods compose the complete request but do not transmit it. Retrieve the generated request with GetLastDebugRequest. The default is _FALSE_. Safe request inspection: This is useful for validating signing, headers, query strings, multipart formatting, and body encoding without contacting the server. The captured request may contain secrets. In debug mode, a composed full-request call reports a synthetic successful result with ResponseStatusCode equal to 201, ResponseStatusText equal to OK, and no actual response header or response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DebugMode
	 * @return bool
	 */
	function get_DebugMode() {
		return CkRest_get_DebugMode($this->_cPtr);
	}

	/**
	 * When _TRUE_, calls to the Send* and FullRequest* methods compose the complete request but do not transmit it. Retrieve the generated request with GetLastDebugRequest. The default is _FALSE_. Safe request inspection: This is useful for validating signing, headers, query strings, multipart formatting, and body encoding without contacting the server. The captured request may contain secrets. In debug mode, a composed full-request call reports a synthetic successful result with ResponseStatusCode equal to 201, ResponseStatusText equal to OK, and no actual response header or response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DebugMode
	 * @param bool $newVal
	 */
	function put_DebugMode($newVal) {
		CkRest_put_DebugMode($this->_cPtr,$newVal);
	}

	/**
	 * Controls automatic resolution of credentials from operating-system secure storage. The default is _FALSE_. When set to _TRUE_, supported credential inputs may contain a secret specification beginning with !! instead of a literal secret. Chilkat resolves the specification from Windows Credential Manager on Windows or Apple Keychain on macOS. The specification format is: !![appName|]service[|domain]|usernameFor this class, secret specifications apply to the username and password supplied to SetAuthBasic. Why use secret resolution: It avoids embedding long-lived credentials directly in source code, configuration files, command lines, or logs. The requested secret must already exist in the platform credential store.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#EnableSecrets
	 * @return bool
	 */
	function get_EnableSecrets() {
		return CkRest_get_EnableSecrets($this->_cPtr);
	}

	/**
	 * Controls automatic resolution of credentials from operating-system secure storage. The default is _FALSE_. When set to _TRUE_, supported credential inputs may contain a secret specification beginning with !! instead of a literal secret. Chilkat resolves the specification from Windows Credential Manager on Windows or Apple Keychain on macOS. The specification format is: !![appName|]service[|domain]|usernameFor this class, secret specifications apply to the username and password supplied to SetAuthBasic. Why use secret resolution: It avoids embedding long-lived credentials directly in source code, configuration files, command lines, or logs. The requested secret must already exist in the platform credential store.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#EnableSecrets
	 * @param bool $newVal
	 */
	function put_EnableSecrets($newVal) {
		CkRest_put_EnableSecrets($this->_cPtr,$newVal);
	}

	/**
	 * Controls the interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables periodic AbortCheck callbacks. Choosing an interval: A smaller value makes cancellation more responsive but causes callbacks more frequently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkRest_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Controls the interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables periodic AbortCheck callbacks. Choosing an interval: A smaller value makes cancellation more responsive but causes callbacks more frequently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkRest_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Gets or sets the value of the HTTP Host request header. Usually automatic: For a normal connection, Chilkat derives the Host header from the server established by Connect or UseConnection. Set this property explicitly only when the request must present a different virtual-host value. An explicitly assigned value overrides the generated HTTP Host field, but it does not change the TCP/TLS destination established by Connect or UseConnection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Host
	 * @param CkString $str
	 */
	function get_Host($str) {
		CkRest_get_Host($this->_cPtr,$str);
	}

	/**
	 * Gets or sets the value of the HTTP Host request header. Usually automatic: For a normal connection, Chilkat derives the Host header from the server established by Connect or UseConnection. Set this property explicitly only when the request must present a different virtual-host value. An explicitly assigned value overrides the generated HTTP Host field, but it does not change the TCP/TLS destination established by Connect or UseConnection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Host
	 * @return ?string
	 */
	function host() {
		return CkRest_host($this->_cPtr);
	}

	/**
	 * Gets or sets the value of the HTTP Host request header. Usually automatic: For a normal connection, Chilkat derives the Host header from the server established by Connect or UseConnection. Set this property explicitly only when the request must present a different virtual-host value. An explicitly assigned value overrides the generated HTTP Host field, but it does not change the TCP/TLS destination established by Connect or UseConnection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Host
	 * @param ?string $newVal
	 */
	function put_Host($newVal) {
		CkRest_put_Host($this->_cPtr,$newVal);
	}

	/**
	 * Gets or sets the maximum number of milliseconds to wait when a send or receive operation has stopped making progress. The default is 30000 milliseconds. Not an overall deadline: This is an inactivity timeout. An operation can run longer than this value as long as data continues to be sent or received. A value of 0 disables the inactivity timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#IdleTimeoutMs
	 * @return int
	 */
	function get_IdleTimeoutMs() {
		return CkRest_get_IdleTimeoutMs($this->_cPtr);
	}

	/**
	 * Gets or sets the maximum number of milliseconds to wait when a send or receive operation has stopped making progress. The default is 30000 milliseconds. Not an overall deadline: This is an inactivity timeout. An operation can run longer than this value as long as data continues to be sent or received. A value of 0 disables the inactivity timeout.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#IdleTimeoutMs
	 * @param int $newVal
	 */
	function put_IdleTimeoutMs($newVal) {
		CkRest_put_IdleTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkRest_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkRest_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkRest_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkRest_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkRest_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkRest_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkRest_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkRest_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Returns the redirect target from the most recently received response. The value comes from the response Location header. If the most recent response was not a redirect or did not contain a Location header, this property is empty. Relative redirects: A Location value may be an absolute URL or a relative reference, depending on the server response. Redirect handling: The value is the response Location field as received. Rest does not automatically follow the redirect; send a new request explicitly, reconnecting first if the target host changes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRedirectUrl
	 * @param CkString $str
	 */
	function get_LastRedirectUrl($str) {
		CkRest_get_LastRedirectUrl($this->_cPtr,$str);
	}

	/**
	 * Returns the redirect target from the most recently received response. The value comes from the response Location header. If the most recent response was not a redirect or did not contain a Location header, this property is empty. Relative redirects: A Location value may be an absolute URL or a relative reference, depending on the server response. Redirect handling: The value is the response Location field as received. Rest does not automatically follow the redirect; send a new request explicitly, reconnecting first if the target host changes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRedirectUrl
	 * @return ?string
	 */
	function lastRedirectUrl() {
		return CkRest_lastRedirectUrl($this->_cPtr);
	}

	/**
	 * Returns the HTTP header fields from the most recently composed request, excluding the request line. The request line is available separately in LastRequestStartLine. Sensitive data: The header block may contain credentials, bearer tokens, cookies, or signatures. Avoid writing it to logs unless sensitive values are redacted. A failed Connect does not clear this value. It continues to describe the last request that was composed and sent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRequestHeader
	 * @param CkString $str
	 */
	function get_LastRequestHeader($str) {
		CkRest_get_LastRequestHeader($this->_cPtr,$str);
	}

	/**
	 * Returns the HTTP header fields from the most recently composed request, excluding the request line. The request line is available separately in LastRequestStartLine. Sensitive data: The header block may contain credentials, bearer tokens, cookies, or signatures. Avoid writing it to logs unless sensitive values are redacted. A failed Connect does not clear this value. It continues to describe the last request that was composed and sent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRequestHeader
	 * @return ?string
	 */
	function lastRequestHeader() {
		return CkRest_lastRequestHeader($this->_cPtr);
	}

	/**
	 * Returns the request line from the most recently composed request. It contains the HTTP method, request target, and HTTP version, for example: GET /v1/items?limit=10 HTTP/1.1A failed Connect does not clear this value. It continues to describe the last request that was composed and sent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRequestStartLine
	 * @param CkString $str
	 */
	function get_LastRequestStartLine($str) {
		CkRest_get_LastRequestStartLine($this->_cPtr,$str);
	}

	/**
	 * Returns the request line from the most recently composed request. It contains the HTTP method, request target, and HTTP version, for example: GET /v1/items?limit=10 HTTP/1.1A failed Connect does not clear this value. It continues to describe the last request that was composed and sent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastRequestStartLine
	 * @return ?string
	 */
	function lastRequestStartLine() {
		return CkRest_lastRequestStartLine($this->_cPtr);
	}

	/**
	 * Returns the number of response header field occurrences. Enumerate them with zero-based indexes using ResponseHdrName and ResponseHdrValue. A failed Connect does not reset this count; it remains the count for the last received HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#NumResponseHeaders
	 * @return int
	 */
	function get_NumResponseHeaders() {
		return CkRest_get_NumResponseHeaders($this->_cPtr);
	}

	/**
	 * Selects the multipart entity or part targeted by multipart-building operations such as AddHeader, RemoveHeader, and the SetMultipartBody* methods. ValueSelected target  (empty)The top-level request entity 0The top-level request entity 1The first multipart subpart 2The second multipart subpart 1.2The second child within the first subpart Multipart indexes: PartSelector uses one-based part numbers, unlike most Chilkat indexed accessors. Reset it to the empty string before modifying top-level request headers. The value 0, like the empty string, selects the top-level request entity. A nonnumeric selector is invalid. A body assignment can create a higher-numbered part even when lower-numbered parts do not yet exist.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#PartSelector
	 * @param CkString $str
	 */
	function get_PartSelector($str) {
		CkRest_get_PartSelector($this->_cPtr,$str);
	}

	/**
	 * Selects the multipart entity or part targeted by multipart-building operations such as AddHeader, RemoveHeader, and the SetMultipartBody* methods. ValueSelected target  (empty)The top-level request entity 0The top-level request entity 1The first multipart subpart 2The second multipart subpart 1.2The second child within the first subpart Multipart indexes: PartSelector uses one-based part numbers, unlike most Chilkat indexed accessors. Reset it to the empty string before modifying top-level request headers. The value 0, like the empty string, selects the top-level request entity. A nonnumeric selector is invalid. A body assignment can create a higher-numbered part even when lower-numbered parts do not yet exist.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#PartSelector
	 * @return ?string
	 */
	function partSelector() {
		return CkRest_partSelector($this->_cPtr);
	}

	/**
	 * Selects the multipart entity or part targeted by multipart-building operations such as AddHeader, RemoveHeader, and the SetMultipartBody* methods. ValueSelected target  (empty)The top-level request entity 0The top-level request entity 1The first multipart subpart 2The second multipart subpart 1.2The second child within the first subpart Multipart indexes: PartSelector uses one-based part numbers, unlike most Chilkat indexed accessors. Reset it to the empty string before modifying top-level request headers. The value 0, like the empty string, selects the top-level request entity. A nonnumeric selector is invalid. A body assignment can create a higher-numbered part even when lower-numbered parts do not yet exist.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#PartSelector
	 * @param ?string $newVal
	 */
	function put_PartSelector($newVal) {
		CkRest_put_PartSelector($this->_cPtr,$newVal);
	}

	/**
	 * Applies to the full-request methods that both upload a request and download a response. It selects which direction is represented by percent-complete events. _FALSE_ (default): progress refers to receiving the response body. _TRUE_: progress refers to sending the request body. A known total is required: Percentage can be calculated only when the total byte count is known. For example, a chunked response may not provide a total response length.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#PercentDoneOnSend
	 * @return bool
	 */
	function get_PercentDoneOnSend() {
		return CkRest_get_PercentDoneOnSend($this->_cPtr);
	}

	/**
	 * Applies to the full-request methods that both upload a request and download a response. It selects which direction is represented by percent-complete events. _FALSE_ (default): progress refers to receiving the response body. _TRUE_: progress refers to sending the request body. A known total is required: Percentage can be calculated only when the total byte count is known. For example, a chunked response may not provide a total response length.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#PercentDoneOnSend
	 * @param bool $newVal
	 */
	function put_PercentDoneOnSend($newVal) {
		CkRest_put_PercentDoneOnSend($this->_cPtr,$newVal);
	}

	/**
	 * Returns the HTTP response header fields from the most recently received response, excluding the status line. Status line: The numeric status and reason text are available through ResponseStatusCode and ResponseStatusText. Repeated fields remain separate and in received order. The field-name casing is preserved. A failed Connect does not clear this value; it continues to describe the last received HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseHeader
	 * @param CkString $str
	 */
	function get_ResponseHeader($str) {
		CkRest_get_ResponseHeader($this->_cPtr,$str);
	}

	/**
	 * Returns the HTTP response header fields from the most recently received response, excluding the status line. Status line: The numeric status and reason text are available through ResponseStatusCode and ResponseStatusText. Repeated fields remain separate and in received order. The field-name casing is preserved. A failed Connect does not clear this value; it continues to describe the last received HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseHeader
	 * @return ?string
	 */
	function responseHeader() {
		return CkRest_responseHeader($this->_cPtr);
	}

	/**
	 * Returns the numeric HTTP status code from the most recently received response, such as 200, 404, or 503. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Last completed exchange: A failed Connect does not clear this property. It continues to report the status of the last HTTP response that was actually received.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseStatusCode
	 * @return int
	 */
	function get_ResponseStatusCode() {
		return CkRest_get_ResponseStatusCode($this->_cPtr);
	}

	/**
	 * Returns the reason phrase associated with the most recently received HTTP status code, such as OK or Not Found. Reason phrases are informational: Applications should make decisions from the numeric status code. A reason phrase may be empty or may vary by server. Last completed exchange: A failed Connect does not clear this property; it remains associated with the last received HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseStatusText
	 * @param CkString $str
	 */
	function get_ResponseStatusText($str) {
		CkRest_get_ResponseStatusText($this->_cPtr,$str);
	}

	/**
	 * Returns the reason phrase associated with the most recently received HTTP status code, such as OK or Not Found. Reason phrases are informational: Applications should make decisions from the numeric status code. A reason phrase may be empty or may vary by server. Last completed exchange: A failed Connect does not clear this property; it remains associated with the last received HTTP response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseStatusText
	 * @return ?string
	 */
	function responseStatusText() {
		return CkRest_responseStatusText($this->_cPtr);
	}

	/**
	 * Controls whether streaming uploads use a Content-Length header instead of HTTP/1.1 chunked transfer encoding when the total body size is known. The default is _TRUE_. When _TRUE_ and the stream length can be determined—for example, a file stream—Chilkat sends a non-chunked request. When _FALSE_, Chilkat uses Transfer-Encoding: chunked. If the stream length is unknown, chunked transfer may still be required. Server compatibility: Some HTTP servers and signing schemes require Content-Length and do not accept chunked request bodies; others support either form.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#StreamNonChunked
	 * @return bool
	 */
	function get_StreamNonChunked() {
		return CkRest_get_StreamNonChunked($this->_cPtr);
	}

	/**
	 * Controls whether streaming uploads use a Content-Length header instead of HTTP/1.1 chunked transfer encoding when the total body size is known. The default is _TRUE_. When _TRUE_ and the stream length can be determined—for example, a file stream—Chilkat sends a non-chunked request. When _FALSE_, Chilkat uses Transfer-Encoding: chunked. If the stream length is unknown, chunked transfer may still be required. Server compatibility: Some HTTP servers and signing schemes require Content-Length and do not accept chunked request bodies; others support either form.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#StreamNonChunked
	 * @param bool $newVal
	 */
	function put_StreamNonChunked($newVal) {
		CkRest_put_StreamNonChunked($this->_cPtr,$newVal);
	}

	/**
	 * Provides opt-in compatibility settings for uncommon requirements. The default is the empty string. Supply one or more comma-separated keywords: KeywordEffect AllowDuplicateQueryParamsAllows AddQueryParam to append another parameter having the same name instead of replacing the existing one. AllowInsecureBasicAuthAllows Basic authentication over a non-TLS remote connection. By default, Basic authentication is allowed only over TLS, an SSH tunnel, or localhost. DisableTls13Disables TLS 1.3 so negotiation is limited to TLS 1.2 or earlier supported versions. ProtectFromVpnOn Android, attempts to bypass an installed or active VPN for this connection. Use sparingly: Leave this property empty unless a documented interoperability requirement calls for one of these options. In particular, AllowInsecureBasicAuth can expose credentials to network observers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkRest_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides opt-in compatibility settings for uncommon requirements. The default is the empty string. Supply one or more comma-separated keywords: KeywordEffect AllowDuplicateQueryParamsAllows AddQueryParam to append another parameter having the same name instead of replacing the existing one. AllowInsecureBasicAuthAllows Basic authentication over a non-TLS remote connection. By default, Basic authentication is allowed only over TLS, an SSH tunnel, or localhost. DisableTls13Disables TLS 1.3 so negotiation is limited to TLS 1.2 or earlier supported versions. ProtectFromVpnOn Android, attempts to bypass an installed or active VPN for this connection. Use sparingly: Leave this property empty unless a documented interoperability requirement calls for one of these options. In particular, AllowInsecureBasicAuth can expose credentials to network observers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkRest_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides opt-in compatibility settings for uncommon requirements. The default is the empty string. Supply one or more comma-separated keywords: KeywordEffect AllowDuplicateQueryParamsAllows AddQueryParam to append another parameter having the same name instead of replacing the existing one. AllowInsecureBasicAuthAllows Basic authentication over a non-TLS remote connection. By default, Basic authentication is allowed only over TLS, an SSH tunnel, or localhost. DisableTls13Disables TLS 1.3 so negotiation is limited to TLS 1.2 or earlier supported versions. ProtectFromVpnOn Android, attempts to bypass an installed or active VPN for this connection. Use sparingly: Leave this property empty unless a documented interoperability requirement calls for one of these options. In particular, AllowInsecureBasicAuth can expose credentials to network observers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkRest_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkRest_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkRest_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkRest_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkRest_version($this->_cPtr);
	}

	/**
	 * Adds an HTTP request header using ARG1 as the field name and ARG2 as the field value. If a request header with the same name is already present for the selected part, its value is replaced. HTTP header names: Field names are case-insensitive. Pass only the field name in ARG1—for example, Content-Type—and not the trailing colon. For multipart requests, PartSelector determines whether the header is added to the top-level request or to a MIME part. Request headers persist across requests until removed or cleared. Adding the same header again replaces its current value. Multipart selection: Header operations apply to the entity selected by PartSelector. Use an empty selector or 0 for top-level request headers; select a part before adding that part's headers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddHeader
	 * @param ?string $name
	 * @param ?string $value
	 * @return bool
	 */
	function AddHeader($name,$value) {
		return CkRest_AddHeader($this->_cPtr,$name,$value);
	}

	/**
	 * Computes the Amazon Marketplace Web Service (MWS) request signature using the secret key in ARG4 and adds or replaces the Signature query parameter. Call this after all other MWS request parameters have been added. ARG1 is the HTTP method, ARG2 is the MWS resource path, and ARG3 is the request domain. ARG1 and ARG2 should match the values used by the request-sending method. The method also adds or replaces the Timestamp parameter using the current system time. Legacy MWS signing: This method implements Amazon MWS signing and is not a general AWS request signer. For AWS services that use AWS authorization providers, use SetAuthAws.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddMwsSignature
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param ?string $domain
	 * @param ?string $mwsSecretKey
	 * @return bool
	 */
	function AddMwsSignature($httpVerb,$uriPath,$domain,$mwsSecretKey) {
		return CkRest_AddMwsSignature($this->_cPtr,$httpVerb,$uriPath,$domain,$mwsSecretKey);
	}

	/**
	 * Adds or replaces a path-substitution parameter. Before a request is sent, occurrences of ARG1 in the supplied request path are replaced by ARG2. For example, setting ARG1 to fileId and ARG2 to abc123 transforms /drive/v3/files/fileId into /drive/v3/files/abc123. Placeholders are application-defined: The placeholder syntax is simply the text stored in ARG1; braces are not required unless they are part of the placeholder text you choose. Matching is case-sensitive and literal. Every occurrence of ARG1 is replaced, substitutions are applied in the order they were added, and replacement applies to the entire uriPath string, including any query portion. Reserved characters: The replacement is not a general path-segment encoder. Spaces are percent-encoded, but reserved URI characters such as /, ?, and # remain delimiters, and existing percent escapes are preserved. Percent-encode reserved characters before calling this method when they are intended to be data. Path substitutions persist across requests until replaced or removed with ClearAllPathParams.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddPathParam
	 * @param ?string $name
	 * @param ?string $value
	 * @return bool
	 */
	function AddPathParam($name,$value) {
		return CkRest_AddPathParam($this->_cPtr,$name,$value);
	}

	/**
	 * Adds or replaces a query-string parameter using ARG1 as the parameter name and ARG2 as its value. The stored query parameters are incorporated into the request target when a request is sent. Duplicate parameter names: By default, adding the same name again replaces the existing value. To retain repeated names such as tag=red&tag=blue, include AllowDuplicateQueryParams in UncommonOptions. Encoding and lifetime: ARG1 and ARG2 are unencoded text. Chilkat percent-encodes the parameter name and value when composing the request target. For example, a space becomes %20, a literal plus sign becomes %2B, and a literal percent sign becomes %25. Existing percent escapes supplied to this method are therefore encoded again. Parameters persist across ordinary requests until removed or cleared. Parameter names are case-sensitive. Adding the same exact name again replaces the existing value unless AllowDuplicateQueryParams is enabled in UncommonOptions.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddQueryParam
	 * @param ?string $name
	 * @param ?string $value
	 * @return bool
	 */
	function AddQueryParam($name,$value) {
		return CkRest_AddQueryParam($this->_cPtr,$name,$value);
	}

	/**
	 * Adds multiple query parameters from the query-string text in ARG1. Use the form field1=value1&field2=value2&field3=value3. Encoding responsibility: The values in ARG1 must already be percent-encoded as required for a URL query string. Do not include a leading ?. For individual name/value pairs, AddQueryParam is usually clearer. The input is parsed as an already URL-encoded query string and then normalized when the request is composed. A plus sign is interpreted as a space and is emitted as %20; encoded delimiters such as %26 remain data; an item without an equals sign is treated as a parameter with an empty value. Parameters added by this method persist across ordinary requests. When ARG2 of a request method already contains a query string, those inline parameters appear first, followed by the parameters stored in the Rest object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddQueryParams
	 * @param ?string $queryString
	 * @return bool
	 */
	function AddQueryParams($queryString) {
		return CkRest_AddQueryParams($this->_cPtr,$queryString);
	}

	/**
	 * Adds or replaces a query-string parameter named by ARG1. The parameter value is read from the StringBuilder in ARG2. Duplicate parameter names: By default, the same name is replaced. Include AllowDuplicateQueryParams in UncommonOptions to retain repeated names. Encoding and lifetime: ARG1 and the text in ARG2 are unencoded text. Chilkat percent-encodes the parameter name and value when composing the request target. For example, a space becomes %20, a literal plus sign becomes %2B, and a literal percent sign becomes %25. Existing percent escapes supplied to this method are therefore encoded again. Parameters persist across ordinary requests until removed or cleared. Parameter names are case-sensitive. Adding the same exact name again replaces the existing value unless AllowDuplicateQueryParams is enabled in UncommonOptions.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#AddQueryParamSb
	 * @param ?string $name
	 * @param CkStringBuilder $value
	 * @return bool
	 */
	function AddQueryParamSb($name,$value) {
		return CkRest_AddQueryParamSb($this->_cPtr,$name,$value);
	}

	/**
	 * Removes all HTTP request headers previously added to this object. The method clears headers only from the entity selected by PartSelector. Clearing top-level headers also clears the Authorization property. Automatically generated fields such as Host and Content-Length may be added again when the next request is composed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearAllHeaders
	 * @return bool
	 */
	function ClearAllHeaders() {
		return CkRest_ClearAllHeaders($this->_cPtr);
	}

	/**
	 * Removes all multipart subparts from the request under construction. This is useful when reusing the same REST object after sending a multipart request. Scope: This clears multipart parts; it does not by itself clear query parameters, path parameters, authentication settings, or ordinary top-level request headers. This also resets PartSelector to the empty string and releases the configured multipart bodies and part streams.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearAllParts
	 * @return bool
	 */
	function ClearAllParts() {
		return CkRest_ClearAllParts($this->_cPtr);
	}

	/**
	 * Removes all path-substitution parameters previously added with AddPathParam. After clearing, placeholder text in later request paths is sent unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearAllPathParams
	 * @return bool
	 */
	function ClearAllPathParams() {
		return CkRest_ClearAllPathParams($this->_cPtr);
	}

	/**
	 * Removes all query parameters previously added to this object. This clears the stored query/form parameter collection. It does not remove a query string written directly in the uriPath argument of a request method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearAllQueryParams
	 * @return bool
	 */
	function ClearAllQueryParams() {
		return CkRest_ClearAllQueryParams($this->_cPtr);
	}

	/**
	 * Clears authentication providers and credentials previously configured through this class's SetAuth* methods. This removes the active authentication provider and clears the manually configured Authorization value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearAuth
	 * @return bool
	 */
	function ClearAuth() {
		return CkRest_ClearAuth($this->_cPtr);
	}

	/**
	 * Clears the response-stream destination previously configured by SetResponseBodyStream. Subsequent full-request methods return or store the response body normally instead of streaming it to that destination. Clearing the destination does not alter the Stream object itself.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ClearResponseBodyStream
	 */
	function ClearResponseBodyStream() {
		CkRest_ClearResponseBodyStream($this->_cPtr);
	}

	/**
	 * Establishes the initial TCP connection to the REST server. ARG1 is a DNS hostname or an IPv4/IPv6 address, ARG2 is the server port, and ARG3 specifies whether the connection uses TLS. Common combinations are port 80 with ARG3 set to _FALSE_, and port 443 with ARG3 set to _TRUE_. If ARG4 is _TRUE_, subsequent requests may automatically reconnect when the existing connection is no longer usable. If ARG4 is _FALSE_, a lost connection causes the request to fail until the application explicitly reconnects. Hostname only: ARG1 is not a URL. For https://api.example.com/v1/items, pass api.example.com. The request path is supplied separately to the request method. Advanced connections: Connect is intended for a direct connection. For HTTP or SOCKS proxies, SSH tunneling, client certificates, custom socket routing, or other advanced connection setup, establish a connected Socket and pass it to UseConnection. Reconnect behavior: When ARG4 is _TRUE_, Chilkat can establish a new connection before a later request after the previous connection has closed. This option should not be interpreted as a promise to replay a request that failed after transmission began. Result-state behavior: A successful connection resets ConnectFailReason to 0. A failed Connect does not clear the HTTP response and last-request properties from an earlier completed exchange; those properties continue to describe the last HTTP request/response, not the failed connection attempt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Connect
	 * @param ?string $hostname
	 * @param int $port
	 * @param bool $tls
	 * @param bool $autoReconnect
	 * @return bool
	 */
	function Connect($hostname,$port,$tls,$autoReconnect) {
		return CkRest_Connect($this->_cPtr,$hostname,$port,$tls,$autoReconnect);
	}

	/**
	 * Creates an asynchronous task to call the Connect method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ConnectAsync
	 * @param ?string $hostname
	 * @param int $port
	 * @param bool $tls
	 * @param bool $autoReconnect
	 * @return ?CkTask
	 */
	function ConnectAsync($hostname,$port,$tls,$autoReconnect) {
		return chilkat::_obj(CkRest_ConnectAsync($this->_cPtr,$hostname,$port,$tls,$autoReconnect),'CkTask');
	}

	/**
	 * Closes the current HTTP connection, if one is open. ARG1 is the maximum number of milliseconds to wait while attempting a clean shutdown. Tunneled connections: When the REST connection uses an SSH tunnel, this closes the logical channel used by REST. It does not close the underlying SSH session itself. Socket supplied by UseConnection: When the transport was supplied through UseConnection, calling Disconnect also disconnects that Socket. If automatic reconnection was enabled, a later request may reconnect it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#Disconnect
	 * @param int $maxWaitMs
	 * @return bool
	 */
	function Disconnect($maxWaitMs) {
		return CkRest_Disconnect($this->_cPtr,$maxWaitMs);
	}

	/**
	 * Creates an asynchronous task to call the Disconnect method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#DisconnectAsync
	 * @param int $maxWaitMs
	 * @return ?CkTask
	 */
	function DisconnectAsync($maxWaitMs) {
		return chilkat::_obj(CkRest_DisconnectAsync($this->_cPtr,$maxWaitMs),'CkTask');
	}

	/**
	 * Sends a complete request whose binary body is read from the BinData in ARG3. The text response body replaces the existing contents of the StringBuilder in ARG4. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. ARG4 is cleared and replaced with the textual response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestBd
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $binData
	 * @param CkStringBuilder $responseBody
	 * @return bool
	 */
	function FullRequestBd($httpVerb,$uriPath,$binData,$responseBody) {
		return CkRest_FullRequestBd($this->_cPtr,$httpVerb,$uriPath,$binData,$responseBody);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestBdAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $binData
	 * @param CkStringBuilder $responseBody
	 * @return ?CkTask
	 */
	function FullRequestBdAsync($httpVerb,$uriPath,$binData,$responseBody) {
		return chilkat::_obj(CkRest_FullRequestBdAsync($this->_cPtr,$httpVerb,$uriPath,$binData,$responseBody),'CkTask');
	}

	/**
	 * Sends a complete request whose body is the binary data in ARG3. The response body is read as text and returned. This convenience method performs the equivalent of SendReqBinaryBody, ReadResponseHeader, and ReadRespBodyString in sequence. For full-request methods, PercentDoneOnSend selects whether percent-complete events refer to sending the request body or receiving the response body. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Preferred binary API: For new code, FullRequestBd uses BinData and avoids language-specific byte-array limitations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestBinary
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkByteData $bodyBytes
	 * @return ?string
	 */
	function fullRequestBinary($httpVerb,$uriPath,$bodyBytes) {
		return CkRest_fullRequestBinary($this->_cPtr,$httpVerb,$uriPath,$bodyBytes);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestBinary method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestBinaryAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkByteData $bodyBytes
	 * @return ?CkTask
	 */
	function FullRequestBinaryAsync($httpVerb,$uriPath,$bodyBytes) {
		return chilkat::_obj(CkRest_FullRequestBinaryAsync($this->_cPtr,$httpVerb,$uriPath,$bodyBytes),'CkTask');
	}

	/**
	 * Sends a complete application/x-www-form-urlencoded request. The form body is built from the parameters previously added with AddQueryParam or AddQueryParams. The request Content-Type is set to application/x-www-form-urlencoded. The response body is read as text and returned. This convenience method performs the equivalent of SendReqFormUrlEncoded, ReadResponseHeader, and ReadRespBodyString in sequence. For full-request methods, PercentDoneOnSend selects whether percent-complete events refer to sending the request body or receiving the response body. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Parameters written directly in ARG2 remain in the URL query string. Parameters stored through AddQueryParam, AddQueryParamSb, or AddQueryParams are encoded into the form body. After the form request is sent, this stored parameter collection is cleared. Persistent Content-Type: The method sets the top-level Content-Type to application/x-www-form-urlencoded. That request-header value remains configured for later requests until it is replaced or cleared.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestFormUrlEncoded
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?string
	 */
	function fullRequestFormUrlEncoded($httpVerb,$uriPath) {
		return CkRest_fullRequestFormUrlEncoded($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestFormUrlEncoded method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestFormUrlEncodedAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function FullRequestFormUrlEncodedAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_FullRequestFormUrlEncodedAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a complete multipart request using the parts and part headers configured through PartSelector, AddHeader, and the multipart-body methods. The response body is read as text and returned. This convenience method performs the equivalent of SendReqMultipart, ReadResponseHeader, and ReadRespBodyString in sequence. For full-request methods, PercentDoneOnSend selects whether percent-complete events refer to sending the request body or receiving the response body. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Multipart state: Assigning a body to a selected part creates that part. Parts may be created out of numeric order, persist across multipart requests, and retain the generated boundary while the multipart structure remains configured. The default top-level media type is multipart/form-data. The request fails if no multipart subparts are configured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestMultipart
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?string
	 */
	function fullRequestMultipart($httpVerb,$uriPath) {
		return CkRest_fullRequestMultipart($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestMultipart method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestMultipartAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function FullRequestMultipartAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_FullRequestMultipartAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a complete request with no request body. The response body is read as text and returned. This convenience method performs the equivalent of SendReqNoBody, ReadResponseHeader, and ReadRespBodyString in sequence. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBody
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?string
	 */
	function fullRequestNoBody($httpVerb,$uriPath) {
		return CkRest_fullRequestNoBody($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestNoBody method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBodyAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function FullRequestNoBodyAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_FullRequestNoBodyAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a complete request without a request body and stores the binary response body in the BinData in ARG3, replacing its previous contents. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. ARG3 is cleared and replaced with the binary response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBodyBd
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $binData
	 * @return bool
	 */
	function FullRequestNoBodyBd($httpVerb,$uriPath,$binData) {
		return CkRest_FullRequestNoBodyBd($this->_cPtr,$httpVerb,$uriPath,$binData);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestNoBodyBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBodyBdAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $binData
	 * @return ?CkTask
	 */
	function FullRequestNoBodyBdAsync($httpVerb,$uriPath,$binData) {
		return chilkat::_obj(CkRest_FullRequestNoBodyBdAsync($this->_cPtr,$httpVerb,$uriPath,$binData),'CkTask');
	}

	/**
	 * Sends a complete request without a request body and stores the text response body in the StringBuilder in ARG3, replacing its previous contents. This is the StringBuilder form of FullRequestNoBody. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. ARG3 is cleared and replaced with the textual response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBodySb
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function FullRequestNoBodySb($httpVerb,$uriPath,$sb) {
		return CkRest_FullRequestNoBodySb($this->_cPtr,$httpVerb,$uriPath,$sb);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestNoBodySb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestNoBodySbAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $sb
	 * @return ?CkTask
	 */
	function FullRequestNoBodySbAsync($httpVerb,$uriPath,$sb) {
		return chilkat::_obj(CkRest_FullRequestNoBodySbAsync($this->_cPtr,$httpVerb,$uriPath,$sb),'CkTask');
	}

	/**
	 * Sends a complete request whose text body is read from the StringBuilder in ARG3. The text response body replaces the existing contents of the StringBuilder in ARG4. This is the StringBuilder form of FullRequestString. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Text encoding and Content-Type: Unicode text is encoded as UTF-8. Generic string-body methods do not automatically add a Content-Type header; add the media type required by the API, such as application/json; charset=utf-8. ARG4 is cleared and replaced with the response body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestSb
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $requestBody
	 * @param CkStringBuilder $responseBody
	 * @return bool
	 */
	function FullRequestSb($httpVerb,$uriPath,$requestBody,$responseBody) {
		return CkRest_FullRequestSb($this->_cPtr,$httpVerb,$uriPath,$requestBody,$responseBody);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestSbAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $requestBody
	 * @param CkStringBuilder $responseBody
	 * @return ?CkTask
	 */
	function FullRequestSbAsync($httpVerb,$uriPath,$requestBody,$responseBody) {
		return chilkat::_obj(CkRest_FullRequestSbAsync($this->_cPtr,$httpVerb,$uriPath,$requestBody,$responseBody),'CkTask');
	}

	/**
	 * Sends a complete request whose body is read from the Stream in ARG3. Streaming is appropriate for large uploads because the entire request body need not be held in memory. The response body is read as text and returned. This convenience method performs the equivalent of SendReqStreamBody, ReadResponseHeader, and ReadRespBodyString in sequence. For full-request methods, PercentDoneOnSend selects whether percent-complete events refer to sending the request body or receiving the response body. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Stream position: The stream is read from its current position and is left consumed. Reusing the same stream without resetting its source or position sends only the unread remainder, which may be zero bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestStream
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStream $stream
	 * @return ?string
	 */
	function fullRequestStream($httpVerb,$uriPath,$stream) {
		return CkRest_fullRequestStream($this->_cPtr,$httpVerb,$uriPath,$stream);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestStream method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestStreamAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStream $stream
	 * @return ?CkTask
	 */
	function FullRequestStreamAsync($httpVerb,$uriPath,$stream) {
		return chilkat::_obj(CkRest_FullRequestStreamAsync($this->_cPtr,$httpVerb,$uriPath,$stream),'CkTask');
	}

	/**
	 * Sends a complete request whose text body is supplied in ARG3, such as JSON, XML, or plain text. The response body is read as text and returned. This convenience method performs the equivalent of SendReqStringBody, ReadResponseHeader, and ReadRespBodyString in sequence. For full-request methods, PercentDoneOnSend selects whether percent-complete events refer to sending the request body or receiving the response body. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. HTTP status versus method success: Receiving an HTTP error status such as 400 or 404 is still a completed HTTP exchange. Inspect ResponseStatusCode and the response body to determine the API-level result. Text encoding and Content-Type: Unicode text is encoded as UTF-8. Generic string-body methods do not automatically add a Content-Type header; add the media type required by the API, such as application/json; charset=utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestString
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param ?string $bodyText
	 * @return ?string
	 */
	function fullRequestString($httpVerb,$uriPath,$bodyText) {
		return CkRest_fullRequestString($this->_cPtr,$httpVerb,$uriPath,$bodyText);
	}

	/**
	 * Creates an asynchronous task to call the FullRequestString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#FullRequestStringAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param ?string $bodyText
	 * @return ?CkTask
	 */
	function FullRequestStringAsync($httpVerb,$uriPath,$bodyText) {
		return chilkat::_obj(CkRest_FullRequestStringAsync($this->_cPtr,$httpVerb,$uriPath,$bodyText),'CkTask');
	}

	/**
	 * Copies the fully composed HTTP request generated while DebugMode was _TRUE_ into the BinData in ARG1. The captured data includes the request line, headers, and body that would have been sent. Diagnostic use: Debug mode does not transmit the request. Treat captured requests as sensitive because they may contain authorization headers, cookies, tokens, or request-body credentials. ARG1 is cleared and replaced. The captured bytes are the complete composed HTTP/1.1 request, including chunk framing when a streaming request uses Transfer-Encoding: chunked.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#GetLastDebugRequest
	 * @param CkBinData $bd
	 * @return bool
	 */
	function GetLastDebugRequest($bd) {
		return CkRest_GetLastDebugRequest($this->_cPtr,$bd);
	}

	/**
	 * Copies operation-specific diagnostic or result information from the most recently completed method into the JsonObject in ARG1. Many methods do not produce additional JSON details, in which case the object is empty. Read immediately: Call this immediately after the operation of interest because a later method call may replace the stored JSON data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#GetLastJsonData
	 * @param CkJsonObject $json
	 */
	function GetLastJsonData($json) {
		CkRest_GetLastJsonData($this->_cPtr,$json);
	}

	/**
	 * Deprecated. Use GetLastJsonData instead. Returns a JSON object containing operation-specific details from the most recently completed method when such details are available. Many operations produce an empty object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LastJsonData
	 * @return ?CkJsonObject
	 * @deprecated
	 */
	function LastJsonData() {
		return chilkat::_obj(CkRest_LastJsonData($this->_cPtr),'CkJsonObject');
	}

	/**
	 * Loads into this object the caller associated with the Task in ARG1. ARG1 must be a task returned by an asynchronous method of this class. Async task relationship: This method recovers the originating REST caller state from a task; it does not load arbitrary task data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkRest_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Reads the response body as raw bytes into the BinData in ARG1, replacing its previous contents. Call this after ReadResponseHeader. Use for binary content: Typical examples include images, archives, PDFs, encrypted data, and arbitrary file downloads. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBd
	 * @param CkBinData $responseBody
	 * @return bool
	 */
	function ReadRespBd($responseBody) {
		return CkRest_ReadRespBd($this->_cPtr,$responseBody);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBdAsync
	 * @param CkBinData $responseBody
	 * @return ?CkTask
	 */
	function ReadRespBdAsync($responseBody) {
		return chilkat::_obj(CkRest_ReadRespBdAsync($this->_cPtr,$responseBody),'CkTask');
	}

	/**
	 * Reads the response body as raw bytes. Call this only after ReadResponseHeader has returned the response status code. Choosing a response type: Use this method for images, archives, encrypted content, and other non-text data. For new code, ReadRespBd stores the bytes in BinData. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyBinary
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function ReadRespBodyBinary($outBytes) {
		return CkRest_ReadRespBodyBinary($this->_cPtr,$outBytes);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespBodyBinary method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyBinaryAsync
	 * @return ?CkTask
	 */
	function ReadRespBodyBinaryAsync() {
		return chilkat::_obj(CkRest_ReadRespBodyBinaryAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Reads the response body into the Stream supplied in ARG1. Call this after ReadResponseHeader. If ARG2 is _TRUE_ and the response is textual, the stream's StringCharset is set from the charset parameter in the response Content-Type header when one is present. ARG2 has no effect for binary responses. Large downloads: A stream can write directly to a file or another destination, avoiding the need to buffer the entire response body in memory. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyStream
	 * @param CkStream $stream
	 * @param bool $autoSetStreamCharset
	 * @return bool
	 */
	function ReadRespBodyStream($stream,$autoSetStreamCharset) {
		return CkRest_ReadRespBodyStream($this->_cPtr,$stream,$autoSetStreamCharset);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespBodyStream method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyStreamAsync
	 * @param CkStream $stream
	 * @param bool $autoSetStreamCharset
	 * @return ?CkTask
	 */
	function ReadRespBodyStreamAsync($stream,$autoSetStreamCharset) {
		return chilkat::_obj(CkRest_ReadRespBodyStreamAsync($this->_cPtr,$stream,$autoSetStreamCharset),'CkTask');
	}

	/**
	 * Reads the response body as text and returns it. Call this only after ReadResponseHeader has returned the response status code. Text versus binary: Use this method for JSON, XML, HTML, and other textual responses. Use ReadRespBd or ReadRespBodyStream for binary or very large content. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyString
	 * @return ?string
	 */
	function readRespBodyString() {
		return CkRest_readRespBodyString($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespBodyString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespBodyStringAsync
	 * @return ?CkTask
	 */
	function ReadRespBodyStringAsync() {
		return chilkat::_obj(CkRest_ReadRespBodyStringAsync($this->_cPtr),'CkTask');
	}

	/**
	 * After ReadResponseHeader has been called, reads the next chunk of the response body and appends the received bytes to the BinData in ARG2. The call waits until at least ARG1 bytes are available, unless the response ends first. The final chunk may contain fewer than ARG1 bytes, including zero bytes. Return valueMeaning -1The read failed; inspect LastErrorText. 0The remaining response bytes were returned and the response body is complete. 1A chunk was returned and more response bytes remain. Incremental processing: This method is useful when the application wants to process a long response progressively rather than buffer it all at once.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespChunkBd
	 * @param int $minSize
	 * @param CkBinData $bd
	 * @return int
	 */
	function ReadRespChunkBd($minSize,$bd) {
		return CkRest_ReadRespChunkBd($this->_cPtr,$minSize,$bd);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespChunkBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespChunkBdAsync
	 * @param int $minSize
	 * @param CkBinData $bd
	 * @return ?CkTask
	 */
	function ReadRespChunkBdAsync($minSize,$bd) {
		return chilkat::_obj(CkRest_ReadRespChunkBdAsync($this->_cPtr,$minSize,$bd),'CkTask');
	}

	/**
	 * Reads the HTTP response status line and header fields. The returned integer is the HTTP status code, such as 200, 404, or 500. A return value of -1 means that no HTTP response was received because a transport or protocol error occurred. After this method returns a status code, inspect ResponseHeader, ResponseStatusCode, ResponseStatusText, and the response-header access methods. If the response has a body, read it with ReadRespBodyString, ReadRespBd, ReadRespBodyStream, or another appropriate body-reading method. Do not infer body presence from the request method alone: Whether a response body is present depends on the response status, request method, and response headers. For example, a GET commonly has a response body, while HEAD does not. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadResponseHeader
	 * @return int
	 */
	function ReadResponseHeader() {
		return CkRest_ReadResponseHeader($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ReadResponseHeader method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadResponseHeaderAsync
	 * @return ?CkTask
	 */
	function ReadResponseHeaderAsync() {
		return chilkat::_obj(CkRest_ReadResponseHeaderAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Reads the response body as text into the StringBuilder in ARG1, replacing its previous contents. Call this after ReadResponseHeader. Read once: A response body may be consumed by only one body-reading method. A second attempt fails immediately. Fully consume the body before sending another request on the same connection; otherwise unread bytes can prevent the next response header from being parsed correctly. Responses without a body: Do not call a body-reading method for a HEAD response. A 204 No Content response is handled as a successful empty body.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespSb
	 * @param CkStringBuilder $responseBody
	 * @return bool
	 */
	function ReadRespSb($responseBody) {
		return CkRest_ReadRespSb($this->_cPtr,$responseBody);
	}

	/**
	 * Creates an asynchronous task to call the ReadRespSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ReadRespSbAsync
	 * @param CkStringBuilder $responseBody
	 * @return ?CkTask
	 */
	function ReadRespSbAsync($responseBody) {
		return chilkat::_obj(CkRest_ReadRespSbAsync($this->_cPtr,$responseBody),'CkTask');
	}

	/**
	 * Deprecated. Use LastRedirectUrl instead. If the most recent response is a redirect and contains a Location header, returns that redirect URL.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#RedirectUrl
	 * @return ?CkUrl
	 * @deprecated
	 */
	function RedirectUrl() {
		return chilkat::_obj(CkRest_RedirectUrl($this->_cPtr),'CkUrl');
	}

	/**
	 * Removes all request headers named by ARG1 from the request level selected by PartSelector. Header-name matching is case-insensitive, and all matching occurrences are removed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#RemoveHeader
	 * @param ?string $name
	 * @return bool
	 */
	function RemoveHeader($name) {
		return CkRest_RemoveHeader($this->_cPtr,$name);
	}

	/**
	 * Removes all query parameters whose name matches ARG1. Parameter-name matching is case-sensitive. All parameters having the exact name are removed. A _TRUE_ return value indicates that the operation completed successfully; it does not guarantee that a matching parameter existed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#RemoveQueryParam
	 * @param ?string $name
	 * @return bool
	 */
	function RemoveQueryParam($name) {
		return CkRest_RemoveQueryParam($this->_cPtr,$name);
	}

	/**
	 * Returns the value of the response header field named by ARG1. HTTP header field names are case-insensitive. Repeated response headers: When the response can contain repeated fields, use NumResponseHeaders, ResponseHdrName, and ResponseHdrValue to enumerate every field occurrence. If the response contains the same field more than once, this method returns the first matching value. Enumerate the header collection to obtain all occurrences, especially for fields such as Set-Cookie.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseHdrByName
	 * @param ?string $name
	 * @return ?string
	 */
	function responseHdrByName($name) {
		return CkRest_responseHdrByName($this->_cPtr,$name);
	}

	/**
	 * Returns the field name of the response header at zero-based index ARG1. Valid indexes range from 0 through NumResponseHeaders minus one. Enumeration preserves the response-field order and the field-name casing received from the server. Repeated fields occupy separate indexes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseHdrName
	 * @param int $index
	 * @return ?string
	 */
	function responseHdrName($index) {
		return CkRest_responseHdrName($this->_cPtr,$index);
	}

	/**
	 * Returns the field value of the response header at zero-based index ARG1. Use the same index with ResponseHdrName to obtain the corresponding field name. Repeated response fields occupy separate indexes and are returned in received order.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#ResponseHdrValue
	 * @param int $index
	 * @return ?string
	 */
	function responseHdrValue($index) {
		return CkRest_responseHdrValue($this->_cPtr,$index);
	}

	/**
	 * Sends a request whose binary body is read from the BinData in ARG3. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqBd
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $body
	 * @return bool
	 */
	function SendReqBd($httpVerb,$uriPath,$body) {
		return CkRest_SendReqBd($this->_cPtr,$httpVerb,$uriPath,$body);
	}

	/**
	 * Creates an asynchronous task to call the SendReqBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqBdAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkBinData $body
	 * @return ?CkTask
	 */
	function SendReqBdAsync($httpVerb,$uriPath,$body) {
		return chilkat::_obj(CkRest_SendReqBdAsync($this->_cPtr,$httpVerb,$uriPath,$body),'CkTask');
	}

	/**
	 * Sends a request whose body is the binary byte array in ARG3. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Preferred binary API: For new code, SendReqBd uses BinData.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqBinaryBody
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkByteData $body
	 * @return bool
	 */
	function SendReqBinaryBody($httpVerb,$uriPath,$body) {
		return CkRest_SendReqBinaryBody($this->_cPtr,$httpVerb,$uriPath,$body);
	}

	/**
	 * Creates an asynchronous task to call the SendReqBinaryBody method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqBinaryBodyAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkByteData $body
	 * @return ?CkTask
	 */
	function SendReqBinaryBodyAsync($httpVerb,$uriPath,$body) {
		return chilkat::_obj(CkRest_SendReqBinaryBodyAsync($this->_cPtr,$httpVerb,$uriPath,$body),'CkTask');
	}

	/**
	 * Sends an application/x-www-form-urlencoded request. The request body is built from the parameters added with AddQueryParam or AddQueryParams. Any manually supplied Content-Type value is replaced with application/x-www-form-urlencoded. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Parameters written directly in ARG2 remain in the URL query string. Parameters stored through AddQueryParam, AddQueryParamSb, or AddQueryParams are encoded into the form body. After the form request is sent, this stored parameter collection is cleared. Persistent Content-Type: The method sets the top-level Content-Type to application/x-www-form-urlencoded. That request-header value remains configured for later requests until it is replaced or cleared.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqFormUrlEncoded
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return bool
	 */
	function SendReqFormUrlEncoded($httpVerb,$uriPath) {
		return CkRest_SendReqFormUrlEncoded($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the SendReqFormUrlEncoded method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqFormUrlEncodedAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function SendReqFormUrlEncodedAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_SendReqFormUrlEncodedAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a multipart request using the parts configured with PartSelector, AddHeader, and the multipart-body methods. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Multipart state: Assigning a body to a selected part creates that part. Parts may be created out of numeric order, persist across multipart requests, and retain the generated boundary while the multipart structure remains configured. The default top-level media type is multipart/form-data. The request fails if no multipart subparts are configured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqMultipart
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return bool
	 */
	function SendReqMultipart($httpVerb,$uriPath) {
		return CkRest_SendReqMultipart($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the SendReqMultipart method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqMultipartAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function SendReqMultipartAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_SendReqMultipartAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a request without a request body. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqNoBody
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return bool
	 */
	function SendReqNoBody($httpVerb,$uriPath) {
		return CkRest_SendReqNoBody($this->_cPtr,$httpVerb,$uriPath);
	}

	/**
	 * Creates an asynchronous task to call the SendReqNoBody method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqNoBodyAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @return ?CkTask
	 */
	function SendReqNoBodyAsync($httpVerb,$uriPath) {
		return chilkat::_obj(CkRest_SendReqNoBodyAsync($this->_cPtr,$httpVerb,$uriPath),'CkTask');
	}

	/**
	 * Sends a request whose text body is read from the StringBuilder in ARG3. Set the request Content-Type header to identify the body format. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Text encoding and Content-Type: Unicode text is encoded as UTF-8. Generic string-body methods do not automatically add a Content-Type header; add the media type required by the API, such as application/json; charset=utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqSb
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $bodySb
	 * @return bool
	 */
	function SendReqSb($httpVerb,$uriPath,$bodySb) {
		return CkRest_SendReqSb($this->_cPtr,$httpVerb,$uriPath,$bodySb);
	}

	/**
	 * Creates an asynchronous task to call the SendReqSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqSbAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStringBuilder $bodySb
	 * @return ?CkTask
	 */
	function SendReqSbAsync($httpVerb,$uriPath,$bodySb) {
		return chilkat::_obj(CkRest_SendReqSbAsync($this->_cPtr,$httpVerb,$uriPath,$bodySb),'CkTask');
	}

	/**
	 * Sends a request whose body is read from the Stream in ARG3. The stream may supply text or binary data; set the request Content-Type header as required by the API. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Streaming: The stream is consumed while the request is sent, which is useful when the body is too large to hold in memory. Stream position: The stream is read from its current position and is left consumed. Reusing the same stream without resetting its source or position sends only the unread remainder, which may be zero bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqStreamBody
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStream $stream
	 * @return bool
	 */
	function SendReqStreamBody($httpVerb,$uriPath,$stream) {
		return CkRest_SendReqStreamBody($this->_cPtr,$httpVerb,$uriPath,$stream);
	}

	/**
	 * Creates an asynchronous task to call the SendReqStreamBody method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqStreamBodyAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param CkStream $stream
	 * @return ?CkTask
	 */
	function SendReqStreamBodyAsync($httpVerb,$uriPath,$stream) {
		return chilkat::_obj(CkRest_SendReqStreamBodyAsync($this->_cPtr,$httpVerb,$uriPath,$stream),'CkTask');
	}

	/**
	 * Sends a request whose text body is supplied in ARG3, such as JSON, XML, form-independent text, or another textual representation. Set the request Content-Type header to identify the body format expected by the server. ARG1 is the HTTP method, such as GET, POST, or PUT. ARG2 is the request path. This is a low-level send operation: after it succeeds, call ReadResponseHeader and then read the response body with the appropriate response method. Request path: ARG2 is the path and optional query portion of the URL, not a complete URL. For https://api.example.com/v1/items?limit=10, pass /v1/items?limit=10. The server name and port are established separately by Connect or UseConnection. Text encoding and Content-Type: Unicode text is encoded as UTF-8. Generic string-body methods do not automatically add a Content-Type header; add the media type required by the API, such as application/json; charset=utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqStringBody
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param ?string $bodyText
	 * @return bool
	 */
	function SendReqStringBody($httpVerb,$uriPath,$bodyText) {
		return CkRest_SendReqStringBody($this->_cPtr,$httpVerb,$uriPath,$bodyText);
	}

	/**
	 * Creates an asynchronous task to call the SendReqStringBody method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SendReqStringBodyAsync
	 * @param ?string $httpVerb
	 * @param ?string $uriPath
	 * @param ?string $bodyText
	 * @return ?CkTask
	 */
	function SendReqStringBodyAsync($httpVerb,$uriPath,$bodyText) {
		return chilkat::_obj(CkRest_SendReqStringBodyAsync($this->_cPtr,$httpVerb,$uriPath,$bodyText),'CkTask');
	}

	/**
	 * Associates the AuthAws provider in ARG1 with this REST object. Chilkat uses the provider to sign each AWS request after the request method, path, query parameters, headers, and body are known. The application does not need to construct the Authorization header manually. Request signing: AWS signatures cover selected request components. Add all required headers and query parameters before sending the request so the generated signature matches the final request.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthAws
	 * @param CkAuthAws $authProvider
	 * @return bool
	 */
	function SetAuthAws($authProvider) {
		return CkRest_SetAuthAws($this->_cPtr,$authProvider);
	}

	/**
	 * Associates the AuthAzureSAS provider in ARG1 with this REST object. Chilkat automatically adds an Authorization: SharedAccessSignature ... header to subsequent requests. SAS terminology: This method is for Azure services that use a Shared Access Signature in the Authorization header. It is distinct from a storage SAS token embedded in a URL query string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthAzureSas
	 * @param CkAuthAzureSAS $authProvider
	 * @return bool
	 */
	function SetAuthAzureSas($authProvider) {
		return CkRest_SetAuthAzureSas($this->_cPtr,$authProvider);
	}

	/**
	 * Associates the AuthAzureStorage provider in ARG1 with this REST object. Chilkat automatically applies the Azure Storage authorization information required for each request. Azure Storage authentication: The provider contains the storage account credentials and signing settings. Request-specific headers and the resource path are incorporated when the request is sent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthAzureStorage
	 * @param CkAuthAzureStorage $authProvider
	 * @return bool
	 */
	function SetAuthAzureStorage($authProvider) {
		return CkRest_SetAuthAzureStorage($this->_cPtr,$authProvider);
	}

	/**
	 * Configures HTTP Basic authentication using ARG1 as the username and ARG2 as the password. Chilkat automatically generates an Authorization: Basic ... header for subsequent requests. Security requirement: Basic authentication is an encoding scheme, not encryption. Use it only over TLS. By default, Chilkat blocks Basic authentication on an insecure remote connection; AllowInsecureBasicAuth in UncommonOptions exists only for exceptional compatibility needs. Some APIs use an account ID, client ID, API key, token, or secret in place of a traditional username or password; follow the service documentation. Authorization precedence: The Basic authentication provider generates the Authorization header when each request is composed and takes precedence over a value set through the Authorization property or AddHeader.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthBasic
	 * @param ?string $username
	 * @param ?string $password
	 * @return bool
	 */
	function SetAuthBasic($username,$password) {
		return CkRest_SetAuthBasic($this->_cPtr,$username,$password);
	}

	/**
	 * Configures HTTP Basic authentication using the SecureString objects in ARG1 and ARG2 for the username and password. The resulting request behavior is the same as SetAuthBasic. Credential handling: SecureString reduces unnecessary exposure of the credential text in ordinary application strings. Basic authentication must still be protected by TLS. Authorization precedence: The Basic authentication provider generates the Authorization header when each request is composed and takes precedence over a manually configured Authorization value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthBasicSecure
	 * @param CkSecureString $username
	 * @param CkSecureString $password
	 * @return bool
	 */
	function SetAuthBasicSecure($username,$password) {
		return CkRest_SetAuthBasicSecure($this->_cPtr,$username,$password);
	}

	/**
	 * Associates the AuthGoogle provider in ARG1 with this REST object. Chilkat uses the provider to add the Google authentication information needed by subsequent API requests.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthGoogle
	 * @param CkAuthGoogle $authProvider
	 * @return bool
	 */
	function SetAuthGoogle($authProvider) {
		return CkRest_SetAuthGoogle($this->_cPtr,$authProvider);
	}

	/**
	 * Associates the OAuth1 provider in ARG1 with this REST object for OAuth 1.0 or OAuth 1.0a request signing. If ARG2 is _TRUE_, the OAuth parameters and signature are placed in the query string. If ARG2 is _FALSE_, they are placed in the Authorization header. Final request matters: OAuth 1 signatures depend on the HTTP method, request URL, and parameters. Configure the request completely before sending it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthOAuth1
	 * @param CkOAuth1 $authProvider
	 * @param bool $useQueryParams
	 * @return bool
	 */
	function SetAuthOAuth1($authProvider,$useQueryParams) {
		return CkRest_SetAuthOAuth1($this->_cPtr,$authProvider,$useQueryParams);
	}

	/**
	 * Associates the OAuth2 provider in ARG1 with this REST object. The provider must contain a valid access token. Chilkat then adds an Authorization: Bearer <access-token> header to each request. The access token may be obtained through an OAuth flow or loaded from previously saved token data. Token acquisition, refresh, expiration, and scope requirements are determined by the authorization server. Manual bearer headers: A bearer token can also be supplied with AddHeader. Do not configure both approaches for the same request because they compete for the same Authorization header. Provider lifetime: The OAuth2 object is consulted when each request is composed rather than copied at setup time. Keep it alive while the Rest object uses it. Changing its AccessToken changes the bearer token used by subsequent requests. Authorization precedence: The provider-generated bearer header takes precedence over a manually configured Authorization value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetAuthOAuth2
	 * @param CkOAuth2 $authProvider
	 * @return bool
	 */
	function SetAuthOAuth2($authProvider) {
		return CkRest_SetAuthOAuth2($this->_cPtr,$authProvider);
	}

	/**
	 * Sets the binary body of the multipart part identified by PartSelector from the BinData in ARG1. Setting the body creates the selected part when it does not already exist. Part numbers do not need to be created consecutively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetMultipartBodyBd
	 * @param CkBinData $bodyData
	 * @return bool
	 */
	function SetMultipartBodyBd($bodyData) {
		return CkRest_SetMultipartBodyBd($this->_cPtr,$bodyData);
	}

	/**
	 * Sets the selected multipart part body to the binary bytes in ARG1. The target part is identified by PartSelector. Part headers: While the same part is selected, use AddHeader to set fields such as Content-Disposition and Content-Type. For new code, SetMultipartBodyBd uses BinData. Setting the body creates the selected part when it does not already exist. Part numbers do not need to be created consecutively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetMultipartBodyBinary
	 * @param CkByteData $bodyData
	 * @return bool
	 */
	function SetMultipartBodyBinary($bodyData) {
		return CkRest_SetMultipartBodyBinary($this->_cPtr,$bodyData);
	}

	/**
	 * Sets the text body of the multipart part identified by PartSelector from the StringBuilder in ARG1. Setting the body creates the selected part when it does not already exist. Part numbers do not need to be created consecutively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetMultipartBodySb
	 * @param CkStringBuilder $bodySb
	 * @return bool
	 */
	function SetMultipartBodySb($bodySb) {
		return CkRest_SetMultipartBodySb($this->_cPtr,$bodySb);
	}

	/**
	 * Uses the Stream in ARG1 as the body source for the multipart part identified by PartSelector. Large multipart parts: The stream is read while the request is sent, allowing a large file or generated body to be uploaded without first loading the entire part into memory. Object lifetime and position: The selected part retains the stream as its body source until the multipart request is sent or the parts are cleared. Keep the Stream object alive. It is read from its current position and must be reset before reusing it for another upload. Setting the body creates the selected part when it does not already exist. Part numbers do not need to be created consecutively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetMultipartBodyStream
	 * @param CkStream $stream
	 * @return bool
	 */
	function SetMultipartBodyStream($stream) {
		return CkRest_SetMultipartBodyStream($this->_cPtr,$stream);
	}

	/**
	 * Sets the text body of the multipart part identified by PartSelector to ARG1. Part metadata: Use AddHeader while the part is selected to set its Content-Type, Content-Disposition, and other part headers. Setting the body creates the selected part when it does not already exist. Part numbers do not need to be created consecutively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetMultipartBodyString
	 * @param ?string $bodyText
	 * @return bool
	 */
	function SetMultipartBodyString($bodyText) {
		return CkRest_SetMultipartBodyString($this->_cPtr,$bodyText);
	}

	/**
	 * Configures a Stream destination for response bodies received by the full-request convenience methods. The body is written to ARG3 only when the response status code matches ARG1. If streaming occurs successfully, a string-returning full-request method returns OK. If streaming was selected but writing the body fails, it returns FAIL. If the status code does not match ARG1, the body is not streamed and remains available as the method's normal response text. If ARG2 is _TRUE_ and the response is textual, the destination stream's StringCharset is set from the response Content-Type charset when available. ARG2 is ignored for binary responses. ARG1 valueStatus codes that are streamed 200Only 200 -200200 through 299 -210210 through 219 Resetting this behavior: Call ClearResponseBodyStream to restore normal response-body handling. For Boolean-returning full-request methods, successful streaming returns _TRUE_. A StringBuilder response output receives OK; a BinData response output is cleared. If the status does not match ARG1, the stream is not written and the method returns the response through its normal output. The configuration remains active for later full-request calls until ClearResponseBodyStream is called.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#SetResponseBodyStream
	 * @param int $expectedStatus
	 * @param bool $autoSetStreamCharset
	 * @param CkStream $responseStream
	 * @return bool
	 */
	function SetResponseBodyStream($expectedStatus,$autoSetStreamCharset,$responseStream) {
		return CkRest_SetResponseBodyStream($this->_cPtr,$expectedStatus,$autoSetStreamCharset,$responseStream);
	}

	/**
	 * Uses the already-connected Socket in ARG1 as the transport for REST requests. The socket may represent a direct TCP or TLS connection, an HTTP or SOCKS proxy connection, an SSH-tunneled channel, or another connection configured through the Socket API. ARG2 controls automatic reconnection for later requests when the connection is no longer usable. When to use this method: Use Connect for ordinary direct connections. Use UseConnection when the connection requires advanced Socket configuration. Socket lifetime: The Rest object retains and operates on the supplied Socket. Keep the Socket object alive while it is attached. Calling Disconnect disconnects it; with ARG2 set to _TRUE_, a later request can reconnect the same Socket object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkRestRef.html#UseConnection
	 * @param CkSocket $connection
	 * @param bool $autoReconnect
	 * @return bool
	 */
	function UseConnection($connection,$autoReconnect) {
		return CkRest_UseConnection($this->_cPtr,$connection,$autoReconnect);
	}
}
