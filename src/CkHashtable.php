<?php

/* CkHashtable.php -- Chilkat PHP extension proxy class CkHashtable.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Store, look up, remove, enumerate, import, and serialize key/value pairs.
 *
 * Chilkat.Hashtable provides a simple key/value collection for storing string and integer values by key. It can add or replace entries, look up values, test whether keys exist, remove entries, count items, enumerate keys, import URL query parameters, serialize values back to a query string, and save or load the table in XML format.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html
 */
class CkHashtable {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkHashtable') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkHashtable();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkHashtable_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkHashtable_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * The number of entries currently stored in the hashtable. Each key contributes one entry regardless of whether its value is a string or an integer. Adding a new key increases Count; replacing the value of an existing key does not. Removing an entry decreases it, and Clear resets it to 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Count
	 * @return int
	 */
	function get_Count() {
		return CkHashtable_get_Count($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkHashtable_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkHashtable_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkHashtable_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkHashtable_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkHashtable_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkHashtable_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkHashtable_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkHashtable_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkHashtable_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkHashtable_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkHashtable_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkHashtable_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkHashtable_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkHashtable_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkHashtable_version($this->_cPtr);
	}

	/**
	 * Reads hashtable XML previously produced by ToXmlSb from the StringBuilder passed in ARG1 and adds the entries to this table. The operation merges with the existing table: keys not present in the XML remain, and an imported entry replaces the current value for the same key. Call Clear first when the XML should completely replace the current contents. Returns _TRUE_ when the XML is parsed and imported successfully; otherwise returns _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#AddFromXmlSb
	 * @param CkStringBuilder $sbXml
	 * @return bool
	 */
	function AddFromXmlSb($sbXml) {
		return CkHashtable_AddFromXmlSb($this->_cPtr,$sbXml);
	}

	/**
	 * Adds an entry whose string key is ARG1 and whose value is the integer ARG2. If the key already exists, its previous value is replaced. Returns _TRUE_ when the entry is added or replaced. Returns _FALSE_ if the operation cannot be completed, such as because memory could not be allocated. One entry per key: Adding the same key again replaces the existing value; it does not create a second entry. Replacing a value does not increase Count. Use LookupInt to retrieve an integer value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#AddInt
	 * @param ?string $key
	 * @param int $value
	 * @return bool
	 */
	function AddInt($key,$value) {
		return CkHashtable_AddInt($this->_cPtr,$key,$value);
	}

	/**
	 * Parses URL query parameters from ARG1 and adds them to the hashtable as string entries. Pass the query component in the following form: field1=value1&field2=value2&field3=value3Each value is expected to be URL encoded and is URL decoded before being stored. If an imported key already exists, its previous value is replaced. Returns _TRUE_ on success and _FALSE_ if the query parameters cannot be imported. Repeated parameter names: A hashtable stores one value per key. It cannot preserve a query string containing several independent values with the same field name. Use a list-oriented parser when repeated names must remain distinct.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#AddQueryParams
	 * @param ?string $queryParams
	 * @return bool
	 */
	function AddQueryParams($queryParams) {
		return CkHashtable_AddQueryParams($this->_cPtr,$queryParams);
	}

	/**
	 * Adds an entry whose string key is ARG1 and whose value is the string ARG2. If the key already exists, its previous value is replaced. Returns _TRUE_ when the entry is added or replaced. Returns _FALSE_ if the operation cannot be completed, such as because memory could not be allocated. One entry per key: Adding the same key again replaces the existing value; it does not create a second entry. Replacing a value does not increase Count. Use LookupStr to retrieve a string value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#AddStr
	 * @param ?string $key
	 * @param ?string $value
	 * @return bool
	 */
	function AddStr($key,$value) {
		return CkHashtable_AddStr($this->_cPtr,$key,$value);
	}

	/**
	 * Removes every entry from the hashtable. After this call, Count is 0. The current hash-table capacity is retained. To clear the entries and also select a different bucket capacity, call ClearWithNewCapacity.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Clear
	 */
	function Clear() {
		CkHashtable_Clear($this->_cPtr);
	}

	/**
	 * Removes every entry and reinitializes the hashtable with ARG1 buckets. The initial default capacity of a new Hashtable object is 521. Returns _TRUE_ on success and _FALSE_ if the table cannot be reinitialized. TermMeaning CountThe number of stored entries. CapacityThe number of hash buckets, not a maximum entry count. Performance note: More buckets can reduce collisions for a large table, but use more memory. Choosing roughly twice the expected number of entries and using a prime number are tuning guidelines, not correctness requirements.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#ClearWithNewCapacity
	 * @param int $capacity
	 * @return bool
	 */
	function ClearWithNewCapacity($capacity) {
		return CkHashtable_ClearWithNewCapacity($this->_cPtr,$capacity);
	}

	/**
	 * Returns _TRUE_ if the hashtable contains the string key ARG1; otherwise returns _FALSE_. The result tests key existence regardless of the value stored for that key. Useful before lookup: Call Contains when a lookup result could be ambiguous. For example, LookupInt returns 0 both for a missing key and for an entry whose value is actually zero.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Contains
	 * @param ?string $key
	 * @return bool
	 */
	function Contains($key) {
		return CkHashtable_Contains($this->_cPtr,$key);
	}

	/**
	 * Returns _TRUE_ if the hashtable contains the integer key ARG1; otherwise returns _FALSE_. Key type: This method accepts an integer key. It is distinct from Contains, which tests for a string key, and from AddInt, whose key is a string and whose value is an integer.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#ContainsIntKey
	 * @param int $key
	 * @return bool
	 */
	function ContainsIntKey($key) {
		return CkHashtable_ContainsIntKey($this->_cPtr,$key);
	}

	/**
	 * Appends all hashtable key strings to the StringTable passed in ARG1. Existing strings already contained in ARG1 are not cleared. Returns _TRUE_ on success and _FALSE_ on failure. Enumeration order: Hashtable keys may be returned in any order. Do not depend on insertion order or sorted order. Clear the destination StringTable first when it should contain only the current hashtable keys.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#GetKeys
	 * @param CkStringTable $strTable
	 * @return bool
	 */
	function GetKeys($strTable) {
		return CkHashtable_GetKeys($this->_cPtr,$strTable);
	}

	/**
	 * Returns the integer value associated with the string key ARG1. This method is intended for entries stored with AddInt. If the key does not exist, the return value is 0. Missing key versus zero: Because 0 is also a valid stored value, call Contains first when the application must distinguish a missing key from an entry containing zero.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LookupInt
	 * @param ?string $key
	 * @return int
	 */
	function LookupInt($key) {
		return CkHashtable_LookupInt($this->_cPtr,$key);
	}

	/**
	 * Returns the string value associated with the string key ARG1. This method is intended for entries stored as strings, including entries added by AddStr or AddQueryParams. Check lookup success: The representation of a failed string-returning call varies by programming language and may be null, Nothing, or an empty string. An empty string may also be a legitimate value. Use Contains or LastMethodSuccess when the distinction matters.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#LookupStr
	 * @param ?string $key
	 * @return ?string
	 */
	function lookupStr($key) {
		return CkHashtable_lookupStr($this->_cPtr,$key);
	}

	/**
	 * Removes the entry whose string key is ARG1. Returns _TRUE_ if the key existed and the entry was removed. Returns _FALSE_ if the key was not present. A _FALSE_ result caused by an absent key is a normal lookup outcome, not an indication that the hashtable was damaged. Removing an existing entry decreases Count by one.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#Remove
	 * @param ?string $key
	 * @return bool
	 */
	function Remove($key) {
		return CkHashtable_Remove($this->_cPtr,$key);
	}

	/**
	 * Serializes the hashtable as a URL query string in the following form: key1=value1&key2=value2&key3=value3Each value is URL encoded. The returned string does not include a leading ?. Important limitations: Key order is not defined, and a hashtable contains only one value per key. Therefore the output is not suitable when parameter order is significant or repeated field names must be preserved. In language bindings where string-return failure is ambiguous, check LastMethodSuccess.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#ToQueryString
	 * @return ?string
	 */
	function toQueryString() {
		return CkHashtable_toQueryString($this->_cPtr);
	}

	/**
	 * Serializes the current hashtable to Chilkat's hashtable XML format and appends the XML to the StringBuilder passed in ARG1. Existing content in the StringBuilder is preserved. Returns _TRUE_ on success and _FALSE_ on failure. The generated XML can be loaded by AddFromXmlSb. &lt;hashtable&gt;   &lt;e&gt;&lt;k&gt;key&lt;/k&gt;&lt;v&gt;value&lt;/v&gt;&lt;/e&gt; &lt;/hashtable&gt;Ordering: Because hashtable enumeration order is not defined, do not rely on entries appearing in insertion order or use the serialized text as a canonical, byte-for-byte representation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkHashtableRef.html#ToXmlSb
	 * @param CkStringBuilder $sbXml
	 * @return bool
	 */
	function ToXmlSb($sbXml) {
		return CkHashtable_ToXmlSb($this->_cPtr,$sbXml);
	}
}
