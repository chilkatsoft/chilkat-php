<?php

/* CkLog.php -- Chilkat PHP extension proxy class CkLog.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Build readable structured diagnostic logs with nested context and tagged values.
 *
 * Chilkat.Log is a lightweight structured logging helper for creating readable diagnostic logs from application code. It can record nested operation contexts, informational messages, error messages, timestamps, integers, 64-bit integers, tagged values, binary data, Base64 data, hex data, shortened data previews, and computed hashes. It is useful when code needs a clear step-by-step record of operations, inputs, outputs, and diagnostic values for troubleshooting.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html
 */
class CkLog {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkLog') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkLog();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkLog_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkLog_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkLog_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkLog_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkLog_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkLog_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkLog_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkLog_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkLog_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkLog_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkLog_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkLog_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkLog_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkLog_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkLog_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkLog_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkLog_version($this->_cPtr);
	}

	/**
	 * Clears all existing log entries and starts a new log whose top-level context is named initialTag. Typical use: Call this at the beginning of an operation or test run, then add entries with the other Log* methods. The completed structured log can be inspected through the Log object's diagnostic text, such as LastErrorText.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#Clear
	 * @param ?string $initialTag
	 */
	function Clear($initialTag) {
		CkLog_Clear($this->_cPtr,$initialTag);
	}

	/**
	 * Begins a nested logging context named tag. Entries written after this call are grouped inside the new context until a matching LeaveContext is called. Balanced contexts: Pair each EnterContext with LeaveContext. Contexts may be nested to reflect operation hierarchy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#EnterContext
	 * @param ?string $tag
	 */
	function EnterContext($tag) {
		CkLog_EnterContext($this->_cPtr,$tag);
	}

	/**
	 * Ends the current nested logging context and returns logging to its parent context. Empty contexts are omitted: If nothing was logged after entering the current context, Chilkat automatically removes that empty context when it is left.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LeaveContext
	 */
	function LeaveContext() {
		CkLog_LeaveContext($this->_cPtr);
	}

	/**
	 * Adds a tagged text value to the current context. The output associates tag with message, making this useful for named values such as filenames, paths, IDs, states, or protocol fields. Sensitive data: Avoid logging passwords, private keys, access tokens, session secrets, or other sensitive values unless disclosure is intentional.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogData
	 * @param ?string $tag
	 * @param ?string $message
	 */
	function LogData($tag,$message) {
		CkLog_LogData($this->_cPtr,$tag,$message);
	}

	/**
	 * Encodes the supplied binary data as Base64 and adds the encoded value to the current log under tag. Base64: Base64 makes arbitrary bytes printable in a text log. It is an encoding, not encryption.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogDataBase64
	 * @param ?string $tag
	 * @param CkByteData $data
	 */
	function LogDataBase64($tag,$data) {
		CkLog_LogDataBase64($this->_cPtr,$tag,$data);
	}

	/**
	 * Encodes the supplied binary data as hexadecimal text and adds the encoded value to the current log under tag. Hex output: Hex is convenient for short protocol fields, identifiers, digests, and byte-level troubleshooting because each byte is represented by two hex digits.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogDataHex
	 * @param ?string $tag
	 * @param CkByteData $data
	 */
	function LogDataHex($tag,$data) {
		CkLog_LogDataHex($this->_cPtr,$tag,$data);
	}

	/**
	 * Adds a tagged text value to the current context, but limits the logged value to at most the first maxNumChars characters of message. Diagnostic previews: Use this when a value may be very large and only a leading preview is needed. The original input string is not modified.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogDataMax
	 * @param ?string $tag
	 * @param ?string $message
	 * @param int $maxNumChars
	 */
	function LogDataMax($tag,$message,$maxNumChars) {
		CkLog_LogDataMax($this->_cPtr,$tag,$message,$maxNumChars);
	}

	/**
	 * Logs the current date and time under tag using an RFC 822-style date/time representation. If gmt is _TRUE_, the logged time is GMT/UTC; otherwise local system time is used. Date/time vs. timestamp: Use LogDateTime for a calendar date with timezone context. Use LogTimestamp for a time-of-day marker.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogDateTime
	 * @param ?string $tag
	 * @param bool $gmt
	 */
	function LogDateTime($tag,$gmt) {
		CkLog_LogDateTime($this->_cPtr,$tag,$gmt);
	}

	/**
	 * Adds message as an error entry in the current logging context. Application diagnostics: This is application-defined logging. It is separate from LastErrorText produced by other Chilkat objects when their operations fail.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogError
	 * @param ?string $message
	 */
	function LogError($message) {
		CkLog_LogError($this->_cPtr,$message);
	}

	/**
	 * Adds message as an informational entry in the current logging context. Structured tracing: Use informational entries for significant steps and decisions. Use LogData when the entry is better represented as a named value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogInfo
	 * @param ?string $message
	 */
	function LogInfo($message) {
		CkLog_LogInfo($this->_cPtr,$message);
	}

	/**
	 * Adds the integer value to the current log under tag.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogInt
	 * @param ?string $tag
	 * @param int $value
	 */
	function LogInt($tag,$value) {
		CkLog_LogInt($this->_cPtr,$tag,$value);
	}

	/**
	 * Adds the signed 64-bit integer value to the current log under tag. 64-bit values: Useful for values such as large byte counts, file sizes, sequence numbers, or timestamps that can exceed ordinary integer ranges.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogInt64
	 * @param ?string $tag
	 * @param int $value
	 */
	function LogInt64($tag,$value) {
		CkLog_LogInt64($this->_cPtr,$tag,$value);
	}

	/**
	 * Logs the current time-of-day under tag in HH:MM:SS:mmm form, where mmm represents milliseconds. Timing note: This is a time-of-day marker, not a monotonic elapsed-time measurement.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkLogRef.html#LogTimestamp
	 * @param ?string $tag
	 */
	function LogTimestamp($tag) {
		CkLog_LogTimestamp($this->_cPtr,$tag);
	}
}
