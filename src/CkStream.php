<?php

/* CkStream.php -- Chilkat PHP extension proxy class CkStream.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Read, write, pipe, monitor, and control byte or text streams.
 *
 * Chilkat.Stream is a general-purpose streaming object for moving data between sources and sinks without requiring the entire payload to be handled at once. A stream can read from files, byte arrays, strings, or other sources; write to files, memory, BinData, StringBuilder, or other sinks; handle binary and text data; apply character-set rules for strings; process file parts; track bytes sent and received; and support timeouts, polling, aborts, async task integration, and end-of-stream handling.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html
 */
class CkStream {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkStream') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkStream();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkStream_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkStream_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkStream_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkStream_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * When set to _TRUE_, causes the currently running method to abort.  Methods that always finish quickly (i.e.have no length file operations or network communications) are not affected.  If no method is running, then this property is automatically reset to _FALSE_ when the next method is called.  When the abort occurs, this property is reset to _FALSE_.  Both synchronous and asynchronous method calls can be aborted.  (A synchronous method call could be aborted by setting this property from a separate thread.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkStream_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * _TRUE_ if the stream supports reading.  Otherwise _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#CanRead
	 * @return bool
	 */
	function get_CanRead() {
		return CkStream_get_CanRead($this->_cPtr);
	}

	/**
	 * _TRUE_ if the stream supports writing.  Otherwise _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#CanWrite
	 * @return bool
	 */
	function get_CanWrite() {
		return CkStream_get_CanWrite($this->_cPtr);
	}

	/**
	 * _TRUE_ if it is known for sure that data is ready and waiting to be read.   _FALSE_ if it is not known for sure (it may be that data is immediately available, but reading the stream with a ReadTimeoutMs of 0, which is to poll the stream, is the way to find out).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DataAvailable
	 * @return bool
	 */
	function get_DataAvailable() {
		return CkStream_get_DataAvailable($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkStream_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkStream_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkStream_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * The default internal chunk size for reading or writing.  The default value is 65536.  If this property is set to 0, it will cause the default chunk size (65536) to be used.   Note: The chunk size can have significant performance impact. If performance is an issue, be sure to experiment with different chunk sizes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DefaultChunkSize
	 * @return int
	 */
	function get_DefaultChunkSize() {
		return CkStream_get_DefaultChunkSize($this->_cPtr);
	}

	/**
	 * The default internal chunk size for reading or writing.  The default value is 65536.  If this property is set to 0, it will cause the default chunk size (65536) to be used.   Note: The chunk size can have significant performance impact. If performance is an issue, be sure to experiment with different chunk sizes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#DefaultChunkSize
	 * @param int $newVal
	 */
	function put_DefaultChunkSize($newVal) {
		CkStream_put_DefaultChunkSize($this->_cPtr,$newVal);
	}

	/**
	 * _TRUE_ if the end-of-stream has already been reached.  When the stream has already ended, all calls to Read* methods will return _FALSE_ with the ReadFailReason set to 3 (already at end-of-stream).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#EndOfStream
	 * @return bool
	 */
	function get_EndOfStream() {
		return CkStream_get_EndOfStream($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkStream_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * The interval in milliseconds between each AbortCheck event callback, which enables an application to abort certain method calls before they complete. By default, HeartbeatMs is set to 0, meaning no AbortCheck event callbacks will trigger.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkStream_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * _TRUE_ if the stream is closed for writing.  Once closed, no more data may be written to the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#IsWriteClosed
	 * @return bool
	 */
	function get_IsWriteClosed() {
		return CkStream_get_IsWriteClosed($this->_cPtr);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkStream_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkStream_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkStream_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkStream_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkStream_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkStream_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkStream_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkStream_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * The length (in bytes) of the stream's source.  If unknown, then this property will have a value of -1.  This property may be set by the application if it knows in advance the length of the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Length
	 * @return int
	 */
	function get_Length() {
		return CkStream_get_Length($this->_cPtr);
	}

	/**
	 * The length (in bytes) of the stream's source.  If unknown, then this property will have a value of -1.  This property may be set by the application if it knows in advance the length of the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Length
	 * @param int $newVal
	 */
	function put_Length($newVal) {
		CkStream_put_Length($this->_cPtr,$newVal);
	}

	/**
	 * The length (in bytes) of the stream's source.  If unknown, then this property will have a value of -1.  This property may be set by the application if it knows in advance the length of the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Length32
	 * @return int
	 */
	function get_Length32() {
		return CkStream_get_Length32($this->_cPtr);
	}

	/**
	 * The length (in bytes) of the stream's source.  If unknown, then this property will have a value of -1.  This property may be set by the application if it knows in advance the length of the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Length32
	 * @param int $newVal
	 */
	function put_Length32($newVal) {
		CkStream_put_Length32($this->_cPtr,$newVal);
	}

	/**
	 * The number of bytes received by the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#NumReceived
	 * @return int
	 */
	function get_NumReceived() {
		return CkStream_get_NumReceived($this->_cPtr);
	}

	/**
	 * The number of bytes sent by the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#NumSent
	 * @return int
	 */
	function get_NumSent() {
		return CkStream_get_NumSent($this->_cPtr);
	}

	/**
	 * This property is automatically set when a Read* method is called.  It indicates the reason for failure.  Possible values are: 0> No failure (success)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadFailReason
	 * @return int
	 */
	function get_ReadFailReason() {
		return CkStream_get_ReadFailReason($this->_cPtr);
	}

	/**
	 * The maximum number of seconds to wait while reading.  The default value is 30 seconds (i.e. 30000ms).  A value of 0 indicates a poll.  (A polling read is to return with a timeout if no data is immediately available.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadTimeoutMs
	 * @return int
	 */
	function get_ReadTimeoutMs() {
		return CkStream_get_ReadTimeoutMs($this->_cPtr);
	}

	/**
	 * The maximum number of seconds to wait while reading.  The default value is 30 seconds (i.e. 30000ms).  A value of 0 indicates a poll.  (A polling read is to return with a timeout if no data is immediately available.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadTimeoutMs
	 * @param int $newVal
	 */
	function put_ReadTimeoutMs($newVal) {
		CkStream_put_ReadTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Sets the sink  to the path of a file.  The file does not need to exist at the time of setting this property.  The sink file will be automatically opened on demand, when the stream is first written. Note: This property takes priority over other potential sinks.  Make sure this property is set to an empty string if the stream's sink is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SinkFile
	 * @param CkString $str
	 */
	function get_SinkFile($str) {
		CkStream_get_SinkFile($this->_cPtr,$str);
	}

	/**
	 * Sets the sink  to the path of a file.  The file does not need to exist at the time of setting this property.  The sink file will be automatically opened on demand, when the stream is first written. Note: This property takes priority over other potential sinks.  Make sure this property is set to an empty string if the stream's sink is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SinkFile
	 * @return ?string
	 */
	function sinkFile() {
		return CkStream_sinkFile($this->_cPtr);
	}

	/**
	 * Sets the sink  to the path of a file.  The file does not need to exist at the time of setting this property.  The sink file will be automatically opened on demand, when the stream is first written. Note: This property takes priority over other potential sinks.  Make sure this property is set to an empty string if the stream's sink is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SinkFile
	 * @param ?string $newVal
	 */
	function put_SinkFile($newVal) {
		CkStream_put_SinkFile($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_, the stream appends to the SinkFile rather than truncating and re-writing the sink file.  The default value is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SinkFileAppend
	 * @return bool
	 */
	function get_SinkFileAppend() {
		return CkStream_get_SinkFileAppend($this->_cPtr);
	}

	/**
	 * If _TRUE_, the stream appends to the SinkFile rather than truncating and re-writing the sink file.  The default value is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SinkFileAppend
	 * @param bool $newVal
	 */
	function put_SinkFileAppend($newVal) {
		CkStream_put_SinkFileAppend($this->_cPtr,$newVal);
	}

	/**
	 * Sets the source to the path of a file.  The file does not need to exist at the time of setting this property.  The source file will be automatically opened on demand, when the stream is first read. Note: This property takes priority over other potential sources.  Make sure this property is set to an empty string if the stream's source is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFile
	 * @param CkString $str
	 */
	function get_SourceFile($str) {
		CkStream_get_SourceFile($this->_cPtr,$str);
	}

	/**
	 * Sets the source to the path of a file.  The file does not need to exist at the time of setting this property.  The source file will be automatically opened on demand, when the stream is first read. Note: This property takes priority over other potential sources.  Make sure this property is set to an empty string if the stream's source is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFile
	 * @return ?string
	 */
	function sourceFile() {
		return CkStream_sourceFile($this->_cPtr);
	}

	/**
	 * Sets the source to the path of a file.  The file does not need to exist at the time of setting this property.  The source file will be automatically opened on demand, when the stream is first read. Note: This property takes priority over other potential sources.  Make sure this property is set to an empty string if the stream's source is to be something else.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFile
	 * @param ?string $newVal
	 */
	function put_SourceFile($newVal) {
		CkStream_put_SourceFile($this->_cPtr,$newVal);
	}

	/**
	 * If the source is a file, then this property can be used to stream one part of the file.   The SourceFilePartSize property defines the size (in bytes) of each part.  The SourceFilePart and SourceFilePartSize have default values of 0, which means the entire SourceFile is streamed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFilePart
	 * @return int
	 */
	function get_SourceFilePart() {
		return CkStream_get_SourceFilePart($this->_cPtr);
	}

	/**
	 * If the source is a file, then this property can be used to stream one part of the file.   The SourceFilePartSize property defines the size (in bytes) of each part.  The SourceFilePart and SourceFilePartSize have default values of 0, which means the entire SourceFile is streamed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFilePart
	 * @param int $newVal
	 */
	function put_SourceFilePart($newVal) {
		CkStream_put_SourceFilePart($this->_cPtr,$newVal);
	}

	/**
	 * If the source is a file, then this property, in conjunction with the SourceFilePart property, can be used to stream a single part of the file.   This property defines the size (in bytes) of each part.  The SourceFilePart and SourceFilePartSize have default values of 0, which means that by default, the entire SourceFile is streamed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFilePartSize
	 * @return int
	 */
	function get_SourceFilePartSize() {
		return CkStream_get_SourceFilePartSize($this->_cPtr);
	}

	/**
	 * If the source is a file, then this property, in conjunction with the SourceFilePart property, can be used to stream a single part of the file.   This property defines the size (in bytes) of each part.  The SourceFilePart and SourceFilePartSize have default values of 0, which means that by default, the entire SourceFile is streamed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SourceFilePartSize
	 * @param int $newVal
	 */
	function put_SourceFilePartSize($newVal) {
		CkStream_put_SourceFilePartSize($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_, then include the BOM when creating a string source via SetSourceString where the charset is  utf-8, utf-16, etc.  (The term BOM stands for Byte Order Mark, also known as the preamble.)    Also, if _TRUE_, then include the BOM when writing a string via the WriteString method.  The default value of this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#StringBom
	 * @return bool
	 */
	function get_StringBom() {
		return CkStream_get_StringBom($this->_cPtr);
	}

	/**
	 * If _TRUE_, then include the BOM when creating a string source via SetSourceString where the charset is  utf-8, utf-16, etc.  (The term BOM stands for Byte Order Mark, also known as the preamble.)    Also, if _TRUE_, then include the BOM when writing a string via the WriteString method.  The default value of this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#StringBom
	 * @param bool $newVal
	 */
	function put_StringBom($newVal) {
		CkStream_put_StringBom($this->_cPtr,$newVal);
	}

	/**
	 * Indicates the expected character encoding, such as utf-8, windows-1256, utf-16, etc. for methods that read text such as: ReadString, ReadToCRLF, ReadUntilMatch.  Also controls the character encoding when writing strings with the WriteString method.  The supported charsets are indicated at the link below. The default value is utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#StringCharset
	 * @param CkString $str
	 */
	function get_StringCharset($str) {
		CkStream_get_StringCharset($this->_cPtr,$str);
	}

	/**
	 * Indicates the expected character encoding, such as utf-8, windows-1256, utf-16, etc. for methods that read text such as: ReadString, ReadToCRLF, ReadUntilMatch.  Also controls the character encoding when writing strings with the WriteString method.  The supported charsets are indicated at the link below. The default value is utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#StringCharset
	 * @return ?string
	 */
	function stringCharset() {
		return CkStream_stringCharset($this->_cPtr);
	}

	/**
	 * Indicates the expected character encoding, such as utf-8, windows-1256, utf-16, etc. for methods that read text such as: ReadString, ReadToCRLF, ReadUntilMatch.  Also controls the character encoding when writing strings with the WriteString method.  The supported charsets are indicated at the link below. The default value is utf-8.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#StringCharset
	 * @param ?string $newVal
	 */
	function put_StringCharset($newVal) {
		CkStream_put_StringCharset($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkStream_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkStream_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkStream_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkStream_version($this->_cPtr);
	}

	/**
	 * This property is automatically set when a Write* method is called.  It indicates the reason for failure.  Possible values are: 0> No failure (success)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteFailReason
	 * @return int
	 */
	function get_WriteFailReason() {
		return CkStream_get_WriteFailReason($this->_cPtr);
	}

	/**
	 * The maximum number of seconds to wait while writing.  The default value is 30 seconds (i.e. 30000ms).  A value of 0 indicates to return immediately if it is not possible to write to the sink immediately.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteTimeoutMs
	 * @return int
	 */
	function get_WriteTimeoutMs() {
		return CkStream_get_WriteTimeoutMs($this->_cPtr);
	}

	/**
	 * The maximum number of seconds to wait while writing.  The default value is 30 seconds (i.e. 30000ms).  A value of 0 indicates to return immediately if it is not possible to write to the sink immediately.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteTimeoutMs
	 * @param int $newVal
	 */
	function put_WriteTimeoutMs($newVal) {
		CkStream_put_WriteTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Loads the caller of the task's async method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkStream_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Read as much data as is immediately available on the stream.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.  The incoming data is appended to ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBd
	 * @param CkBinData $binData
	 * @return bool
	 */
	function ReadBd($binData) {
		return CkStream_ReadBd($this->_cPtr,$binData);
	}

	/**
	 * Creates an asynchronous task to call the ReadBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBdAsync
	 * @param CkBinData $binData
	 * @return ?CkTask
	 */
	function ReadBdAsync($binData) {
		return chilkat::_obj(CkStream_ReadBdAsync($this->_cPtr,$binData),'CkTask');
	}

	/**
	 * Read as much data as is immediately available on the stream.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBytes
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function ReadBytes($outBytes) {
		return CkStream_ReadBytes($this->_cPtr,$outBytes);
	}

	/**
	 * Creates an asynchronous task to call the ReadBytes method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBytesAsync
	 * @return ?CkTask
	 */
	function ReadBytesAsync() {
		return chilkat::_obj(CkStream_ReadBytesAsync($this->_cPtr),'CkTask');
	}

	/**
	 * The same as ReadBytes, except returns the received bytes in encoded string form.  The ARG1 argument indicates the encoding, which can be base64, hex, or any of the multitude of encodings indicated in the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBytesENC
	 * @param ?string $encoding
	 * @return ?string
	 */
	function readBytesENC($encoding) {
		return CkStream_readBytesENC($this->_cPtr,$encoding);
	}

	/**
	 * Creates an asynchronous task to call the ReadBytesENC method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadBytesENCAsync
	 * @param ?string $encoding
	 * @return ?CkTask
	 */
	function ReadBytesENCAsync($encoding) {
		return chilkat::_obj(CkStream_ReadBytesENCAsync($this->_cPtr,$encoding),'CkTask');
	}

	/**
	 * Reads exactly ARG1 bytes from the stream.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadNBytes
	 * @param int $numBytes
	 * @param CkByteData $outBytes
	 * @return bool
	 */
	function ReadNBytes($numBytes,$outBytes) {
		return CkStream_ReadNBytes($this->_cPtr,$numBytes,$outBytes);
	}

	/**
	 * Creates an asynchronous task to call the ReadNBytes method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadNBytesAsync
	 * @param int $numBytes
	 * @return ?CkTask
	 */
	function ReadNBytesAsync($numBytes) {
		return chilkat::_obj(CkStream_ReadNBytesAsync($this->_cPtr,$numBytes),'CkTask');
	}

	/**
	 * The same as ReadNBytes, except returns the received bytes in encoded string form.  The ARG2 argument indicates the encoding, which can be base64, hex, or any of the multitude of encodings indicated in the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadNBytesENC
	 * @param int $numBytes
	 * @param ?string $encoding
	 * @return ?string
	 */
	function readNBytesENC($numBytes,$encoding) {
		return CkStream_readNBytesENC($this->_cPtr,$numBytes,$encoding);
	}

	/**
	 * Creates an asynchronous task to call the ReadNBytesENC method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadNBytesENCAsync
	 * @param int $numBytes
	 * @param ?string $encoding
	 * @return ?CkTask
	 */
	function ReadNBytesENCAsync($numBytes,$encoding) {
		return chilkat::_obj(CkStream_ReadNBytesENCAsync($this->_cPtr,$numBytes,$encoding),'CkTask');
	}

	/**
	 * Read as much data as is immediately available on the stream.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.  The data is appended to ARG1.  The incoming bytes are interpreted according to the StringCharset property.  For example, if utf-8 bytes are expected, then StringCharset should be set to utf-8 prior to calling ReadSb.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function ReadSb($sb) {
		return CkStream_ReadSb($this->_cPtr,$sb);
	}

	/**
	 * Creates an asynchronous task to call the ReadSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadSbAsync
	 * @param CkStringBuilder $sb
	 * @return ?CkTask
	 */
	function ReadSbAsync($sb) {
		return chilkat::_obj(CkStream_ReadSbAsync($this->_cPtr,$sb),'CkTask');
	}

	/**
	 * Read as much data as is immediately available on the stream.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.  The data is returned as a string.  The incoming bytes are interpreted according to the StringCharset property.  For example, if utf-8 bytes are expected, then StringCharset should be set to utf-8 prior to calling ReadString.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadString
	 * @return ?string
	 */
	function readString() {
		return CkStream_readString($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ReadString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadStringAsync
	 * @return ?CkTask
	 */
	function ReadStringAsync() {
		return chilkat::_obj(CkStream_ReadStringAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Reads the stream until a CRLF is received.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.  The data is returned as a string.  The incoming bytes are interpreted according to the StringCharset property.  For example, if utf-8 bytes are expected, then StringCharset should be set to utf-8 prior to calling ReadString.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadToCRLF
	 * @return ?string
	 */
	function readToCRLF() {
		return CkStream_readToCRLF($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ReadToCRLF method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadToCRLFAsync
	 * @return ?CkTask
	 */
	function ReadToCRLFAsync() {
		return chilkat::_obj(CkStream_ReadToCRLFAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Reads the stream until the string indicated by ARG1 is received.  If no data is immediately available, it waits up to ReadTimeoutMs milliseconds for data to arrive.  The data is returned as a string.  The incoming bytes are interpreted according to the StringCharset property.  For example, if utf-8 bytes are expected, then StringCharset should be set to utf-8 prior to calling ReadString.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadUntilMatch
	 * @param ?string $matchStr
	 * @return ?string
	 */
	function readUntilMatch($matchStr) {
		return CkStream_readUntilMatch($this->_cPtr,$matchStr);
	}

	/**
	 * Creates an asynchronous task to call the ReadUntilMatch method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#ReadUntilMatchAsync
	 * @param ?string $matchStr
	 * @return ?CkTask
	 */
	function ReadUntilMatchAsync($matchStr) {
		return chilkat::_obj(CkStream_ReadUntilMatchAsync($this->_cPtr,$matchStr),'CkTask');
	}

	/**
	 * Resets the stream.  If a source or sink is open, then it is closed.  Properties such as EndOfStream and IsWriteClose are reset to default values.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#Reset
	 */
	function Reset() {
		CkStream_Reset($this->_cPtr);
	}

	/**
	 * Sets the stream's source to the contents of ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SetSourceBytes
	 * @param CkByteData $sourceData
	 * @return bool
	 */
	function SetSourceBytes($sourceData) {
		return CkStream_SetSourceBytes($this->_cPtr,$sourceData);
	}

	/**
	 * Sets the stream's source to the contents of ARG1.  The ARG2 indicates the character encoding to be used for the byte representation of the ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#SetSourceString
	 * @param ?string $srcStr
	 * @param ?string $charset
	 * @return bool
	 */
	function SetSourceString($srcStr,$charset) {
		return CkStream_SetSourceString($this->_cPtr,$srcStr,$charset);
	}

	/**
	 * Writes the contents of ARG1 to the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBd
	 * @param CkBinData $binData
	 * @return bool
	 */
	function WriteBd($binData) {
		return CkStream_WriteBd($this->_cPtr,$binData);
	}

	/**
	 * Creates an asynchronous task to call the WriteBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBdAsync
	 * @param CkBinData $binData
	 * @return ?CkTask
	 */
	function WriteBdAsync($binData) {
		return chilkat::_obj(CkStream_WriteBdAsync($this->_cPtr,$binData),'CkTask');
	}

	/**
	 * Writes a single byte to the stream.  The ARG1 must have a value from 0 to 255.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteByte
	 * @param int $byteVal
	 * @return bool
	 */
	function WriteByte($byteVal) {
		return CkStream_WriteByte($this->_cPtr,$byteVal);
	}

	/**
	 * Creates an asynchronous task to call the WriteByte method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteByteAsync
	 * @param int $byteVal
	 * @return ?CkTask
	 */
	function WriteByteAsync($byteVal) {
		return chilkat::_obj(CkStream_WriteByteAsync($this->_cPtr,$byteVal),'CkTask');
	}

	/**
	 * Writes binary bytes to a stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBytes
	 * @param CkByteData $byteData
	 * @return bool
	 */
	function WriteBytes($byteData) {
		return CkStream_WriteBytes($this->_cPtr,$byteData);
	}

	/**
	 * Creates an asynchronous task to call the WriteBytes method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBytesAsync
	 * @param CkByteData $byteData
	 * @return ?CkTask
	 */
	function WriteBytesAsync($byteData) {
		return chilkat::_obj(CkStream_WriteBytesAsync($this->_cPtr,$byteData),'CkTask');
	}

	/**
	 * Writes binary bytes to a stream. The byte data is passed in encoded string form, where the ARG2 can be base64, hex, or any of the supported binary encodings listed at the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBytesENC
	 * @param ?string $byteData
	 * @param ?string $encoding
	 * @return bool
	 */
	function WriteBytesENC($byteData,$encoding) {
		return CkStream_WriteBytesENC($this->_cPtr,$byteData,$encoding);
	}

	/**
	 * Creates an asynchronous task to call the WriteBytesENC method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteBytesENCAsync
	 * @param ?string $byteData
	 * @param ?string $encoding
	 * @return ?CkTask
	 */
	function WriteBytesENCAsync($byteData,$encoding) {
		return chilkat::_obj(CkStream_WriteBytesENCAsync($this->_cPtr,$byteData,$encoding),'CkTask');
	}

	/**
	 * Indicates that no more data will be written to the stream.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteClose
	 * @return bool
	 */
	function WriteClose() {
		return CkStream_WriteClose($this->_cPtr);
	}

	/**
	 * Writes the contents of ARG1 to the stream. The actual bytes written are the byte representation of the string as indicated by the StringCharset property.  For example, to write utf-8 bytes, first set StringCharset equal to utf-8 and then call WriteSb.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function WriteSb($sb) {
		return CkStream_WriteSb($this->_cPtr,$sb);
	}

	/**
	 * Creates an asynchronous task to call the WriteSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteSbAsync
	 * @param CkStringBuilder $sb
	 * @return ?CkTask
	 */
	function WriteSbAsync($sb) {
		return chilkat::_obj(CkStream_WriteSbAsync($this->_cPtr,$sb),'CkTask');
	}

	/**
	 * Writes a string to a stream. The actual bytes written are the byte representation of the string as indicated by the StringCharset property.  For example, to write utf-8 bytes, first set StringCharset equal to utf-8 and then call WriteString.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteString
	 * @param ?string $str
	 * @return bool
	 */
	function WriteString($str) {
		return CkStream_WriteString($this->_cPtr,$str);
	}

	/**
	 * Creates an asynchronous task to call the WriteString method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkStreamRef.html#WriteStringAsync
	 * @param ?string $str
	 * @return ?CkTask
	 */
	function WriteStringAsync($str) {
		return chilkat::_obj(CkStream_WriteStringAsync($this->_cPtr,$str),'CkTask');
	}
}
