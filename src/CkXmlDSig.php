<?php

/* CkXmlDSig.php -- Chilkat PHP extension proxy class CkXmlDSig.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Load, inspect, canonicalize, and verify XML Digital Signatures.
 *
 * Chilkat.XmlDSig loads and verifies XML Digital Signatures. It can inspect signed XML, select among multiple signatures in one document, verify the overall SignatureValue, verify individual Reference digests, handle external references, extract KeyInfo, retrieve embedded certificates or public keys, canonicalize XML, and add an EncapsulatedTimeStamp to an existing signature.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html
 */
class CkXmlDSig {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkXmlDSig') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkXmlDSig();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkXmlDSig_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkXmlDSig_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkXmlDSig_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkXmlDSig_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkXmlDSig_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * May contain a set of directory paths specifying where referenced external files are located.  Directory paths should be separated using a semicolon character.  The default value of this property is the empty string which means no directories are automatically searched.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#ExternalRefDirs
	 * @param CkString $str
	 */
	function get_ExternalRefDirs($str) {
		CkXmlDSig_get_ExternalRefDirs($this->_cPtr,$str);
	}

	/**
	 * May contain a set of directory paths specifying where referenced external files are located.  Directory paths should be separated using a semicolon character.  The default value of this property is the empty string which means no directories are automatically searched.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#ExternalRefDirs
	 * @return ?string
	 */
	function externalRefDirs() {
		return CkXmlDSig_externalRefDirs($this->_cPtr);
	}

	/**
	 * May contain a set of directory paths specifying where referenced external files are located.  Directory paths should be separated using a semicolon character.  The default value of this property is the empty string which means no directories are automatically searched.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#ExternalRefDirs
	 * @param ?string $newVal
	 */
	function put_ExternalRefDirs($newVal) {
		CkXmlDSig_put_ExternalRefDirs($this->_cPtr,$newVal);
	}

	/**
	 * If _TRUE_, then ignore failures caused by external references not being available.  This allows for the XML signature to be at least partially validated if the external referenced files are not available.  The default value of this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#IgnoreExternalRefs
	 * @return bool
	 */
	function get_IgnoreExternalRefs() {
		return CkXmlDSig_get_IgnoreExternalRefs($this->_cPtr);
	}

	/**
	 * If _TRUE_, then ignore failures caused by external references not being available.  This allows for the XML signature to be at least partially validated if the external referenced files are not available.  The default value of this property is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#IgnoreExternalRefs
	 * @param bool $newVal
	 */
	function put_IgnoreExternalRefs($newVal) {
		CkXmlDSig_put_IgnoreExternalRefs($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkXmlDSig_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkXmlDSig_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkXmlDSig_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkXmlDSig_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkXmlDSig_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkXmlDSig_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkXmlDSig_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkXmlDSig_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * The number of data objects referenced in the XML digital signature.  A data object may be self-contained within the loaded XML signature, or it may be an external URI reference.  An application can check each reference to see if it's external by calling IsReferenceExternal, and can get each reference URI by calling ReferenceUri.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#NumReferences
	 * @return int
	 */
	function get_NumReferences() {
		return CkXmlDSig_get_NumReferences($this->_cPtr);
	}

	/**
	 * The number of digital signatures found within the loaded XML.  Each digital signature is composed of XML having the following structure:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#NumSignatures
	 * @return int
	 */
	function get_NumSignatures() {
		return CkXmlDSig_get_NumSignatures($this->_cPtr);
	}

	/**
	 * Indicates the failure reason for the last call to VerifyReferenceDigest.   Possible values are:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#RefFailReason
	 * @return int
	 */
	function get_RefFailReason() {
		return CkXmlDSig_get_RefFailReason($this->_cPtr);
	}

	/**
	 * If the loaded XML contains multiple signatures, this property can be set to specify which signature is in effect when calling other methods or properties.  In most cases, the loaded XML contains a single signature and this property will remain at the default value of 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#Selector
	 * @return int
	 */
	function get_Selector() {
		return CkXmlDSig_get_Selector($this->_cPtr);
	}

	/**
	 * If the loaded XML contains multiple signatures, this property can be set to specify which signature is in effect when calling other methods or properties.  In most cases, the loaded XML contains a single signature and this property will remain at the default value of 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#Selector
	 * @param int $newVal
	 */
	function put_Selector($newVal) {
		CkXmlDSig_put_Selector($this->_cPtr,$newVal);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs.  This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkXmlDSig_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs.  This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkXmlDSig_uncommonOptions($this->_cPtr);
	}

	/**
	 * This is a catch-all property to be used for uncommon needs.  This property defaults to the empty string, and should typically remain empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkXmlDSig_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkXmlDSig_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkXmlDSig_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkXmlDSig_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkXmlDSig_version($this->_cPtr);
	}

	/**
	 * Adds an EncapsulatedTimeStamp to the signature indicated by the Selector property.  The updated signed XML with the added EncapsulatedTimeStamp is returned in ARG2.  See the example below for details.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#AddEncapsulatedTimeStamp
	 * @param CkJsonObject $json
	 * @param CkStringBuilder $sbOut
	 * @return bool
	 */
	function AddEncapsulatedTimeStamp($json,$sbOut) {
		return CkXmlDSig_AddEncapsulatedTimeStamp($this->_cPtr,$json,$sbOut);
	}

	/**
	 * Applies XML canonicalization to a fragment of the passed-in XML, and returns the canonicalized XML string.  The ARG2 identifies the XML element where output begins.  It is the XML element having an id attribute equal to ARG2.  The ARG3 may be one of the following:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#CanonicalizeFragment
	 * @param ?string $xml
	 * @param ?string $fragmentId
	 * @param ?string $version
	 * @param ?string $prefixList
	 * @param bool $withComments
	 * @return ?string
	 */
	function canonicalizeFragment($xml,$fragmentId,$version,$prefixList,$withComments) {
		return CkXmlDSig_canonicalizeFragment($this->_cPtr,$xml,$fragmentId,$version,$prefixList,$withComments);
	}

	/**
	 * Applies XML canonicalization to the passed-in XML, and returns the canonicalized XML string.  The ARG2 may be one of the following:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#CanonicalizeXml
	 * @param ?string $xml
	 * @param ?string $version
	 * @param bool $withComments
	 * @return ?string
	 */
	function canonicalizeXml($xml,$version,$withComments) {
		return CkXmlDSig_canonicalizeXml($this->_cPtr,$xml,$version,$withComments);
	}

	/**
	 * Returns the certificates found in the signature indicated by the Selector property.  The base64 representation of each certificate is returned.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#GetCerts
	 * @param CkStringArray $sa
	 * @return bool
	 */
	function GetCerts($sa) {
		return CkXmlDSig_GetCerts($this->_cPtr,$sa);
	}

	/**
	 * Returns the KeyInfo XML for the signature indicated by the Selector property.  Returns _NULL_ if no KeyInfo exists.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#GetKeyInfo
	 * @return ?CkXml
	 */
	function GetKeyInfo() {
		return chilkat::_obj(CkXmlDSig_GetKeyInfo($this->_cPtr),'CkXml');
	}

	/**
	 * Returns the public key from the KeyInfo XML for the signature indicated by the Selector property.  Returns _NULL_ if no KeyInfo exists, or if no public key is actually contained in the KeyInfo.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#GetPublicKey
	 * @return ?CkPublicKey
	 */
	function GetPublicKey() {
		return chilkat::_obj(CkXmlDSig_GetPublicKey($this->_cPtr),'CkPublicKey');
	}

	/**
	 * Returns _TRUE_ if the signature indicated by the Selector property contains an EncapsulatedTimeStamp.  Otherwise returns _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#HasEncapsulatedTimeStamp
	 * @return bool
	 */
	function HasEncapsulatedTimeStamp() {
		return CkXmlDSig_HasEncapsulatedTimeStamp($this->_cPtr);
	}

	/**
	 * Returns _TRUE_ if the reference at ARG1 is external.  Each external reference would first need to be provided by the application prior to validating the signature.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#IsReferenceExternal
	 * @param int $index
	 * @return bool
	 */
	function IsReferenceExternal($index) {
		return CkXmlDSig_IsReferenceExternal($this->_cPtr,$index);
	}

	/**
	 * Loads an XML document containing 1 or more XML digital signatures.  An application would first load XML containing digital signature(s), then validate.  After loading, it is also possible to use various methods and properties to get information about the signature, such as the key info, references, etc.  If external data is referenced by the signature, it may be necessary to provide the referenced data prior to validating.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LoadSignature
	 * @param ?string $xmlSig
	 * @return bool
	 */
	function LoadSignature($xmlSig) {
		return CkXmlDSig_LoadSignature($this->_cPtr,$xmlSig);
	}

	/**
	 * Loads an XML document containing one or more XML digital signatures from the contents of a BinData object.  An application would first load the XML, then validate.  After loading, it is also possible to use various methods and properties to get information about the signature, such as the key info, references, etc.  If external data is referenced by the signature, it may be necessary to provide the referenced data prior to validating.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LoadSignatureBd
	 * @param CkBinData $binData
	 * @return bool
	 */
	function LoadSignatureBd($binData) {
		return CkXmlDSig_LoadSignatureBd($this->_cPtr,$binData);
	}

	/**
	 * Loads an XML document containing one or more XML digital signatures from the contents of a StringBuilder object.  An application would first load the XML, then validate.  After loading, it is also possible to use various methods and properties to get information about the signature, such as the key info, references, etc.  If external data is referenced by the signature, it may be necessary to provide the referenced data prior to validating.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#LoadSignatureSb
	 * @param CkStringBuilder $sbXmlSig
	 * @return bool
	 */
	function LoadSignatureSb($sbXmlSig) {
		return CkXmlDSig_LoadSignatureSb($this->_cPtr,$sbXmlSig);
	}

	/**
	 * Returns the URI of the Nth reference specified by ARG1.  (The 1st reference is at index 0.)   URI's beginning with a pound sign ('#') are internal references.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#ReferenceUri
	 * @param int $index
	 * @return ?string
	 */
	function referenceUri($index) {
		return CkXmlDSig_referenceUri($this->_cPtr,$index);
	}

	/**
	 * Sets the HMAC key to be used if the Signature used an HMAC signing algorithm.  The ARG2 specifies the encoding of ARG1, and can be hex, base64, ascii, or any of the binary encodings supported by Chilkat in the link below.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetHmacKey
	 * @param ?string $key
	 * @param ?string $encoding
	 * @return bool
	 */
	function SetHmacKey($key,$encoding) {
		return CkXmlDSig_SetHmacKey($this->_cPtr,$key,$encoding);
	}

	/**
	 * Sets the HTTP object to be used to communicate with OCSP responders, CRL distribution points, or timestamp authority (TSA) servers if needed.   The ARG1 is used to send the requests, and it allows for connection related settings and timeouts to be set.  For example, if HTTP or SOCKS proxies are required, these features can be specified on the ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetHttpObj
	 * @param CkHttp $http
	 */
	function SetHttpObj($http) {
		CkXmlDSig_SetHttpObj($this->_cPtr,$http);
	}

	/**
	 * Sets the public key to be used for verifying the signature indicated by the Selector property.  A public key only needs to be explicitly provided by the application for those XML signatures where the public key is not already available within the KeyInfo of the Signature.  In some cases, the KeyInfo within the Signature contains information about what public key was used for signing.  The application can use this information to retrieve the matching public key and provide it via this method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetPublicKey
	 * @param CkPublicKey $pubKey
	 * @return bool
	 */
	function SetPublicKey($pubKey) {
		return CkXmlDSig_SetPublicKey($this->_cPtr,$pubKey);
	}

	/**
	 * Provides the binary data for the external reference at ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetRefDataBd
	 * @param int $index
	 * @param CkBinData $binData
	 * @return bool
	 */
	function SetRefDataBd($index,$binData) {
		return CkXmlDSig_SetRefDataBd($this->_cPtr,$index,$binData);
	}

	/**
	 * Provides the data for the external reference at ARG1.   When validating the signature, the data contained in ARG2 will be streamed to compute the digest for validation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetRefDataFile
	 * @param int $index
	 * @param ?string $path
	 * @return bool
	 */
	function SetRefDataFile($index,$path) {
		return CkXmlDSig_SetRefDataFile($this->_cPtr,$index,$path);
	}

	/**
	 * Provides the text data for the external reference at ARG1.   The ARG3 specifies the byte representation of the text, such as utf-8, utf-16, windows-1252, etc.    (If in doubt, try utf-8 first.)
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#SetRefDataSb
	 * @param int $index
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @return bool
	 */
	function SetRefDataSb($index,$sb,$charset) {
		return CkXmlDSig_SetRefDataSb($this->_cPtr,$index,$sb,$charset);
	}

	/**
	 * Adds an XML certificate vault to the object's internal list of sources to be searched for certificates having public keys when verifying an XML signature.  A single XML certificate vault may be used.  If UseCertVault is called multiple times, only the last certificate vault will be used, as each call to UseCertVault will replace the certificate vault provided in previous calls.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#UseCertVault
	 * @param CkXmlCertVault $certVault
	 * @return bool
	 */
	function UseCertVault($certVault) {
		return CkXmlDSig_UseCertVault($this->_cPtr,$certVault);
	}

	/**
	 * This method allows for an application to verify the digest for each reference separately.  This can be helpful if the full XMLDSIG validation fails, then one can test each referenced data's digest to see which, if any, fail to match.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#VerifyReferenceDigest
	 * @param int $index
	 * @return bool
	 */
	function VerifyReferenceDigest($index) {
		return CkXmlDSig_VerifyReferenceDigest($this->_cPtr,$index);
	}

	/**
	 * Verifies the signature indicated by the Selector property.  If ARG1 is  _TRUE_, then the digest of each Reference within the signature's SignedInfo is also validated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkXmlDSigRef.html#VerifySignature
	 * @param bool $verifyReferenceDigests
	 * @return bool
	 */
	function VerifySignature($verifyReferenceDigests) {
		return CkXmlDSig_VerifySignature($this->_cPtr,$verifyReferenceDigests);
	}
}
