<?php

/* CkFtp2.php -- Chilkat PHP extension proxy class CkFtp2.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Connect, secure, transfer, synchronize, and manage files on FTP servers.
 *
 * Ftp2 is a comprehensive FTP and FTPS client class for applications that need reliable file transfer and remote file management. It supports connection setup, explicit and implicit TLS, passive and active data connections, uploads, downloads, directory listings, recursive operations, synchronization, proxy traversal, credential protection, and detailed diagnostics.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html
 */
class CkFtp2 {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkFtp2') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkFtp2();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkFtp2_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkFtp2_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html
	 * @param ?CkFtp2Progress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkFtp2_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Set to _TRUE_ to request cancellation of the operation currently running on this object. Long-running network and file-transfer methods periodically check this property; methods that complete quickly may finish before the request is observed. A synchronous call can be cancelled from another thread by setting this property on the same object. The property is reset to _FALSE_ after cancellation is processed, and a stale request is cleared when a later method begins.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkFtp2_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to request cancellation of the operation currently running on this object. Long-running network and file-transfer methods periodically check this property; methods that complete quickly may finish before the request is observed. A synchronous call can be cancelled from another thread by setting this property on the same object. The property is reset to _FALSE_ after cancellation is processed, and a stale request is cleared when a later method begins.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkFtp2_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * Very rarely needed today: Almost all modern FTP servers rely exclusively on the USER and PASS commands for authentication and access control. The ACCT command is a legacy feature from early computing systems that sometimes required a separate account code for billing or resource allocation. Today, those details are usually associated directly with the user's server-side profile, making ACCT obsolete for most applications. Leave this property empty unless the server administrator or login instructions explicitly require an FTP account value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Account
	 * @param CkString $str
	 */
	function get_Account($str) {
		CkFtp2_get_Account($this->_cPtr,$str);
	}

	/**
	 * Very rarely needed today: Almost all modern FTP servers rely exclusively on the USER and PASS commands for authentication and access control. The ACCT command is a legacy feature from early computing systems that sometimes required a separate account code for billing or resource allocation. Today, those details are usually associated directly with the user's server-side profile, making ACCT obsolete for most applications. Leave this property empty unless the server administrator or login instructions explicitly require an FTP account value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Account
	 * @return ?string
	 */
	function account() {
		return CkFtp2_account($this->_cPtr);
	}

	/**
	 * Very rarely needed today: Almost all modern FTP servers rely exclusively on the USER and PASS commands for authentication and access control. The ACCT command is a legacy feature from early computing systems that sometimes required a separate account code for billing or resource allocation. Today, those details are usually associated directly with the user's server-side profile, making ACCT obsolete for most applications. Leave this property empty unless the server administrator or login instructions explicitly require an FTP account value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Account
	 * @param ?string $newVal
	 */
	function put_Account($newVal) {
		CkFtp2_put_Account($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the last local TCP port that may be used for active-mode data connections. The default is 0. Valid nonzero values are 1 through 65535. The range beginning with ActivePortRangeStart is inclusive. If ActivePortRangeStart is 0, this property is ignored and the operating system chooses an ephemeral listening port. If this value is 0 or is less than ActivePortRangeStart, Chilkat behaves as though both endpoints equal ActivePortRangeStart.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ActivePortRangeEnd
	 * @return int
	 */
	function get_ActivePortRangeEnd() {
		return CkFtp2_get_ActivePortRangeEnd($this->_cPtr);
	}

	/**
	 * Specifies the last local TCP port that may be used for active-mode data connections. The default is 0. Valid nonzero values are 1 through 65535. The range beginning with ActivePortRangeStart is inclusive. If ActivePortRangeStart is 0, this property is ignored and the operating system chooses an ephemeral listening port. If this value is 0 or is less than ActivePortRangeStart, Chilkat behaves as though both endpoints equal ActivePortRangeStart.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ActivePortRangeEnd
	 * @param int $newVal
	 */
	function put_ActivePortRangeEnd($newVal) {
		CkFtp2_put_ActivePortRangeEnd($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the first local TCP port that may be used for active-mode data connections. The default is 0. Valid nonzero values are 1 through 65535. When this property is 0, ActivePortRangeEnd is ignored and Chilkat binds the listening socket to port 0, allowing the operating system to choose an available ephemeral port. When this property is nonzero, the effective range is inclusive. If the ending value is 0 or is less than the starting value, Chilkat tries only the starting port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ActivePortRangeStart
	 * @return int
	 */
	function get_ActivePortRangeStart() {
		return CkFtp2_get_ActivePortRangeStart($this->_cPtr);
	}

	/**
	 * Specifies the first local TCP port that may be used for active-mode data connections. The default is 0. Valid nonzero values are 1 through 65535. When this property is 0, ActivePortRangeEnd is ignored and Chilkat binds the listening socket to port 0, allowing the operating system to choose an available ephemeral port. When this property is nonzero, the effective range is inclusive. If the ending value is 0 or is less than the starting value, Chilkat tries only the starting port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ActivePortRangeStart
	 * @param int $newVal
	 */
	function put_ActivePortRangeStart($newVal) {
		CkFtp2_put_ActivePortRangeStart($this->_cPtr,$newVal);
	}

	/**
	 * Rarely needed: The FTP ALLO command pre-allocates disk space on the server before an upload. It is generally needed only by certain legacy systems, such as older mainframes, that require the exact file size in advance to reserve storage. Most modern FTP servers allocate space dynamically and either ignore ALLO or do not require it. Leave this property at its default value of 0 unless the server explicitly requires pre-allocation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AllocateSize
	 * @return int
	 */
	function get_AllocateSize() {
		return CkFtp2_get_AllocateSize($this->_cPtr);
	}

	/**
	 * Rarely needed: The FTP ALLO command pre-allocates disk space on the server before an upload. It is generally needed only by certain legacy systems, such as older mainframes, that require the exact file size in advance to reserve storage. Most modern FTP servers allocate space dynamically and either ignore ALLO or do not require it. Leave this property at its default value of 0 unless the server explicitly requires pre-allocation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AllocateSize
	 * @param int $newVal
	 */
	function put_AllocateSize($newVal) {
		CkFtp2_put_AllocateSize($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether machine-readable directory listings are requested with MLSD when the server advertises support. The default is _TRUE_. When this property is _TRUE_ and MLSD is advertised, Chilkat uses MLSD. Otherwise, a legacy listing is used: NLST when PreferNlst is _TRUE_, or LIST when it is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AllowMlsd
	 * @return bool
	 */
	function get_AllowMlsd() {
		return CkFtp2_get_AllowMlsd($this->_cPtr);
	}

	/**
	 * Controls whether machine-readable directory listings are requested with MLSD when the server advertises support. The default is _TRUE_. When this property is _TRUE_ and MLSD is advertised, Chilkat uses MLSD. Otherwise, a legacy listing is used: NLST when PreferNlst is _TRUE_, or LIST when it is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AllowMlsd
	 * @param bool $newVal
	 */
	function put_AllowMlsd($newVal) {
		CkFtp2_put_AllowMlsd($this->_cPtr,$newVal);
	}

	/**
	 * Deprecated. Returns the number of bytes received during the current FTP download as a 32-bit unsigned integer. The value is updated while the transfer is running. Use CurBytesReceived instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesReceived
	 * @return int
	 * @deprecated
	 */
	function get_AsyncBytesReceived() {
		return CkFtp2_get_AsyncBytesReceived($this->_cPtr);
	}

	/**
	 * Deprecated. Returns the number of bytes received during the current FTP download as a 64-bit integer. The value is updated while the transfer is running. Use CurBytesReceived64 instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesReceived64
	 * @return int
	 * @deprecated
	 */
	function get_AsyncBytesReceived64() {
		return CkFtp2_get_AsyncBytesReceived64($this->_cPtr);
	}

	/**
	 * Deprecated. Returns the number of bytes received during the current FTP download as a decimal string. The value is updated while the transfer is running. Use CurBytesReceivedStr instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesReceivedStr
	 * @param CkString $str
	 * @deprecated
	 */
	function get_AsyncBytesReceivedStr($str) {
		CkFtp2_get_AsyncBytesReceivedStr($this->_cPtr,$str);
	}

	/**
	 * Deprecated. Returns the number of bytes received during the current FTP download as a decimal string. The value is updated while the transfer is running. Use CurBytesReceivedStr instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesReceivedStr
	 * @return ?string
	 * @deprecated
	 */
	function asyncBytesReceivedStr() {
		return CkFtp2_asyncBytesReceivedStr($this->_cPtr);
	}

	/**
	 * Deprecated. Returns the number of bytes sent during the current FTP upload as a 32-bit unsigned integer. The value is updated while the transfer is running. Use CurBytesSent instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesSent
	 * @return int
	 * @deprecated
	 */
	function get_AsyncBytesSent() {
		return CkFtp2_get_AsyncBytesSent($this->_cPtr);
	}

	/**
	 * Deprecated. Returns the number of bytes sent during the current FTP upload as a 64-bit integer. The value is updated while the transfer is running. Use CurBytesSent64 instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesSent64
	 * @return int
	 * @deprecated
	 */
	function get_AsyncBytesSent64() {
		return CkFtp2_get_AsyncBytesSent64($this->_cPtr);
	}

	/**
	 * Deprecated. Returns the number of bytes sent during the current FTP upload as a decimal string. The value is updated while the transfer is running. Use CurBytesSentStr instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesSentStr
	 * @param CkString $str
	 * @deprecated
	 */
	function get_AsyncBytesSentStr($str) {
		CkFtp2_get_AsyncBytesSentStr($this->_cPtr,$str);
	}

	/**
	 * Deprecated. Returns the number of bytes sent during the current FTP upload as a decimal string. The value is updated while the transfer is running. Use CurBytesSentStr instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AsyncBytesSentStr
	 * @return ?string
	 * @deprecated
	 */
	function asyncBytesSentStr() {
		return CkFtp2_asyncBytesSentStr($this->_cPtr);
	}

	/**
	 * Legacy and very uncommon: AUTH SSL originated in early FTP security drafts from the era when SSL was the standard encryption protocol. For modern FTP servers, use AuthTls instead. Most security-conscious servers require AUTH TLS and may reject AUTH SSL to enforce current security policies and prevent protocol downgrades. This property is normally needed only for a decades-old legacy server that was never updated to support AUTH TLS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AuthSsl
	 * @return bool
	 */
	function get_AuthSsl() {
		return CkFtp2_get_AuthSsl($this->_cPtr);
	}

	/**
	 * Legacy and very uncommon: AUTH SSL originated in early FTP security drafts from the era when SSL was the standard encryption protocol. For modern FTP servers, use AuthTls instead. Most security-conscious servers require AUTH TLS and may reject AUTH SSL to enforce current security policies and prevent protocol downgrades. This property is normally needed only for a decades-old legacy server that was never updated to support AUTH TLS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AuthSsl
	 * @param bool $newVal
	 */
	function put_AuthSsl($newVal) {
		CkFtp2_put_AuthSsl($this->_cPtr,$newVal);
	}

	/**
	 * Set to _TRUE_ to use explicit FTP over TLS by sending AUTH TLS on the FTP control channel. The client first establishes a plain FTP control connection, receives the server greeting, and then upgrades that connection to TLS. Port 21 is conventionally used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AuthTls
	 * @return bool
	 */
	function get_AuthTls() {
		return CkFtp2_get_AuthTls($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to use explicit FTP over TLS by sending AUTH TLS on the FTP control channel. The client first establishes a plain FTP control connection, receives the server greeting, and then upgrades that connection to TLS. Port 21 is conventionally used.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AuthTls
	 * @param bool $newVal
	 */
	function put_AuthTls($newVal) {
		CkFtp2_put_AuthTls($this->_cPtr,$newVal);
	}

	/**
	 * Rarely changed: Leave this property at its default value of _TRUE_ for nearly all modern FTP servers. The FEAT command was standardized in RFC 2389 in 1998 and is broadly supported by mainstream FTP software, including FileZilla Server, vsftpd, ProFTPD, and IIS. Set it to _FALSE_ only for a legacy or nonconforming server that mishandles FEAT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoFeat
	 * @return bool
	 */
	function get_AutoFeat() {
		return CkFtp2_get_AutoFeat($this->_cPtr);
	}

	/**
	 * Rarely changed: Leave this property at its default value of _TRUE_ for nearly all modern FTP servers. The FEAT command was standardized in RFC 2389 in 1998 and is broadly supported by mainstream FTP software, including FileZilla Server, vsftpd, ProFTPD, and IIS. Set it to _FALSE_ only for a legacy or nonconforming server that mishandles FEAT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoFeat
	 * @param bool $newVal
	 */
	function put_AutoFeat($newVal) {
		CkFtp2_put_AutoFeat($this->_cPtr,$newVal);
	}

	/**
	 * Controls automatic adjustment of Ssl, AuthSsl, and AuthTls based solely on Port. The default is _TRUE_. The only recognized ports are 21 and 990. PortAutomatic adjustment 21Sets Ssl to _FALSE_. AuthTls and AuthSsl continue to determine whether explicit TLS is requested. 990Sets Ssl to _TRUE_ and sets AuthTls and AuthSsl to _FALSE_. Any other portMakes no changes because the intended plain, explicit-TLS, or implicit-TLS mode cannot be inferred from a custom port number.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoFix
	 * @return bool
	 */
	function get_AutoFix() {
		return CkFtp2_get_AutoFix($this->_cPtr);
	}

	/**
	 * Controls automatic adjustment of Ssl, AuthSsl, and AuthTls based solely on Port. The default is _TRUE_. The only recognized ports are 21 and 990. PortAutomatic adjustment 21Sets Ssl to _FALSE_. AuthTls and AuthSsl continue to determine whether explicit TLS is requested. 990Sets Ssl to _TRUE_ and sets AuthTls and AuthSsl to _FALSE_. Any other portMakes no changes because the intended plain, explicit-TLS, or implicit-TLS mode cannot be inferred from a custom port number.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoFix
	 * @param bool $newVal
	 */
	function put_AutoFix($newVal) {
		CkFtp2_put_AutoFix($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether Chilkat sends a SIZE command before a download so percentage-based progress can be calculated. The default is _FALSE_. Each size query adds a control-channel round trip. A nonzero one-shot value in ProgressMonSize or ProgressMonSize64 supplies the expected size directly and avoids this additional query. Even when this property is _FALSE_, percentage progress may be available if the server includes a usable size in its preliminary transfer reply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoGetSizeForProgress
	 * @return bool
	 */
	function get_AutoGetSizeForProgress() {
		return CkFtp2_get_AutoGetSizeForProgress($this->_cPtr);
	}

	/**
	 * Controls whether Chilkat sends a SIZE command before a download so percentage-based progress can be calculated. The default is _FALSE_. Each size query adds a control-channel round trip. A nonzero one-shot value in ProgressMonSize or ProgressMonSize64 supplies the expected size directly and avoids this additional query. Even when this property is _FALSE_, percentage progress may be available if the server includes a usable size in its preliminary transfer reply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoGetSizeForProgress
	 * @param bool $newVal
	 */
	function put_AutoGetSizeForProgress($newVal) {
		CkFtp2_put_AutoGetSizeForProgress($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether OPTS UTF8 ON is sent automatically when a processed FEAT response advertises UTF8. The default is _TRUE_. When UTF-8 is advertised, Chilkat sets CommandCharset and DirListingCharset to utf-8, overwriting values previously assigned by the application. If UTF8 is not advertised, the existing charset property values are left unchanged. This behavior applies both during automatic capability discovery and when Feat is called explicitly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoOptsUtf8
	 * @return bool
	 */
	function get_AutoOptsUtf8() {
		return CkFtp2_get_AutoOptsUtf8($this->_cPtr);
	}

	/**
	 * Controls whether OPTS UTF8 ON is sent automatically when a processed FEAT response advertises UTF8. The default is _TRUE_. When UTF-8 is advertised, Chilkat sets CommandCharset and DirListingCharset to utf-8, overwriting values previously assigned by the application. If UTF8 is not advertised, the existing charset property values are left unchanged. This behavior applies both during automatic capability discovery and when Feat is called explicitly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoOptsUtf8
	 * @param bool $newVal
	 */
	function put_AutoOptsUtf8($newVal) {
		CkFtp2_put_AutoOptsUtf8($this->_cPtr,$newVal);
	}

	/**
	 * Recommended setting: Applications should set this property to _TRUE_. The default is _FALSE_ in current versions, but will change to _TRUE_ in Chilkat v12.0.0. EPSV is preferred on modern networks because it supports both IPv4 and IPv6, whereas PASV cannot represent an IPv6 address. It is also more reliable through NAT and firewalls because the EPSV reply contains only a port number and relies on the address of the existing control connection. In contrast, a PASV reply includes an IP address, which can fail when a server behind NAT returns its private internal address.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoSetUseEpsv
	 * @return bool
	 */
	function get_AutoSetUseEpsv() {
		return CkFtp2_get_AutoSetUseEpsv($this->_cPtr);
	}

	/**
	 * Recommended setting: Applications should set this property to _TRUE_. The default is _FALSE_ in current versions, but will change to _TRUE_ in Chilkat v12.0.0. EPSV is preferred on modern networks because it supports both IPv4 and IPv6, whereas PASV cannot represent an IPv6 address. It is also more reliable through NAT and firewalls because the EPSV reply contains only a port number and relies on the address of the existing control connection. In contrast, a PASV reply includes an IP address, which can fail when a server behind NAT returns its private internal address.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoSetUseEpsv
	 * @param bool $newVal
	 */
	function put_AutoSetUseEpsv($newVal) {
		CkFtp2_put_AutoSetUseEpsv($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether a SYST command is sent automatically after login. The default is _TRUE_. The server's operating-system type can help Chilkat interpret legacy LIST output and other server-specific behavior. When both this property and AutoFeat are _TRUE_, automatic SYST occurs before automatic FEAT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoSyst
	 * @return bool
	 */
	function get_AutoSyst() {
		return CkFtp2_get_AutoSyst($this->_cPtr);
	}

	/**
	 * Controls whether a SYST command is sent automatically after login. The default is _TRUE_. The server's operating-system type can help Chilkat interpret legacy LIST output and other server-specific behavior. When both this property and AutoFeat are _TRUE_, automatic SYST occurs before automatic FEAT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoSyst
	 * @param bool $newVal
	 */
	function put_AutoSyst($newVal) {
		CkFtp2_put_AutoSyst($this->_cPtr,$newVal);
	}

	/**
	 * Controls automatic post-upload verification with the server-specific XCRC command. The default is _FALSE_. When enabled and the server supports XCRC, Chilkat requests a CRC after an upload and treats a mismatch as transfer failure. If XCRC support is not detected, Chilkat does not send the verification command and the upload proceeds normally. This extension is not part of the core FTP standard and is not supported by every server. Capability detection normally depends on a processed FEAT response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoXcrc
	 * @return bool
	 */
	function get_AutoXcrc() {
		return CkFtp2_get_AutoXcrc($this->_cPtr);
	}

	/**
	 * Controls automatic post-upload verification with the server-specific XCRC command. The default is _FALSE_. When enabled and the server supports XCRC, Chilkat requests a CRC after an upload and treats a mismatch as transfer failure. If XCRC support is not detected, Chilkat does not send the verification command and the upload proceeds normally. This extension is not part of the core FTP standard and is not supported by every server. Capability detection normally depends on a processed FEAT response.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AutoXcrc
	 * @param bool $newVal
	 */
	function put_AutoXcrc($newVal) {
		CkFtp2_put_AutoXcrc($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the approximate maximum download rate in bytes per second. The default value of 0 disables throttling. Client-side download throttling is approximate because the server continues sending as permitted by TCP. The operating system's socket buffers absorb data between reads, so very small transfers may complete before the configured rate can be meaningfully enforced.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#BandwidthThrottleDown
	 * @return int
	 */
	function get_BandwidthThrottleDown() {
		return CkFtp2_get_BandwidthThrottleDown($this->_cPtr);
	}

	/**
	 * Specifies the approximate maximum download rate in bytes per second. The default value of 0 disables throttling. Client-side download throttling is approximate because the server continues sending as permitted by TCP. The operating system's socket buffers absorb data between reads, so very small transfers may complete before the configured rate can be meaningfully enforced.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#BandwidthThrottleDown
	 * @param int $newVal
	 */
	function put_BandwidthThrottleDown($newVal) {
		CkFtp2_put_BandwidthThrottleDown($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the approximate maximum upload rate in bytes per second. The default value of 0 disables throttling. The limit is averaged over the transfer. Very small uploads may complete before the configured rate can be closely approximated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#BandwidthThrottleUp
	 * @return int
	 */
	function get_BandwidthThrottleUp() {
		return CkFtp2_get_BandwidthThrottleUp($this->_cPtr);
	}

	/**
	 * Specifies the approximate maximum upload rate in bytes per second. The default value of 0 disables throttling. The limit is averaged over the transfer. Very small uploads may complete before the configured rate can be closely approximated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#BandwidthThrottleUp
	 * @param int $newVal
	 */
	function put_BandwidthThrottleUp($newVal) {
		CkFtp2_put_BandwidthThrottleUp($this->_cPtr,$newVal);
	}

	/**
	 * This property is normally left unset. Set it only on a multihomed computer when the application must bind the outgoing connection to a specific local network interface.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClientIpAddress
	 * @param CkString $str
	 */
	function get_ClientIpAddress($str) {
		CkFtp2_get_ClientIpAddress($this->_cPtr,$str);
	}

	/**
	 * This property is normally left unset. Set it only on a multihomed computer when the application must bind the outgoing connection to a specific local network interface.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClientIpAddress
	 * @return ?string
	 */
	function clientIpAddress() {
		return CkFtp2_clientIpAddress($this->_cPtr);
	}

	/**
	 * This property is normally left unset. Set it only on a multihomed computer when the application must bind the outgoing connection to a specific local network interface.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClientIpAddress
	 * @param ?string $newVal
	 */
	function put_ClientIpAddress($newVal) {
		CkFtp2_put_ClientIpAddress($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the character encoding used by high-level FTP operations for commands containing non-ASCII text, including remote paths and filenames. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. An unrecognized charset name is clamped to utf-8, and the getter returns utf-8. Usually changed automatically to UTF-8: Although the default value is ANSI, when the server advertises UTF-8 and AutoOptsUtf8 is enabled, Chilkat automatically changes this property to utf-8. Nearly all modern FTP servers support UTF-8, so utf-8 is typically the effective value after connecting. If UTF-8 is not advertised, the existing value is left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CommandCharset
	 * @param CkString $str
	 */
	function get_CommandCharset($str) {
		CkFtp2_get_CommandCharset($this->_cPtr,$str);
	}

	/**
	 * Specifies the character encoding used by high-level FTP operations for commands containing non-ASCII text, including remote paths and filenames. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. An unrecognized charset name is clamped to utf-8, and the getter returns utf-8. Usually changed automatically to UTF-8: Although the default value is ANSI, when the server advertises UTF-8 and AutoOptsUtf8 is enabled, Chilkat automatically changes this property to utf-8. Nearly all modern FTP servers support UTF-8, so utf-8 is typically the effective value after connecting. If UTF-8 is not advertised, the existing value is left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CommandCharset
	 * @return ?string
	 */
	function commandCharset() {
		return CkFtp2_commandCharset($this->_cPtr);
	}

	/**
	 * Specifies the character encoding used by high-level FTP operations for commands containing non-ASCII text, including remote paths and filenames. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. An unrecognized charset name is clamped to utf-8, and the getter returns utf-8. Usually changed automatically to UTF-8: Although the default value is ANSI, when the server advertises UTF-8 and AutoOptsUtf8 is enabled, Chilkat automatically changes this property to utf-8. Nearly all modern FTP servers support UTF-8, so utf-8 is typically the effective value after connecting. If UTF-8 is not advertised, the existing value is left unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CommandCharset
	 * @param ?string $newVal
	 */
	function put_CommandCharset($newVal) {
		CkFtp2_put_CommandCharset($this->_cPtr,$newVal);
	}

	/**
	 * Contains a numeric reason code after Connect or ConnectOnly fails. Use this value for broad classification and inspect LastErrorText for detailed diagnostics. CodeMeaning 0Success. 1The hostname is empty. 2DNS lookup failed. 3DNS lookup timed out. 4Cancelled by the application. 5Internal failure. 6TCP connection timed out. 7TCP connection was rejected or otherwise failed. 100Internal TLS failure. 101Failed to send the TLS ClientHello. 102An unexpected TLS handshake message was received. 103Failed to read the ServerHello. 104The server did not provide a certificate. 105Unexpected TLS protocol version. 106Server certificate verification failed. 107The negotiated TLS version is not allowed. 109Failed while reading TLS handshake messages. 110Failed to send the client-certificate handshake message. 111Failed to send the client key exchange. 112The client certificate private key is not accessible. 113Failed to send client certificate verification. 114Failed to send ChangeCipherSpec. 115Failed to send the Finished handshake message. 116The server Finished message is invalid. 200Connected, but the FTP greeting was not received. 201The AUTH TLS or AUTH SSL upgrade failed. 300An asynchronous operation is already in progress. 301FTP authentication failed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectFailReason
	 * @return int
	 */
	function get_ConnectFailReason() {
		return CkFtp2_get_ConnectFailReason($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of seconds to wait for a remote endpoint to accept a TCP connection. The default is 30. A value of 0 or any negative value waits indefinitely. This timeout applies whenever Ftp2 establishes a TCP connection, including the FTP control connection, FTP data connections, and connections to configured proxy servers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectTimeout
	 * @return int
	 */
	function get_ConnectTimeout() {
		return CkFtp2_get_ConnectTimeout($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of seconds to wait for a remote endpoint to accept a TCP connection. The default is 30. A value of 0 or any negative value waits indefinitely. This timeout applies whenever Ftp2 establishes a TCP connection, including the FTP control connection, FTP data connections, and connections to configured proxy servers.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectTimeout
	 * @param int $newVal
	 */
	function put_ConnectTimeout($newVal) {
		CkFtp2_put_ConnectTimeout($this->_cPtr,$newVal);
	}

	/**
	 * Indicates whether the TCP connection to the FTP server was successfully established during the most recent connection attempt. It does not indicate that FTP authentication succeeded; see LoginVerified. This is a verification result, not a current-socket-state property. It remains _TRUE_ after Disconnect if the preceding connection was successfully established.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectVerified
	 * @return bool
	 */
	function get_ConnectVerified() {
		return CkFtp2_get_ConnectVerified($this->_cPtr);
	}

	/**
	 * Controls line-ending conversion when downloading in ASCII transfer mode. The default is 0. ValueDownloaded text 0Leaves line endings as received. 1Converts line endings to CRLF. 2Converts line endings to LF. 3Converts line endings to CR.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CrlfMode
	 * @return int
	 */
	function get_CrlfMode() {
		return CkFtp2_get_CrlfMode($this->_cPtr);
	}

	/**
	 * Controls line-ending conversion when downloading in ASCII transfer mode. The default is 0. ValueDownloaded text 0Leaves line endings as received. 1Converts line endings to CRLF. 2Converts line endings to LF. 3Converts line endings to CR.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CrlfMode
	 * @param int $newVal
	 */
	function put_CrlfMode($newVal) {
		CkFtp2_put_CrlfMode($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of bytes received by the current or most recently completed FTP download as a 32-bit unsigned integer. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent upload does not clear the retained download count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesReceived
	 * @return int
	 */
	function get_CurBytesReceived() {
		return CkFtp2_get_CurBytesReceived($this->_cPtr);
	}

	/**
	 * Returns the number of bytes received by the current or most recently completed FTP download as a 64-bit integer. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent upload does not clear the retained download count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesReceived64
	 * @return int
	 */
	function get_CurBytesReceived64() {
		return CkFtp2_get_CurBytesReceived64($this->_cPtr);
	}

	/**
	 * Returns the number of bytes received by the current or most recently completed FTP download as a decimal string. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent upload does not clear the retained download count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesReceivedStr
	 * @param CkString $str
	 */
	function get_CurBytesReceivedStr($str) {
		CkFtp2_get_CurBytesReceivedStr($this->_cPtr,$str);
	}

	/**
	 * Returns the number of bytes received by the current or most recently completed FTP download as a decimal string. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent upload does not clear the retained download count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesReceivedStr
	 * @return ?string
	 */
	function curBytesReceivedStr() {
		return CkFtp2_curBytesReceivedStr($this->_cPtr);
	}

	/**
	 * Returns the number of bytes sent by the current or most recently completed FTP upload as a 32-bit unsigned integer. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent download does not clear the retained upload count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesSent
	 * @return int
	 */
	function get_CurBytesSent() {
		return CkFtp2_get_CurBytesSent($this->_cPtr);
	}

	/**
	 * Returns the number of bytes sent by the current or most recently completed FTP upload as a 64-bit integer. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent download does not clear the retained upload count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesSent64
	 * @return int
	 */
	function get_CurBytesSent64() {
		return CkFtp2_get_CurBytesSent64($this->_cPtr);
	}

	/**
	 * Returns the number of bytes sent by the current or most recently completed FTP upload as a decimal string. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent download does not clear the retained upload count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesSentStr
	 * @param CkString $str
	 */
	function get_CurBytesSentStr($str) {
		CkFtp2_get_CurBytesSentStr($this->_cPtr,$str);
	}

	/**
	 * Returns the number of bytes sent by the current or most recently completed FTP upload as a decimal string. The value is updated while the transfer is in progress and may be read from another thread to display progress. A subsequent download does not clear the retained upload count.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CurBytesSentStr
	 * @return ?string
	 */
	function curBytesSentStr() {
		return CkFtp2_curBytesSentStr($this->_cPtr);
	}

	/**
	 * Specifies the protection level for FTP data connections. The value is compared case-insensitively. The default is control. ValueBehavior controlData connections use the same protection state as the control connection. If the control connection is unencrypted, no PBSZ or PROT commands are sent. clearData connections are requested as unencrypted. If the control connection is also unencrypted, no PBSZ or PROT commands are sent. If the control connection uses TLS, Chilkat sends PBSZ 0 followed by PROT C. If the server rejects PROT C but accepts PROT P, Chilkat falls back to private data protection. privateData connections are protected with TLS. For an encrypted control connection, Chilkat sends PBSZ 0 followed by PROT P.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DataProtection
	 * @param CkString $str
	 */
	function get_DataProtection($str) {
		CkFtp2_get_DataProtection($this->_cPtr,$str);
	}

	/**
	 * Specifies the protection level for FTP data connections. The value is compared case-insensitively. The default is control. ValueBehavior controlData connections use the same protection state as the control connection. If the control connection is unencrypted, no PBSZ or PROT commands are sent. clearData connections are requested as unencrypted. If the control connection is also unencrypted, no PBSZ or PROT commands are sent. If the control connection uses TLS, Chilkat sends PBSZ 0 followed by PROT C. If the server rejects PROT C but accepts PROT P, Chilkat falls back to private data protection. privateData connections are protected with TLS. For an encrypted control connection, Chilkat sends PBSZ 0 followed by PROT P.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DataProtection
	 * @return ?string
	 */
	function dataProtection() {
		return CkFtp2_dataProtection($this->_cPtr);
	}

	/**
	 * Specifies the protection level for FTP data connections. The value is compared case-insensitively. The default is control. ValueBehavior controlData connections use the same protection state as the control connection. If the control connection is unencrypted, no PBSZ or PROT commands are sent. clearData connections are requested as unencrypted. If the control connection is also unencrypted, no PBSZ or PROT commands are sent. If the control connection uses TLS, Chilkat sends PBSZ 0 followed by PROT C. If the server rejects PROT C but accepts PROT P, Chilkat falls back to private data protection. privateData connections are protected with TLS. For an encrypted control connection, Chilkat sends PBSZ 0 followed by PROT P.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DataProtection
	 * @param ?string $newVal
	 */
	function put_DataProtection($newVal) {
		CkFtp2_put_DataProtection($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkFtp2_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkFtp2_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkFtp2_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the character encoding used only to decode directory-listing and related server-response bytes. It applies to responses from LIST, NLST, and MLSD. It does not affect commands, remote paths, filenames, usernames, passwords, or other text sent to the server. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. ANSI means the platform's system-default single-byte encoding or default system locale; on Windows, this is the active code page (ACP).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DirListingCharset
	 * @param CkString $str
	 */
	function get_DirListingCharset($str) {
		CkFtp2_get_DirListingCharset($this->_cPtr,$str);
	}

	/**
	 * Specifies the character encoding used only to decode directory-listing and related server-response bytes. It applies to responses from LIST, NLST, and MLSD. It does not affect commands, remote paths, filenames, usernames, passwords, or other text sent to the server. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. ANSI means the platform's system-default single-byte encoding or default system locale; on Windows, this is the active code page (ACP).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DirListingCharset
	 * @return ?string
	 */
	function dirListingCharset() {
		return CkFtp2_dirListingCharset($this->_cPtr);
	}

	/**
	 * Specifies the character encoding used only to decode directory-listing and related server-response bytes. It applies to responses from LIST, NLST, and MLSD. It does not affect commands, remote paths, filenames, usernames, passwords, or other text sent to the server. The default is ANSI. Charset names and the keyword ANSI are case-insensitive. ANSI means the platform's system-default single-byte encoding or default system locale; on Windows, this is the active code page (ACP).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DirListingCharset
	 * @param ?string $newVal
	 */
	function put_DirListingCharset($newVal) {
		CkFtp2_put_DirListingCharset($this->_cPtr,$newVal);
	}

	/**
	 * Returns the current average download rate in bytes per second. The value is updated during synchronous and asynchronous downloads and describes the download in progress or the most recently completed download. A subsequent upload does not clear the retained download rate.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DownloadTransferRate
	 * @return int
	 */
	function get_DownloadTransferRate() {
		return CkFtp2_get_DownloadTransferRate($this->_cPtr);
	}

	/**
	 * Controls automatic resolution of credential values from secure operating-system storage. The default is _FALSE_. When enabled, supported password properties may contain a secret specification beginning with !! instead of the literal password. !![appName|]service[|domain]|username Secret specifications are supported by Password, HttpProxyPassword, ProxyPassword, and SocksPassword. Chilkat resolves them through Windows Credential Manager on Windows or Apple Keychain on macOS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#EnableSecrets
	 * @return bool
	 */
	function get_EnableSecrets() {
		return CkFtp2_get_EnableSecrets($this->_cPtr);
	}

	/**
	 * Controls automatic resolution of credential values from secure operating-system storage. The default is _FALSE_. When enabled, supported password properties may contain a secret specification beginning with !! instead of the literal password. !![appName|]service[|domain]|username Secret specifications are supported by Password, HttpProxyPassword, ProxyPassword, and SocksPassword. Chilkat resolves them through Windows Credential Manager on Windows or Apple Keychain on macOS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#EnableSecrets
	 * @param bool $newVal
	 */
	function put_EnableSecrets($newVal) {
		CkFtp2_put_EnableSecrets($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the IPv4 address advertised to the server for active-mode data connections. This property affects both PORT and EPRT. Supply a numeric IPv4 address, the keyword control, or an IPv4 address prefixed with bind-. IPv6 values are not accepted. Keywords and prefixes are case-insensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ForcePortIpAddress
	 * @param CkString $str
	 */
	function get_ForcePortIpAddress($str) {
		CkFtp2_get_ForcePortIpAddress($this->_cPtr,$str);
	}

	/**
	 * Specifies the IPv4 address advertised to the server for active-mode data connections. This property affects both PORT and EPRT. Supply a numeric IPv4 address, the keyword control, or an IPv4 address prefixed with bind-. IPv6 values are not accepted. Keywords and prefixes are case-insensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ForcePortIpAddress
	 * @return ?string
	 */
	function forcePortIpAddress() {
		return CkFtp2_forcePortIpAddress($this->_cPtr);
	}

	/**
	 * Specifies the IPv4 address advertised to the server for active-mode data connections. This property affects both PORT and EPRT. Supply a numeric IPv4 address, the keyword control, or an IPv4 address prefixed with bind-. IPv6 values are not accepted. Keywords and prefixes are case-insensitive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ForcePortIpAddress
	 * @param ?string $newVal
	 */
	function put_ForcePortIpAddress($newVal) {
		CkFtp2_put_ForcePortIpAddress($this->_cPtr,$newVal);
	}

	/**
	 * Contains the initial FTP server greeting received for the most recent connection immediately after the TCP or implicit-TLS connection was established. It normally begins with reply code 220 and may include server identification or policy information. The value remains available after Disconnect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Greeting
	 * @param CkString $str
	 */
	function get_Greeting($str) {
		CkFtp2_get_Greeting($this->_cPtr,$str);
	}

	/**
	 * Contains the initial FTP server greeting received for the most recent connection immediately after the TCP or implicit-TLS connection was established. It normally begins with reply code 220 and may include server identification or policy information. The value remains available after Disconnect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Greeting
	 * @return ?string
	 */
	function greeting() {
		return CkFtp2_greeting($this->_cPtr);
	}

	/**
	 * Indicates whether the FTP server advertised support for MODE Z, which compresses FTP data streams using the DEFLATE algorithm. This property becomes meaningful after Chilkat has processed a successful FEAT response, either automatically because AutoFeat is _TRUE_ or through an explicit call to Feat. Before capability discovery, a value of _FALSE_ does not establish that the server lacks MODE Z support.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HasModeZ
	 * @return bool
	 */
	function get_HasModeZ() {
		return CkFtp2_get_HasModeZ($this->_cPtr);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck callbacks during methods that support periodic cancellation checks. The default is 0, which disables these callbacks. Smaller values can improve cancellation responsiveness but increase callback overhead. Methods that complete quickly may not generate a callback.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkFtp2_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck callbacks during methods that support periodic cancellation checks. The default is 0, which disables these callbacks. Smaller values can improve cancellation responsiveness but increase callback overhead. Methods that complete quickly may not generate a callback.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkFtp2_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the FTP server hostname or numeric IPv4/IPv6 address. Do not include a URL scheme, path, or port; configure the port separately with Port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Hostname
	 * @param CkString $str
	 */
	function get_Hostname($str) {
		CkFtp2_get_Hostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the FTP server hostname or numeric IPv4/IPv6 address. Do not include a URL scheme, path, or port; configure the port separately with Port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Hostname
	 * @return ?string
	 */
	function hostname() {
		return CkFtp2_hostname($this->_cPtr);
	}

	/**
	 * Specifies the FTP server hostname or numeric IPv4/IPv6 address. Do not include a URL scheme, path, or port; configure the port separately with Port.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Hostname
	 * @param ?string $newVal
	 */
	function put_Hostname($newVal) {
		CkFtp2_put_Hostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the authentication method used with an HTTP proxy. Supported values are Basic and NTLM. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyAuthMethod
	 * @param CkString $str
	 */
	function get_HttpProxyAuthMethod($str) {
		CkFtp2_get_HttpProxyAuthMethod($this->_cPtr,$str);
	}

	/**
	 * Specifies the authentication method used with an HTTP proxy. Supported values are Basic and NTLM. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyAuthMethod
	 * @return ?string
	 */
	function httpProxyAuthMethod() {
		return CkFtp2_httpProxyAuthMethod($this->_cPtr);
	}

	/**
	 * Specifies the authentication method used with an HTTP proxy. Supported values are Basic and NTLM. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyAuthMethod
	 * @param ?string $newVal
	 */
	function put_HttpProxyAuthMethod($newVal) {
		CkFtp2_put_HttpProxyAuthMethod($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the Windows domain used for NTLM authentication to the HTTP proxy. It is ignored for Basic authentication and may be left empty when the username already includes the required domain information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyDomain
	 * @param CkString $str
	 */
	function get_HttpProxyDomain($str) {
		CkFtp2_get_HttpProxyDomain($this->_cPtr,$str);
	}

	/**
	 * Specifies the Windows domain used for NTLM authentication to the HTTP proxy. It is ignored for Basic authentication and may be left empty when the username already includes the required domain information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyDomain
	 * @return ?string
	 */
	function httpProxyDomain() {
		return CkFtp2_httpProxyDomain($this->_cPtr);
	}

	/**
	 * Specifies the Windows domain used for NTLM authentication to the HTTP proxy. It is ignored for Basic authentication and may be left empty when the username already includes the required domain information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyDomain
	 * @param ?string $newVal
	 */
	function put_HttpProxyDomain($newVal) {
		CkFtp2_put_HttpProxyDomain($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy used to establish the FTP control connection. Leave empty to connect directly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyHostname
	 * @param CkString $str
	 */
	function get_HttpProxyHostname($str) {
		CkFtp2_get_HttpProxyHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy used to establish the FTP control connection. Leave empty to connect directly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyHostname
	 * @return ?string
	 */
	function httpProxyHostname() {
		return CkFtp2_httpProxyHostname($this->_cPtr);
	}

	/**
	 * Specifies the hostname or numeric IP address of an HTTP proxy used to establish the FTP control connection. Leave empty to connect directly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyHostname
	 * @param ?string $newVal
	 */
	function put_HttpProxyHostname($newVal) {
		CkFtp2_put_HttpProxyHostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used to authenticate to the HTTP proxy. It is used together with HttpProxyUsername and HttpProxyAuthMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyPassword
	 * @param CkString $str
	 */
	function get_HttpProxyPassword($str) {
		CkFtp2_get_HttpProxyPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used to authenticate to the HTTP proxy. It is used together with HttpProxyUsername and HttpProxyAuthMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyPassword
	 * @return ?string
	 */
	function httpProxyPassword() {
		return CkFtp2_httpProxyPassword($this->_cPtr);
	}

	/**
	 * Specifies the password used to authenticate to the HTTP proxy. It is used together with HttpProxyUsername and HttpProxyAuthMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyPassword
	 * @param ?string $newVal
	 */
	function put_HttpProxyPassword($newVal) {
		CkFtp2_put_HttpProxyPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the TCP port of the HTTP proxy. Common values include 8080 and 3128, but the correct value is determined by the proxy configuration.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyPort
	 * @return int
	 */
	function get_HttpProxyPort() {
		return CkFtp2_get_HttpProxyPort($this->_cPtr);
	}

	/**
	 * Specifies the TCP port of the HTTP proxy. Common values include 8080 and 3128, but the correct value is determined by the proxy configuration.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyPort
	 * @param int $newVal
	 */
	function put_HttpProxyPort($newVal) {
		CkFtp2_put_HttpProxyPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the username used to authenticate to the HTTP proxy. Leave empty when the proxy allows unauthenticated connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyUsername
	 * @param CkString $str
	 */
	function get_HttpProxyUsername($str) {
		CkFtp2_get_HttpProxyUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the username used to authenticate to the HTTP proxy. Leave empty when the proxy allows unauthenticated connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyUsername
	 * @return ?string
	 */
	function httpProxyUsername() {
		return CkFtp2_httpProxyUsername($this->_cPtr);
	}

	/**
	 * Specifies the username used to authenticate to the HTTP proxy. Leave empty when the proxy allows unauthenticated connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#HttpProxyUsername
	 * @param ?string $newVal
	 */
	function put_HttpProxyUsername($newVal) {
		CkFtp2_put_HttpProxyUsername($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum period, in milliseconds, during which no control-channel response or forward upload progress is observed. The default is 60000. A value of 0 or any negative value waits indefinitely. During an upload, the same limit applies when the operating-system send buffer remains full because the server is not consuming data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#IdleTimeoutMs
	 * @return int
	 */
	function get_IdleTimeoutMs() {
		return CkFtp2_get_IdleTimeoutMs($this->_cPtr);
	}

	/**
	 * Specifies the maximum period, in milliseconds, during which no control-channel response or forward upload progress is observed. The default is 60000. A value of 0 or any negative value waits indefinitely. During an upload, the same limit applies when the operating-system send buffer remains full because the server is not consuming data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#IdleTimeoutMs
	 * @param int $newVal
	 */
	function put_IdleTimeoutMs($newVal) {
		CkFtp2_put_IdleTimeoutMs($this->_cPtr,$newVal);
	}

	/**
	 * Deprecated. Indicates whether the object appears to be connected and authenticated. Reading this property may send NOOP to test the session. Use CheckConnection instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#IsConnected
	 * @return bool
	 */
	function get_IsConnected() {
		return CkFtp2_get_IsConnected($this->_cPtr);
	}

	/**
	 * Controls collection of an in-memory FTP protocol log. When enabled, commands and server replies are available in SessionLog. Use ClearSessionLog to discard previously collected text.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#KeepSessionLog
	 * @return bool
	 */
	function get_KeepSessionLog() {
		return CkFtp2_get_KeepSessionLog($this->_cPtr);
	}

	/**
	 * Controls collection of an in-memory FTP protocol log. When enabled, commands and server replies are available in SessionLog. Use ClearSessionLog to discard previously collected text.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#KeepSessionLog
	 * @param bool $newVal
	 */
	function put_KeepSessionLog($newVal) {
		CkFtp2_put_KeepSessionLog($this->_cPtr,$newVal);
	}

	/**
	 * Controls an additional keepalive measure for very long uploads and downloads. The default is _FALSE_. When enabled, Chilkat sends NOOP on the FTP control channel once per minute while a long-running file transfer is in progress. This helps prevent the otherwise idle control connection from being closed by the server, a firewall, or a NAT device while file data continues on the separate data connection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LargeFileMeasures
	 * @return bool
	 */
	function get_LargeFileMeasures() {
		return CkFtp2_get_LargeFileMeasures($this->_cPtr);
	}

	/**
	 * Controls an additional keepalive measure for very long uploads and downloads. The default is _FALSE_. When enabled, Chilkat sends NOOP on the FTP control channel once per minute while a long-running file transfer is in progress. This helps prevent the otherwise idle control connection from being closed by the server, a firewall, or a NAT device while file data continues on the separate data connection.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LargeFileMeasures
	 * @param bool $newVal
	 */
	function put_LargeFileMeasures($newVal) {
		CkFtp2_put_LargeFileMeasures($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkFtp2_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkFtp2_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkFtp2_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkFtp2_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkFtp2_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkFtp2_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkFtp2_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkFtp2_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Contains the most recent FTP control-channel reply, including its three-digit status code and message. Examples include 250 Directory successfully changed. and 550 Failed to change directory. The value is updated by successful as well as failed commands. After Disconnect, it normally contains the server's reply to QUIT, such as 221 Goodbye.. Some replies are multiline; use this property together with the method return value and LastErrorText when diagnosing a failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastReply
	 * @param CkString $str
	 */
	function get_LastReply($str) {
		CkFtp2_get_LastReply($this->_cPtr,$str);
	}

	/**
	 * Contains the most recent FTP control-channel reply, including its three-digit status code and message. Examples include 250 Directory successfully changed. and 550 Failed to change directory. The value is updated by successful as well as failed commands. After Disconnect, it normally contains the server's reply to QUIT, such as 221 Goodbye.. Some replies are multiline; use this property together with the method return value and LastErrorText when diagnosing a failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LastReply
	 * @return ?string
	 */
	function lastReply() {
		return CkFtp2_lastReply($this->_cPtr);
	}

	/**
	 * Specifies an option string appended literally to the legacy LIST command, such as -a. For example, setting this property to -a sends LIST -a. Chilkat does not interpret the option locally. It has no effect when AllowMlsd causes MLSD to be used or when PreferNlst causes NLST to be used. LIST options and output formats are not standardized by FTP. Many UNIX-like servers pass the option to an underlying ls implementation, while other servers may reject or interpret it differently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListOption
	 * @param CkString $str
	 */
	function get_ListOption($str) {
		CkFtp2_get_ListOption($this->_cPtr,$str);
	}

	/**
	 * Specifies an option string appended literally to the legacy LIST command, such as -a. For example, setting this property to -a sends LIST -a. Chilkat does not interpret the option locally. It has no effect when AllowMlsd causes MLSD to be used or when PreferNlst causes NLST to be used. LIST options and output formats are not standardized by FTP. Many UNIX-like servers pass the option to an underlying ls implementation, while other servers may reject or interpret it differently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListOption
	 * @return ?string
	 */
	function listOption() {
		return CkFtp2_listOption($this->_cPtr);
	}

	/**
	 * Specifies an option string appended literally to the legacy LIST command, such as -a. For example, setting this property to -a sends LIST -a. Chilkat does not interpret the option locally. It has no effect when AllowMlsd causes MLSD to be used or when PreferNlst causes NLST to be used. LIST options and output formats are not standardized by FTP. Many UNIX-like servers pass the option to an underlying ls implementation, while other servers may reject or interpret it differently.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListOption
	 * @param ?string $newVal
	 */
	function put_ListOption($newVal) {
		CkFtp2_put_ListOption($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the case-insensitive wildcard pattern applied to the cached listing of the current remote directory. The default is *. It affects methods such as GetDirCount, GetFilename, GetIsDirectory, GetSize, and the listing timestamp methods. Chilkat retrieves the full directory listing and applies this pattern locally; the pattern is not appended to LIST, NLST, or MLSD. For example, *.txt also matches a name such as UPPER.TXT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListPattern
	 * @param CkString $str
	 */
	function get_ListPattern($str) {
		CkFtp2_get_ListPattern($this->_cPtr,$str);
	}

	/**
	 * Specifies the case-insensitive wildcard pattern applied to the cached listing of the current remote directory. The default is *. It affects methods such as GetDirCount, GetFilename, GetIsDirectory, GetSize, and the listing timestamp methods. Chilkat retrieves the full directory listing and applies this pattern locally; the pattern is not appended to LIST, NLST, or MLSD. For example, *.txt also matches a name such as UPPER.TXT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListPattern
	 * @return ?string
	 */
	function listPattern() {
		return CkFtp2_listPattern($this->_cPtr);
	}

	/**
	 * Specifies the case-insensitive wildcard pattern applied to the cached listing of the current remote directory. The default is *. It affects methods such as GetDirCount, GetFilename, GetIsDirectory, GetSize, and the listing timestamp methods. Chilkat retrieves the full directory listing and applies this pattern locally; the pattern is not appended to LIST, NLST, or MLSD. For example, *.txt also matches a name such as UPPER.TXT.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ListPattern
	 * @param ?string $newVal
	 */
	function put_ListPattern($newVal) {
		CkFtp2_put_ListPattern($this->_cPtr,$newVal);
	}

	/**
	 * Indicates whether FTP authentication succeeded during the most recent login attempt. A TCP connection may be established even when this property is _FALSE_; for example, rejected credentials leave ConnectVerified _TRUE_ and this property _FALSE_. This is a verification result, not a current-socket-state property. It remains _TRUE_ after Disconnect if the preceding session was successfully authenticated.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LoginVerified
	 * @return bool
	 */
	function get_LoginVerified() {
		return CkFtp2_get_LoginVerified($this->_cPtr);
	}

	/**
	 * Indicates whether the most recent download method received some, but not all, of the remote file. It is _FALSE_ when no data was received or when the complete file was received. This property helps distinguish a mid-transfer failure from a failure that occurred before any file data arrived.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PartialTransfer
	 * @return bool
	 */
	function get_PartialTransfer() {
		return CkFtp2_get_PartialTransfer($this->_cPtr);
	}

	/**
	 * Selects the FTP data-connection mode. The default is _TRUE_ for passive mode. Set to _FALSE_ for active mode. ModeConnection direction PassiveThe server listens and the client connects to the server for each data transfer. ActiveThe client listens and the server connects back to the client.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Passive
	 * @return bool
	 */
	function get_Passive() {
		return CkFtp2_get_Passive($this->_cPtr);
	}

	/**
	 * Selects the FTP data-connection mode. The default is _TRUE_ for passive mode. Set to _FALSE_ for active mode. ModeConnection direction PassiveThe server listens and the client connects to the server for each data transfer. ActiveThe client listens and the server connects back to the client.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Passive
	 * @param bool $newVal
	 */
	function put_Passive($newVal) {
		CkFtp2_put_Passive($this->_cPtr,$newVal);
	}

	/**
	 * Controls how the address returned by PASV is handled. The default is _TRUE_. When enabled, Chilkat ignores the IP address embedded in the server's PASV reply and connects to the control connection's server address using only the returned port. This avoids private or otherwise unreachable addresses returned by FTP servers behind NAT, especially when TLS prevents a network device from rewriting the reply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PassiveUseHostAddr
	 * @return bool
	 */
	function get_PassiveUseHostAddr() {
		return CkFtp2_get_PassiveUseHostAddr($this->_cPtr);
	}

	/**
	 * Controls how the address returned by PASV is handled. The default is _TRUE_. When enabled, Chilkat ignores the IP address embedded in the server's PASV reply and connects to the control connection's server address using only the returned port. This avoids private or otherwise unreachable addresses returned by FTP servers behind NAT, especially when TLS prevents a network device from rewriting the reply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PassiveUseHostAddr
	 * @param bool $newVal
	 */
	function put_PassiveUseHostAddr($newVal) {
		CkFtp2_put_PassiveUseHostAddr($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used for FTP authentication. For anonymous FTP, this is commonly an email address or arbitrary identifying string. Applications that already store the password in a SecureString should use SetSecurePassword instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Password
	 * @param CkString $str
	 */
	function get_Password($str) {
		CkFtp2_get_Password($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used for FTP authentication. For anonymous FTP, this is commonly an email address or arbitrary identifying string. Applications that already store the password in a SecureString should use SetSecurePassword instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Password
	 * @return ?string
	 */
	function password() {
		return CkFtp2_password($this->_cPtr);
	}

	/**
	 * Specifies the password used for FTP authentication. For anonymous FTP, this is commonly an email address or arbitrary identifying string. Applications that already store the password in a SecureString should use SetSecurePassword instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Password
	 * @param ?string $newVal
	 */
	function put_Password($newVal) {
		CkFtp2_put_Password($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the integer value that represents 100 percent in PercentDone callbacks. The default is 100. For example, a scale of 1000 allows one-tenth-percent resolution, where callback value 453 represents 45.3 percent. The value is limited to the range 10 through 100000. This property is relevant only in environments that support event callbacks and only for operations whose total work can be measured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PercentDoneScale
	 * @return int
	 */
	function get_PercentDoneScale() {
		return CkFtp2_get_PercentDoneScale($this->_cPtr);
	}

	/**
	 * Specifies the integer value that represents 100 percent in PercentDone callbacks. The default is 100. For example, a scale of 1000 allows one-tenth-percent resolution, where callback value 453 represents 45.3 percent. The value is limited to the range 10 through 100000. This property is relevant only in environments that support event callbacks and only for operations whose total work can be measured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PercentDoneScale
	 * @param int $newVal
	 */
	function put_PercentDoneScale($newVal) {
		CkFtp2_put_PercentDoneScale($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the FTP server port. Port 21 is conventionally used for plain FTP or explicit FTPS, and port 990 is conventionally used for implicit FTPS. When AutoFix is _TRUE_, only these two port numbers cause automatic changes to Ssl, AuthSsl, and AuthTls. A custom port leaves all three TLS-mode properties unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Port
	 * @return int
	 */
	function get_Port() {
		return CkFtp2_get_Port($this->_cPtr);
	}

	/**
	 * Specifies the FTP server port. Port 21 is conventionally used for plain FTP or explicit FTPS, and port 990 is conventionally used for implicit FTPS. When AutoFix is _TRUE_, only these two port numbers cause automatic changes to Ssl, AuthSsl, and AuthTls. A custom port leaves all three TLS-mode properties unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Port
	 * @param int $newVal
	 */
	function put_Port($newVal) {
		CkFtp2_put_Port($this->_cPtr,$newVal);
	}

	/**
	 * Controls the preferred address family when DNS resolution returns both IPv4 and IPv6 addresses. The default is _FALSE_, which prefers IPv4. Set it to _TRUE_ to prefer IPv6.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PreferIpv6
	 * @return bool
	 */
	function get_PreferIpv6() {
		return CkFtp2_get_PreferIpv6($this->_cPtr);
	}

	/**
	 * Controls the preferred address family when DNS resolution returns both IPv4 and IPv6 addresses. The default is _FALSE_, which prefers IPv4. Set it to _TRUE_ to prefer IPv6.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PreferIpv6
	 * @param bool $newVal
	 */
	function put_PreferIpv6($newVal) {
		CkFtp2_put_PreferIpv6($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether NLST is preferred over LIST when a legacy directory listing is required. The default is _FALSE_. This property is consulted only when AllowMlsd does not result in MLSD being used. NLST generally returns names only. Do not rely on indexed sizes, timestamps, ownership, group, or permission information after an NLST listing. Because NLST does not reliably identify entry types, Chilkat may send additional commands, such as directory-change tests, to determine which names are directories.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PreferNlst
	 * @return bool
	 */
	function get_PreferNlst() {
		return CkFtp2_get_PreferNlst($this->_cPtr);
	}

	/**
	 * Controls whether NLST is preferred over LIST when a legacy directory listing is required. The default is _FALSE_. This property is consulted only when AllowMlsd does not result in MLSD being used. NLST generally returns names only. Do not rely on indexed sizes, timestamps, ownership, group, or permission information after an NLST listing. Because NLST does not reliably identify entry types, Chilkat may send additional commands, such as directory-change tests, to determine which names are directories.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PreferNlst
	 * @param bool $newVal
	 */
	function put_PreferNlst($newVal) {
		CkFtp2_put_PreferNlst($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the expected size, in bytes, of the next file downloaded by GetFile when the size is not otherwise known. Chilkat uses the value to calculate percentage progress. This is a one-shot setting. It is consumed by the next applicable download and automatically reset to 0. Set it immediately before the download. When a nonzero value is supplied, Chilkat does not need the additional size query controlled by AutoGetSizeForProgress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProgressMonSize
	 * @return int
	 */
	function get_ProgressMonSize() {
		return CkFtp2_get_ProgressMonSize($this->_cPtr);
	}

	/**
	 * Specifies the expected size, in bytes, of the next file downloaded by GetFile when the size is not otherwise known. Chilkat uses the value to calculate percentage progress. This is a one-shot setting. It is consumed by the next applicable download and automatically reset to 0. Set it immediately before the download. When a nonzero value is supplied, Chilkat does not need the additional size query controlled by AutoGetSizeForProgress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProgressMonSize
	 * @param int $newVal
	 */
	function put_ProgressMonSize($newVal) {
		CkFtp2_put_ProgressMonSize($this->_cPtr,$newVal);
	}

	/**
	 * The 64-bit form of ProgressMonSize. Specifies the expected size, in bytes, of the next download when the value may exceed the 32-bit integer range. It is consumed by the next applicable download and automatically reset to 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProgressMonSize64
	 * @return int
	 */
	function get_ProgressMonSize64() {
		return CkFtp2_get_ProgressMonSize64($this->_cPtr);
	}

	/**
	 * The 64-bit form of ProgressMonSize. Specifies the expected size, in bytes, of the next download when the value may exceed the 32-bit integer range. It is consumed by the next applicable download and automatically reset to 0.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProgressMonSize64
	 * @param int $newVal
	 */
	function put_ProgressMonSize64($newVal) {
		CkFtp2_put_ProgressMonSize64($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the hostname or IP address of a traditional FTP application proxy or firewall. This is distinct from an HTTP CONNECT proxy and from a SOCKS proxy. The login sequence is selected by ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyHostname
	 * @param CkString $str
	 */
	function get_ProxyHostname($str) {
		CkFtp2_get_ProxyHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the hostname or IP address of a traditional FTP application proxy or firewall. This is distinct from an HTTP CONNECT proxy and from a SOCKS proxy. The login sequence is selected by ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyHostname
	 * @return ?string
	 */
	function proxyHostname() {
		return CkFtp2_proxyHostname($this->_cPtr);
	}

	/**
	 * Specifies the hostname or IP address of a traditional FTP application proxy or firewall. This is distinct from an HTTP CONNECT proxy and from a SOCKS proxy. The login sequence is selected by ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyHostname
	 * @param ?string $newVal
	 */
	function put_ProxyHostname($newVal) {
		CkFtp2_put_ProxyHostname($this->_cPtr,$newVal);
	}

	/**
	 * Selects the login sequence required by a traditional FTP proxy or firewall. The default is 0, meaning no FTP proxy. Configure ProxyHostname, ProxyPort, and the required proxy credentials before connecting. ValueSequence sent 1 — SITEUSER ProxyUsername PASS ProxyPassword SITE Hostname USER Username PASS Password 2 — USER user@siteUSER Username@Hostname:Port PASS Password 3 — USER with proxy loginUSER ProxyUsername PASS ProxyPassword USER Username@Hostname:Port PASS Password 4 — USER/PASS/ACCTUSER Username@Hostname:Port ProxyUsername PASS Password ACCT ProxyPassword 5 — OPENUSER ProxyUsername PASS ProxyPassword OPEN Hostname USER Username PASS Password 6 — firewall IDUSER ProxyUsername@Hostname USER Username PASS Password 7USER ProxyUsername USER ProxyPassword SITE Hostname:Port USER Username PASS Password 8USER Username@ProxyUsername@Hostname PASS Password@ProxyPassword 9ProxyUsername ProxyPassword Username Password
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyMethod
	 * @return int
	 */
	function get_ProxyMethod() {
		return CkFtp2_get_ProxyMethod($this->_cPtr);
	}

	/**
	 * Selects the login sequence required by a traditional FTP proxy or firewall. The default is 0, meaning no FTP proxy. Configure ProxyHostname, ProxyPort, and the required proxy credentials before connecting. ValueSequence sent 1 — SITEUSER ProxyUsername PASS ProxyPassword SITE Hostname USER Username PASS Password 2 — USER user@siteUSER Username@Hostname:Port PASS Password 3 — USER with proxy loginUSER ProxyUsername PASS ProxyPassword USER Username@Hostname:Port PASS Password 4 — USER/PASS/ACCTUSER Username@Hostname:Port ProxyUsername PASS Password ACCT ProxyPassword 5 — OPENUSER ProxyUsername PASS ProxyPassword OPEN Hostname USER Username PASS Password 6 — firewall IDUSER ProxyUsername@Hostname USER Username PASS Password 7USER ProxyUsername USER ProxyPassword SITE Hostname:Port USER Username PASS Password 8USER Username@ProxyUsername@Hostname PASS Password@ProxyPassword 9ProxyUsername ProxyPassword Username Password
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyMethod
	 * @param int $newVal
	 */
	function put_ProxyMethod($newVal) {
		CkFtp2_put_ProxyMethod($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyPassword
	 * @param CkString $str
	 */
	function get_ProxyPassword($str) {
		CkFtp2_get_ProxyPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyPassword
	 * @return ?string
	 */
	function proxyPassword() {
		return CkFtp2_proxyPassword($this->_cPtr);
	}

	/**
	 * Specifies the password used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyPassword
	 * @param ?string $newVal
	 */
	function put_ProxyPassword($newVal) {
		CkFtp2_put_ProxyPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the TCP port on which the traditional FTP proxy or firewall accepts connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyPort
	 * @return int
	 */
	function get_ProxyPort() {
		return CkFtp2_get_ProxyPort($this->_cPtr);
	}

	/**
	 * Specifies the TCP port on which the traditional FTP proxy or firewall accepts connections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyPort
	 * @param int $newVal
	 */
	function put_ProxyPort($newVal) {
		CkFtp2_put_ProxyPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the username used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyUsername
	 * @param CkString $str
	 */
	function get_ProxyUsername($str) {
		CkFtp2_get_ProxyUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the username used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyUsername
	 * @return ?string
	 */
	function proxyUsername() {
		return CkFtp2_proxyUsername($this->_cPtr);
	}

	/**
	 * Specifies the username used to authenticate to a traditional FTP proxy or firewall. Its placement in the login exchange depends on ProxyMethod.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ProxyUsername
	 * @param ?string $newVal
	 */
	function put_ProxyUsername($newVal) {
		CkFtp2_put_ProxyUsername($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum number of seconds that a data connection may remain without receiving another byte. The default is 60. A value of 0 or any negative value waits indefinitely. This is an inactivity timeout, not a limit on total transfer duration. Receiving any data, even a single byte, resets the timer and begins a new full timeout interval. A large download may therefore continue indefinitely as long as data keeps arriving within the configured interval.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ReadTimeout
	 * @return int
	 */
	function get_ReadTimeout() {
		return CkFtp2_get_ReadTimeout($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of seconds that a data connection may remain without receiving another byte. The default is 60. A value of 0 or any negative value waits indefinitely. This is an inactivity timeout, not a limit on total transfer duration. Receiving any data, even a single byte, resets the timer and begins a new full timeout interval. A large download may therefore continue indefinitely as long as data keeps arriving within the configured interval.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ReadTimeout
	 * @param int $newVal
	 */
	function put_ReadTimeout($newVal) {
		CkFtp2_put_ReadTimeout($this->_cPtr,$newVal);
	}

	/**
	 * Controls verification of the FTP server's TLS certificate chain. ValueBehavior _FALSE_ (default)A connection is not rejected solely because normal certificate-chain verification fails. _TRUE_The connection fails when the certificate is expired, is not yet valid, its signature is invalid, or its chain cannot be verified to a trusted root.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RequireSslCertVerify
	 * @return bool
	 */
	function get_RequireSslCertVerify() {
		return CkFtp2_get_RequireSslCertVerify($this->_cPtr);
	}

	/**
	 * Controls verification of the FTP server's TLS certificate chain. ValueBehavior _FALSE_ (default)A connection is not rejected solely because normal certificate-chain verification fails. _TRUE_The connection fails when the certificate is expired, is not yet valid, its signature is invalid, or its chain cannot be verified to a trusted root.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RequireSslCertVerify
	 * @param bool $newVal
	 */
	function put_RequireSslCertVerify($newVal) {
		CkFtp2_put_RequireSslCertVerify($this->_cPtr,$newVal);
	}

	/**
	 * Set to _TRUE_ to request that the next applicable upload or download resume from existing content instead of starting over. Unrelated FTP commands do not consume the setting. The next applicable transfer consumes it and automatically resets it to _FALSE_. For a resumed upload, Chilkat obtains the existing remote size, begins reading the local content at that offset, and appends the remaining bytes. For a resumed download, Chilkat uses the existing destination length as the REST offset and appends the remaining remote bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RestartNext
	 * @return bool
	 */
	function get_RestartNext() {
		return CkFtp2_get_RestartNext($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to request that the next applicable upload or download resume from existing content instead of starting over. Unrelated FTP commands do not consume the setting. The next applicable transfer consumes it and automatically resets it to _FALSE_. For a resumed upload, Chilkat obtains the existing remote size, begins reading the local content at that offset, and appends the remaining bytes. For a resumed download, Chilkat uses the existing destination length as the REST offset and appends the remaining remote bytes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RestartNext
	 * @param bool $newVal
	 */
	function put_RestartNext($newVal) {
		CkFtp2_put_RestartNext($this->_cPtr,$newVal);
	}

	/**
	 * Contains the in-memory FTP session log collected while KeepSessionLog is _TRUE_. The log includes protocol commands and server replies and is useful for troubleshooting. Passwords and other sensitive information may appear; handle the text accordingly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SessionLog
	 * @param CkString $str
	 */
	function get_SessionLog($str) {
		CkFtp2_get_SessionLog($this->_cPtr,$str);
	}

	/**
	 * Contains the in-memory FTP session log collected while KeepSessionLog is _TRUE_. The log includes protocol commands and server replies and is useful for troubleshooting. Passwords and other sensitive information may appear; handle the text accordingly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SessionLog
	 * @return ?string
	 */
	function sessionLog() {
		return CkFtp2_sessionLog($this->_cPtr);
	}

	/**
	 * Specifies the hostname or IP address of the SOCKS proxy. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksHostname
	 * @param CkString $str
	 */
	function get_SocksHostname($str) {
		CkFtp2_get_SocksHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the hostname or IP address of the SOCKS proxy. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksHostname
	 * @return ?string
	 */
	function socksHostname() {
		return CkFtp2_socksHostname($this->_cPtr);
	}

	/**
	 * Specifies the hostname or IP address of the SOCKS proxy. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksHostname
	 * @param ?string $newVal
	 */
	function put_SocksHostname($newVal) {
		CkFtp2_put_SocksHostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password for SOCKS5 username/password authentication. It is ignored for SOCKS4, which has no password-authentication field.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksPassword
	 * @param CkString $str
	 */
	function get_SocksPassword($str) {
		CkFtp2_get_SocksPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password for SOCKS5 username/password authentication. It is ignored for SOCKS4, which has no password-authentication field.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksPassword
	 * @return ?string
	 */
	function socksPassword() {
		return CkFtp2_socksPassword($this->_cPtr);
	}

	/**
	 * Specifies the password for SOCKS5 username/password authentication. It is ignored for SOCKS4, which has no password-authentication field.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksPassword
	 * @param ?string $newVal
	 */
	function put_SocksPassword($newVal) {
		CkFtp2_put_SocksPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the SOCKS proxy port. The default is 1080. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksPort
	 * @return int
	 */
	function get_SocksPort() {
		return CkFtp2_get_SocksPort($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS proxy port. The default is 1080. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksPort
	 * @param int $newVal
	 */
	function put_SocksPort($newVal) {
		CkFtp2_put_SocksPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the SOCKS user identifier. SOCKS5 may use it with password authentication; SOCKS4 sends it as the protocol user ID. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksUsername
	 * @param CkString $str
	 */
	function get_SocksUsername($str) {
		CkFtp2_get_SocksUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the SOCKS user identifier. SOCKS5 may use it with password authentication; SOCKS4 sends it as the protocol user ID. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksUsername
	 * @return ?string
	 */
	function socksUsername() {
		return CkFtp2_socksUsername($this->_cPtr);
	}

	/**
	 * Specifies the SOCKS user identifier. SOCKS5 may use it with password authentication; SOCKS4 sends it as the protocol user ID. It is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksUsername
	 * @param ?string $newVal
	 */
	function put_SocksUsername($newVal) {
		CkFtp2_put_SocksUsername($this->_cPtr,$newVal);
	}

	/**
	 * Selects SOCKS proxy usage. ValueBehavior 0Default. Connect directly without a SOCKS proxy. 4Connect through a SOCKS4 proxy. 5Connect through a SOCKS5 proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksVersion
	 * @return int
	 */
	function get_SocksVersion() {
		return CkFtp2_get_SocksVersion($this->_cPtr);
	}

	/**
	 * Selects SOCKS proxy usage. ValueBehavior 0Default. Connect directly without a SOCKS proxy. 4Connect through a SOCKS4 proxy. 5Connect through a SOCKS5 proxy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SocksVersion
	 * @param int $newVal
	 */
	function put_SocksVersion($newVal) {
		CkFtp2_put_SocksVersion($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the socket receive-buffer size. The default is 4194304 bytes. Normally this property should remain unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SoRcvBuf
	 * @return int
	 */
	function get_SoRcvBuf() {
		return CkFtp2_get_SoRcvBuf($this->_cPtr);
	}

	/**
	 * Specifies the socket receive-buffer size. The default is 4194304 bytes. Normally this property should remain unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SoRcvBuf
	 * @param int $newVal
	 */
	function put_SoRcvBuf($newVal) {
		CkFtp2_put_SoRcvBuf($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the socket send-buffer size. The default is 262144 bytes. Normally this property should remain unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SoSndBuf
	 * @return int
	 */
	function get_SoSndBuf() {
		return CkFtp2_get_SoSndBuf($this->_cPtr);
	}

	/**
	 * Specifies the socket send-buffer size. The default is 262144 bytes. Normally this property should remain unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SoSndBuf
	 * @param int $newVal
	 */
	function put_SoSndBuf($newVal) {
		CkFtp2_put_SoSndBuf($this->_cPtr,$newVal);
	}

	/**
	 * Set to _TRUE_ to use implicit FTP over TLS. TLS begins immediately after the TCP connection is established, before the FTP greeting is received. Port 990 is conventionally used for implicit FTPS. This property has priority over AuthTls and AuthSsl. If Ssl is _TRUE_, implicit TLS is used even when either explicit-TLS property is also _TRUE_. ConnectOnly performs the implicit TLS handshake but does not send USER or PASS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Ssl
	 * @return bool
	 */
	function get_Ssl() {
		return CkFtp2_get_Ssl($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to use implicit FTP over TLS. TLS begins immediately after the TCP connection is established, before the FTP greeting is received. Port 990 is conventionally used for implicit FTPS. This property has priority over AuthTls and AuthSsl. If Ssl is _TRUE_, implicit TLS is used even when either explicit-TLS property is also _TRUE_. ConnectOnly performs the implicit TLS handshake but does not send USER or PASS.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Ssl
	 * @param bool $newVal
	 */
	function put_Ssl($newVal) {
		CkFtp2_put_Ssl($this->_cPtr,$newVal);
	}

	/**
	 * Restricts the cipher suites and selected TLS security requirements offered for FTP TLS connections. Leave this property empty to allow all cipher suites implemented by the installed Chilkat version. To restrict negotiation, provide a comma-separated list in preference order, for example:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslAllowedCiphers
	 * @param CkString $str
	 */
	function get_SslAllowedCiphers($str) {
		CkFtp2_get_SslAllowedCiphers($this->_cPtr,$str);
	}

	/**
	 * Restricts the cipher suites and selected TLS security requirements offered for FTP TLS connections. Leave this property empty to allow all cipher suites implemented by the installed Chilkat version. To restrict negotiation, provide a comma-separated list in preference order, for example:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslAllowedCiphers
	 * @return ?string
	 */
	function sslAllowedCiphers() {
		return CkFtp2_sslAllowedCiphers($this->_cPtr);
	}

	/**
	 * Restricts the cipher suites and selected TLS security requirements offered for FTP TLS connections. Leave this property empty to allow all cipher suites implemented by the installed Chilkat version. To restrict negotiation, provide a comma-separated list in preference order, for example:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslAllowedCiphers
	 * @param ?string $newVal
	 */
	function put_SslAllowedCiphers($newVal) {
		CkFtp2_put_SslAllowedCiphers($this->_cPtr,$newVal);
	}

	/**
	 * Selects the TLS protocol version or minimum version allowed for secure FTP connections. The complete list of accepted values is:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslProtocol
	 * @param CkString $str
	 */
	function get_SslProtocol($str) {
		CkFtp2_get_SslProtocol($this->_cPtr,$str);
	}

	/**
	 * Selects the TLS protocol version or minimum version allowed for secure FTP connections. The complete list of accepted values is:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslProtocol
	 * @return ?string
	 */
	function sslProtocol() {
		return CkFtp2_sslProtocol($this->_cPtr);
	}

	/**
	 * Selects the TLS protocol version or minimum version allowed for secure FTP connections. The complete list of accepted values is:
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslProtocol
	 * @param ?string $newVal
	 */
	function put_SslProtocol($newVal) {
		CkFtp2_put_SslProtocol($this->_cPtr,$newVal);
	}

	/**
	 * Indicates whether the FTP server certificate chain was successfully verified for the current or most recent TLS connection. This property reports certificate-chain verification only. It does not report hostname matching or public-key pinning. If hostname matching or TlsPinSet pinning is required and the check fails, the TLS connection itself fails.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SslServerCertVerified
	 * @return bool
	 */
	function get_SslServerCertVerified() {
		return CkFtp2_get_SslServerCertVerified($this->_cPtr);
	}

	/**
	 * Controls whether download synchronization creates empty remote directories on the local filesystem. The default is _TRUE_. When _FALSE_, a local directory is created only when it is needed to contain a downloaded file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncCreateAllLocalDirs
	 * @return bool
	 */
	function get_SyncCreateAllLocalDirs() {
		return CkFtp2_get_SyncCreateAllLocalDirs($this->_cPtr);
	}

	/**
	 * Controls whether download synchronization creates empty remote directories on the local filesystem. The default is _TRUE_. When _FALSE_, a local directory is created only when it is needed to contain a downloaded file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncCreateAllLocalDirs
	 * @param bool $newVal
	 */
	function put_SyncCreateAllLocalDirs($newVal) {
		CkFtp2_put_SyncCreateAllLocalDirs($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that are eligible for synchronization and tree operations. For example: *.xml;*.txt;*.csv. When nonempty, a file must match at least one pattern. Matching is performed against the file's basename, not its relative path. Thus keep.txt can match a file in any traversed subdirectory, while a pattern such as subdir/*.txt does not match a nested relative path.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatch
	 * @param CkString $str
	 */
	function get_SyncMustMatch($str) {
		CkFtp2_get_SyncMustMatch($this->_cPtr,$str);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that are eligible for synchronization and tree operations. For example: *.xml;*.txt;*.csv. When nonempty, a file must match at least one pattern. Matching is performed against the file's basename, not its relative path. Thus keep.txt can match a file in any traversed subdirectory, while a pattern such as subdir/*.txt does not match a nested relative path.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatch
	 * @return ?string
	 */
	function syncMustMatch() {
		return CkFtp2_syncMustMatch($this->_cPtr);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that are eligible for synchronization and tree operations. For example: *.xml;*.txt;*.csv. When nonempty, a file must match at least one pattern. Matching is performed against the file's basename, not its relative path. Thus keep.txt can match a file in any traversed subdirectory, while a pattern such as subdir/*.txt does not match a nested relative path.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatch
	 * @param ?string $newVal
	 */
	function put_SyncMustMatch($newVal) {
		CkFtp2_put_SyncMustMatch($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods are allowed to enter. For example: src;data_*;reports. When nonempty, a directory must match at least one pattern before its contents are traversed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatchDir
	 * @param CkString $str
	 */
	function get_SyncMustMatchDir($str) {
		CkFtp2_get_SyncMustMatchDir($this->_cPtr,$str);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods are allowed to enter. For example: src;data_*;reports. When nonempty, a directory must match at least one pattern before its contents are traversed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatchDir
	 * @return ?string
	 */
	function syncMustMatchDir() {
		return CkFtp2_syncMustMatchDir($this->_cPtr);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods are allowed to enter. For example: src;data_*;reports. When nonempty, a directory must match at least one pattern before its contents are traversed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustMatchDir
	 * @param ?string $newVal
	 */
	function put_SyncMustMatchDir($newVal) {
		CkFtp2_put_SyncMustMatchDir($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that must be excluded from synchronization and tree operations. For example: *.tmp;*.bak;~*. Matching is performed against the file's basename, not its relative path. Exclusion is applied in addition to SyncMustMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatch
	 * @param CkString $str
	 */
	function get_SyncMustNotMatch($str) {
		CkFtp2_get_SyncMustNotMatch($this->_cPtr,$str);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that must be excluded from synchronization and tree operations. For example: *.tmp;*.bak;~*. Matching is performed against the file's basename, not its relative path. Exclusion is applied in addition to SyncMustMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatch
	 * @return ?string
	 */
	function syncMustNotMatch() {
		return CkFtp2_syncMustNotMatch($this->_cPtr);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for files that must be excluded from synchronization and tree operations. For example: *.tmp;*.bak;~*. Matching is performed against the file's basename, not its relative path. Exclusion is applied in addition to SyncMustMatch.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatch
	 * @param ?string $newVal
	 */
	function put_SyncMustNotMatch($newVal) {
		CkFtp2_put_SyncMustNotMatch($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods must not enter. For example: .git;temp;archive_*. Exclusion is applied in addition to SyncMustMatchDir.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatchDir
	 * @param CkString $str
	 */
	function get_SyncMustNotMatchDir($str) {
		CkFtp2_get_SyncMustNotMatchDir($this->_cPtr,$str);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods must not enter. For example: .git;temp;archive_*. Exclusion is applied in addition to SyncMustMatchDir.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatchDir
	 * @return ?string
	 */
	function syncMustNotMatchDir() {
		return CkFtp2_syncMustNotMatchDir($this->_cPtr);
	}

	/**
	 * Specifies a semicolon-separated list of case-insensitive wildcard patterns for directories that synchronization methods must not enter. For example: .git;temp;archive_*. Exclusion is applied in addition to SyncMustMatchDir.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncMustNotMatchDir
	 * @param ?string $newVal
	 */
	function put_SyncMustNotMatchDir($newVal) {
		CkFtp2_put_SyncMustNotMatchDir($this->_cPtr,$newVal);
	}

	/**
	 * Contains the preview produced by SyncRemoteTree2 when ARG4 is _TRUE_. Each CRLF-delimited line is an absolute local path that would be uploaded if preview mode were disabled. The value is replaced at the start of each preview operation. It is empty when no files qualify, when the preview fails, or when ARG2 is not a valid synchronization mode. A subsequent non-preview synchronization operation also clears it. Unrelated methods do not clear the most recent preview.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncPreview
	 * @param CkString $str
	 */
	function get_SyncPreview($str) {
		CkFtp2_get_SyncPreview($this->_cPtr,$str);
	}

	/**
	 * Contains the preview produced by SyncRemoteTree2 when ARG4 is _TRUE_. Each CRLF-delimited line is an absolute local path that would be uploaded if preview mode were disabled. The value is replaced at the start of each preview operation. It is empty when no files qualify, when the preview fails, or when ARG2 is not a valid synchronization mode. A subsequent non-preview synchronization operation also clears it. Unrelated methods do not clear the most recent preview.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncPreview
	 * @return ?string
	 */
	function syncPreview() {
		return CkFtp2_syncPreview($this->_cPtr);
	}

	/**
	 * Contains the cipher suite negotiated for the current or most recent successful TLS connection, such as TLS_AES_256_GCM_SHA384. It is empty before a TLS session has been established or after a failed attempt that produced no negotiated suite.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsCipherSuite
	 * @param CkString $str
	 */
	function get_TlsCipherSuite($str) {
		CkFtp2_get_TlsCipherSuite($this->_cPtr,$str);
	}

	/**
	 * Contains the cipher suite negotiated for the current or most recent successful TLS connection, such as TLS_AES_256_GCM_SHA384. It is empty before a TLS session has been established or after a failed attempt that produced no negotiated suite.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsCipherSuite
	 * @return ?string
	 */
	function tlsCipherSuite() {
		return CkFtp2_tlsCipherSuite($this->_cPtr);
	}

	/**
	 * Specifies one or more expected Subject Public Key Info (SPKI) fingerprints for TLS public-key pinning. If none of the configured pins matches the server certificate, the TLS handshake fails. Pinning supplements normal certificate-chain verification; it does not replace it. A matching pin does not make an expired or otherwise invalid certificate acceptable. When a pin set is configured, pin matching is enforced even if RequireSslCertVerify is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsPinSet
	 * @param CkString $str
	 */
	function get_TlsPinSet($str) {
		CkFtp2_get_TlsPinSet($this->_cPtr,$str);
	}

	/**
	 * Specifies one or more expected Subject Public Key Info (SPKI) fingerprints for TLS public-key pinning. If none of the configured pins matches the server certificate, the TLS handshake fails. Pinning supplements normal certificate-chain verification; it does not replace it. A matching pin does not make an expired or otherwise invalid certificate acceptable. When a pin set is configured, pin matching is enforced even if RequireSslCertVerify is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsPinSet
	 * @return ?string
	 */
	function tlsPinSet() {
		return CkFtp2_tlsPinSet($this->_cPtr);
	}

	/**
	 * Specifies one or more expected Subject Public Key Info (SPKI) fingerprints for TLS public-key pinning. If none of the configured pins matches the server certificate, the TLS handshake fails. Pinning supplements normal certificate-chain verification; it does not replace it. A matching pin does not make an expired or otherwise invalid certificate acceptable. When a pin set is configured, pin matching is enforced even if RequireSslCertVerify is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsPinSet
	 * @param ?string $newVal
	 */
	function put_TlsPinSet($newVal) {
		CkFtp2_put_TlsPinSet($this->_cPtr,$newVal);
	}

	/**
	 * Contains the protocol version negotiated for the current or most recent successful TLS connection, such as TLS 1.2 or TLS 1.3. It is empty when no TLS session has been established.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsVersion
	 * @param CkString $str
	 */
	function get_TlsVersion($str) {
		CkFtp2_get_TlsVersion($this->_cPtr,$str);
	}

	/**
	 * Contains the protocol version negotiated for the current or most recent successful TLS connection, such as TLS 1.2 or TLS 1.3. It is empty when no TLS session has been established.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#TlsVersion
	 * @return ?string
	 */
	function tlsVersion() {
		return CkFtp2_tlsVersion($this->_cPtr);
	}

	/**
	 * Provides a comma-separated list of compatibility options for uncommon platform or server requirements. The default is empty and should normally remain empty. KeywordEffect OpenNonExclusiveOn Windows, opens a local download file without exclusive sharing so another process may access it while data is being written. ProtectFromVpnOn Android, requests that FTP sockets bypass an active VPN. EnableTls13Compatibility switch that explicitly enables offering TLS 1.3 in builds where it is not already enabled by policy. DisableTls13Prevents TLS 1.3 negotiation for compatibility with a problematic peer. NoPreserveFileTimeLeaves downloaded files with the current local time instead of attempting to preserve the remote modification time.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkFtp2_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides a comma-separated list of compatibility options for uncommon platform or server requirements. The default is empty and should normally remain empty. KeywordEffect OpenNonExclusiveOn Windows, opens a local download file without exclusive sharing so another process may access it while data is being written. ProtectFromVpnOn Android, requests that FTP sockets bypass an active VPN. EnableTls13Compatibility switch that explicitly enables offering TLS 1.3 in builds where it is not already enabled by policy. DisableTls13Prevents TLS 1.3 negotiation for compatibility with a problematic peer. NoPreserveFileTimeLeaves downloaded files with the current local time instead of attempting to preserve the remote modification time.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkFtp2_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides a comma-separated list of compatibility options for uncommon platform or server requirements. The default is empty and should normally remain empty. KeywordEffect OpenNonExclusiveOn Windows, opens a local download file without exclusive sharing so another process may access it while data is being written. ProtectFromVpnOn Android, requests that FTP sockets bypass an active VPN. EnableTls13Compatibility switch that explicitly enables offering TLS 1.3 in builds where it is not already enabled by policy. DisableTls13Prevents TLS 1.3 negotiation for compatibility with a problematic peer. NoPreserveFileTimeLeaves downloaded files with the current local time instead of attempting to preserve the remote modification time.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkFtp2_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * Returns the current average upload rate in bytes per second. The value is updated during synchronous and asynchronous uploads and describes the upload in progress or the most recently completed upload. A subsequent download does not clear the retained upload rate.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UploadTransferRate
	 * @return int
	 */
	function get_UploadTransferRate() {
		return CkFtp2_get_UploadTransferRate($this->_cPtr);
	}

	/**
	 * Controls whether passive data connections first use EPSV instead of PASV. The default is _FALSE_. This property has no effect when Passive is _FALSE_. If EPSV is rejected, Chilkat automatically retries the data connection using PASV. The rejection does not disable EPSV for the remainder of the session, so a later passive data connection may try EPSV again.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UseEpsv
	 * @return bool
	 */
	function get_UseEpsv() {
		return CkFtp2_get_UseEpsv($this->_cPtr);
	}

	/**
	 * Controls whether passive data connections first use EPSV instead of PASV. The default is _FALSE_. This property has no effect when Passive is _FALSE_. If EPSV is rejected, Chilkat automatically retries the data connection using PASV. The rejection does not disable EPSV for the remainder of the session, so a later passive data connection may try EPSV again.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#UseEpsv
	 * @param bool $newVal
	 */
	function put_UseEpsv($newVal) {
		CkFtp2_put_UseEpsv($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the FTP login name. The default is anonymous. Set it to an empty string to have Connect establish the connection without performing authentication; authentication may then be handled separately with LoginAfterConnectOnly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Username
	 * @param CkString $str
	 */
	function get_Username($str) {
		CkFtp2_get_Username($this->_cPtr,$str);
	}

	/**
	 * Specifies the FTP login name. The default is anonymous. Set it to an empty string to have Connect establish the connection without performing authentication; authentication may then be handled separately with LoginAfterConnectOnly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Username
	 * @return ?string
	 */
	function username() {
		return CkFtp2_username($this->_cPtr);
	}

	/**
	 * Specifies the FTP login name. The default is anonymous. Set it to an empty string to have Connect establish the connection without performing authentication; authentication may then be handled separately with LoginAfterConnectOnly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Username
	 * @param ?string $newVal
	 */
	function put_Username($newVal) {
		CkFtp2_put_Username($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkFtp2_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkFtp2_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkFtp2_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Version
	 * @return ?string
	 */
	function version() {
		return CkFtp2_version($this->_cPtr);
	}

	/**
	 * Appends the contents of the local file at ARG1 to the remote file at ARG2. Both arguments are paths in their respective local and remote filesystems. Uses the FTP APPE command. On servers with normal APPE behavior, a missing remote file is created and an existing file is extended. A failed or interrupted append can leave the bytes already accepted by the server in place; the operation is not transactional.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFile
	 * @param ?string $localFilePath
	 * @param ?string $remoteFilePath
	 * @return bool
	 */
	function AppendFile($localFilePath,$remoteFilePath) {
		return CkFtp2_AppendFile($this->_cPtr,$localFilePath,$remoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the AppendFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileAsync
	 * @param ?string $localFilePath
	 * @param ?string $remoteFilePath
	 * @return ?CkTask
	 */
	function AppendFileAsync($localFilePath,$remoteFilePath) {
		return chilkat::_obj(CkFtp2_AppendFileAsync($this->_cPtr,$localFilePath,$remoteFilePath),'CkTask');
	}

	/**
	 * Appends the bytes in the BinData object ARG2 to the remote file at ARG1. No text encoding or line-ending conversion is performed. Uses the FTP APPE command. On servers with normal APPE behavior, a missing remote file is created and an existing file is extended. A failed or interrupted append can leave the bytes already accepted by the server in place; the operation is not transactional.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileBd
	 * @param ?string $remoteFilename
	 * @param CkBinData $bd
	 * @return bool
	 */
	function AppendFileBd($remoteFilename,$bd) {
		return CkFtp2_AppendFileBd($this->_cPtr,$remoteFilename,$bd);
	}

	/**
	 * Creates an asynchronous task to call the AppendFileBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileBdAsync
	 * @param ?string $remoteFilename
	 * @param CkBinData $bd
	 * @return ?CkTask
	 */
	function AppendFileBdAsync($remoteFilename,$bd) {
		return chilkat::_obj(CkFtp2_AppendFileBdAsync($this->_cPtr,$remoteFilename,$bd),'CkTask');
	}

	/**
	 * Appends the bytes in ARG2 to the remote file named by ARG1. No character encoding or line-ending conversion is performed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileFromBinaryData
	 * @param ?string $remoteFilename
	 * @param CkByteData $content
	 * @return bool
	 */
	function AppendFileFromBinaryData($remoteFilename,$content) {
		return CkFtp2_AppendFileFromBinaryData($this->_cPtr,$remoteFilename,$content);
	}

	/**
	 * Creates an asynchronous task to call the AppendFileFromBinaryData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileFromBinaryDataAsync
	 * @param ?string $remoteFilename
	 * @param CkByteData $content
	 * @return ?CkTask
	 */
	function AppendFileFromBinaryDataAsync($remoteFilename,$content) {
		return chilkat::_obj(CkFtp2_AppendFileFromBinaryDataAsync($this->_cPtr,$remoteFilename,$content),'CkTask');
	}

	/**
	 * Encodes the text in ARG2 using the character encoding named by ARG3 and appends the resulting bytes to the remote file at ARG1. This method controls the encoding of the file content. The encoding used for the remote filename itself is controlled separately by CommandCharset.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileFromTextData
	 * @param ?string $remoteFilename
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return bool
	 */
	function AppendFileFromTextData($remoteFilename,$textData,$charset) {
		return CkFtp2_AppendFileFromTextData($this->_cPtr,$remoteFilename,$textData,$charset);
	}

	/**
	 * Creates an asynchronous task to call the AppendFileFromTextData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileFromTextDataAsync
	 * @param ?string $remoteFilename
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function AppendFileFromTextDataAsync($remoteFilename,$textData,$charset) {
		return chilkat::_obj(CkFtp2_AppendFileFromTextDataAsync($this->_cPtr,$remoteFilename,$textData,$charset),'CkTask');
	}

	/**
	 * Encodes the text in the StringBuilder ARG2 using the charset named by ARG3 and appends the resulting bytes to the remote file at ARG1. Uses the FTP APPE command. On servers with normal APPE behavior, a missing remote file is created and an existing file is extended. A failed or interrupted append can leave the bytes already accepted by the server in place; the operation is not transactional.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileSb
	 * @param ?string $remoteFilename
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @return bool
	 */
	function AppendFileSb($remoteFilename,$sb,$charset) {
		return CkFtp2_AppendFileSb($this->_cPtr,$remoteFilename,$sb,$charset);
	}

	/**
	 * Creates an asynchronous task to call the AppendFileSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#AppendFileSbAsync
	 * @param ?string $remoteFilename
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function AppendFileSbAsync($remoteFilename,$sb,$charset) {
		return chilkat::_obj(CkFtp2_AppendFileSbAsync($this->_cPtr,$remoteFilename,$sb,$charset),'CkTask');
	}

	/**
	 * Changes the current working directory on the FTP server to ARG1. A relative path is resolved from the current remote directory; an absolute path is interpreted according to the server's path syntax. Chilkat sends one CWD command containing ARG1. It does not split a multi-level path into separate directory changes. A server may accept a path such as a/b/c in one command or may require the application to change one level at a time.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ChangeRemoteDir
	 * @param ?string $remoteDirPath
	 * @return bool
	 */
	function ChangeRemoteDir($remoteDirPath) {
		return CkFtp2_ChangeRemoteDir($this->_cPtr,$remoteDirPath);
	}

	/**
	 * Creates an asynchronous task to call the ChangeRemoteDir method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ChangeRemoteDirAsync
	 * @param ?string $remoteDirPath
	 * @return ?CkTask
	 */
	function ChangeRemoteDirAsync($remoteDirPath) {
		return chilkat::_obj(CkFtp2_ChangeRemoteDirAsync($this->_cPtr,$remoteDirPath),'CkTask');
	}

	/**
	 * Returns whether Chilkat currently considers the FTP control connection to be open. It does not require that the session be authenticated: it can return _TRUE_ after ConnectOnly and before LoginAfterConnectOnly. This is a local connection-state check. It does not send NOOP, reconnect automatically, or prove that a silently broken network path will carry the next command.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CheckConnection
	 * @return bool
	 */
	function CheckConnection() {
		return CkFtp2_CheckConnection($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the CheckConnection method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CheckConnectionAsync
	 * @return ?CkTask
	 */
	function CheckConnectionAsync() {
		return chilkat::_obj(CkFtp2_CheckConnectionAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Requests that an explicit-FTPS control connection return to clear text after authentication. Data connections may remain protected according to DataProtection. Security and compatibility: This is a legacy workaround for FTP-aware NAT devices that must inspect PORT or PASV commands. It exposes FTP commands and replies, including path names, and should not be used unless required by the network environment.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClearControlChannel
	 * @return bool
	 */
	function ClearControlChannel() {
		return CkFtp2_ClearControlChannel($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ClearControlChannel method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClearControlChannelAsync
	 * @return ?CkTask
	 */
	function ClearControlChannelAsync() {
		return chilkat::_obj(CkFtp2_ClearControlChannelAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Clears the cached listing for the current remote directory. The next call to GetDirCount or another indexed listing method retrieves a fresh listing from the server. The cache is automatically invalidated when the current remote directory or ListPattern changes. Uploading, deleting, renaming, creating, or removing a remote item does not automatically refresh the cache. Changing AllowMlsd, PreferNlst, ListOption, or DirListingCharset also leaves the existing cache intact.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClearDirCache
	 */
	function ClearDirCache() {
		CkFtp2_ClearDirCache($this->_cPtr);
	}

	/**
	 * Clears the in-memory FTP protocol log collected while KeepSessionLog is enabled.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ClearSessionLog
	 */
	function ClearSessionLog() {
		CkFtp2_ClearSessionLog($this->_cPtr);
	}

	/**
	 * Connects to the server specified by Hostname and Port, negotiates any configured proxy and TLS layer, receives the FTP greeting, and authenticates using Username, Password, and Account. For implicit or explicit FTPS, TLS is established before USER and PASS are sent. Chilkat sends PBSZ 0 and the PROT command required by DataProtection. If the server requires login before accepting those protection commands, Chilkat authenticates and retries them. Rejection of a required protection command causes the connection attempt to fail.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Connect
	 * @return bool
	 */
	function Connect() {
		return CkFtp2_Connect($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Connect method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectAsync
	 * @return ?CkTask
	 */
	function ConnectAsync() {
		return chilkat::_obj(CkFtp2_ConnectAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Establishes the configured proxy and TCP connection, performs any configured implicit or explicit TLS setup, and receives the FTP server greeting, but does not send USER or PASS. Complete authentication by calling LoginAfterConnectOnly. With implicit FTPS, the TLS handshake occurs immediately after the TCP connection is established and before the greeting is received. With explicit FTPS, Chilkat first receives the greeting, sends AUTH TLS or AUTH SSL, and then performs the TLS handshake. When the control channel is protected, Chilkat also sends PBSZ 0 and the PROT command required by DataProtection, even though login has not yet occurred.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectOnly
	 * @return bool
	 */
	function ConnectOnly() {
		return CkFtp2_ConnectOnly($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ConnectOnly method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConnectOnlyAsync
	 * @return ?CkTask
	 */
	function ConnectOnlyAsync() {
		return chilkat::_obj(CkFtp2_ConnectOnlyAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Upgrades an already-connected clear FTP control channel to explicit TLS. This method is intended for a manually staged connection created with ConnectOnly while Ssl, AuthTls, and AuthSsl were disabled. Chilkat sends AUTH TLS, performs the TLS handshake, sends PBSZ 0, and then sends the PROT command required by DataProtection. The method does not authenticate; call LoginAfterConnectOnly afterward.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConvertToTls
	 * @return bool
	 */
	function ConvertToTls() {
		return CkFtp2_ConvertToTls($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the ConvertToTls method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#ConvertToTlsAsync
	 * @return ?CkTask
	 */
	function ConvertToTlsAsync() {
		return chilkat::_obj(CkFtp2_ConvertToTlsAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Creates and returns a textual upload plan for the local directory at ARG1. The object must be connected because the plan records the current absolute remote directory together with the local source paths. The generated plan contains absolute local paths and absolute remote directory operations. Treat the text as a Chilkat-generated plan for PutPlan rather than as a portable or stable interchange format.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CreatePlan
	 * @param ?string $localDir
	 * @return ?string
	 */
	function createPlan($localDir) {
		return CkFtp2_createPlan($this->_cPtr,$localDir);
	}

	/**
	 * Creates an asynchronous task to call the CreatePlan method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CreatePlanAsync
	 * @param ?string $localDir
	 * @return ?CkTask
	 */
	function CreatePlanAsync($localDir) {
		return chilkat::_obj(CkFtp2_CreatePlanAsync($this->_cPtr,$localDir),'CkTask');
	}

	/**
	 * Creates the remote directory at ARG1 by sending one MKD command. The path is interpreted relative to the current remote directory unless the server recognizes it as absolute. Chilkat does not split a multi-level path and does not issue separate commands to create missing parent directories. Some servers create all levels from one MKD a/b/c command, while others require the parent directories to exist.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CreateRemoteDir
	 * @param ?string $remoteDirPath
	 * @return bool
	 */
	function CreateRemoteDir($remoteDirPath) {
		return CkFtp2_CreateRemoteDir($this->_cPtr,$remoteDirPath);
	}

	/**
	 * Creates an asynchronous task to call the CreateRemoteDir method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#CreateRemoteDirAsync
	 * @param ?string $remoteDirPath
	 * @return ?CkTask
	 */
	function CreateRemoteDirAsync($remoteDirPath) {
		return chilkat::_obj(CkFtp2_CreateRemoteDirAsync($this->_cPtr,$remoteDirPath),'CkTask');
	}

	/**
	 * Deletes files in the current remote directory whose names match the wildcard pattern in ARG1. The method returns the number deleted, or -1 if the operation fails. Chilkat sends a server-side legacy listing request such as LIST *.txt, parses the returned entries, and sends DELE for matching files. Matching case sensitivity and wildcard behavior therefore depend on the server. Matching directories are not removed by this method.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteMatching
	 * @param ?string $remotePattern
	 * @return int
	 */
	function DeleteMatching($remotePattern) {
		return CkFtp2_DeleteMatching($this->_cPtr,$remotePattern);
	}

	/**
	 * Creates an asynchronous task to call the DeleteMatching method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteMatchingAsync
	 * @param ?string $remotePattern
	 * @return ?CkTask
	 */
	function DeleteMatchingAsync($remotePattern) {
		return chilkat::_obj(CkFtp2_DeleteMatchingAsync($this->_cPtr,$remotePattern),'CkTask');
	}

	/**
	 * Deletes the remote file at ARG1. The path may be relative to the current remote directory or absolute according to the server's path syntax. A successful deletion does not refresh the cached indexed listing. Call ClearDirCache when a fresh listing is required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteRemoteFile
	 * @param ?string $remoteFilePath
	 * @return bool
	 */
	function DeleteRemoteFile($remoteFilePath) {
		return CkFtp2_DeleteRemoteFile($this->_cPtr,$remoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the DeleteRemoteFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteRemoteFileAsync
	 * @param ?string $remoteFilePath
	 * @return ?CkTask
	 */
	function DeleteRemoteFileAsync($remoteFilePath) {
		return chilkat::_obj(CkFtp2_DeleteRemoteFileAsync($this->_cPtr,$remoteFilePath),'CkTask');
	}

	/**
	 * Recursively deletes the files and subdirectories beneath the current remote directory. Child directories are removed after their contents are deleted, but the current root directory itself remains in place and remains the current remote directory. Destructive operation: Set the current remote directory carefully before calling this method. The VerifyDeleteFile and VerifyDeleteDir callbacks can set their skip output to prevent selected items from being deleted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteTree
	 * @return bool
	 */
	function DeleteTree() {
		return CkFtp2_DeleteTree($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the DeleteTree method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DeleteTreeAsync
	 * @return ?CkTask
	 */
	function DeleteTreeAsync() {
		return chilkat::_obj(CkFtp2_DeleteTreeAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Deprecated. Tries the supported traditional FTP-proxy login sequences and returns the first successful ProxyMethod value. Returns 0 when none succeeds and -1 when the test cannot be completed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DetermineProxyMethod
	 * @return int
	 * @deprecated
	 */
	function DetermineProxyMethod() {
		return CkFtp2_DetermineProxyMethod($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the DetermineProxyMethod method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DetermineProxyMethodAsync
	 * @return ?CkTask
	 * @deprecated
	 */
	function DetermineProxyMethodAsync() {
		return chilkat::_obj(CkFtp2_DetermineProxyMethodAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Deprecated. Tests combinations of Ssl, AuthTls, AuthSsl, Port, Passive, and PassiveUseHostAddr by attempting a directory listing, then returns an XML report. The method performs multiple network attempts and can take significant time. Prefer configuring the connection explicitly from the server's documented FTP/FTPS settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DetermineSettings
	 * @return ?string
	 * @deprecated
	 */
	function determineSettings() {
		return CkFtp2_determineSettings($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the DetermineSettings method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DetermineSettingsAsync
	 * @return ?CkTask
	 * @deprecated
	 */
	function DetermineSettingsAsync() {
		return chilkat::_obj(CkFtp2_DetermineSettingsAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Returns an XML representation of the complete remote directory tree rooted at the current remote directory. The result uses a <dirTree> root, nested <dir name="..."> elements, and file elements containing the name, size, and available modification time. <dirTree>   <file sz="123" dt="Mon, 27 Jul 2026 06:55:00 -0500">root.txt</file>   <dir name="subdir">     <file sz="456" dt="...">nested.txt</file>   </dir> </dirTree> Chilkat traverses by listing each directory and temporarily changing the server's current directory. Empty directories are included, and the original current remote directory is restored after successful traversal.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DirTreeXml
	 * @return ?string
	 */
	function dirTreeXml() {
		return CkFtp2_dirTreeXml($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the DirTreeXml method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DirTreeXmlAsync
	 * @return ?CkTask
	 */
	function DirTreeXmlAsync() {
		return chilkat::_obj(CkFtp2_DirTreeXmlAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Ends the FTP session and closes the control connection. When connected, Chilkat sends QUIT when possible and then closes the socket. The method is idempotent. Calling it before a connection exists or calling it again after the session is already closed succeeds without sending another FTP command. Historical properties such as ConnectVerified, LoginVerified, Greeting, and LastReply may retain information from the completed session; use CheckConnection for current connection state.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Disconnect
	 * @return bool
	 */
	function Disconnect() {
		return CkFtp2_Disconnect($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Disconnect method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DisconnectAsync
	 * @return ?CkTask
	 */
	function DisconnectAsync() {
		return chilkat::_obj(CkFtp2_DisconnectAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Downloads all selected files and subdirectories beneath the current remote directory and recreates the tree under the local directory at ARG1. Existing local files are overwritten, and eligible empty remote directories are created locally. The file and directory filters in SyncMustMatch, SyncMustNotMatch, SyncMustMatchDir, and SyncMustNotMatchDir apply. Use SetOldestDateStr to exclude files older than a chosen date.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DownloadTree
	 * @param ?string $localRoot
	 * @return bool
	 */
	function DownloadTree($localRoot) {
		return CkFtp2_DownloadTree($this->_cPtr,$localRoot);
	}

	/**
	 * Creates an asynchronous task to call the DownloadTree method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#DownloadTreeAsync
	 * @param ?string $localRoot
	 * @return ?CkTask
	 */
	function DownloadTreeAsync($localRoot) {
		return chilkat::_obj(CkFtp2_DownloadTreeAsync($this->_cPtr,$localRoot),'CkTask');
	}

	/**
	 * Sends FEAT and returns the server's multiline capability reply. A server may return an error when the command is unsupported or not permitted in the current session state. A successful explicit call performs the same capability processing as automatic FEAT. It can update HasModeZ, listing-extension detection, UTF-8 handling through AutoOptsUtf8, and UseEpsv through AutoSetUseEpsv, even when AutoFeat is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Feat
	 * @return ?string
	 */
	function feat() {
		return CkFtp2_feat($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Feat method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#FeatAsync
	 * @return ?CkTask
	 */
	function FeatAsync() {
		return chilkat::_obj(CkFtp2_FeatAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Deprecated. Returns a new DateTime containing the creation time for cached directory-listing entry ARG1. Use GetCreateTimeStr when a string result is preferred. ARG1 is a zero-based index from 0 through one less than GetDirCount.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateDt
	 * @param int $index
	 * @return ?CkDateTime
	 * @deprecated
	 */
	function GetCreateDt($index) {
		return chilkat::_obj(CkFtp2_GetCreateDt($this->_cPtr,$index),'CkDateTime');
	}

	/**
	 * Creates an asynchronous task to call the GetCreateDt method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateDtAsync
	 * @param int $index
	 * @return ?CkTask
	 * @deprecated
	 */
	function GetCreateDtAsync($index) {
		return chilkat::_obj(CkFtp2_GetCreateDtAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Deprecated. Returns a new DateTime containing the creation time for the file or directory named by ARG1 in the current remote directory. Use GetCreateTimeByNameStr when a string result is preferred. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name so the item is present in the cached listing.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateDtByName
	 * @param ?string $filename
	 * @return ?CkDateTime
	 * @deprecated
	 */
	function GetCreateDtByName($filename) {
		return chilkat::_obj(CkFtp2_GetCreateDtByName($this->_cPtr,$filename),'CkDateTime');
	}

	/**
	 * Creates an asynchronous task to call the GetCreateDtByName method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateDtByNameAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 * @deprecated
	 */
	function GetCreateDtByNameAsync($filename) {
		return chilkat::_obj(CkFtp2_GetCreateDtByNameAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Returns the creation time for the file or directory named by ARG1 in the current remote directory as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name so the item is present in the cached listing.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeByNameStr
	 * @param ?string $filename
	 * @return ?string
	 */
	function getCreateTimeByNameStr($filename) {
		return CkFtp2_getCreateTimeByNameStr($this->_cPtr,$filename);
	}

	/**
	 * Returns the creation time for the file or directory named by ARG1 in the current remote directory as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name so the item is present in the cached listing.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeByNameStr
	 * @param ?string $filename
	 * @return ?string
	 */
	function createTimeByNameStr($filename) {
		return CkFtp2_createTimeByNameStr($this->_cPtr,$filename);
	}

	/**
	 * Creates an asynchronous task to call the GetCreateTimeByNameStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeByNameStrAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 */
	function GetCreateTimeByNameStrAsync($filename) {
		return chilkat::_obj(CkFtp2_GetCreateTimeByNameStrAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Returns the creation time for cached directory-listing entry ARG1 as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 is a zero-based index from 0 through one less than GetDirCount. Creation-time availability depends on the server and listing format. An invalid index or unavailable creation time is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeStr
	 * @param int $index
	 * @return ?string
	 */
	function getCreateTimeStr($index) {
		return CkFtp2_getCreateTimeStr($this->_cPtr,$index);
	}

	/**
	 * Returns the creation time for cached directory-listing entry ARG1 as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 is a zero-based index from 0 through one less than GetDirCount. Creation-time availability depends on the server and listing format. An invalid index or unavailable creation time is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeStr
	 * @param int $index
	 * @return ?string
	 */
	function createTimeStr($index) {
		return CkFtp2_createTimeStr($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetCreateTimeStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCreateTimeStrAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetCreateTimeStrAsync($index) {
		return chilkat::_obj(CkFtp2_GetCreateTimeStrAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the current working directory reported by the FTP server, normally by using the PWD command.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCurrentRemoteDir
	 * @return ?string
	 */
	function getCurrentRemoteDir() {
		return CkFtp2_getCurrentRemoteDir($this->_cPtr);
	}

	/**
	 * Returns the current working directory reported by the FTP server, normally by using the PWD command.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCurrentRemoteDir
	 * @return ?string
	 */
	function currentRemoteDir() {
		return CkFtp2_currentRemoteDir($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the GetCurrentRemoteDir method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetCurrentRemoteDirAsync
	 * @return ?CkTask
	 */
	function GetCurrentRemoteDirAsync() {
		return chilkat::_obj(CkFtp2_GetCurrentRemoteDirAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Returns the number of files and directories in the cached listing of the current remote directory after applying the case-insensitive ListPattern. On the first call, Chilkat obtains and caches the full directory listing and then applies the pattern locally. Repeated indexed listing calls use the cache until it is invalidated. Returns -1 if the listing cannot be obtained or parsed. The cache is not automatically refreshed by uploads, deletions, renames, directory creation/removal, raw commands, or changes to the listing-command selection properties. Call ClearDirCache when the server contents or desired listing format may have changed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetDirCount
	 * @return int
	 */
	function GetDirCount() {
		return CkFtp2_GetDirCount($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the GetDirCount method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetDirCountAsync
	 * @return ?CkTask
	 */
	function GetDirCountAsync() {
		return chilkat::_obj(CkFtp2_GetDirCountAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1 to the local filesystem path at ARG2. The local parent directory must already exist; this method does not create missing local directories. When not resuming, the downloaded file replaces an existing destination after the server accepts the transfer. If the server rejects the request before transfer begins, such as when the remote file does not exist, an existing destination file is preserved.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFile
	 * @param ?string $remoteFilePath
	 * @param ?string $localFilePath
	 * @return bool
	 */
	function GetFile($remoteFilePath,$localFilePath) {
		return CkFtp2_GetFile($this->_cPtr,$remoteFilePath,$localFilePath);
	}

	/**
	 * Creates an asynchronous task to call the GetFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileAsync
	 * @param ?string $remoteFilePath
	 * @param ?string $localFilePath
	 * @return ?CkTask
	 */
	function GetFileAsync($remoteFilePath,$localFilePath) {
		return chilkat::_obj(CkFtp2_GetFileAsync($this->_cPtr,$remoteFilePath,$localFilePath),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1 and appends its bytes to the existing contents of the BinData object in ARG2. The object is not cleared automatically. No text decoding or line-ending conversion is performed. When RestartNext is _TRUE_, the current byte count in ARG2 is used as the resume offset and only the remaining remote bytes are appended.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileBd
	 * @param ?string $remoteFilePath
	 * @param CkBinData $binData
	 * @return bool
	 */
	function GetFileBd($remoteFilePath,$binData) {
		return CkFtp2_GetFileBd($this->_cPtr,$remoteFilePath,$binData);
	}

	/**
	 * Creates an asynchronous task to call the GetFileBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileBdAsync
	 * @param ?string $remoteFilePath
	 * @param CkBinData $binData
	 * @return ?CkTask
	 */
	function GetFileBdAsync($remoteFilePath,$binData) {
		return chilkat::_obj(CkFtp2_GetFileBdAsync($this->_cPtr,$remoteFilePath,$binData),'CkTask');
	}

	/**
	 * Returns the name of cached directory-listing entry ARG1. ARG1 is a zero-based index from 0 through one less than GetDirCount. The returned value is the name from the listing, not necessarily a full remote path. An out-of-range or negative index is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFilename
	 * @param int $index
	 * @return ?string
	 */
	function getFilename($index) {
		return CkFtp2_getFilename($this->_cPtr,$index);
	}

	/**
	 * Returns the name of cached directory-listing entry ARG1. ARG1 is a zero-based index from 0 through one less than GetDirCount. The returned value is the name from the listing, not necessarily a full remote path. An out-of-range or negative index is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFilename
	 * @param int $index
	 * @return ?string
	 */
	function filename($index) {
		return CkFtp2_filename($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetFilename method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFilenameAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetFilenameAsync($index) {
		return chilkat::_obj(CkFtp2_GetFilenameAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1, decodes its bytes using the charset named by ARG2, and appends the resulting text to the existing contents of the StringBuilder in ARG3. The destination is not cleared automatically. A byte-order mark for the selected encoding is recognized and removed from the decoded text. Invalid byte sequences are replaced during decoding rather than causing the transfer itself to fail.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileSb
	 * @param ?string $remoteFilePath
	 * @param ?string $charset
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function GetFileSb($remoteFilePath,$charset,$sb) {
		return CkFtp2_GetFileSb($this->_cPtr,$remoteFilePath,$charset,$sb);
	}

	/**
	 * Creates an asynchronous task to call the GetFileSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileSbAsync
	 * @param ?string $remoteFilePath
	 * @param ?string $charset
	 * @param CkStringBuilder $sb
	 * @return ?CkTask
	 */
	function GetFileSbAsync($remoteFilePath,$charset,$sb) {
		return chilkat::_obj(CkFtp2_GetFileSbAsync($this->_cPtr,$remoteFilePath,$charset,$sb),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1 and writes its bytes to the Stream in ARG2. For a synchronous call, ARG2 must be configured with a destination sink before the method begins. For an asynchronous call, application code may read from the stream while the background download produces data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileToStream
	 * @param ?string $remoteFilePath
	 * @param CkStream $toStream
	 * @return bool
	 */
	function GetFileToStream($remoteFilePath,$toStream) {
		return CkFtp2_GetFileToStream($this->_cPtr,$remoteFilePath,$toStream);
	}

	/**
	 * Creates an asynchronous task to call the GetFileToStream method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetFileToStreamAsync
	 * @param ?string $remoteFilePath
	 * @param CkStream $toStream
	 * @return ?CkTask
	 */
	function GetFileToStreamAsync($remoteFilePath,$toStream) {
		return chilkat::_obj(CkFtp2_GetFileToStreamAsync($this->_cPtr,$remoteFilePath,$toStream),'CkTask');
	}

	/**
	 * Returns the group name or identifier for cached directory-listing entry ARG1. The value is empty when the server does not provide group metadata. MLSD metadata: MLSD commonly provides accurate size and time facts but often omits owner and group. On a UNIX-like server, disabling AllowMlsd may expose owner/group through LIST, at the cost of less standardized and potentially less accurate listing data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetGroup
	 * @param int $index
	 * @return ?string
	 */
	function getGroup($index) {
		return CkFtp2_getGroup($this->_cPtr,$index);
	}

	/**
	 * Returns the group name or identifier for cached directory-listing entry ARG1. The value is empty when the server does not provide group metadata. MLSD metadata: MLSD commonly provides accurate size and time facts but often omits owner and group. On a UNIX-like server, disabling AllowMlsd may expose owner/group through LIST, at the cost of less standardized and potentially less accurate listing data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetGroup
	 * @param int $index
	 * @return ?string
	 */
	function group($index) {
		return CkFtp2_group($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetGroup method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetGroupAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetGroupAsync($index) {
		return chilkat::_obj(CkFtp2_GetGroupAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns _TRUE_ when cached directory-listing entry ARG1 is identified as a directory. ARG1 is a zero-based index into the current cached listing. For an invalid index the method returns _FALSE_ and sets LastMethodSuccess to _FALSE_. Check that property when _FALSE_ must be distinguished from a valid non-directory entry.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetIsDirectory
	 * @param int $index
	 * @return bool
	 */
	function GetIsDirectory($index) {
		return CkFtp2_GetIsDirectory($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetIsDirectory method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetIsDirectoryAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetIsDirectoryAsync($index) {
		return chilkat::_obj(CkFtp2_GetIsDirectoryAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns _TRUE_ when cached directory-listing entry ARG1 is identified as a symbolic link. This information is available only when the server's listing format reports it. For an invalid index the method returns _FALSE_ and sets LastMethodSuccess to _FALSE_. A valid _FALSE_ result can also mean the entry is not a link or the listing did not provide link metadata.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetIsSymbolicLink
	 * @param int $index
	 * @return bool
	 */
	function GetIsSymbolicLink($index) {
		return CkFtp2_GetIsSymbolicLink($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetIsSymbolicLink method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetIsSymbolicLinkAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetIsSymbolicLinkAsync($index) {
		return chilkat::_obj(CkFtp2_GetIsSymbolicLinkAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Copies structured supplemental JSON produced by the immediately preceding method into the JsonObject in ARG1. TLS connection and protected data-connection operations can populate information such as the negotiated TLS version. The data is transient. A later method may replace it or clear it to an empty object, including ordinary commands such as NOOP, raw commands, or Disconnect. Call this method immediately after the operation whose details are needed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastJsonData
	 * @param CkJsonObject $json
	 */
	function GetLastJsonData($json) {
		CkFtp2_GetLastJsonData($this->_cPtr,$json);
	}

	/**
	 * Deprecated. Returns a new DateTime containing the last-modified time for cached directory-listing entry ARG1. Use GetLastModifiedTimeStr when a string result is preferred.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModDt
	 * @param int $index
	 * @return ?CkDateTime
	 * @deprecated
	 */
	function GetLastModDt($index) {
		return chilkat::_obj(CkFtp2_GetLastModDt($this->_cPtr,$index),'CkDateTime');
	}

	/**
	 * Creates an asynchronous task to call the GetLastModDt method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModDtAsync
	 * @param int $index
	 * @return ?CkTask
	 * @deprecated
	 */
	function GetLastModDtAsync($index) {
		return chilkat::_obj(CkFtp2_GetLastModDtAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Deprecated. Returns a new DateTime containing the last-modified time for the item named by ARG1 in the current remote directory. Use GetLastModifiedTimeByNameStr when a string result is preferred. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModDtByName
	 * @param ?string $filename
	 * @return ?CkDateTime
	 * @deprecated
	 */
	function GetLastModDtByName($filename) {
		return chilkat::_obj(CkFtp2_GetLastModDtByName($this->_cPtr,$filename),'CkDateTime');
	}

	/**
	 * Creates an asynchronous task to call the GetLastModDtByName method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModDtByNameAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 * @deprecated
	 */
	function GetLastModDtByNameAsync($filename) {
		return chilkat::_obj(CkFtp2_GetLastModDtByNameAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Returns the last-modified time for the item named by ARG1 in the current remote directory as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeByNameStr
	 * @param ?string $filename
	 * @return ?string
	 */
	function getLastModifiedTimeByNameStr($filename) {
		return CkFtp2_getLastModifiedTimeByNameStr($this->_cPtr,$filename);
	}

	/**
	 * Returns the last-modified time for the item named by ARG1 in the current remote directory as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeByNameStr
	 * @param ?string $filename
	 * @return ?string
	 */
	function lastModifiedTimeByNameStr($filename) {
		return CkFtp2_lastModifiedTimeByNameStr($this->_cPtr,$filename);
	}

	/**
	 * Creates an asynchronous task to call the GetLastModifiedTimeByNameStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeByNameStrAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 */
	function GetLastModifiedTimeByNameStrAsync($filename) {
		return chilkat::_obj(CkFtp2_GetLastModifiedTimeByNameStrAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Returns the last-modified time for cached directory-listing entry ARG1 as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 is a zero-based index into the current cached listing. An invalid index or unavailable timestamp is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeStr
	 * @param int $index
	 * @return ?string
	 */
	function getLastModifiedTimeStr($index) {
		return CkFtp2_getLastModifiedTimeStr($this->_cPtr,$index);
	}

	/**
	 * Returns the last-modified time for cached directory-listing entry ARG1 as an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. ARG1 is a zero-based index into the current cached listing. An invalid index or unavailable timestamp is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeStr
	 * @param int $index
	 * @return ?string
	 */
	function lastModifiedTimeStr($index) {
		return CkFtp2_lastModifiedTimeStr($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetLastModifiedTimeStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetLastModifiedTimeStrAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetLastModifiedTimeStrAsync($index) {
		return chilkat::_obj(CkFtp2_GetLastModifiedTimeStrAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the owner name or identifier for cached directory-listing entry ARG1. The value is empty when a valid entry has no owner metadata. An invalid index is a method failure and sets LastMethodSuccess to _FALSE_. MLSD metadata: MLSD commonly provides accurate size and time facts but often omits owner and group. On a UNIX-like server, disabling AllowMlsd may expose owner/group through LIST, at the cost of less standardized and potentially less accurate listing data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetOwner
	 * @param int $index
	 * @return ?string
	 */
	function getOwner($index) {
		return CkFtp2_getOwner($this->_cPtr,$index);
	}

	/**
	 * Returns the owner name or identifier for cached directory-listing entry ARG1. The value is empty when a valid entry has no owner metadata. An invalid index is a method failure and sets LastMethodSuccess to _FALSE_. MLSD metadata: MLSD commonly provides accurate size and time facts but often omits owner and group. On a UNIX-like server, disabling AllowMlsd may expose owner/group through LIST, at the cost of less standardized and potentially less accurate listing data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetOwner
	 * @param int $index
	 * @return ?string
	 */
	function owner($index) {
		return CkFtp2_owner($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetOwner method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetOwnerAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetOwnerAsync($index) {
		return chilkat::_obj(CkFtp2_GetOwnerAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the permissions or status string for cached directory-listing entry ARG1. The value is empty when the server provides no such metadata. Use GetPermType to determine how to interpret the result. Permission typeReturned information mlsdThe RFC 3659 perm fact. Letters describe FTP operations available to the logged-in user, such as append, create, delete, enter, list, rename, or write. unixA UNIX-style string such as rwxr-xr-x. netwareNetWare trustee-right letters such as read, write, create, erase, modify, file scan, access control, and supervisor. openvmsAn OpenVMS protection string such as (RWED,RWED,R,R). batchStatusFlagsA fixed-position Connect:Enterprise batch-status string such as -CR--M----.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermissions
	 * @param int $index
	 * @return ?string
	 */
	function getPermissions($index) {
		return CkFtp2_getPermissions($this->_cPtr,$index);
	}

	/**
	 * Returns the permissions or status string for cached directory-listing entry ARG1. The value is empty when the server provides no such metadata. Use GetPermType to determine how to interpret the result. Permission typeReturned information mlsdThe RFC 3659 perm fact. Letters describe FTP operations available to the logged-in user, such as append, create, delete, enter, list, rename, or write. unixA UNIX-style string such as rwxr-xr-x. netwareNetWare trustee-right letters such as read, write, create, erase, modify, file scan, access control, and supervisor. openvmsAn OpenVMS protection string such as (RWED,RWED,R,R). batchStatusFlagsA fixed-position Connect:Enterprise batch-status string such as -CR--M----.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermissions
	 * @param int $index
	 * @return ?string
	 */
	function permissions($index) {
		return CkFtp2_permissions($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetPermissions method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermissionsAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetPermissionsAsync($index) {
		return chilkat::_obj(CkFtp2_GetPermissionsAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the format identifier for the permissions string available for cached listing entry ARG1. The value is empty when no permissions are available. ValueFormat mlsdThe RFC 3659 perm fact. unixA UNIX/POSIX-style mode string. netwareNetWare trustee rights. openvmsAn OpenVMS protection string. batchStatusFlagsConnect:Enterprise batch status flags.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermType
	 * @param int $index
	 * @return ?string
	 */
	function getPermType($index) {
		return CkFtp2_getPermType($this->_cPtr,$index);
	}

	/**
	 * Returns the format identifier for the permissions string available for cached listing entry ARG1. The value is empty when no permissions are available. ValueFormat mlsdThe RFC 3659 perm fact. unixA UNIX/POSIX-style mode string. netwareNetWare trustee rights. openvmsAn OpenVMS protection string. batchStatusFlagsConnect:Enterprise batch status flags.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermType
	 * @param int $index
	 * @return ?string
	 */
	function permType($index) {
		return CkFtp2_permType($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetPermType method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetPermTypeAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetPermTypeAsync($index) {
		return chilkat::_obj(CkFtp2_GetPermTypeAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1 and returns its bytes. No character decoding or line-ending conversion is performed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileBinaryData
	 * @param ?string $remoteFilename
	 * @param CkByteData $outData
	 * @return bool
	 */
	function GetRemoteFileBinaryData($remoteFilename,$outData) {
		return CkFtp2_GetRemoteFileBinaryData($this->_cPtr,$remoteFilename,$outData);
	}

	/**
	 * Creates an asynchronous task to call the GetRemoteFileBinaryData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileBinaryDataAsync
	 * @param ?string $remoteFilename
	 * @return ?CkTask
	 */
	function GetRemoteFileBinaryDataAsync($remoteFilename) {
		return chilkat::_obj(CkFtp2_GetRemoteFileBinaryDataAsync($this->_cPtr,$remoteFilename),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1, decodes the bytes using the character encoding named by ARG2, and returns the text. A byte-order mark for the selected encoding is recognized and omitted from the returned text. This method controls file-content decoding. The encoding used for the remote path itself is controlled by CommandCharset.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextC
	 * @param ?string $remoteFilename
	 * @param ?string $charset
	 * @return ?string
	 */
	function getRemoteFileTextC($remoteFilename,$charset) {
		return CkFtp2_getRemoteFileTextC($this->_cPtr,$remoteFilename,$charset);
	}

	/**
	 * Downloads the remote file at ARG1, decodes the bytes using the character encoding named by ARG2, and returns the text. A byte-order mark for the selected encoding is recognized and omitted from the returned text. This method controls file-content decoding. The encoding used for the remote path itself is controlled by CommandCharset.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextC
	 * @param ?string $remoteFilename
	 * @param ?string $charset
	 * @return ?string
	 */
	function remoteFileTextC($remoteFilename,$charset) {
		return CkFtp2_remoteFileTextC($this->_cPtr,$remoteFilename,$charset);
	}

	/**
	 * Creates an asynchronous task to call the GetRemoteFileTextC method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextCAsync
	 * @param ?string $remoteFilename
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function GetRemoteFileTextCAsync($remoteFilename,$charset) {
		return chilkat::_obj(CkFtp2_GetRemoteFileTextCAsync($this->_cPtr,$remoteFilename,$charset),'CkTask');
	}

	/**
	 * Downloads the remote file at ARG1 and returns its contents decoded using the ANSI character encoding. Use GetRemoteFileTextC when the file uses UTF-8 or another explicitly known encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextData
	 * @param ?string $remoteFilename
	 * @return ?string
	 */
	function getRemoteFileTextData($remoteFilename) {
		return CkFtp2_getRemoteFileTextData($this->_cPtr,$remoteFilename);
	}

	/**
	 * Downloads the remote file at ARG1 and returns its contents decoded using the ANSI character encoding. Use GetRemoteFileTextC when the file uses UTF-8 or another explicitly known encoding.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextData
	 * @param ?string $remoteFilename
	 * @return ?string
	 */
	function remoteFileTextData($remoteFilename) {
		return CkFtp2_remoteFileTextData($this->_cPtr,$remoteFilename);
	}

	/**
	 * Creates an asynchronous task to call the GetRemoteFileTextData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetRemoteFileTextDataAsync
	 * @param ?string $remoteFilename
	 * @return ?CkTask
	 */
	function GetRemoteFileTextDataAsync($remoteFilename) {
		return chilkat::_obj(CkFtp2_GetRemoteFileTextDataAsync($this->_cPtr,$remoteFilename),'CkTask');
	}

	/**
	 * Loads into the Cert object in ARG1 the leaf certificate presented on the currently connected FTPS control channel. The method requires an active TLS control connection. A protected data connection does not replace the control-channel certificate returned here, and the certificate is not retained for retrieval after Disconnect.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetServerCert
	 * @param CkCert $cert
	 * @return bool
	 */
	function GetServerCert($cert) {
		return CkFtp2_GetServerCert($this->_cPtr,$cert);
	}

	/**
	 * Returns the size in bytes of cached directory-listing entry ARG1 as a 32-bit integer. Use GetSize64 for large files. Returns -1 when ARG1 is invalid or the size is unavailable. Because this method returns an integer, do not use LastMethodSuccess to interpret the result.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSize
	 * @param int $index
	 * @return int
	 */
	function GetSize($index) {
		return CkFtp2_GetSize($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetSize method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetSizeAsync($index) {
		return chilkat::_obj(CkFtp2_GetSizeAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the size in bytes of cached directory-listing entry ARG1 as a 64-bit integer. Returns -1 when ARG1 is invalid or the size is unavailable. Because this method returns an integer, do not use LastMethodSuccess to interpret the result.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSize64
	 * @param int $index
	 * @return int
	 */
	function GetSize64($index) {
		return CkFtp2_GetSize64($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetSize64 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSize64Async
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetSize64Async($index) {
		return chilkat::_obj(CkFtp2_GetSize64Async($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the size in bytes of the file named by ARG1 in the current remote directory as a 32-bit integer. Returns -1 when the file is not found or the size cannot be obtained. Use GetSizeByName64 for large files. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeByName
	 * @param ?string $filename
	 * @return int
	 */
	function GetSizeByName($filename) {
		return CkFtp2_GetSizeByName($this->_cPtr,$filename);
	}

	/**
	 * Creates an asynchronous task to call the GetSizeByName method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeByNameAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 */
	function GetSizeByNameAsync($filename) {
		return chilkat::_obj(CkFtp2_GetSizeByNameAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Sends a SIZE command for ARG1 and returns the remote size in bytes as a 64-bit integer. Returns -1 when the server rejects the command, the item is not found, or the size is unavailable. This method does not depend on the indexed directory cache or ListPattern. Filename and path case sensitivity are determined by the FTP server and its filesystem.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeByName64
	 * @param ?string $filename
	 * @return int
	 */
	function GetSizeByName64($filename) {
		return CkFtp2_GetSizeByName64($this->_cPtr,$filename);
	}

	/**
	 * Creates an asynchronous task to call the GetSizeByName64 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeByName64Async
	 * @param ?string $filename
	 * @return ?CkTask
	 */
	function GetSizeByName64Async($filename) {
		return chilkat::_obj(CkFtp2_GetSizeByName64Async($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Returns the size in bytes of cached directory-listing entry ARG1 as a decimal string. This form avoids integer-size limits in languages without a convenient 64-bit type. An out-of-range or negative index is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStr
	 * @param int $index
	 * @return ?string
	 */
	function getSizeStr($index) {
		return CkFtp2_getSizeStr($this->_cPtr,$index);
	}

	/**
	 * Returns the size in bytes of cached directory-listing entry ARG1 as a decimal string. This form avoids integer-size limits in languages without a convenient 64-bit type. An out-of-range or negative index is a method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStr
	 * @param int $index
	 * @return ?string
	 */
	function sizeStr($index) {
		return CkFtp2_sizeStr($this->_cPtr,$index);
	}

	/**
	 * Creates an asynchronous task to call the GetSizeStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStrAsync
	 * @param int $index
	 * @return ?CkTask
	 */
	function GetSizeStrAsync($index) {
		return chilkat::_obj(CkFtp2_GetSizeStrAsync($this->_cPtr,$index),'CkTask');
	}

	/**
	 * Returns the size in bytes of the file named by ARG1 in the current remote directory as a decimal string. This form avoids language-specific integer limits. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStrByName
	 * @param ?string $filename
	 * @return ?string
	 */
	function getSizeStrByName($filename) {
		return CkFtp2_getSizeStrByName($this->_cPtr,$filename);
	}

	/**
	 * Returns the size in bytes of the file named by ARG1 in the current remote directory as a decimal string. This form avoids language-specific integer limits. ARG1 must be a name, not a path. Set the current remote directory first, and ensure ListPattern matches the name.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStrByName
	 * @param ?string $filename
	 * @return ?string
	 */
	function sizeStrByName($filename) {
		return CkFtp2_sizeStrByName($this->_cPtr,$filename);
	}

	/**
	 * Creates an asynchronous task to call the GetSizeStrByName method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSizeStrByNameAsync
	 * @param ?string $filename
	 * @return ?CkTask
	 */
	function GetSizeStrByNameAsync($filename) {
		return chilkat::_obj(CkFtp2_GetSizeStrByNameAsync($this->_cPtr,$filename),'CkTask');
	}

	/**
	 * Deprecated. Returns a new Cert containing the certificate presented by the FTP server during the current or most recent TLS connection. Use GetServerCert instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSslServerCert
	 * @return ?CkCert
	 * @deprecated
	 */
	function GetSslServerCert() {
		return chilkat::_obj(CkFtp2_GetSslServerCert($this->_cPtr),'CkCert');
	}

	/**
	 * Appends to the StringTable in ARG1 the relative paths affected by the most recent synchronization operation. Existing entries in ARG1 are preserved. The appended paths include files uploaded, downloaded, or deleted by SyncDeleteRemote, SyncLocalDir, SyncLocalTree, SyncRemoteTree, or SyncRemoteTree2. When a successful preview has no candidates, no placeholder or empty-string entry is appended.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetSyncedFiles
	 * @param CkStringTable $strTable
	 */
	function GetSyncedFiles($strTable) {
		CkFtp2_GetSyncedFiles($this->_cPtr,$strTable);
	}

	/**
	 * Sends a legacy LIST request and returns the server's raw textual listing. ARG1 is appended literally as a server-side listing argument. An empty ARG1 lists the current remote directory; a value such as *.txt sends a command such as LIST *.txt. ListOption is also appended literally before ARG1. For example, ListOption = "-a" and ARG1 equal to *.txt send LIST -a *.txt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetTextDirListing
	 * @param ?string $pattern
	 * @return ?string
	 */
	function getTextDirListing($pattern) {
		return CkFtp2_getTextDirListing($this->_cPtr,$pattern);
	}

	/**
	 * Sends a legacy LIST request and returns the server's raw textual listing. ARG1 is appended literally as a server-side listing argument. An empty ARG1 lists the current remote directory; a value such as *.txt sends a command such as LIST *.txt. ListOption is also appended literally before ARG1. For example, ListOption = "-a" and ARG1 equal to *.txt send LIST -a *.txt.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetTextDirListing
	 * @param ?string $pattern
	 * @return ?string
	 */
	function textDirListing($pattern) {
		return CkFtp2_textDirListing($this->_cPtr,$pattern);
	}

	/**
	 * Creates an asynchronous task to call the GetTextDirListing method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetTextDirListingAsync
	 * @param ?string $pattern
	 * @return ?CkTask
	 */
	function GetTextDirListingAsync($pattern) {
		return chilkat::_obj(CkFtp2_GetTextDirListingAsync($this->_cPtr,$pattern),'CkTask');
	}

	/**
	 * Retrieves a directory listing for ARG1 and returns parsed entries as an XML document rooted at <remoteDir>. Files are represented by <file> elements containing <name>, <size>, and available time elements; directories are represented by <dir> elements. <remoteDir>   <file>     <name>report.txt</name>     <size>1234</size>     <lastModTime full="20260727-065541" ... />   </file>   <dir lastModTime="20260727-065542" ...>archive</dir> </remoteDir> When ARG1 is empty, Chilkat uses the normal listing selection: MLSD when allowed and advertised, otherwise LIST or NLST according to the listing properties. When ARG1 is nonempty, Chilkat sends LIST ARG1, so wildcard matching and case sensitivity are server-dependent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetXmlDirListing
	 * @param ?string $pattern
	 * @return ?string
	 */
	function getXmlDirListing($pattern) {
		return CkFtp2_getXmlDirListing($this->_cPtr,$pattern);
	}

	/**
	 * Retrieves a directory listing for ARG1 and returns parsed entries as an XML document rooted at <remoteDir>. Files are represented by <file> elements containing <name>, <size>, and available time elements; directories are represented by <dir> elements. <remoteDir>   <file>     <name>report.txt</name>     <size>1234</size>     <lastModTime full="20260727-065541" ... />   </file>   <dir lastModTime="20260727-065542" ...>archive</dir> </remoteDir> When ARG1 is empty, Chilkat uses the normal listing selection: MLSD when allowed and advertised, otherwise LIST or NLST according to the listing properties. When ARG1 is nonempty, Chilkat sends LIST ARG1, so wildcard matching and case sensitivity are server-dependent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetXmlDirListing
	 * @param ?string $pattern
	 * @return ?string
	 */
	function xmlDirListing($pattern) {
		return CkFtp2_xmlDirListing($this->_cPtr,$pattern);
	}

	/**
	 * Creates an asynchronous task to call the GetXmlDirListing method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#GetXmlDirListingAsync
	 * @param ?string $pattern
	 * @return ?CkTask
	 */
	function GetXmlDirListingAsync($pattern) {
		return chilkat::_obj(CkFtp2_GetXmlDirListingAsync($this->_cPtr,$pattern),'CkTask');
	}

	/**
	 * Uploads the local file at ARG1 to the remote path at ARG2 as a sequence of data transfers, each containing up to ARG3 bytes. The first chunk is sent with STOR, which creates or replaces ARG2, and each later chunk is sent with APPE. This method addresses a specific FTP failure mode: during one very long data transfer, the control connection is idle and may be silently dropped by a firewall, NAT device, or server timeout. Completing the upload as multiple shorter transfers periodically uses the control channel.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LargeFileUpload
	 * @param ?string $localPath
	 * @param ?string $remotePath
	 * @param int $chunkSize
	 * @return bool
	 */
	function LargeFileUpload($localPath,$remotePath,$chunkSize) {
		return CkFtp2_LargeFileUpload($this->_cPtr,$localPath,$remotePath,$chunkSize);
	}

	/**
	 * Creates an asynchronous task to call the LargeFileUpload method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LargeFileUploadAsync
	 * @param ?string $localPath
	 * @param ?string $remotePath
	 * @param int $chunkSize
	 * @return ?CkTask
	 */
	function LargeFileUploadAsync($localPath,$remotePath,$chunkSize) {
		return chilkat::_obj(CkFtp2_LargeFileUploadAsync($this->_cPtr,$localPath,$remotePath,$chunkSize),'CkTask');
	}

	/**
	 * Loads the original caller object associated with the asynchronous Task in ARG1. This is an internal support method for Chilkat's task infrastructure and is not normally needed by application code.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkFtp2_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Authenticates an FTP control connection previously established by ConnectOnly. Chilkat sends USER, PASS, and ACCT when required, then performs the normal post-login initialization such as TYPE I, SYST, capability discovery, and UTF-8 setup. If authentication is rejected, the same connection cannot be retried by simply changing the credentials and calling this method again. Disconnect or reconnect with ConnectOnly before attempting another login.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LoginAfterConnectOnly
	 * @return bool
	 */
	function LoginAfterConnectOnly() {
		return CkFtp2_LoginAfterConnectOnly($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the LoginAfterConnectOnly method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#LoginAfterConnectOnlyAsync
	 * @return ?CkTask
	 */
	function LoginAfterConnectOnlyAsync() {
		return chilkat::_obj(CkFtp2_LoginAfterConnectOnlyAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Downloads files in the current remote directory that match ARG1 into the existing local directory at ARG2. Subdirectories are not traversed. The method returns the number of files downloaded, or -1 on failure. ARG2 must already exist; the method does not create it. Existing local files with matching names are replaced. Chilkat sends a server-side listing request for ARG1, so wildcard matching and case sensitivity depend on the FTP server and remote filesystem.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#MGetFiles
	 * @param ?string $remotePattern
	 * @param ?string $localDir
	 * @return int
	 */
	function MGetFiles($remotePattern,$localDir) {
		return CkFtp2_MGetFiles($this->_cPtr,$remotePattern,$localDir);
	}

	/**
	 * Creates an asynchronous task to call the MGetFiles method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#MGetFilesAsync
	 * @param ?string $remotePattern
	 * @param ?string $localDir
	 * @return ?CkTask
	 */
	function MGetFilesAsync($remotePattern,$localDir) {
		return chilkat::_obj(CkFtp2_MGetFilesAsync($this->_cPtr,$remotePattern,$localDir),'CkTask');
	}

	/**
	 * Uploads each local file matching ARG1 to the current remote directory. ARG1 may include a local directory and wildcard, such as C:/data/*.txt. Subdirectories are not traversed. Local wildcard behavior follows the local operating system; for example, Windows matching is case-insensitive. Only each matching file's basename is used as the remote filename. Existing remote files are replaced using normal STOR behavior.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#MPutFiles
	 * @param ?string $pattern
	 * @return int
	 */
	function MPutFiles($pattern) {
		return CkFtp2_MPutFiles($this->_cPtr,$pattern);
	}

	/**
	 * Creates an asynchronous task to call the MPutFiles method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#MPutFilesAsync
	 * @param ?string $pattern
	 * @return ?CkTask
	 */
	function MPutFilesAsync($pattern) {
		return chilkat::_obj(CkFtp2_MPutFilesAsync($this->_cPtr,$pattern),'CkTask');
	}

	/**
	 * Sends NLST with ARG1 appended literally and returns the names in XML form. An empty ARG1 sends NLST; a value such as *.txt sends NLST *.txt. <nlst>   <e>name1</e>   <e>name2</e> </nlst> The response is decoded using DirListingCharset. NLST normally returns names only; the XML does not distinguish files from directories and does not provide reliable sizes, times, ownership, or permissions. Wildcard matching and case sensitivity are server-dependent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#NlstXml
	 * @param ?string $remoteDirPattern
	 * @return ?string
	 */
	function nlstXml($remoteDirPattern) {
		return CkFtp2_nlstXml($this->_cPtr,$remoteDirPattern);
	}

	/**
	 * Creates an asynchronous task to call the NlstXml method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#NlstXmlAsync
	 * @param ?string $remoteDirPattern
	 * @return ?CkTask
	 */
	function NlstXmlAsync($remoteDirPattern) {
		return chilkat::_obj(CkFtp2_NlstXmlAsync($this->_cPtr,$remoteDirPattern),'CkTask');
	}

	/**
	 * Sends the FTP NOOP command. The server must return a positive FTP reply for the method to succeed. Whether NOOP is accepted before login is server-dependent. This method does not reconnect a closed session.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Noop
	 * @return bool
	 */
	function Noop() {
		return CkFtp2_Noop($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Noop method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#NoopAsync
	 * @return ?CkTask
	 */
	function NoopAsync() {
		return chilkat::_obj(CkFtp2_NoopAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Uploads the local filesystem file at ARG1 to the remote path at ARG2. Uses the FTP STOR command. A normal upload creates a missing remote file or truncates and replaces an existing one. Chilkat does not create missing remote parent directories and does not upload through a temporary remote name followed by an atomic rename.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFile
	 * @param ?string $localFilePath
	 * @param ?string $remoteFilePath
	 * @return bool
	 */
	function PutFile($localFilePath,$remoteFilePath) {
		return CkFtp2_PutFile($this->_cPtr,$localFilePath,$remoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the PutFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileAsync
	 * @param ?string $localFilePath
	 * @param ?string $remoteFilePath
	 * @return ?CkTask
	 */
	function PutFileAsync($localFilePath,$remoteFilePath) {
		return chilkat::_obj(CkFtp2_PutFileAsync($this->_cPtr,$localFilePath,$remoteFilePath),'CkTask');
	}

	/**
	 * Uploads the bytes contained in the BinData object in ARG1 to the remote path at ARG2. No character encoding or line-ending conversion is performed. Uses the FTP STOR command. A normal upload creates a missing remote file or truncates and replaces an existing one. Chilkat does not create missing remote parent directories and does not upload through a temporary remote name followed by an atomic rename.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileBd
	 * @param CkBinData $binData
	 * @param ?string $remoteFilePath
	 * @return bool
	 */
	function PutFileBd($binData,$remoteFilePath) {
		return CkFtp2_PutFileBd($this->_cPtr,$binData,$remoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the PutFileBd method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileBdAsync
	 * @param CkBinData $binData
	 * @param ?string $remoteFilePath
	 * @return ?CkTask
	 */
	function PutFileBdAsync($binData,$remoteFilePath) {
		return chilkat::_obj(CkFtp2_PutFileBdAsync($this->_cPtr,$binData,$remoteFilePath),'CkTask');
	}

	/**
	 * Creates or replaces the remote file at ARG1 with the bytes in ARG2. No character encoding or line-ending conversion is performed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileFromBinaryData
	 * @param ?string $remoteFilename
	 * @param CkByteData $content
	 * @return bool
	 */
	function PutFileFromBinaryData($remoteFilename,$content) {
		return CkFtp2_PutFileFromBinaryData($this->_cPtr,$remoteFilename,$content);
	}

	/**
	 * Creates an asynchronous task to call the PutFileFromBinaryData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileFromBinaryDataAsync
	 * @param ?string $remoteFilename
	 * @param CkByteData $content
	 * @return ?CkTask
	 */
	function PutFileFromBinaryDataAsync($remoteFilename,$content) {
		return chilkat::_obj(CkFtp2_PutFileFromBinaryDataAsync($this->_cPtr,$remoteFilename,$content),'CkTask');
	}

	/**
	 * Encodes the text in ARG2 using the character encoding named by ARG3 and uploads the resulting bytes to the remote file at ARG1. Uses the FTP STOR command. A normal upload creates a missing remote file or truncates and replaces an existing one. Chilkat does not create missing remote parent directories and does not upload through a temporary remote name followed by an atomic rename.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileFromTextData
	 * @param ?string $remoteFilename
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return bool
	 */
	function PutFileFromTextData($remoteFilename,$textData,$charset) {
		return CkFtp2_PutFileFromTextData($this->_cPtr,$remoteFilename,$textData,$charset);
	}

	/**
	 * Creates an asynchronous task to call the PutFileFromTextData method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileFromTextDataAsync
	 * @param ?string $remoteFilename
	 * @param ?string $textData
	 * @param ?string $charset
	 * @return ?CkTask
	 */
	function PutFileFromTextDataAsync($remoteFilename,$textData,$charset) {
		return chilkat::_obj(CkFtp2_PutFileFromTextDataAsync($this->_cPtr,$remoteFilename,$textData,$charset),'CkTask');
	}

	/**
	 * Encodes the text in the StringBuilder ARG1 using the charset named by ARG2 and uploads it to the remote path at ARG4. When ARG3 is _TRUE_, a byte-order mark is included when the selected encoding defines one. Uses the FTP STOR command. A normal upload creates a missing remote file or truncates and replaces an existing one. Chilkat does not create missing remote parent directories and does not upload through a temporary remote name followed by an atomic rename.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileSb
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @param bool $includeBom
	 * @param ?string $remoteFilePath
	 * @return bool
	 */
	function PutFileSb($sb,$charset,$includeBom,$remoteFilePath) {
		return CkFtp2_PutFileSb($this->_cPtr,$sb,$charset,$includeBom,$remoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the PutFileSb method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutFileSbAsync
	 * @param CkStringBuilder $sb
	 * @param ?string $charset
	 * @param bool $includeBom
	 * @param ?string $remoteFilePath
	 * @return ?CkTask
	 */
	function PutFileSbAsync($sb,$charset,$includeBom,$remoteFilePath) {
		return chilkat::_obj(CkFtp2_PutFileSbAsync($this->_cPtr,$sb,$charset,$includeBom,$remoteFilePath),'CkTask');
	}

	/**
	 * Executes the upload plan in ARG1. ARG2 is the local path of a completion log; each successful plan operation is recorded there. The absolute remote paths stored in the plan are used even if the current remote directory has changed since the plan was created. Local files are opened when this method executes, so changes made after CreatePlan are reflected in the upload.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutPlan
	 * @param ?string $plan
	 * @param ?string $alreadyDoneFilename
	 * @return bool
	 */
	function PutPlan($plan,$alreadyDoneFilename) {
		return CkFtp2_PutPlan($this->_cPtr,$plan,$alreadyDoneFilename);
	}

	/**
	 * Creates an asynchronous task to call the PutPlan method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutPlanAsync
	 * @param ?string $plan
	 * @param ?string $alreadyDoneFilename
	 * @return ?CkTask
	 */
	function PutPlanAsync($plan,$alreadyDoneFilename) {
		return chilkat::_obj(CkFtp2_PutPlanAsync($this->_cPtr,$plan,$alreadyDoneFilename),'CkTask');
	}

	/**
	 * Uploads the local directory tree rooted at ARG1 into the current remote directory, creating remote subdirectories as needed. The contents of ARG1 become children of the current remote directory. The file and directory filters in SyncMustMatch, SyncMustNotMatch, SyncMustMatchDir, and SyncMustNotMatchDir apply. Empty eligible local directories are created remotely. Existing remote files are overwritten with STOR.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutTree
	 * @param ?string $localDir
	 * @return bool
	 */
	function PutTree($localDir) {
		return CkFtp2_PutTree($this->_cPtr,$localDir);
	}

	/**
	 * Creates an asynchronous task to call the PutTree method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#PutTreeAsync
	 * @param ?string $localDir
	 * @return ?CkTask
	 */
	function PutTreeAsync($localDir) {
		return chilkat::_obj(CkFtp2_PutTreeAsync($this->_cPtr,$localDir),'CkTask');
	}

	/**
	 * Sends the raw FTP command text in ARG1 to the server. Use this for a command not otherwise exposed by the API. ARG1 contains the command and parameters but must not contain CR or LF characters; such input is rejected without sending anything. Chilkat considers the method successful only when the server returns a positive FTP reply. A 4xx or 5xx reply causes failure and remains available through LastReply.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Quote
	 * @param ?string $cmd
	 * @return bool
	 */
	function Quote($cmd) {
		return CkFtp2_Quote($this->_cPtr,$cmd);
	}

	/**
	 * Creates an asynchronous task to call the Quote method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#QuoteAsync
	 * @param ?string $cmd
	 * @return ?CkTask
	 */
	function QuoteAsync($cmd) {
		return chilkat::_obj(CkFtp2_QuoteAsync($this->_cPtr,$cmd),'CkTask');
	}

	/**
	 * Removes the remote directory at ARG1. FTP servers generally require the directory to be empty before it can be removed. A successful call does not refresh the cached indexed listing. Call ClearDirCache before relying on indexed listing methods.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RemoveRemoteDir
	 * @param ?string $remoteDirPath
	 * @return bool
	 */
	function RemoveRemoteDir($remoteDirPath) {
		return CkFtp2_RemoveRemoteDir($this->_cPtr,$remoteDirPath);
	}

	/**
	 * Creates an asynchronous task to call the RemoveRemoteDir method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RemoveRemoteDirAsync
	 * @param ?string $remoteDirPath
	 * @return ?CkTask
	 */
	function RemoveRemoteDirAsync($remoteDirPath) {
		return chilkat::_obj(CkFtp2_RemoveRemoteDirAsync($this->_cPtr,$remoteDirPath),'CkTask');
	}

	/**
	 * Renames or moves a remote file or directory from ARG1 to ARG2 using the FTP RNFR/RNTO sequence. Include directory components in ARG2 to move the item within the same FTP server. Chilkat does not perform a protective destination-existence check. If ARG2 already exists, one server may reject the rename while another may replace the destination. Cross-directory or cross-filesystem moves are also subject to server policy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RenameRemoteFile
	 * @param ?string $existingRemoteFilePath
	 * @param ?string $newRemoteFilePath
	 * @return bool
	 */
	function RenameRemoteFile($existingRemoteFilePath,$newRemoteFilePath) {
		return CkFtp2_RenameRemoteFile($this->_cPtr,$existingRemoteFilePath,$newRemoteFilePath);
	}

	/**
	 * Creates an asynchronous task to call the RenameRemoteFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#RenameRemoteFileAsync
	 * @param ?string $existingRemoteFilePath
	 * @param ?string $newRemoteFilePath
	 * @return ?CkTask
	 */
	function RenameRemoteFileAsync($existingRemoteFilePath,$newRemoteFilePath) {
		return chilkat::_obj(CkFtp2_RenameRemoteFileAsync($this->_cPtr,$existingRemoteFilePath,$newRemoteFilePath),'CkTask');
	}

	/**
	 * Sends the raw FTP command in ARG1 and returns the complete server reply. The command text is sent as UTF-8; CommandCharset is not applied. ARG1 must not contain CR or LF characters; such input is rejected without sending anything. A positive FTP reply is returned normally. A 4xx or 5xx reply is treated as method failure and sets LastMethodSuccess to _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SendCommand
	 * @param ?string $cmd
	 * @return ?string
	 */
	function sendCommand($cmd) {
		return CkFtp2_sendCommand($this->_cPtr,$cmd);
	}

	/**
	 * Creates an asynchronous task to call the SendCommand method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SendCommandAsync
	 * @param ?string $cmd
	 * @return ?CkTask
	 */
	function SendCommandAsync($cmd) {
		return chilkat::_obj(CkFtp2_SendCommandAsync($this->_cPtr,$cmd),'CkTask');
	}

	/**
	 * Enables MODE Z compression for subsequent data connections. Call this after connecting and only when HasModeZ is _TRUE_. Uploads, downloads, and directory listings are then transferred through a DEFLATE-compressed stream. Performance tradeoff: Compression can reduce network traffic for text and other compressible data, but may increase CPU usage and provide little benefit for already-compressed formats such as ZIP, JPEG, or MP4.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetModeZ
	 * @return bool
	 */
	function SetModeZ() {
		return CkFtp2_SetModeZ($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the SetModeZ method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetModeZAsync
	 * @return ?CkTask
	 */
	function SetModeZAsync() {
		return chilkat::_obj(CkFtp2_SetModeZAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Sets the oldest remote modification time eligible for DownloadTree. ARG1 is an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. Files older than the configured value are skipped. The value remains active for subsequent DownloadTree calls; it is not consumed after one operation. Avoid relying on exact equality at the boundary because legacy LIST timestamps can have reduced precision or ambiguous timezone interpretation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetOldestDateStr
	 * @param ?string $oldestDateTimeStr
	 */
	function SetOldestDateStr($oldestDateTimeStr) {
		CkFtp2_SetOldestDateStr($this->_cPtr,$oldestDateTimeStr);
	}

	/**
	 * Enables or disables a named compatibility option. Option-name matching is case-insensitive. Prefix a recognized name with No- to disable it. The recognized option Microsoft-TLS-1.2-Workaround changes data-channel TLS behavior for compatibility with an old Microsoft FTP server defect. It weakens protocol selection and should be used only for the affected legacy server. An unrecognized option name is rejected.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetOption
	 * @param ?string $option
	 * @return bool
	 */
	function SetOption($option) {
		return CkFtp2_SetOption($this->_cPtr,$option);
	}

	/**
	 * Deprecated. Copies the password from the SecureString in ARG1 for subsequent FTP authentication. Use SetSecurePassword instead.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetPassword
	 * @param CkSecureString $password
	 * @return bool
	 * @deprecated
	 */
	function SetPassword($password) {
		return CkFtp2_SetPassword($this->_cPtr,$password);
	}

	/**
	 * Sets the last-modified time of the remote file at ARG2. ARG1 is an RFC 822 date-time string, such as Fri, 21 Nov 1997 09:55:06 -0600. Chilkat converts ARG1 to UTC and sends a server-supported timestamp-setting extension. Depending on the server, the command may be MFMT YYYYMMDDhhmmss path or the nonstandard timestamp-setting form of MDTM YYYYMMDDhhmmss path. Precision is one second.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetRemoteFileDateTimeStr
	 * @param ?string $dateTimeStr
	 * @param ?string $remoteFilename
	 * @return bool
	 */
	function SetRemoteFileDateTimeStr($dateTimeStr,$remoteFilename) {
		return CkFtp2_SetRemoteFileDateTimeStr($this->_cPtr,$dateTimeStr,$remoteFilename);
	}

	/**
	 * Creates an asynchronous task to call the SetRemoteFileDateTimeStr method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetRemoteFileDateTimeStrAsync
	 * @param ?string $dateTimeStr
	 * @param ?string $remoteFilename
	 * @return ?CkTask
	 */
	function SetRemoteFileDateTimeStrAsync($dateTimeStr,$remoteFilename) {
		return chilkat::_obj(CkFtp2_SetRemoteFileDateTimeStrAsync($this->_cPtr,$dateTimeStr,$remoteFilename),'CkTask');
	}

	/**
	 * Sets the last-modified time of the remote file at ARG2 using the CkDateTime in ARG1. Chilkat converts the value to UTC and sends a server-supported timestamp-setting extension. Depending on the server, the command may be MFMT YYYYMMDDhhmmss path or the nonstandard timestamp-setting form of MDTM YYYYMMDDhhmmss path. Precision is one second.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetRemoteFileDt
	 * @param CkDateTime $dt
	 * @param ?string $remoteFilename
	 * @return bool
	 */
	function SetRemoteFileDt($dt,$remoteFilename) {
		return CkFtp2_SetRemoteFileDt($this->_cPtr,$dt,$remoteFilename);
	}

	/**
	 * Creates an asynchronous task to call the SetRemoteFileDt method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetRemoteFileDtAsync
	 * @param CkDateTime $dt
	 * @param ?string $remoteFilename
	 * @return ?CkTask
	 */
	function SetRemoteFileDtAsync($dt,$remoteFilename) {
		return chilkat::_obj(CkFtp2_SetRemoteFileDtAsync($this->_cPtr,$dt,$remoteFilename),'CkTask');
	}

	/**
	 * Copies the password from the SecureString in ARG1 for subsequent FTP authentication. This avoids exposing the password as an ordinary immutable language string. It is equivalent to setting Password for login behavior.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetSecurePassword
	 * @param CkSecureString $password
	 * @return bool
	 */
	function SetSecurePassword($password) {
		return CkFtp2_SetSecurePassword($this->_cPtr,$password);
	}

	/**
	 * Adds a required match against the FTP server certificate. ARG1 selects the certificate field and ARG2 specifies the required value. Supported field names include SubjectDN, SubjectCN, IssuerDN, IssuerCN, and SAN. Wildcards such as * may be used in ARG2. Requirement-value matching is case-sensitive. Multiple calls add requirements that are combined with logical AND; every requirement must match.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetSslCertRequirement
	 * @param ?string $reqName
	 * @param ?string $reqValue
	 */
	function SetSslCertRequirement($reqName,$reqValue) {
		CkFtp2_SetSslCertRequirement($this->_cPtr,$reqName,$reqValue);
	}

	/**
	 * Configures the Cert in ARG1 as the client certificate for FTPS servers that request mutual TLS authentication. The certificate must have access to its private key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetSslClientCert
	 * @param CkCert $cert
	 * @return bool
	 */
	function SetSslClientCert($cert) {
		return CkFtp2_SetSslClientCert($this->_cPtr,$cert);
	}

	/**
	 * Configures a client certificate and private key from PEM data or a PEM file. ARG1 may contain the PEM text or the local filesystem path of a PEM file. ARG2 is the private-key password, or an empty string when the key is not encrypted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetSslClientCertPem
	 * @param ?string $pemDataOrFilename
	 * @param ?string $pemPassword
	 * @return bool
	 */
	function SetSslClientCertPem($pemDataOrFilename,$pemPassword) {
		return CkFtp2_SetSslClientCertPem($this->_cPtr,$pemDataOrFilename,$pemPassword);
	}

	/**
	 * Loads a client certificate and private key from the local PFX/P12 file at ARG1. ARG2 is the file password. The certificate is used when the FTPS server requests mutual TLS authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetSslClientCertPfx
	 * @param ?string $pfxFilename
	 * @param ?string $pfxPassword
	 * @return bool
	 */
	function SetSslClientCertPfx($pfxFilename,$pfxPassword) {
		return CkFtp2_SetSslClientCertPfx($this->_cPtr,$pfxFilename,$pfxPassword);
	}

	/**
	 * Sets the FTP representation type to ASCII for subsequent transfers. In ASCII mode, the FTP endpoints may translate line endings, and CrlfMode can apply additional conversion on downloads. Use carefully: ASCII mode is intended for text. Use SetTypeBinary for executables, archives, images, encrypted data, and any file whose bytes must remain unchanged.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetTypeAscii
	 * @return bool
	 */
	function SetTypeAscii() {
		return CkFtp2_SetTypeAscii($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the SetTypeAscii method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetTypeAsciiAsync
	 * @return ?CkTask
	 */
	function SetTypeAsciiAsync() {
		return chilkat::_obj(CkFtp2_SetTypeAsciiAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Sets the FTP representation type to binary (TYPE I) for subsequent transfers. Binary mode preserves file bytes and is the appropriate default for nearly all modern file types.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetTypeBinary
	 * @return bool
	 */
	function SetTypeBinary() {
		return CkFtp2_SetTypeBinary($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the SetTypeBinary method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SetTypeBinaryAsync
	 * @return ?CkTask
	 */
	function SetTypeBinaryAsync() {
		return chilkat::_obj(CkFtp2_SetTypeBinaryAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Sends a server-specific SITE command. ARG1 contains the text following SITE, for example RECFM=FB LRECL=600. SITE subcommands are defined by the server, not by the FTP standard, and may require special permissions.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Site
	 * @param ?string $siteCommand
	 * @return bool
	 */
	function Site($siteCommand) {
		return CkFtp2_Site($this->_cPtr,$siteCommand);
	}

	/**
	 * Creates an asynchronous task to call the Site method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SiteAsync
	 * @param ?string $siteCommand
	 * @return ?CkTask
	 */
	function SiteAsync($siteCommand) {
		return chilkat::_obj(CkFtp2_SiteAsync($this->_cPtr,$siteCommand),'CkTask');
	}

	/**
	 * Blocks the calling thread for ARG1 milliseconds. This is a utility method and performs no FTP network operation.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SleepMs
	 * @param int $millisec
	 */
	function SleepMs($millisec) {
		CkFtp2_SleepMs($this->_cPtr,$millisec);
	}

	/**
	 * Sends the FTP STAT command and returns the server's reply. Depending on the server and session state, the reply may contain status information or a directory listing. Support, accepted arguments, and whether the command is permitted before login are server-dependent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Stat
	 * @return ?string
	 */
	function ck_stat() {
		return CkFtp2_ck_stat($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Stat method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#StatAsync
	 * @return ?CkTask
	 */
	function StatAsync() {
		return chilkat::_obj(CkFtp2_StatAsync($this->_cPtr),'CkTask');
	}

	/**
	 * Recursively compares the remote tree rooted at the current remote directory with the local tree rooted at ARG1 and deletes remote files that have no corresponding local file. The synchronization file and directory filters apply. Empty remote directories are not deleted. Destructive synchronization: This method deletes server data. Verify the current remote directory, local root, and synchronization filters before calling it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncDeleteRemote
	 * @param ?string $localRoot
	 * @return bool
	 */
	function SyncDeleteRemote($localRoot) {
		return CkFtp2_SyncDeleteRemote($this->_cPtr,$localRoot);
	}

	/**
	 * Creates an asynchronous task to call the SyncDeleteRemote method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncDeleteRemoteAsync
	 * @param ?string $localRoot
	 * @return ?CkTask
	 */
	function SyncDeleteRemoteAsync($localRoot) {
		return chilkat::_obj(CkFtp2_SyncDeleteRemoteAsync($this->_cPtr,$localRoot),'CkTask');
	}

	/**
	 * Synchronizes files from the current remote directory into the local directory at ARG1 without descending into subdirectories. ARG2 selects the same download modes documented for SyncLocalTree. The filters are case-insensitive and match basenames. In mode 99, only unmatched remote files in the current directory are deleted; nested directories are not traversed and their contents are not examined. Extra local files are not deleted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncLocalDir
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return bool
	 */
	function SyncLocalDir($localRoot,$mode) {
		return CkFtp2_SyncLocalDir($this->_cPtr,$localRoot,$mode);
	}

	/**
	 * Creates an asynchronous task to call the SyncLocalDir method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncLocalDirAsync
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return ?CkTask
	 */
	function SyncLocalDirAsync($localRoot,$mode) {
		return chilkat::_obj(CkFtp2_SyncLocalDirAsync($this->_cPtr,$localRoot,$mode),'CkTask');
	}

	/**
	 * Synchronizes the remote tree rooted at the current remote directory into the local directory at ARG1. ARG2 selects which files are downloaded or deleted. ModeAction 0Download all selected files. 1Download files missing locally. 2Download files that are missing locally or newer on the server. 3Download only files that already exist locally and are newer on the server. 5Download files that are missing locally or have a different size. 6Download files that are missing, have a different size, or are newer on the server. 99Do not download; delete remote files that do not exist locally.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncLocalTree
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return bool
	 */
	function SyncLocalTree($localRoot,$mode) {
		return CkFtp2_SyncLocalTree($this->_cPtr,$localRoot,$mode);
	}

	/**
	 * Creates an asynchronous task to call the SyncLocalTree method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncLocalTreeAsync
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return ?CkTask
	 */
	function SyncLocalTreeAsync($localRoot,$mode) {
		return chilkat::_obj(CkFtp2_SyncLocalTreeAsync($this->_cPtr,$localRoot,$mode),'CkTask');
	}

	/**
	 * Synchronizes the local tree rooted at ARG1 into the current remote directory. ARG2 selects which files are uploaded. ModeAction 0Upload all selected files. 1Upload files missing on the server. 2Upload files that are missing or newer locally. 3Upload only files that already exist remotely and are newer locally. 4Upload files that are missing or have a different size. 5Upload files that are missing, have a different size, or are newer locally.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncRemoteTree
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return bool
	 */
	function SyncRemoteTree($localRoot,$mode) {
		return CkFtp2_SyncRemoteTree($this->_cPtr,$localRoot,$mode);
	}

	/**
	 * Creates an asynchronous task to call the SyncRemoteTree method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncRemoteTreeAsync
	 * @param ?string $localRoot
	 * @param int $mode
	 * @return ?CkTask
	 */
	function SyncRemoteTreeAsync($localRoot,$mode) {
		return chilkat::_obj(CkFtp2_SyncRemoteTreeAsync($this->_cPtr,$localRoot,$mode),'CkTask');
	}

	/**
	 * Provides the upload synchronization behavior and valid modes of SyncRemoteTree with traversal and preview controls. ARG1 is the local root, ARG2 is the upload synchronization mode, ARG3 controls whether subdirectories are traversed, and ARG4 selects preview-only mode. File and directory filters apply in both normal and preview operation. When ARG4 is _TRUE_, files are not uploaded and the absolute candidate file paths are placed in SyncPreview. Required remote directories may still be created during preview. If no files qualify, the preview is empty.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncRemoteTree2
	 * @param ?string $localDirPath
	 * @param int $mode
	 * @param bool $bDescend
	 * @param bool $bPreviewOnly
	 * @return bool
	 */
	function SyncRemoteTree2($localDirPath,$mode,$bDescend,$bPreviewOnly) {
		return CkFtp2_SyncRemoteTree2($this->_cPtr,$localDirPath,$mode,$bDescend,$bPreviewOnly);
	}

	/**
	 * Creates an asynchronous task to call the SyncRemoteTree2 method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SyncRemoteTree2Async
	 * @param ?string $localDirPath
	 * @param int $mode
	 * @param bool $bDescend
	 * @param bool $bPreviewOnly
	 * @return ?CkTask
	 */
	function SyncRemoteTree2Async($localDirPath,$mode,$bDescend,$bPreviewOnly) {
		return chilkat::_obj(CkFtp2_SyncRemoteTree2Async($this->_cPtr,$localDirPath,$mode,$bDescend,$bPreviewOnly),'CkTask');
	}

	/**
	 * Sends SYST and returns the server's operating-system type reply. Chilkat can use this information when interpreting legacy directory listings. Whether the command is accepted before authentication is server-dependent.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#Syst
	 * @return ?string
	 */
	function syst() {
		return CkFtp2_syst($this->_cPtr);
	}

	/**
	 * Creates an asynchronous task to call the Syst method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkFtp2Ref.html#SystAsync
	 * @return ?CkTask
	 */
	function SystAsync() {
		return chilkat::_obj(CkFtp2_SystAsync($this->_cPtr),'CkTask');
	}
}
