<?php

/* CkJwe.php -- Chilkat PHP extension proxy class CkJwe.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Create and decrypt JSON Web Encryption payloads in compact or JSON form.
 *
 * Chilkat.Jwe is the Chilkat class for creating and decrypting JSON Web Encryption objects. It supports text and binary payloads, compact serialization, flattened JSON serialization, general JSON serialization, protected and unprotected headers, Additional Authenticated Data, single or multiple recipients, and several key-management approaches including RSA, AES key wrapping, direct symmetric encryption, AES-GCM key wrapping, and PBES2 password-based key wrapping.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html
 */
class CkJwe {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkJwe') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkJwe();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkJwe_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkJwe_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkJwe_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkJwe_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkJwe_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkJwe_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkJwe_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkJwe_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkJwe_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkJwe_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkJwe_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkJwe_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkJwe_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of recipients in the loaded JWE or in the JWE currently being configured. Recipient indexes are zero-based and must be in the range 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#NumRecipients
	 * @return int
	 */
	function get_NumRecipients() {
		return CkJwe_get_NumRecipients($this->_cPtr);
	}

	/**
	 * Controls whether encryption prefers JWE Compact Serialization. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#PreferCompact
	 * @return bool
	 */
	function get_PreferCompact() {
		return CkJwe_get_PreferCompact($this->_cPtr);
	}

	/**
	 * Controls whether encryption prefers JWE Compact Serialization. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#PreferCompact
	 * @param bool $newVal
	 */
	function put_PreferCompact($newVal) {
		CkJwe_put_PreferCompact($this->_cPtr,$newVal);
	}

	/**
	 * Controls which JWE JSON Serialization is produced for a single recipient. The default is _TRUE_. It has no effect when compact form is selected or when multiple recipients require general JSON.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#PreferFlattened
	 * @return bool
	 */
	function get_PreferFlattened() {
		return CkJwe_get_PreferFlattened($this->_cPtr);
	}

	/**
	 * Controls which JWE JSON Serialization is produced for a single recipient. The default is _TRUE_. It has no effect when compact form is selected or when multiple recipients require general JSON.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#PreferFlattened
	 * @param bool $newVal
	 */
	function put_PreferFlattened($newVal) {
		CkJwe_put_PreferFlattened($this->_cPtr,$newVal);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal JWE creation and decryption. No public keywords documented: This reference does not currently define any public Jwe.UncommonOptions keywords. Leave the property empty unless a Chilkat release note, example, or support response provides the exact option name, syntax, and intended use.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkJwe_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal JWE creation and decryption. No public keywords documented: This reference does not currently define any public Jwe.UncommonOptions keywords. Leave the property empty unless a Chilkat release note, example, or support response provides the exact option name, syntax, and intended use.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkJwe_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal JWE creation and decryption. No public keywords documented: This reference does not currently define any public Jwe.UncommonOptions keywords. Leave the property empty unless a Chilkat release note, example, or support response provides the exact option name, syntax, and intended use.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkJwe_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkJwe_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkJwe_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkJwe_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkJwe_version($this->_cPtr);
	}

	/**
	 * Decrypts recipient ARG1 from the currently loaded JWE and returns the plaintext decoded using charset ARG2. The valid recipient index range is 0 through NumRecipients - 1. Call LoadJwe or LoadJweSb. Inspect headers and select an acceptable recipient and algorithm under application policy. Set the required private key, wrapping key, or password at the same index. Call this method. Loaded AAD is used automatically. Authentication is completed before plaintext is returned. For zip:DEF, decompression occurs only after successful authentication, and no partial decompressed output is returned. Charset errors: Charset conversion errors cause Decrypt to fail.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#Decrypt
	 * @param int $index
	 * @param ?string $charset
	 * @return ?string
	 */
	function decrypt($index,$charset) {
		return CkJwe_decrypt($this->_cPtr,$index,$charset);
	}

	/**
	 * Decrypts recipient ARG1 and appends the authenticated plaintext bytes to ARG2. No character-set conversion is performed. Loaded AAD is used automatically, and zip:DEF is decompressed only after authentication succeeds. Atomic destination behavior: On success, plaintext bytes are appended and existing bytes are preserved. On failure, ARG2 is left unchanged; no partial or unauthenticated plaintext is delivered.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#DecryptBd
	 * @param int $index
	 * @param CkBinData $bd
	 * @return bool
	 */
	function DecryptBd($index,$bd) {
		return CkJwe_DecryptBd($this->_cPtr,$index,$bd);
	}

	/**
	 * Decrypts recipient ARG1, authenticates the JWE, decodes the plaintext bytes using charset ARG2, and appends the resulting text to ARG3. Loaded AAD is used automatically, and zip:DEF is decompressed only after authentication succeeds. Atomic destination behavior: On success, plaintext is appended and existing text is preserved. On failure, ARG3 is left unchanged; no unauthenticated or partially decoded plaintext is delivered. Charset handling: Invalid byte sequences for ARG2 cause failure. Use DecryptBd for raw binary plaintext.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#DecryptSb
	 * @param int $index
	 * @param ?string $charset
	 * @param CkStringBuilder $contentSb
	 * @return bool
	 */
	function DecryptSb($index,$charset,$contentSb) {
		return CkJwe_DecryptSb($this->_cPtr,$index,$charset,$contentSb);
	}

	/**
	 * Encrypts ARG1 and returns the resulting compact, flattened JSON, or general JSON JWE. ARG1 is converted to bytes using charset ARG2. If the text cannot be represented in that charset, the method fails. Before calling this method, configure the protected header and each recipient's required public key, wrapping key, or password. The selected alg and enc values determine the required key type and length; final compatibility checks occur during this call. Chilkat generates the content-encryption IV automatically: 12 bytes for AES-GCM content encryption and 16 bytes for AES-CBC-HMAC content encryption. Serialization is selected by NumRecipients, PreferCompact, PreferFlattened, and whether a shared unprotected header is present. A single per-recipient header is omitted if compact serialization is selected. Configured AAD is also ignored in compact form and causes a warning. Generated state persists: Encryption replaces any previously loaded JWE and can add epk, AES-GCM wrapping iv/tag, and other generated parameters to the stored headers. Use a fresh object or reset headers for independent operations, especially to prevent AES-GCM wrapping-IV reuse. Compression and extensions: zip:DEF compresses before encryption. Unknown zip values are ignored. The class does not enforce crit; applications must apply any critical-extension policy themselves.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#Encrypt
	 * @param ?string $content
	 * @param ?string $charset
	 * @return ?string
	 */
	function encrypt($content,$charset) {
		return CkJwe_encrypt($this->_cPtr,$content,$charset);
	}

	/**
	 * Encrypts the exact bytes in ARG1 and appends the resulting JWE text to ARG2. No character-set conversion is performed, making this the appropriate method for arbitrary binary content. The same recipient configuration, serialization-selection, AAD, compression, and generated-header rules described for Encrypt apply. Failure behavior: Check the _TRUE_/_FALSE_ return before using ARG2. A failure does not append a partial JWE.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#EncryptBd
	 * @param CkBinData $contentBd
	 * @param CkStringBuilder $jweSb
	 * @return bool
	 */
	function EncryptBd($contentBd,$jweSb) {
		return CkJwe_EncryptBd($this->_cPtr,$contentBd,$jweSb);
	}

	/**
	 * Encodes the text in ARG1 using charset ARG2, encrypts it, and appends the resulting JWE text to ARG3. Existing text in ARG3 is preserved. If the input contains characters that cannot be represented in ARG2, the call fails. The same recipient configuration, serialization-selection, AAD, compression, and generated-header rules described for Encrypt apply. Failure behavior: Check the _TRUE_/_FALSE_ return before using ARG3. A failure does not append a partial JWE.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#EncryptSb
	 * @param CkStringBuilder $contentSb
	 * @param ?string $charset
	 * @param CkStringBuilder $jweSb
	 * @return bool
	 */
	function EncryptSb($contentSb,$charset,$jweSb) {
		return CkJwe_EncryptSb($this->_cPtr,$contentSb,$charset,$jweSb);
	}

	/**
	 * Searches only the per-recipient unprotected headers for a member named ARG1 whose string value equals ARG2. ARG3 controls whether both the member-name and value comparisons are case-sensitive. Returns the zero-based index of the first matching recipient, or -1 when no match is found. A value of -1 is also returned when the object cannot be searched; treat it simply as “no matching recipient found.” The protected and shared unprotected headers are not searched. A common use is to load a JWE, find a recipient by kid, set the corresponding key or password at the returned index, and decrypt using that same index. Routing only: Per-recipient headers are unprotected and attacker-controlled. Validate acceptable key identifiers and algorithms under application policy, and trust the result only after authenticated decryption succeeds.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#FindRecipient
	 * @param ?string $paramName
	 * @param ?string $paramValue
	 * @param bool $caseSensitive
	 * @return int
	 */
	function FindRecipient($paramName,$paramValue,$caseSensitive) {
		return CkJwe_FindRecipient($this->_cPtr,$paramName,$paramValue,$caseSensitive);
	}

	/**
	 * Copies the entire currently loaded JWE structure into ARG1 as a JSON object. This is the full JWE representation, not a decoded or merged JOSE header. The object can contain protected in its original base64url-encoded form, unprotected, header, recipients, encrypted_key, iv, ciphertext, tag, aad, and any retained unknown top-level members. A JWE originally loaded from compact form is also exposed in this JSON-object representation. On success, ARG1 is overwritten. The method requires a loaded JWE; on failure, ARG1 is left unchanged. Use GetProtectedHeader to obtain the decoded protected-header JSON. No effective-header merge: This method does not decode protected and does not combine protected, shared unprotected, and per-recipient members. Applications must inspect the relevant objects explicitly.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#GetHeader
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetHeader($json) {
		return CkJwe_GetHeader($this->_cPtr,$json);
	}

	/**
	 * Copies the decoded shared JWE Protected Header into ARG1. Only the protected header is returned; it is not merged with the shared unprotected header or any per-recipient header. The method succeeds after a protected header was configured with SetProtectedHeader, or after a JWE containing a protected header was loaded. On success ARG1 is overwritten; on failure it is left unchanged. Use this method to inspect alg, enc, zip, crit, and other protected parameters before choosing a key or applying application policy. Authentication timing: The header can be decoded before a key is supplied, but its integrity is established only when authenticated decryption succeeds. The Jwe class does not enforce crit; applications requiring critical-extension processing must do so themselves.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#GetProtectedHeader
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetProtectedHeader($json) {
		return CkJwe_GetProtectedHeader($this->_cPtr,$json);
	}

	/**
	 * Loads ARG1 as JWE Compact Serialization, Flattened JWE JSON Serialization, or General JWE JSON Serialization. Loading parses structure and headers but does not decrypt, authenticate, or apply application algorithm policy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LoadJwe
	 * @param ?string $jwe
	 * @return bool
	 */
	function LoadJwe($jwe) {
		return CkJwe_LoadJwe($this->_cPtr,$jwe);
	}

	/**
	 * Loads the complete JWE text in ARG1. It accepts compact, flattened JSON, or general JSON serialization and otherwise follows all parsing, state-replacement, deferred-validation, and security behavior documented for LoadJwe. The source StringBuilder is read during the call and does not need to remain alive afterward.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#LoadJweSb
	 * @param CkStringBuilder $sb
	 * @return bool
	 */
	function LoadJweSb($sb) {
		return CkJwe_LoadJweSb($this->_cPtr,$sb);
	}

	/**
	 * Sets external Additional Authenticated Data (AAD) for a JWE being encrypted. ARG1 is encoded using charset ARG2, and the resulting bytes replace any previously configured AAD. Chilkat copies the bytes. Passing an empty string clears AAD. If encoding or another validation step fails, the previous AAD remains unchanged. In JSON serialization, AAD is base64url-encoded into the top-level aad member and included in the authentication calculation. It is visible but cannot be modified without causing authentication failure. Compact serialization: AAD does not force JSON serialization. If compact form is selected, Chilkat ignores the configured AAD and logs a warning. Set PreferCompact to _FALSE_ when AAD must be emitted. Decryption: After loading a JSON JWE, Chilkat automatically uses its serialized aad value. Do not call this method merely to repeat AAD before decryption. Exact bytes: Character encoding is part of the AAD definition. Use SetAadBd when the protocol defines exact binary AAD.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetAad
	 * @param ?string $aad
	 * @param ?string $charset
	 * @return bool
	 */
	function SetAad($aad,$charset) {
		return CkJwe_SetAad($this->_cPtr,$aad,$charset);
	}

	/**
	 * Sets external Additional Authenticated Data (AAD) to the exact bytes in ARG1. Chilkat copies the bytes, replacing previous AAD. Passing empty BinData clears AAD and causes no aad member to be emitted. If the setter fails, the previous AAD remains unchanged. In JSON serialization, the bytes are base64url-encoded into the top-level aad member and authenticated without being encrypted. Compact serialization: AAD does not force JSON output. If compact form is selected, the configured AAD is ignored and a warning is logged. Set PreferCompact to _FALSE_ when AAD is required. Decryption: A loaded JSON JWE supplies its own AAD automatically; do not set it again before decrypting.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetAadBd
	 * @param CkBinData $aad
	 * @return bool
	 */
	function SetAadBd($aad) {
		return CkJwe_SetAadBd($this->_cPtr,$aad);
	}

	/**
	 * Sets the PBES2 password for recipient ARG1. The valid index range is 0 through 1000. Chilkat converts ARG2 to UTF-8, copies it, and replaces only the password at that index.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetPassword
	 * @param int $index
	 * @param ?string $password
	 * @return bool
	 */
	function SetPassword($index,$password) {
		return CkJwe_SetPassword($this->_cPtr,$index,$password);
	}

	/**
	 * Sets the private key used to decrypt recipient ARG1. The valid index range is 0 through 1000. Chilkat copies ARG2 and releases any previously stored private key at that index; the source object may be discarded after this call.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetPrivateKey
	 * @param int $index
	 * @param CkPrivateKey $privKey
	 * @return bool
	 */
	function SetPrivateKey($index,$privKey) {
		return CkJwe_SetPrivateKey($this->_cPtr,$index,$privKey);
	}

	/**
	 * Sets the shared JWE Protected Header from ARG1. Chilkat copies the source JsonObject, replacing the previously configured protected header only if the call succeeds. The caller does not need to keep ARG1 alive or call this method again after modifying the original object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetProtectedHeader
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetProtectedHeader($json) {
		return CkJwe_SetProtectedHeader($this->_cPtr,$json);
	}

	/**
	 * Sets the public key used to encrypt for recipient ARG1. The valid index range is 0 through 1000. Chilkat copies ARG2 and replaces only the public key at that index; the source object need not remain alive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetPublicKey
	 * @param int $index
	 * @param CkPublicKey $pubKey
	 * @return bool
	 */
	function SetPublicKey($index,$pubKey) {
		return CkJwe_SetPublicKey($this->_cPtr,$index,$pubKey);
	}

	/**
	 * Sets the per-recipient unprotected header for zero-based recipient index ARG1. The valid index range is 0 through 1000. Chilkat copies ARG2, and calling this method again at the same index replaces only that recipient header. In flattened JSON this becomes the top-level header member. In general JSON it becomes the header member of the corresponding element in the recipients array. An empty object is retained and emitted as "header":{}; it does not remove the recipient, and no recipient-removal method exists. Typical members include kid, recipient-specific alg, PBES2 p2s/p2c, or AES-GCM key-wrap iv/tag. Use the same index with the appropriate key or password setter. Compact serialization: A per-recipient header alone does not force JSON output. If compact serialization is selected for a single recipient, this header is omitted. Set PreferCompact to _FALSE_ when it must be serialized. Recipient counting: Before encryption, NumRecipients is determined by configured public keys, wrapping keys, and passwords. A recipient header by itself does not increase that count. Not authenticated: Per-recipient headers are unprotected. Treat values such as kid as routing hints and accept them only under application policy and after authenticated decryption succeeds.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetRecipientHeader
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetRecipientHeader($index,$json) {
		return CkJwe_SetRecipientHeader($this->_cPtr,$index,$json);
	}

	/**
	 * Sets the shared JWE Unprotected Header from ARG1. Chilkat copies the source JsonObject; a successful call replaces the previous shared unprotected header, while a failed call preserves it. The header applies to every recipient and can be represented only by JWE JSON Serialization. Setting it forces JSON output even when ARG1 is empty, in which case the output contains "unprotected":{}. Not authenticated: This header is neither encrypted nor integrity-protected. Do not place algorithm-policy, identity, authorization, or trusted key-location decisions here unless the application independently validates them. Loading a JWE: LoadJwe and LoadJweSb discard this configured header and replace all header state with the loaded message.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetUnprotectedHeader
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetUnprotectedHeader($json) {
		return CkJwe_SetUnprotectedHeader($this->_cPtr,$json);
	}

	/**
	 * Sets the symmetric key for recipient ARG1. ARG2 contains the key bytes encoded according to ARG3, such as hex or base64. The valid index range is 0 through 1000. Chilkat decodes and copies the key. A decoding failure returns _FALSE_ and leaves the previous key at that index unchanged. Algorithm-specific key length is validated during encryption or decryption.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJweRef.html#SetWrappingKey
	 * @param int $index
	 * @param ?string $encodedKey
	 * @param ?string $encoding
	 * @return bool
	 */
	function SetWrappingKey($index,$encodedKey,$encoding) {
		return CkJwe_SetWrappingKey($this->_cPtr,$index,$encodedKey,$encoding);
	}
}
