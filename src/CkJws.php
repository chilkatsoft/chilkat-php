<?php

/* CkJws.php -- Chilkat PHP extension proxy class CkJws.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Create, serialize, load, inspect, and validate JSON Web Signatures.
 *
 * Chilkat.Jws is the Chilkat class for creating and validating JSON Web Signatures. It supports compact serialization, flattened JSON serialization, general JSON serialization, single or multiple signatures, protected and unprotected headers, text and binary payloads, detached or embedded payload workflows, and signature algorithms such as HMAC, RSA, ECDSA, and RSA-PSS.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html
 */
class CkJws {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkJws') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkJws();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkJws_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkJws_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkJws_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkJws_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkJws_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkJws_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkJws_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkJws_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkJws_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkJws_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkJws_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkJws_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkJws_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of signature slots represented by this object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#NumSignatures
	 * @return int
	 */
	function get_NumSignatures() {
		return CkJws_get_NumSignatures($this->_cPtr);
	}

	/**
	 * Controls whether Chilkat uses JWS Compact Serialization for a single signature. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#PreferCompact
	 * @return bool
	 */
	function get_PreferCompact() {
		return CkJws_get_PreferCompact($this->_cPtr);
	}

	/**
	 * Controls whether Chilkat uses JWS Compact Serialization for a single signature. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#PreferCompact
	 * @param bool $newVal
	 */
	function put_PreferCompact($newVal) {
		CkJws_put_PreferCompact($this->_cPtr,$newVal);
	}

	/**
	 * Controls which JSON form is used when creating a JWS with exactly one signature and PreferCompact is _FALSE_. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#PreferFlattened
	 * @return bool
	 */
	function get_PreferFlattened() {
		return CkJws_get_PreferFlattened($this->_cPtr);
	}

	/**
	 * Controls which JSON form is used when creating a JWS with exactly one signature and PreferCompact is _FALSE_. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#PreferFlattened
	 * @param bool $newVal
	 */
	function put_PreferFlattened($newVal) {
		CkJws_put_PreferFlattened($this->_cPtr,$newVal);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options. The default is an empty string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkJws_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options. The default is an empty string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkJws_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options. The default is an empty string.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkJws_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkJws_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkJws_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkJws_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkJws_version($this->_cPtr);
	}

	/**
	 * Creates and returns a JWS from the current payload, headers, and signing keys.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#CreateJws
	 * @return ?string
	 */
	function createJws() {
		return CkJws_createJws($this->_cPtr);
	}

	/**
	 * Creates a JWS using the same rules as CreateJws and writes it to ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#CreateJwsSb
	 * @param CkStringBuilder $sbJws
	 * @return bool
	 */
	function CreateJwsSb($sbJws) {
		return CkJws_CreateJwsSb($this->_cPtr,$sbJws);
	}

	/**
	 * Returns the loaded payload as text after decoding its bytes using charset ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetPayload
	 * @param ?string $charset
	 * @return ?string
	 */
	function getPayload($charset) {
		return CkJws_getPayload($this->_cPtr,$charset);
	}

	/**
	 * Returns the loaded payload as text after decoding its bytes using charset ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetPayload
	 * @param ?string $charset
	 * @return ?string
	 */
	function payload($charset) {
		return CkJws_payload($this->_cPtr,$charset);
	}

	/**
	 * Writes the exact loaded JWS payload bytes to ARG1 without charset conversion.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetPayloadBd
	 * @param CkBinData $binData
	 * @return bool
	 */
	function GetPayloadBd($binData) {
		return CkJws_GetPayloadBd($this->_cPtr,$binData);
	}

	/**
	 * Decodes the loaded payload bytes using charset ARG1 and writes the resulting text to ARG2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetPayloadSb
	 * @param ?string $charset
	 * @param CkStringBuilder $sbPayload
	 * @return bool
	 */
	function GetPayloadSb($charset,$sbPayload) {
		return CkJws_GetPayloadSb($this->_cPtr,$charset,$sbPayload);
	}

	/**
	 * Copies the decoded protected header of signature ARG1 into ARG2. The valid zero-based index range is 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetProtectedH
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetProtectedH($index,$json) {
		return CkJws_GetProtectedH($this->_cPtr,$index,$json);
	}

	/**
	 * Returns a newly allocated clone of the decoded protected header for signature ARG1. The valid index range is 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetProtectedHeader
	 * @param int $index
	 * @return ?CkJsonObject
	 * @deprecated
	 */
	function GetProtectedHeader($index) {
		return chilkat::_obj(CkJws_GetProtectedHeader($this->_cPtr,$index),'CkJsonObject');
	}

	/**
	 * Copies the optional unprotected header of signature ARG1 into ARG2. The valid zero-based index range is 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetUnprotectedH
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function GetUnprotectedH($index,$json) {
		return CkJws_GetUnprotectedH($this->_cPtr,$index,$json);
	}

	/**
	 * Returns a newly allocated clone of the optional unprotected header for signature ARG1. The valid index range is 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#GetUnprotectedHeader
	 * @param int $index
	 * @return ?CkJsonObject
	 * @deprecated
	 */
	function GetUnprotectedHeader($index) {
		return chilkat::_obj(CkJws_GetUnprotectedHeader($this->_cPtr,$index),'CkJsonObject');
	}

	/**
	 * Loads a JWS from ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LoadJws
	 * @param ?string $jwsStr
	 * @return bool
	 */
	function LoadJws($jwsStr) {
		return CkJws_LoadJws($this->_cPtr,$jwsStr);
	}

	/**
	 * Loads a JWS from the current contents of ARG1.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#LoadJwsSb
	 * @param CkStringBuilder $sbJws
	 * @return bool
	 */
	function LoadJwsSb($sbJws) {
		return CkJws_LoadJwsSb($this->_cPtr,$sbJws);
	}

	/**
	 * Sets the symmetric MAC key for signature ARG1. The valid zero-based index range is 0 through 1000. ARG2 is decoded using binary encoding ARG3, and the resulting bytes are copied.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetMacKey
	 * @param int $index
	 * @param ?string $key
	 * @param ?string $encoding
	 * @return bool
	 */
	function SetMacKey($index,$key,$encoding) {
		return CkJws_SetMacKey($this->_cPtr,$index,$key,$encoding);
	}

	/**
	 * Sets the symmetric MAC key for signature ARG1 from a copy of the raw bytes in ARG2. The valid zero-based index range is 0 through 1000.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetMacKeyBd
	 * @param int $index
	 * @param CkBinData $key
	 * @return bool
	 */
	function SetMacKeyBd($index,$key) {
		return CkJws_SetMacKeyBd($this->_cPtr,$index,$key);
	}

	/**
	 * Replaces the current payload with ARG1 converted to bytes using charset ARG2.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetPayload
	 * @param ?string $payload
	 * @param ?string $charset
	 * @param bool $includeBom
	 * @return bool
	 */
	function SetPayload($payload,$charset,$includeBom) {
		return CkJws_SetPayload($this->_cPtr,$payload,$charset,$includeBom);
	}

	/**
	 * Replaces the current JWS payload with a copy of all bytes in ARG1. Use this method for binary data or whenever the exact byte sequence must be preserved without charset conversion.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetPayloadBd
	 * @param CkBinData $binData
	 * @return bool
	 */
	function SetPayloadBd($binData) {
		return CkJws_SetPayloadBd($this->_cPtr,$binData);
	}

	/**
	 * Replaces the current payload with the text in ARG1, converted to bytes using charset ARG2. The bytes are copied; the source StringBuilder does not need to remain alive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetPayloadSb
	 * @param CkStringBuilder $sbPayload
	 * @param ?string $charset
	 * @param bool $includeBom
	 * @return bool
	 */
	function SetPayloadSb($sbPayload,$charset,$includeBom) {
		return CkJws_SetPayloadSb($this->_cPtr,$sbPayload,$charset,$includeBom);
	}

	/**
	 * Sets a private key for creating signature ARG1. The valid zero-based index range is 0 through 1000. The key is cloned, so the source object does not need to remain alive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetPrivateKey
	 * @param int $index
	 * @param CkPrivateKey $privKey
	 * @return bool
	 */
	function SetPrivateKey($index,$privKey) {
		return CkJws_SetPrivateKey($this->_cPtr,$index,$privKey);
	}

	/**
	 * Sets the protected JOSE header for signature ARG1. The valid zero-based index range is 0 through 1000. Setting either a protected or unprotected header establishes the signature slot and can increase NumSignatures.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetProtectedHeader
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetProtectedHeader($index,$json) {
		return CkJws_SetProtectedHeader($this->_cPtr,$index,$json);
	}

	/**
	 * Sets the public key used to validate signature ARG1. The valid zero-based index range is 0 through 1000. The key is cloned, so the source object does not need to remain alive.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetPublicKey
	 * @param int $index
	 * @param CkPublicKey $pubKey
	 * @return bool
	 */
	function SetPublicKey($index,$pubKey) {
		return CkJws_SetPublicKey($this->_cPtr,$index,$pubKey);
	}

	/**
	 * Sets the certificate whose associated private key will be used to create signature ARG1. The valid zero-based index range is 0 through 1000. This setter attaches key material but does not establish a signature slot.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetSigningCert
	 * @param int $index
	 * @param CkCert $cert
	 * @return bool
	 */
	function SetSigningCert($index,$cert) {
		return CkJws_SetSigningCert($this->_cPtr,$index,$cert);
	}

	/**
	 * Sets the optional unprotected JOSE header for signature ARG1. The valid zero-based index range is 0 through 1000. Setting either header type establishes the signature slot.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#SetUnprotectedHeader
	 * @param int $index
	 * @param CkJsonObject $json
	 * @return bool
	 */
	function SetUnprotectedHeader($index,$json) {
		return CkJws_SetUnprotectedHeader($this->_cPtr,$index,$json);
	}

	/**
	 * Validates exactly one signature, identified by zero-based ARG1, using the MAC key or public key configured at that same index.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwsRef.html#Validate
	 * @param int $index
	 * @return int
	 */
	function Validate($index) {
		return CkJws_Validate($this->_cPtr,$index);
	}
}
