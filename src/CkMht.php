<?php

/* CkMht.php -- Chilkat PHP extension proxy class CkMht.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Create, save, zip, email, and unpack MHT web archives.
 *
 * Chilkat.Mht creates MHT web archives and EML email messages from HTTP/HTTPS pages, local HTML files, or in-memory HTML strings. It analyzes the supplied HTML source, resolves and retrieves supported static resources, rewrites references as needed, and packages the result as MIME. It also supports disk caching, proxies, authentication, file or ZIP output, and unpacking existing MHT data.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html
 */
class CkMht {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkMht') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkMht();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkMht_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkMht_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Sets the event callback object.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html
	 * @param ?CkBaseProgress $progress
	 */
	function put_EventCallbackObject($progress) {
		CkMht_put_EventCallbackObject($this->_cPtr,$progress);
	}

	/**
	 * Set to _TRUE_ to request cancellation of the operation currently running on this object. Long-running network and file operations periodically check this property; a fast operation may complete before the request is observed. A synchronous method can be cancelled from another thread by setting this property on the same object. Chilkat resets the property to _FALSE_ after an abort is processed, and a stale request is cleared when a later method begins. Cancellation request: Cancellation is cooperative rather than instantaneous. Always inspect the method's return value and diagnostic properties to determine the final outcome.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#AbortCurrent
	 * @return bool
	 */
	function get_AbortCurrent() {
		return CkMht_get_AbortCurrent($this->_cPtr);
	}

	/**
	 * Set to _TRUE_ to request cancellation of the operation currently running on this object. Long-running network and file operations periodically check this property; a fast operation may complete before the request is observed. A synchronous method can be cancelled from another thread by setting this property on the same object. Chilkat resets the property to _FALSE_ after an abort is processed, and a stale request is cleared when a later method begins. Cancellation request: Cancellation is cooperative rather than instantaneous. Always inspect the method's return value and diagnostic properties to determine the final outcome.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#AbortCurrent
	 * @param bool $newVal
	 */
	function put_AbortCurrent($newVal) {
		CkMht_put_AbortCurrent($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a web base URL used when relative references in a local HTML file or in-memory HTML string must be resolved in an HTTP/HTTPS context. For an HTTP/HTTPS source page, Chilkat derives the base from the fetched URL automatically. Resolution precedence: An HTML <base href> element takes precedence over this property. If no usable <base> exists, BaseUrl is used; otherwise Chilkat falls back to the source page URL or the directory containing the local HTML file. Local files: Relative filesystem references in a local HTML file can be resolved from the file's own directory without setting this property. Set BaseUrl when the document contains site-relative or root-relative references that should resolve against a web origin. The value should be an HTTP or HTTPS URL, not a filesystem directory. Trailing slash: URL resolution follows normal base-URL rules. Use a trailing slash for a directory-style base, such as https://www.example.com/section/. Without it, the last path segment is treated as a filename and is removed before relative resolution. Relative url(...) and @import references inside a downloaded external stylesheet are resolved against that stylesheet's final URL. References in inline <style> blocks and style attributes are resolved against the page base. If no usable base exists, unresolved relative references remain relative and their resources are not captured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#BaseUrl
	 * @param CkString $str
	 */
	function get_BaseUrl($str) {
		CkMht_get_BaseUrl($this->_cPtr,$str);
	}

	/**
	 * Specifies a web base URL used when relative references in a local HTML file or in-memory HTML string must be resolved in an HTTP/HTTPS context. For an HTTP/HTTPS source page, Chilkat derives the base from the fetched URL automatically. Resolution precedence: An HTML <base href> element takes precedence over this property. If no usable <base> exists, BaseUrl is used; otherwise Chilkat falls back to the source page URL or the directory containing the local HTML file. Local files: Relative filesystem references in a local HTML file can be resolved from the file's own directory without setting this property. Set BaseUrl when the document contains site-relative or root-relative references that should resolve against a web origin. The value should be an HTTP or HTTPS URL, not a filesystem directory. Trailing slash: URL resolution follows normal base-URL rules. Use a trailing slash for a directory-style base, such as https://www.example.com/section/. Without it, the last path segment is treated as a filename and is removed before relative resolution. Relative url(...) and @import references inside a downloaded external stylesheet are resolved against that stylesheet's final URL. References in inline <style> blocks and style attributes are resolved against the page base. If no usable base exists, unresolved relative references remain relative and their resources are not captured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#BaseUrl
	 * @return ?string
	 */
	function baseUrl() {
		return CkMht_baseUrl($this->_cPtr);
	}

	/**
	 * Specifies a web base URL used when relative references in a local HTML file or in-memory HTML string must be resolved in an HTTP/HTTPS context. For an HTTP/HTTPS source page, Chilkat derives the base from the fetched URL automatically. Resolution precedence: An HTML <base href> element takes precedence over this property. If no usable <base> exists, BaseUrl is used; otherwise Chilkat falls back to the source page URL or the directory containing the local HTML file. Local files: Relative filesystem references in a local HTML file can be resolved from the file's own directory without setting this property. Set BaseUrl when the document contains site-relative or root-relative references that should resolve against a web origin. The value should be an HTTP or HTTPS URL, not a filesystem directory. Trailing slash: URL resolution follows normal base-URL rules. Use a trailing slash for a directory-style base, such as https://www.example.com/section/. Without it, the last path segment is treated as a filename and is removed before relative resolution. Relative url(...) and @import references inside a downloaded external stylesheet are resolved against that stylesheet's final URL. References in inline <style> blocks and style attributes are resolved against the page base. If no usable base exists, unresolved relative references remain relative and their resources are not captured.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#BaseUrl
	 * @param ?string $newVal
	 */
	function put_BaseUrl($newVal) {
		CkMht_put_BaseUrl($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum number of seconds allowed for establishing a connection to an HTTP server. The default is 10 seconds. Connection timeout only: This setting limits connection establishment; it is not a limit on the total time required to download a page. Use ReadTimeout to control inactivity while receiving data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ConnectTimeout
	 * @return int
	 */
	function get_ConnectTimeout() {
		return CkMht_get_ConnectTimeout($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of seconds allowed for establishing a connection to an HTTP server. The default is 10 seconds. Connection timeout only: This setting limits connection establishment; it is not a limit on the total time required to download a page. Use ReadTimeout to control inactivity while receiving data.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ConnectTimeout
	 * @param int $newVal
	 */
	function put_ConnectTimeout($newVal) {
		CkMht_put_ConnectTimeout($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a local file path where Chilkat saves the HTML after it has been processed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Compare this file with DebugHtmlBefore to inspect rewritten URLs, removed scripts, and other transformations applied before the MIME document is assembled. Do not enable diagnostic output to a sensitive or publicly accessible location. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlAfter
	 * @param CkString $str
	 */
	function get_DebugHtmlAfter($str) {
		CkMht_get_DebugHtmlAfter($this->_cPtr,$str);
	}

	/**
	 * Specifies a local file path where Chilkat saves the HTML after it has been processed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Compare this file with DebugHtmlBefore to inspect rewritten URLs, removed scripts, and other transformations applied before the MIME document is assembled. Do not enable diagnostic output to a sensitive or publicly accessible location. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlAfter
	 * @return ?string
	 */
	function debugHtmlAfter() {
		return CkMht_debugHtmlAfter($this->_cPtr);
	}

	/**
	 * Specifies a local file path where Chilkat saves the HTML after it has been processed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Compare this file with DebugHtmlBefore to inspect rewritten URLs, removed scripts, and other transformations applied before the MIME document is assembled. Do not enable diagnostic output to a sensitive or publicly accessible location. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlAfter
	 * @param ?string $newVal
	 */
	function put_DebugHtmlAfter($newVal) {
		CkMht_put_DebugHtmlAfter($this->_cPtr,$newVal);
	}

	/**
	 * Specifies a local file path where Chilkat saves the source HTML before it is transformed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Use this together with DebugHtmlAfter to determine whether a problem originates in the input HTML or in the conversion and URL-rewriting process. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlBefore
	 * @param CkString $str
	 */
	function get_DebugHtmlBefore($str) {
		CkMht_get_DebugHtmlBefore($this->_cPtr,$str);
	}

	/**
	 * Specifies a local file path where Chilkat saves the source HTML before it is transformed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Use this together with DebugHtmlAfter to determine whether a problem originates in the input HTML or in the conversion and URL-rewriting process. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlBefore
	 * @return ?string
	 */
	function debugHtmlBefore() {
		return CkMht_debugHtmlBefore($this->_cPtr);
	}

	/**
	 * Specifies a local file path where Chilkat saves the source HTML before it is transformed for MHT or EML generation. Leave the property empty to disable this diagnostic output. Troubleshooting aid: Use this together with DebugHtmlAfter to determine whether a problem originates in the input HTML or in the conversion and URL-rewriting process. Debug-file behavior: An existing debug file is overwritten in place. Parent directories are not created, and a relative path is resolved against the process current working directory. The write is best-effort: failure to create or update the debug file is recorded diagnostically but does not cause the main MHT or EML operation to fail. The write is not atomic, so an I/O error can leave a partial debug file.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugHtmlBefore
	 * @param ?string $newVal
	 */
	function put_DebugHtmlBefore($newVal) {
		CkMht_put_DebugHtmlBefore($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkMht_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkMht_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkMht_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether images referenced by the HTML are downloaded or read from disk and embedded as related MIME parts in both MHT and EML output. The default is _TRUE_. EmbedImagesEmbedLocalOnlyImage behavior _FALSE_EitherNo images are embedded. References are made absolute when sufficient base information is available. _TRUE__FALSE_Local and HTTP/HTTPS images are embedded when they can be obtained. This is the default combination. _TRUE__TRUE_Only local-file images are embedded. HTTP/HTTPS images remain external references. Recognized image source: Chilkat captures the ordinary <img src> value. It does not use srcset or sizes to choose a candidate; those attributes are removed. Obvious spacer or 1×1 placeholder sources are detected so the real image reference can be used when available. Self-contained output: MHT or EML output is self-contained with respect to images only when the settings permit embedding and every required image is successfully obtained. A failed image is omitted and its reference remains an absolute live URL.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#EmbedImages
	 * @return bool
	 */
	function get_EmbedImages() {
		return CkMht_get_EmbedImages($this->_cPtr);
	}

	/**
	 * Controls whether images referenced by the HTML are downloaded or read from disk and embedded as related MIME parts in both MHT and EML output. The default is _TRUE_. EmbedImagesEmbedLocalOnlyImage behavior _FALSE_EitherNo images are embedded. References are made absolute when sufficient base information is available. _TRUE__FALSE_Local and HTTP/HTTPS images are embedded when they can be obtained. This is the default combination. _TRUE__TRUE_Only local-file images are embedded. HTTP/HTTPS images remain external references. Recognized image source: Chilkat captures the ordinary <img src> value. It does not use srcset or sizes to choose a candidate; those attributes are removed. Obvious spacer or 1×1 placeholder sources are detected so the real image reference can be used when available. Self-contained output: MHT or EML output is self-contained with respect to images only when the settings permit embedding and every required image is successfully obtained. A failed image is omitted and its reference remains an absolute live URL.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#EmbedImages
	 * @param bool $newVal
	 */
	function put_EmbedImages($newVal) {
		CkMht_put_EmbedImages($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, only images whose references resolve to the local filesystem are embedded. Images referenced through HTTP or HTTPS remain external. The default is _FALSE_. This property has an effect only when EmbedImages is _TRUE_ and applies to both MHT and EML output. Scope: This option applies to images, including image URLs found in HTML and CSS. It does not prevent Chilkat from fetching non-image web resources such as external style sheets or, for MHT output, supported frames and scripts.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#EmbedLocalOnly
	 * @return bool
	 */
	function get_EmbedLocalOnly() {
		return CkMht_get_EmbedLocalOnly($this->_cPtr);
	}

	/**
	 * When _TRUE_, only images whose references resolve to the local filesystem are embedded. Images referenced through HTTP or HTTPS remain external. The default is _FALSE_. This property has an effect only when EmbedImages is _TRUE_ and applies to both MHT and EML output. Scope: This option applies to images, including image URLs found in HTML and CSS. It does not prevent Chilkat from fetching non-image web resources such as external style sheets or, for MHT output, supported frames and scripts.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#EmbedLocalOnly
	 * @param bool $newVal
	 */
	function put_EmbedLocalOnly($newVal) {
		CkMht_put_EmbedLocalOnly($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, Chilkat attempts to satisfy page and resource requests from the configured disk cache before downloading them. Add one or more cache directories with AddCacheRoot. The default is _FALSE_. Related settings: UpdateCache controls whether network responses are written to the cache. IgnoreMustRevalidate and IgnoreNoCache control whether certain HTTP cache directives are honored.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#FetchFromCache
	 * @return bool
	 */
	function get_FetchFromCache() {
		return CkMht_get_FetchFromCache($this->_cPtr);
	}

	/**
	 * When _TRUE_, Chilkat attempts to satisfy page and resource requests from the configured disk cache before downloading them. Add one or more cache directories with AddCacheRoot. The default is _FALSE_. Related settings: UpdateCache controls whether network responses are written to the cache. IgnoreMustRevalidate and IgnoreNoCache control whether certain HTTP cache directives are honored.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#FetchFromCache
	 * @param bool $newVal
	 */
	function put_FetchFromCache($newVal) {
		CkMht_put_FetchFromCache($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables these periodic callbacks. Cancellation: An event handler may request cancellation when an AbortCheck callback occurs. Choose an interval that is responsive without generating excessive callback traffic.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HeartbeatMs
	 * @return int
	 */
	function get_HeartbeatMs() {
		return CkMht_get_HeartbeatMs($this->_cPtr);
	}

	/**
	 * Specifies the interval, in milliseconds, between AbortCheck event callbacks during supported long-running operations. The default is 0, which disables these periodic callbacks. Cancellation: An event handler may request cancellation when an AbortCheck callback occurs. Choose an interval that is responsive without generating excessive callback traffic.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HeartbeatMs
	 * @param int $newVal
	 */
	function put_HeartbeatMs($newVal) {
		CkMht_put_HeartbeatMs($this->_cPtr,$newVal);
	}

	/**
	 * Controls how cached responses containing Cache-Control: must-revalidate are handled. When _FALSE_ (the default), Chilkat revalidates such a cached response before reuse. When _TRUE_, a still-fresh cached response may be used without revalidation. This property is relevant only when FetchFromCache is _TRUE_. Cache semantics: Ignoring must-revalidate can improve offline or high-latency operation, but it may also use content the origin server intended to be checked before reuse.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#IgnoreMustRevalidate
	 * @return bool
	 */
	function get_IgnoreMustRevalidate() {
		return CkMht_get_IgnoreMustRevalidate($this->_cPtr);
	}

	/**
	 * Controls how cached responses containing Cache-Control: must-revalidate are handled. When _FALSE_ (the default), Chilkat revalidates such a cached response before reuse. When _TRUE_, a still-fresh cached response may be used without revalidation. This property is relevant only when FetchFromCache is _TRUE_. Cache semantics: Ignoring must-revalidate can improve offline or high-latency operation, but it may also use content the origin server intended to be checked before reuse.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#IgnoreMustRevalidate
	 * @param bool $newVal
	 */
	function put_IgnoreMustRevalidate($newVal) {
		CkMht_put_IgnoreMustRevalidate($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether HTTP response directives that prohibit or restrict caching are ignored. The default is _FALSE_, which honors the server's caching instructions. When _TRUE_, Chilkat may store responses that would otherwise be excluded from the disk cache. Use carefully: Caching content against the origin server's instructions can retain stale, private, or sensitive data. Enable this only when the application controls the content or has a specific offline-caching requirement.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#IgnoreNoCache
	 * @return bool
	 */
	function get_IgnoreNoCache() {
		return CkMht_get_IgnoreNoCache($this->_cPtr);
	}

	/**
	 * Controls whether HTTP response directives that prohibit or restrict caching are ignored. The default is _FALSE_, which honors the server's caching instructions. When _TRUE_, Chilkat may store responses that would otherwise be excluded from the disk cache. Use carefully: Caching content against the origin server's instructions can retain stale, private, or sensitive data. Enable this only when the application controls the content or has a specific offline-caching requirement.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#IgnoreNoCache
	 * @param bool $newVal
	 */
	function put_IgnoreNoCache($newVal) {
		CkMht_put_IgnoreNoCache($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkMht_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkMht_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkMht_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkMht_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkMht_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkMht_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkMht_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkMht_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Controls script removal when creating MHT output. When _TRUE_, script content is removed. When _FALSE_ (the default), inline scripts and supported external scripts may be preserved according to PreferMHTScripts. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. EML behavior: Scripts are always removed when generating EML or email-oriented output, regardless of this property. Framed documents are also removed from EML output.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NoScripts
	 * @return bool
	 */
	function get_NoScripts() {
		return CkMht_get_NoScripts($this->_cPtr);
	}

	/**
	 * Controls script removal when creating MHT output. When _TRUE_, script content is removed. When _FALSE_ (the default), inline scripts and supported external scripts may be preserved according to PreferMHTScripts. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. EML behavior: Scripts are always removed when generating EML or email-oriented output, regardless of this property. Framed documents are also removed from EML output.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NoScripts
	 * @param bool $newVal
	 */
	function put_NoScripts($newVal) {
		CkMht_put_NoScripts($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, Chilkat uses NTLM authentication when the origin HTTP server requests authentication. The default is _FALSE_. Set WebSiteLogin, WebSitePassword, and, when required, WebSiteLoginDomain. Origin versus proxy authentication: These credentials authenticate to the website. Proxy credentials are configured separately with ProxyLogin and ProxyPassword. Use HTTPS whenever credentials or authenticated content are transmitted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NtlmAuth
	 * @return bool
	 */
	function get_NtlmAuth() {
		return CkMht_get_NtlmAuth($this->_cPtr);
	}

	/**
	 * When _TRUE_, Chilkat uses NTLM authentication when the origin HTTP server requests authentication. The default is _FALSE_. Set WebSiteLogin, WebSitePassword, and, when required, WebSiteLoginDomain. Origin versus proxy authentication: These credentials authenticate to the website. Proxy credentials are configured separately with ProxyLogin and ProxyPassword. Use HTTPS whenever credentials or authenticated content are transmitted.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NtlmAuth
	 * @param bool $newVal
	 */
	function put_NtlmAuth($newVal) {
		CkMht_put_NtlmAuth($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the number of directory levels used beneath each disk-cache root. The default is 0, which stores cached items directly in the root. A value of 1 distributes items among 256 subdirectories named 0 through 255. A value of 2 uses two such levels, for example 17/204. Chilkat creates the required subdirectories automatically. Why use levels? Directory fan-out prevents very large caches from placing thousands of files in one directory, which can degrade filesystem tools and directory operations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NumCacheLevels
	 * @return int
	 */
	function get_NumCacheLevels() {
		return CkMht_get_NumCacheLevels($this->_cPtr);
	}

	/**
	 * Specifies the number of directory levels used beneath each disk-cache root. The default is 0, which stores cached items directly in the root. A value of 1 distributes items among 256 subdirectories named 0 through 255. A value of 2 uses two such levels, for example 17/204. Chilkat creates the required subdirectories automatically. Why use levels? Directory fan-out prevents very large caches from placing thousands of files in one directory, which can degrade filesystem tools and directory operations.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NumCacheLevels
	 * @param int $newVal
	 */
	function put_NumCacheLevels($newVal) {
		CkMht_put_NumCacheLevels($this->_cPtr,$newVal);
	}

	/**
	 * Returns the number of disk-cache root directories currently configured through AddCacheRoot. Multiple roots allow cached items to be distributed across different directories or storage volumes. Indexing: Cache-root indexes are zero-based. Use GetCacheRoot to retrieve a configured root by index.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#NumCacheRoots
	 * @return int
	 */
	function get_NumCacheRoots() {
		return CkMht_get_NumCacheRoots($this->_cPtr);
	}

	/**
	 * Controls address-family preference when a hostname resolves to both IPv4 and IPv6 addresses. The default is _FALSE_, which prefers IPv4. Set to _TRUE_ to prefer IPv6. Preference, not a requirement: This setting does not guarantee that IPv6 will be used. The selected address must still be reachable, and fallback behavior depends on the available addresses and connection results.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#PreferIpv6
	 * @return bool
	 */
	function get_PreferIpv6() {
		return CkMht_get_PreferIpv6($this->_cPtr);
	}

	/**
	 * Controls address-family preference when a hostname resolves to both IPv4 and IPv6 addresses. The default is _FALSE_, which prefers IPv4. Set to _TRUE_ to prefer IPv6. Preference, not a requirement: This setting does not guarantee that IPv6 will be used. The selected address must still be reachable, and fallback behavior depends on the available addresses and connection results.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#PreferIpv6
	 * @param bool $newVal
	 */
	function put_PreferIpv6($newVal) {
		CkMht_put_PreferIpv6($this->_cPtr,$newVal);
	}

	/**
	 * Controls which content is retained when HTML contains a script together with a <noscript> alternative. When _TRUE_ (the default), the script is preferred and the <noscript> content is discarded. When _FALSE_, the script is removed and the <noscript> content is retained. Interaction with NoScripts: NoScripts takes precedence when script removal is required. EML generation always removes scripts. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#PreferMHTScripts
	 * @return bool
	 */
	function get_PreferMHTScripts() {
		return CkMht_get_PreferMHTScripts($this->_cPtr);
	}

	/**
	 * Controls which content is retained when HTML contains a script together with a <noscript> alternative. When _TRUE_ (the default), the script is preferred and the <noscript> content is discarded. When _FALSE_, the script is removed and the <noscript> content is retained. Interaction with NoScripts: NoScripts takes precedence when script removal is required. EML generation always removes scripts. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#PreferMHTScripts
	 * @param bool $newVal
	 */
	function put_PreferMHTScripts($newVal) {
		CkMht_put_PreferMHTScripts($this->_cPtr,$newVal);
	}

	/**
	 * Specifies an HTTP proxy as hostname:port, for example proxy.example.com:8080. Leave the property empty when no explicit HTTP proxy is required. Formatting: Supply only the proxy host and port; do not include an http:// or https:// scheme. If the proxy requires credentials, set ProxyLogin and ProxyPassword.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#Proxy
	 * @param CkString $str
	 */
	function get_Proxy($str) {
		CkMht_get_Proxy($this->_cPtr,$str);
	}

	/**
	 * Specifies an HTTP proxy as hostname:port, for example proxy.example.com:8080. Leave the property empty when no explicit HTTP proxy is required. Formatting: Supply only the proxy host and port; do not include an http:// or https:// scheme. If the proxy requires credentials, set ProxyLogin and ProxyPassword.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#Proxy
	 * @return ?string
	 */
	function proxy() {
		return CkMht_proxy($this->_cPtr);
	}

	/**
	 * Specifies an HTTP proxy as hostname:port, for example proxy.example.com:8080. Leave the property empty when no explicit HTTP proxy is required. Formatting: Supply only the proxy host and port; do not include an http:// or https:// scheme. If the proxy requires credentials, set ProxyLogin and ProxyPassword.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#Proxy
	 * @param ?string $newVal
	 */
	function put_Proxy($newVal) {
		CkMht_put_Proxy($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the username used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyLogin
	 * @param CkString $str
	 */
	function get_ProxyLogin($str) {
		CkMht_get_ProxyLogin($this->_cPtr,$str);
	}

	/**
	 * Specifies the username used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyLogin
	 * @return ?string
	 */
	function proxyLogin() {
		return CkMht_proxyLogin($this->_cPtr);
	}

	/**
	 * Specifies the username used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyLogin
	 * @param ?string $newVal
	 */
	function put_ProxyLogin($newVal) {
		CkMht_put_ProxyLogin($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication. Credential handling: Treat proxy passwords as secrets and avoid writing them to diagnostic logs or persistent configuration in cleartext.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyPassword
	 * @param CkString $str
	 */
	function get_ProxyPassword($str) {
		CkMht_get_ProxyPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication. Credential handling: Treat proxy passwords as secrets and avoid writing them to diagnostic logs or persistent configuration in cleartext.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyPassword
	 * @return ?string
	 */
	function proxyPassword() {
		return CkMht_proxyPassword($this->_cPtr);
	}

	/**
	 * Specifies the password used to authenticate with the HTTP proxy configured by Proxy. Leave empty when the proxy does not require authentication. Credential handling: Treat proxy passwords as secrets and avoid writing them to diagnostic logs or persistent configuration in cleartext.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ProxyPassword
	 * @param ?string $newVal
	 */
	function put_ProxyPassword($newVal) {
		CkMht_put_ProxyPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the maximum number of seconds that an HTTP read may remain inactive while waiting for additional data. The default is 20 seconds. If data continues to arrive, a long download may run longer than this value. Inactivity timeout: This is not a total-operation timeout. It applies when the incoming data stream stops making progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ReadTimeout
	 * @return int
	 */
	function get_ReadTimeout() {
		return CkMht_get_ReadTimeout($this->_cPtr);
	}

	/**
	 * Specifies the maximum number of seconds that an HTTP read may remain inactive while waiting for additional data. The default is 20 seconds. If data continues to arrive, a long download may run longer than this value. Inactivity timeout: This is not a total-operation timeout. It applies when the incoming data stream stops making progress.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ReadTimeout
	 * @param int $newVal
	 */
	function put_ReadTimeout($newVal) {
		CkMht_put_ReadTimeout($this->_cPtr,$newVal);
	}

	/**
	 * Controls verification of the TLS server certificate for HTTPS connections. When _TRUE_, the connection is rejected if certificate verification fails, such as because the certificate is expired or its chain or signature cannot be validated. The default is _FALSE_. Recommended for HTTPS: Enable certificate verification for production connections to prevent accepting an untrusted or invalid server certificate. Disabling verification removes an important protection against impersonation and man-in-the-middle attacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#RequireSslCertVerify
	 * @return bool
	 */
	function get_RequireSslCertVerify() {
		return CkMht_get_RequireSslCertVerify($this->_cPtr);
	}

	/**
	 * Controls verification of the TLS server certificate for HTTPS connections. When _TRUE_, the connection is rejected if certificate verification fails, such as because the certificate is expired or its chain or signature cannot be validated. The default is _FALSE_. Recommended for HTTPS: Enable certificate verification for production connections to prevent accepting an untrusted or invalid server certificate. Disabling verification removes an important protection against impersonation and man-in-the-middle attacks.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#RequireSslCertVerify
	 * @param bool $newVal
	 */
	function put_RequireSslCertVerify($newVal) {
		CkMht_put_RequireSslCertVerify($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the hostname or dotted-decimal IPv4 address of the SOCKS proxy. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksHostname
	 * @param CkString $str
	 */
	function get_SocksHostname($str) {
		CkMht_get_SocksHostname($this->_cPtr,$str);
	}

	/**
	 * Specifies the hostname or dotted-decimal IPv4 address of the SOCKS proxy. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksHostname
	 * @return ?string
	 */
	function socksHostname() {
		return CkMht_socksHostname($this->_cPtr);
	}

	/**
	 * Specifies the hostname or dotted-decimal IPv4 address of the SOCKS proxy. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksHostname
	 * @param ?string $newVal
	 */
	function put_SocksHostname($newVal) {
		CkMht_put_SocksHostname($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password for SOCKS5 proxy authentication. SOCKS4 does not define password authentication, so this property is ignored when SocksVersion is 4.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksPassword
	 * @param CkString $str
	 */
	function get_SocksPassword($str) {
		CkMht_get_SocksPassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password for SOCKS5 proxy authentication. SOCKS4 does not define password authentication, so this property is ignored when SocksVersion is 4.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksPassword
	 * @return ?string
	 */
	function socksPassword() {
		return CkMht_socksPassword($this->_cPtr);
	}

	/**
	 * Specifies the password for SOCKS5 proxy authentication. SOCKS4 does not define password authentication, so this property is ignored when SocksVersion is 4.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksPassword
	 * @param ?string $newVal
	 */
	function put_SocksPassword($newVal) {
		CkMht_put_SocksPassword($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the TCP port of the SOCKS4 or SOCKS5 proxy. The default is 1080. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksPort
	 * @return int
	 */
	function get_SocksPort() {
		return CkMht_get_SocksPort($this->_cPtr);
	}

	/**
	 * Specifies the TCP port of the SOCKS4 or SOCKS5 proxy. The default is 1080. This property is used only when SocksVersion is 4 or 5.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksPort
	 * @param int $newVal
	 */
	function put_SocksPort($newVal) {
		CkMht_put_SocksPort($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the username sent to the SOCKS4 or SOCKS5 proxy. It is used only when SocksVersion is 4 or 5. For SOCKS5, set SocksPassword when password authentication is required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksUsername
	 * @param CkString $str
	 */
	function get_SocksUsername($str) {
		CkMht_get_SocksUsername($this->_cPtr,$str);
	}

	/**
	 * Specifies the username sent to the SOCKS4 or SOCKS5 proxy. It is used only when SocksVersion is 4 or 5. For SOCKS5, set SocksPassword when password authentication is required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksUsername
	 * @return ?string
	 */
	function socksUsername() {
		return CkMht_socksUsername($this->_cPtr);
	}

	/**
	 * Specifies the username sent to the SOCKS4 or SOCKS5 proxy. It is used only when SocksVersion is 4 or 5. For SOCKS5, set SocksPassword when password authentication is required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksUsername
	 * @param ?string $newVal
	 */
	function put_SocksUsername($newVal) {
		CkMht_put_SocksUsername($this->_cPtr,$newVal);
	}

	/**
	 * Selects whether requests are routed through a SOCKS proxy. ValueBehavior 0Do not use a SOCKS proxy. This is the default. 4Connect through a SOCKS4 proxy. 5Connect through a SOCKS5 proxy. Required companion settings: For versions 4 and 5, set SocksHostname and SocksPort. Configure username and password properties when the proxy requires them.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksVersion
	 * @return int
	 */
	function get_SocksVersion() {
		return CkMht_get_SocksVersion($this->_cPtr);
	}

	/**
	 * Selects whether requests are routed through a SOCKS proxy. ValueBehavior 0Do not use a SOCKS proxy. This is the default. 4Connect through a SOCKS4 proxy. 5Connect through a SOCKS5 proxy. Required companion settings: For versions 4 and 5, set SocksHostname and SocksPort. Configure username and password properties when the proxy requires them.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#SocksVersion
	 * @param int $newVal
	 */
	function put_SocksVersion($newVal) {
		CkMht_put_SocksVersion($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether MHT parts are extracted directly according to their relative Content-Location values. The default is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackDirect
	 * @return bool
	 */
	function get_UnpackDirect() {
		return CkMht_get_UnpackDirect($this->_cPtr);
	}

	/**
	 * Controls whether MHT parts are extracted directly according to their relative Content-Location values. The default is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackDirect
	 * @param bool $newVal
	 */
	function put_UnpackDirect($newVal) {
		CkMht_put_UnpackDirect($this->_cPtr,$newVal);
	}

	/**
	 * Controls how extracted resources are referenced from the rewritten HTML produced by the normal unpacking mode. The default is _TRUE_, which uses relative paths. Set to _FALSE_ to write absolute local paths. Portability: Relative paths keep the unpacked HTML and its resource directory movable as a unit. Absolute paths bind the HTML to the extraction location on the current computer.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackUseRelPaths
	 * @return bool
	 */
	function get_UnpackUseRelPaths() {
		return CkMht_get_UnpackUseRelPaths($this->_cPtr);
	}

	/**
	 * Controls how extracted resources are referenced from the rewritten HTML produced by the normal unpacking mode. The default is _TRUE_, which uses relative paths. Set to _FALSE_ to write absolute local paths. Portability: Relative paths keep the unpacked HTML and its resource directory movable as a unit. Absolute paths bind the HTML to the extraction location on the current computer.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackUseRelPaths
	 * @param bool $newVal
	 */
	function put_UnpackUseRelPaths($newVal) {
		CkMht_put_UnpackUseRelPaths($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether successful HTTP GET responses are written to the configured disk cache. The default is _FALSE_. When _TRUE_, add at least one cache root with AddCacheRoot. Reading and writing are separate: FetchFromCache controls cache reads. Applications may enable either setting independently or enable both for normal read-through caching.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UpdateCache
	 * @return bool
	 */
	function get_UpdateCache() {
		return CkMht_get_UpdateCache($this->_cPtr);
	}

	/**
	 * Controls whether successful HTTP GET responses are written to the configured disk cache. The default is _FALSE_. When _TRUE_, add at least one cache root with AddCacheRoot. Reading and writing are separate: FetchFromCache controls cache reads. Applications may enable either setting independently or enable both for normal read-through caching.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UpdateCache
	 * @param bool $newVal
	 */
	function put_UpdateCache($newVal) {
		CkMht_put_UpdateCache($this->_cPtr,$newVal);
	}

	/**
	 * Controls how embedded resources are identified and referenced in generated MHT and EML MIME documents. The default is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseCids
	 * @return bool
	 */
	function get_UseCids() {
		return CkMht_get_UseCids($this->_cPtr);
	}

	/**
	 * Controls how embedded resources are identified and referenced in generated MHT and EML MIME documents. The default is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseCids
	 * @param bool $newVal
	 */
	function put_UseCids($newVal) {
		CkMht_put_UseCids($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether the generated Content-Disposition for an embedded resource includes a filename parameter. The default is _TRUE_. Setting this property to _TRUE_ forces an inline disposition to be emitted even when UseInline is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseFilename
	 * @return bool
	 */
	function get_UseFilename() {
		return CkMht_get_UseFilename($this->_cPtr);
	}

	/**
	 * Controls whether the generated Content-Disposition for an embedded resource includes a filename parameter. The default is _TRUE_. Setting this property to _TRUE_ forces an inline disposition to be emitted even when UseInline is _FALSE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseFilename
	 * @param bool $newVal
	 */
	function put_UseFilename($newVal) {
		CkMht_put_UseFilename($this->_cPtr,$newVal);
	}

	/**
	 * When _TRUE_, Chilkat uses the proxy configuration associated with Windows Internet Options / Internet Explorer. This setting is relevant on Windows systems. Explicit configuration: Use Proxy when the application should specify a particular proxy directly rather than relying on the current user's Windows proxy settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseIEProxy
	 * @return bool
	 */
	function get_UseIEProxy() {
		return CkMht_get_UseIEProxy($this->_cPtr);
	}

	/**
	 * When _TRUE_, Chilkat uses the proxy configuration associated with Windows Internet Options / Internet Explorer. This setting is relevant on Windows systems. Explicit configuration: Use Proxy when the application should specify a particular proxy directly rather than relying on the current user's Windows proxy settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseIEProxy
	 * @param bool $newVal
	 */
	function put_UseIEProxy($newVal) {
		CkMht_put_UseIEProxy($this->_cPtr,$newVal);
	}

	/**
	 * Controls whether embedded resource parts receive an inline Content-Disposition when UseFilename does not independently require the header. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseInline
	 * @return bool
	 */
	function get_UseInline() {
		return CkMht_get_UseInline($this->_cPtr);
	}

	/**
	 * Controls whether embedded resource parts receive an inline Content-Disposition when UseFilename does not independently require the header. The default is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UseInline
	 * @param bool $newVal
	 */
	function put_UseInline($newVal) {
		CkMht_put_UseInline($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkMht_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkMht_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkMht_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkMht_version($this->_cPtr);
	}

	/**
	 * Specifies the username used when the origin website requires HTTP authentication. It is optional for public pages. For NTLM authentication, also set NtlmAuth to _TRUE_ and provide a domain when required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLogin
	 * @param CkString $str
	 */
	function get_WebSiteLogin($str) {
		CkMht_get_WebSiteLogin($this->_cPtr,$str);
	}

	/**
	 * Specifies the username used when the origin website requires HTTP authentication. It is optional for public pages. For NTLM authentication, also set NtlmAuth to _TRUE_ and provide a domain when required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLogin
	 * @return ?string
	 */
	function webSiteLogin() {
		return CkMht_webSiteLogin($this->_cPtr);
	}

	/**
	 * Specifies the username used when the origin website requires HTTP authentication. It is optional for public pages. For NTLM authentication, also set NtlmAuth to _TRUE_ and provide a domain when required.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLogin
	 * @param ?string $newVal
	 */
	function put_WebSiteLogin($newVal) {
		CkMht_put_WebSiteLogin($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the optional Windows domain used with NTLM website authentication. This property is relevant when NtlmAuth is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLoginDomain
	 * @param CkString $str
	 */
	function get_WebSiteLoginDomain($str) {
		CkMht_get_WebSiteLoginDomain($this->_cPtr,$str);
	}

	/**
	 * Specifies the optional Windows domain used with NTLM website authentication. This property is relevant when NtlmAuth is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLoginDomain
	 * @return ?string
	 */
	function webSiteLoginDomain() {
		return CkMht_webSiteLoginDomain($this->_cPtr);
	}

	/**
	 * Specifies the optional Windows domain used with NTLM website authentication. This property is relevant when NtlmAuth is _TRUE_.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSiteLoginDomain
	 * @param ?string $newVal
	 */
	function put_WebSiteLoginDomain($newVal) {
		CkMht_put_WebSiteLoginDomain($this->_cPtr,$newVal);
	}

	/**
	 * Specifies the password used with WebSiteLogin when the origin website requires HTTP authentication. Credential handling: Treat this value as a secret. Use HTTPS for authenticated pages and avoid exposing credentials through debug output or application logs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSitePassword
	 * @param CkString $str
	 */
	function get_WebSitePassword($str) {
		CkMht_get_WebSitePassword($this->_cPtr,$str);
	}

	/**
	 * Specifies the password used with WebSiteLogin when the origin website requires HTTP authentication. Credential handling: Treat this value as a secret. Use HTTPS for authenticated pages and avoid exposing credentials through debug output or application logs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSitePassword
	 * @return ?string
	 */
	function webSitePassword() {
		return CkMht_webSitePassword($this->_cPtr);
	}

	/**
	 * Specifies the password used with WebSiteLogin when the origin website requires HTTP authentication. Credential handling: Treat this value as a secret. Use HTTPS for authenticated pages and avoid exposing credentials through debug output or application logs.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#WebSitePassword
	 * @param ?string $newVal
	 */
	function put_WebSitePassword($newVal) {
		CkMht_put_WebSitePassword($this->_cPtr,$newVal);
	}

	/**
	 * Adds ARG1 as a persistent root directory for the disk cache. Call this method once for each cache location the application wants Chilkat to use. Multiple roots may be placed on different directories or storage volumes.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#AddCacheRoot
	 * @param ?string $dir
	 */
	function AddCacheRoot($dir) {
		CkMht_AddCacheRoot($this->_cPtr,$dir);
	}

	/**
	 * Adds an HTTP request header to the persistent custom-header collection used for the main page, redirect targets, and every HTTP/HTTPS dependent-resource request, including requests to third-party hosts. ARG1 is the field name without a trailing colon, and ARG2 is the field value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#AddCustomHeader
	 * @param ?string $name
	 * @param ?string $value
	 */
	function AddCustomHeader($name,$value) {
		CkMht_AddCustomHeader($this->_cPtr,$name,$value);
	}

	/**
	 * Adds the style-sheet URL in ARG1 to the persistent list of external CSS resources downloaded and embedded during conversion. This is normally unnecessary because Chilkat automatically discovers <link rel="stylesheet"> references present in the static HTML.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#AddExternalStyleSheet
	 * @param ?string $url
	 */
	function AddExternalStyleSheet($url) {
		CkMht_AddExternalStyleSheet($this->_cPtr,$url);
	}

	/**
	 * Removes all HTTP request headers previously added with AddCustomHeader. Automatically generated and built-in HTTP headers are not removed.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ClearCustomHeaders
	 */
	function ClearCustomHeaders() {
		CkMht_ClearCustomHeaders($this->_cPtr);
	}

	/**
	 * Adds ARG1 to the persistent list of exclusion patterns used while gathering embeddable resources. Despite the historical method name, the test is applied to embeddable resources generally, not only to images. Each candidate is tested against its fully resolved reference: an absolute URL for web content or the resolved local path for a local resource.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#ExcludeImagesMatching
	 * @param ?string $pattern
	 */
	function ExcludeImagesMatching($pattern) {
		CkMht_ExcludeImagesMatching($this->_cPtr,$pattern);
	}

	/**
	 * Creates EML output from the HTTP/HTTPS page or local HTML file in ARG1 and saves it to the destination path in ARG2. Style sheets and their CSS resources are captured. Images are embedded according to EmbedImages and EmbedLocalOnly. Scripts and framed documents are removed. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Return value: Returns _TRUE_ on success and _FALSE_ on failure. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Destination file behavior: Chilkat first generates the complete MIME output in memory and writes the destination only after generation succeeds. An existing destination file is truncated and replaced; data is never appended and no confirmation is requested. Parent directories are not created. Relative paths are resolved against the process current working directory. If generation fails, an existing destination file is left untouched. The final write is direct rather than atomic, so a write-time error such as a full disk or permission failure can leave a truncated or partially written file. A read-only or otherwise unwritable destination causes the method to return _FALSE_. Using the same local file as both source and destination is mechanically allowed because the source is read first, but the subsequent write destroys the original HTML file. On Windows, this method also rejects destination paths containing characters that are invalid in Windows filenames, such as ?, before generation begins. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. EML is an HTML-email draft: The output adds the current RFC-822 Date and X-Unsent: 1, removes scripts, frames, and HTML comments, and contains an HTML root with related resources. It does not create a plain-text alternative, does not generate Message-ID, and does not automatically make a fully addressed, ready-to-send message. Load it into Chilkat.Email or a mail client and set the required addressing and subject information before sending.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndSaveEML
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $emlPath
	 * @return bool
	 */
	function GetAndSaveEML($url_or_htmlFilepath,$emlPath) {
		return CkMht_GetAndSaveEML($this->_cPtr,$url_or_htmlFilepath,$emlPath);
	}

	/**
	 * Creates an asynchronous task to call the GetAndSaveEML method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndSaveEMLAsync
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $emlPath
	 * @return ?CkTask
	 */
	function GetAndSaveEMLAsync($url_or_htmlFilepath,$emlPath) {
		return chilkat::_obj(CkMht_GetAndSaveEMLAsync($this->_cPtr,$url_or_htmlFilepath,$emlPath),'CkTask');
	}

	/**
	 * Creates MHT output from the HTTP/HTTPS page or local HTML file in ARG1 and saves it to the destination path in ARG2. Supported statically referenced resources are captured according to the current MHT properties. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Script output: Script handling is controlled by NoScripts and PreferMHTScripts. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Destination file behavior: Chilkat first generates the complete MIME output in memory and writes the destination only after generation succeeds. An existing destination file is truncated and replaced; data is never appended and no confirmation is requested. Parent directories are not created. Relative paths are resolved against the process current working directory. If generation fails, an existing destination file is left untouched. The final write is direct rather than atomic, so a write-time error such as a full disk or permission failure can leave a truncated or partially written file. A read-only or otherwise unwritable destination causes the method to return _FALSE_. Using the same local file as both source and destination is mechanically allowed because the source is read first, but the subsequent write destroys the original HTML file. On Windows, this method also rejects destination paths containing characters that are invalid in Windows filenames, such as ?, before generation begins. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. MHT MIME structure: The result is a MIME document whose root is HTML (or XML) and whose successfully embedded resources are related parts. When resources are present, the message is normally multipart/related; when none are embedded, it can collapse to a single text/html part. MHT output omits the Date and X-Unsent headers and can preserve scripts, frames, and comments according to the script settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndSaveMHT
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $mhtPath
	 * @return bool
	 */
	function GetAndSaveMHT($url_or_htmlFilepath,$mhtPath) {
		return CkMht_GetAndSaveMHT($this->_cPtr,$url_or_htmlFilepath,$mhtPath);
	}

	/**
	 * Creates an asynchronous task to call the GetAndSaveMHT method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndSaveMHTAsync
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $mhtPath
	 * @return ?CkTask
	 */
	function GetAndSaveMHTAsync($url_or_htmlFilepath,$mhtPath) {
		return chilkat::_obj(CkMht_GetAndSaveMHTAsync($this->_cPtr,$url_or_htmlFilepath,$mhtPath),'CkTask');
	}

	/**
	 * Creates EML output from the HTTP/HTTPS page or local HTML file in ARG1 and stores it as the ZIP entry named by ARG2 inside the archive file in ARG3. The ZIP is created if it does not exist; otherwise the new entry is appended. Style sheets are captured, images follow EmbedImages and EmbedLocalOnly, and scripts and frames are removed. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Argument order: ARG1 is the source, ARG2 is the entry name inside the ZIP, and ARG3 is the ZIP file path. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Entry-name collision: If ARG2 already exists in a valid opened ZIP archive, the method returns _FALSE_. It neither replaces the existing entry nor creates a duplicate.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndZipEML
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $zipEntryFilename
	 * @param ?string $zipFilename
	 * @return bool
	 */
	function GetAndZipEML($url_or_htmlFilepath,$zipEntryFilename,$zipFilename) {
		return CkMht_GetAndZipEML($this->_cPtr,$url_or_htmlFilepath,$zipEntryFilename,$zipFilename);
	}

	/**
	 * Creates an asynchronous task to call the GetAndZipEML method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndZipEMLAsync
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $zipEntryFilename
	 * @param ?string $zipFilename
	 * @return ?CkTask
	 */
	function GetAndZipEMLAsync($url_or_htmlFilepath,$zipEntryFilename,$zipFilename) {
		return chilkat::_obj(CkMht_GetAndZipEMLAsync($this->_cPtr,$url_or_htmlFilepath,$zipEntryFilename,$zipFilename),'CkTask');
	}

	/**
	 * Creates MHT output from the HTTP/HTTPS page or local HTML file in ARG1 and stores it as the ZIP entry named by ARG2 inside the archive file in ARG3. The ZIP is created if it does not exist; otherwise the new entry is appended. Supported statically referenced resources are captured according to the current properties. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Argument order: ARG1 is the source, ARG2 is the entry name inside the ZIP, and ARG3 is the ZIP file path. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Entry-name collision: If ARG2 already exists in a valid opened ZIP archive, the method returns _FALSE_. It neither replaces the existing entry nor creates a duplicate.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndZipMHT
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $zipEntryFilename
	 * @param ?string $zipFilename
	 * @return bool
	 */
	function GetAndZipMHT($url_or_htmlFilepath,$zipEntryFilename,$zipFilename) {
		return CkMht_GetAndZipMHT($this->_cPtr,$url_or_htmlFilepath,$zipEntryFilename,$zipFilename);
	}

	/**
	 * Creates an asynchronous task to call the GetAndZipMHT method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetAndZipMHTAsync
	 * @param ?string $url_or_htmlFilepath
	 * @param ?string $zipEntryFilename
	 * @param ?string $zipFilename
	 * @return ?CkTask
	 */
	function GetAndZipMHTAsync($url_or_htmlFilepath,$zipEntryFilename,$zipFilename) {
		return chilkat::_obj(CkMht_GetAndZipMHTAsync($this->_cPtr,$url_or_htmlFilepath,$zipEntryFilename,$zipFilename),'CkTask');
	}

	/**
	 * Returns the cache-root directory at zero-based index ARG1. Cache roots are added with AddCacheRoot, and their count is available from NumCacheRoots. Index and result handling: Use an index from 0 through NumCacheRoots - 1. The returned string is cleared when the call begins. If no cache root is configured or ARG1 is outside the valid range, LastMethodSuccess is _FALSE_ and the returned string is empty. On success, LastMethodSuccess is _TRUE_ and the returned string contains the root path. Check LastMethodSuccess, not whether the returned string is empty; use LastErrorText for the failure diagnostic.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetCacheRoot
	 * @param int $index
	 * @return ?string
	 */
	function getCacheRoot($index) {
		return CkMht_getCacheRoot($this->_cPtr,$index);
	}

	/**
	 * Returns the cache-root directory at zero-based index ARG1. Cache roots are added with AddCacheRoot, and their count is available from NumCacheRoots. Index and result handling: Use an index from 0 through NumCacheRoots - 1. The returned string is cleared when the call begins. If no cache root is configured or ARG1 is outside the valid range, LastMethodSuccess is _FALSE_ and the returned string is empty. On success, LastMethodSuccess is _TRUE_ and the returned string contains the root path. Check LastMethodSuccess, not whether the returned string is empty; use LastErrorText for the failure diagnostic.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetCacheRoot
	 * @param int $index
	 * @return ?string
	 */
	function cacheRoot($index) {
		return CkMht_cacheRoot($this->_cPtr,$index);
	}

	/**
	 * Creates an EML MIME message from the HTTP/HTTPS page or local HTML file in ARG1 and returns the complete EML text in memory. Style sheets and their referenced CSS resources are captured. Images are embedded according to EmbedImages and EmbedLocalOnly. Scripts and framed documents are removed. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Memory use: The complete MIME message is returned as a string. For large pages, GetAndSaveEML avoids keeping the final EML text solely in the return value. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. EML is an HTML-email draft: The output adds the current RFC-822 Date and X-Unsent: 1, removes scripts, frames, and HTML comments, and contains an HTML root with related resources. It does not create a plain-text alternative, does not generate Message-ID, and does not automatically make a fully addressed, ready-to-send message. Load it into Chilkat.Email or a mail client and set the required addressing and subject information before sending.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetEML
	 * @param ?string $url_or_htmlFilepath
	 * @return ?string
	 */
	function getEML($url_or_htmlFilepath) {
		return CkMht_getEML($this->_cPtr,$url_or_htmlFilepath);
	}

	/**
	 * Creates an EML MIME message from the HTTP/HTTPS page or local HTML file in ARG1 and returns the complete EML text in memory. Style sheets and their referenced CSS resources are captured. Images are embedded according to EmbedImages and EmbedLocalOnly. Scripts and framed documents are removed. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Memory use: The complete MIME message is returned as a string. For large pages, GetAndSaveEML avoids keeping the final EML text solely in the return value. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. EML is an HTML-email draft: The output adds the current RFC-822 Date and X-Unsent: 1, removes scripts, frames, and HTML comments, and contains an HTML root with related resources. It does not create a plain-text alternative, does not generate Message-ID, and does not automatically make a fully addressed, ready-to-send message. Load it into Chilkat.Email or a mail client and set the required addressing and subject information before sending.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetEML
	 * @param ?string $url_or_htmlFilepath
	 * @return ?string
	 */
	function eML($url_or_htmlFilepath) {
		return CkMht_eML($this->_cPtr,$url_or_htmlFilepath);
	}

	/**
	 * Creates an asynchronous task to call the GetEML method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetEMLAsync
	 * @param ?string $url_or_htmlFilepath
	 * @return ?CkTask
	 */
	function GetEMLAsync($url_or_htmlFilepath) {
		return chilkat::_obj(CkMht_GetEMLAsync($this->_cPtr,$url_or_htmlFilepath),'CkTask');
	}

	/**
	 * Creates an MHT web archive from the HTTP/HTTPS page or local HTML file in ARG1 and returns the complete MHT MIME text in memory. Supported statically referenced resources are captured according to the current properties. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Memory use: The complete archive is returned as a string. For large pages, GetAndSaveMHT writes the result directly to a file. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. MHT MIME structure: The result is a MIME document whose root is HTML (or XML) and whose successfully embedded resources are related parts. When resources are present, the message is normally multipart/related; when none are embedded, it can collapse to a single text/html part. MHT output omits the Date and X-Unsent headers and can preserve scripts, frames, and comments according to the script settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetMHT
	 * @param ?string $url_or_htmlFilepath
	 * @return ?string
	 */
	function getMHT($url_or_htmlFilepath) {
		return CkMht_getMHT($this->_cPtr,$url_or_htmlFilepath);
	}

	/**
	 * Creates an MHT web archive from the HTTP/HTTPS page or local HTML file in ARG1 and returns the complete MHT MIME text in memory. Supported statically referenced resources are captured according to the current properties. How ARG1 is classified: Chilkat treats ARG1 as a web URL only when it begins, case-insensitively, with http: or https:. Every other value is treated as a local file path. file:// URLs are not supported; pass a plain filesystem path instead. Relative paths are resolved against the process current working directory, and the local file must already exist and be readable. A value such as www.example.com/page.html has no recognized scheme and is therefore treated as a local path. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Redirects and capture cookies: HTTP redirects are followed for the primary page and dependent resources. After the primary request redirects, relative page references are resolved against the final landing URL. Cookies received during the capture are retained and sent on subsequent requests in that same capture, including redirect and dependent-resource requests. The redirect limit, exact followed status codes, and HTTPS-to-HTTP downgrade policy are governed by the underlying HTTP engine and are not configurable through Mht. Memory use: The complete archive is returned as a string. For large pages, GetAndSaveMHT writes the result directly to a file. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success. This resilience applies only to dependent resources; failure to fetch the primary URL or read the primary local HTML file fails the operation. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. MHT MIME structure: The result is a MIME document whose root is HTML (or XML) and whose successfully embedded resources are related parts. When resources are present, the message is normally multipart/related; when none are embedded, it can collapse to a single text/html part. MHT output omits the Date and X-Unsent headers and can preserve scripts, frames, and comments according to the script settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetMHT
	 * @param ?string $url_or_htmlFilepath
	 * @return ?string
	 */
	function mHT($url_or_htmlFilepath) {
		return CkMht_mHT($this->_cPtr,$url_or_htmlFilepath);
	}

	/**
	 * Creates an asynchronous task to call the GetMHT method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#GetMHTAsync
	 * @param ?string $url_or_htmlFilepath
	 * @return ?CkTask
	 */
	function GetMHTAsync($url_or_htmlFilepath) {
		return chilkat::_obj(CkMht_GetMHTAsync($this->_cPtr,$url_or_htmlFilepath),'CkTask');
	}

	/**
	 * Creates an EML MIME message from the in-memory HTML string in ARG1 and returns the complete EML text. Style sheets and their CSS resources are captured. Images are embedded according to EmbedImages and EmbedLocalOnly. Scripts and framed documents are removed. Relative references: Set BaseUrl when the HTML has no usable <base href> and its relative resources should resolve against a web origin. An HTML <base> element takes precedence. Without a usable base, unresolved relative resources are not captured and their references remain relative. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success, although the resulting MIME document is not fully self-contained for that resource. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Redirecting dependent resources: HTTP redirects encountered while downloading images, style sheets, imported CSS, frames, or other supported resources are followed to the final content. Cookies established during the conversion are retained for later requests in the same conversion. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. EML is an HTML-email draft: The output adds the current RFC-822 Date and X-Unsent: 1, removes scripts, frames, and HTML comments, and contains an HTML root with related resources. It does not create a plain-text alternative, does not generate Message-ID, and does not automatically make a fully addressed, ready-to-send message. Load it into Chilkat.Email or a mail client and set the required addressing and subject information before sending.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToEML
	 * @param ?string $htmlText
	 * @return ?string
	 */
	function htmlToEML($htmlText) {
		return CkMht_htmlToEML($this->_cPtr,$htmlText);
	}

	/**
	 * Creates an asynchronous task to call the HtmlToEML method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToEMLAsync
	 * @param ?string $htmlText
	 * @return ?CkTask
	 */
	function HtmlToEMLAsync($htmlText) {
		return chilkat::_obj(CkMht_HtmlToEMLAsync($this->_cPtr,$htmlText),'CkTask');
	}

	/**
	 * Creates EML output from the in-memory HTML string in ARG1 and saves it to the file path in ARG2. Style sheets and their CSS resources are captured. Images are embedded according to EmbedImages and EmbedLocalOnly. Scripts and framed documents are removed. Relative references: Set BaseUrl when the HTML has no usable <base href> and its relative resources should resolve against a web origin. An HTML <base> element takes precedence. Without a usable base, unresolved relative resources are not captured and their references remain relative. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success, although the resulting MIME document is not fully self-contained for that resource. Destination file behavior: Chilkat first generates the complete MIME output in memory and writes the destination only after generation succeeds. An existing destination file is truncated and replaced; data is never appended and no confirmation is requested. Parent directories are not created. Relative paths are resolved against the process current working directory. If generation fails, an existing destination file is left untouched. The final write is direct rather than atomic, so a write-time error such as a full disk or permission failure can leave a truncated or partially written file. A read-only or otherwise unwritable destination causes the method to return _FALSE_. Redirecting dependent resources: HTTP redirects encountered while downloading supported dependent resources are followed. Cookies established during the conversion are reused for later requests in that same conversion. EML is an HTML-email draft: The output adds the current RFC-822 Date and X-Unsent: 1, removes scripts, frames, and HTML comments, and contains an HTML root with related resources. It does not create a plain-text alternative, does not generate Message-ID, and does not automatically make a fully addressed, ready-to-send message. Load it into Chilkat.Email or a mail client and set the required addressing and subject information before sending.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToEMLFile
	 * @param ?string $html
	 * @param ?string $emlFilename
	 * @return bool
	 */
	function HtmlToEMLFile($html,$emlFilename) {
		return CkMht_HtmlToEMLFile($this->_cPtr,$html,$emlFilename);
	}

	/**
	 * Creates an asynchronous task to call the HtmlToEMLFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToEMLFileAsync
	 * @param ?string $html
	 * @param ?string $emlFilename
	 * @return ?CkTask
	 */
	function HtmlToEMLFileAsync($html,$emlFilename) {
		return chilkat::_obj(CkMht_HtmlToEMLFileAsync($this->_cPtr,$html,$emlFilename),'CkTask');
	}

	/**
	 * Creates an MHT web archive from the in-memory HTML string in ARG1 and returns the complete MHT MIME text. Supported statically referenced resources are captured according to the current properties. Relative references: Set BaseUrl when the HTML has no usable <base href> and its relative resources should resolve against a web origin. An HTML <base> element takes precedence. Without a usable base, unresolved relative resources are not captured and their references remain relative. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Scripts: Script handling is controlled by NoScripts and PreferMHTScripts. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success, although the resulting MIME document is not fully self-contained for that resource. Check LastMethodSuccess: The returned string is cleared when the call begins and may be empty or partially populated if the method fails. A nonempty string does not prove success, and an empty string should not be used as the failure test. Use LastMethodSuccess as the authoritative result; when it is _FALSE_, inspect LastErrorText. A failed call never returns the previous call's string. Redirecting dependent resources: HTTP redirects encountered while downloading images, style sheets, imported CSS, frames, or other supported resources are followed to the final content. Cookies established during the conversion are retained for later requests in the same conversion. Saving the returned string: The returned MIME document uses CRLF line endings and UTF-8 text without a byte-order mark. Preserve the text as opaque UTF-8 bytes without newline translation or reflow. Raw non-ASCII text can occur. Prefer the corresponding file-writing method when exact on-disk bytes are required. MHT MIME structure: The result is a MIME document whose root is HTML (or XML) and whose successfully embedded resources are related parts. When resources are present, the message is normally multipart/related; when none are embedded, it can collapse to a single text/html part. MHT output omits the Date and X-Unsent headers and can preserve scripts, frames, and comments according to the script settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToMHT
	 * @param ?string $htmlText
	 * @return ?string
	 */
	function htmlToMHT($htmlText) {
		return CkMht_htmlToMHT($this->_cPtr,$htmlText);
	}

	/**
	 * Creates an asynchronous task to call the HtmlToMHT method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToMHTAsync
	 * @param ?string $htmlText
	 * @return ?CkTask
	 */
	function HtmlToMHTAsync($htmlText) {
		return chilkat::_obj(CkMht_HtmlToMHTAsync($this->_cPtr,$htmlText),'CkTask');
	}

	/**
	 * Creates MHT output from the in-memory HTML string in ARG1 and saves it to the file path in ARG2. Supported statically referenced resources are captured according to the current properties. Relative references: Set BaseUrl when the HTML has no usable <base href> and its relative resources should resolve against a web origin. An HTML <base> element takes precedence. Without a usable base, unresolved relative resources are not captured and their references remain relative. Static HTML capture: Chilkat does not execute JavaScript or behave as a browser rendering engine. It processes the original HTML source and statically referenced resources. Content created only after JavaScript runs—including AJAX, fetch, XHR, lazy-loaded content, or framework-rendered DOM content—is not captured. MHT output can preserve scripts for execution when the saved archive is later opened; EML output removes scripts. Scripts: Script handling is controlled by NoScripts and PreferMHTScripts. Dependent-resource failures: If an image, style sheet, imported CSS file, frame, script, or other dependent resource cannot be downloaded or included, Chilkat omits that MIME part, creates no empty placeholder, restores the HTML reference to the resource's original absolute URL, records the problem in LastErrorText or the diagnostic log, and continues. The overall conversion still reports success, although the resulting MIME document is not fully self-contained for that resource. Destination file behavior: Chilkat first generates the complete MIME output in memory and writes the destination only after generation succeeds. An existing destination file is truncated and replaced; data is never appended and no confirmation is requested. Parent directories are not created. Relative paths are resolved against the process current working directory. If generation fails, an existing destination file is left untouched. The final write is direct rather than atomic, so a write-time error such as a full disk or permission failure can leave a truncated or partially written file. A read-only or otherwise unwritable destination causes the method to return _FALSE_. Redirecting dependent resources: HTTP redirects encountered while downloading supported dependent resources are followed. Cookies established during the conversion are reused for later requests in that same conversion. MHT MIME structure: The result is a MIME document whose root is HTML (or XML) and whose successfully embedded resources are related parts. When resources are present, the message is normally multipart/related; when none are embedded, it can collapse to a single text/html part. MHT output omits the Date and X-Unsent headers and can preserve scripts, frames, and comments according to the script settings.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToMHTFile
	 * @param ?string $html
	 * @param ?string $mhtFilename
	 * @return bool
	 */
	function HtmlToMHTFile($html,$mhtFilename) {
		return CkMht_HtmlToMHTFile($this->_cPtr,$html,$mhtFilename);
	}

	/**
	 * Creates an asynchronous task to call the HtmlToMHTFile method with the arguments provided.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#HtmlToMHTFileAsync
	 * @param ?string $html
	 * @param ?string $mhtFilename
	 * @return ?CkTask
	 */
	function HtmlToMHTFileAsync($html,$mhtFilename) {
		return chilkat::_obj(CkMht_HtmlToMHTFileAsync($this->_cPtr,$html,$mhtFilename),'CkTask');
	}

	/**
	 * Loads the calling object associated with an asynchronous Task. This method exists to support Chilkat's generated asynchronous calling infrastructure. Normally not called directly: Application code typically creates and runs tasks through the corresponding Async methods. Use this method only when a Chilkat example or support guidance specifically requires it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#LoadTaskCaller
	 * @param CkTask $task
	 * @return bool
	 */
	function LoadTaskCaller($task) {
		return CkMht_LoadTaskCaller($this->_cPtr,$task);
	}

	/**
	 * Removes custom HTTP request headers having the field name in ARG1. Supply the name without a trailing colon. Header names are matched case-insensitively.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#RemoveCustomHeader
	 * @param ?string $name
	 */
	function RemoveCustomHeader($name) {
		CkMht_RemoveCustomHeader($this->_cPtr,$name);
	}

	/**
	 * Restores a limited set of MHT/EML conversion settings to their defaults and clears two method-added collections.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#RestoreDefaults
	 */
	function RestoreDefaults() {
		CkMht_RestoreDefaults($this->_cPtr);
	}

	/**
	 * Extracts MHT data from ARG1. ARG1 may be either a local MHT file path or the literal MHT MIME text; Chilkat distinguishes the two by detecting MIME headers in the supplied value. ARG2 specifies the unpack directory, ARG3 specifies the main HTML filename, and ARG4 specifies the related-parts subdirectory used in normal unpacking. Defaults and file creation: If ARG2 is empty, the unpack directory defaults to . (the process current working directory). If ARG4 is empty, the related-parts subdirectory defaults to html_parts. Chilkat creates the parts subdirectory beneath the unpack directory when needed. The main HTML file and extracted parts overwrite existing files having the same names. An empty MHT input or an empty ARG3 HTML filename is an error and causes the method to return _FALSE_. Unpack options: Set UnpackDirect to preserve relative Content-Location paths without rewriting HTML; in that mode ARG4 is ignored. UnpackUseRelPaths controls relative versus absolute references in normal mode. Write behavior: Extraction writes directly to the target files rather than using an atomic temporary-file replacement. A write-time I/O failure can therefore leave partially written output. Untrusted archives: Extract untrusted MHT input into an isolated directory and inspect the resulting files before opening or publishing them. Required MIME structure: Normal unpacking requires a multipart/related message containing a primary HTML body. If either is missing, the method returns _FALSE_. When several HTML-like parts exist, the primary HTML part is selected and the others are treated as related resources.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackMHT
	 * @param ?string $mhtFilename
	 * @param ?string $unpackDir
	 * @param ?string $htmlFilename
	 * @param ?string $partsSubDir
	 * @return bool
	 */
	function UnpackMHT($mhtFilename,$unpackDir,$htmlFilename,$partsSubDir) {
		return CkMht_UnpackMHT($this->_cPtr,$mhtFilename,$unpackDir,$htmlFilename,$partsSubDir);
	}

	/**
	 * Extracts MHT data from ARG1. Despite the historical method name, ARG1 may be either literal MHT MIME text or a local MHT file path; Chilkat distinguishes the two by detecting MIME headers in the supplied value. ARG2 specifies the unpack directory, ARG3 specifies the main HTML filename, and ARG4 specifies the related-parts subdirectory used in normal unpacking. Defaults and file creation: If ARG2 is empty, the unpack directory defaults to . (the process current working directory). If ARG4 is empty, the related-parts subdirectory defaults to html_parts. Chilkat creates the parts subdirectory beneath the unpack directory when needed. Existing files having the same names are overwritten. An empty MHT input or an empty ARG3 HTML filename is an error and causes the method to return _FALSE_. Direct mode: When UnpackDirect is _TRUE_, ARG4 is ignored and relative Content-Location paths are used directly beneath the unpack directory. Otherwise, UnpackUseRelPaths controls relative versus absolute references in the rewritten HTML. Write behavior: Extraction writes directly to the target files rather than using an atomic temporary-file replacement. A write-time I/O failure can therefore leave partially written output. Required MIME structure: Normal unpacking requires a multipart/related message containing a primary HTML body. If either is missing, the method returns _FALSE_. When several HTML-like parts exist, the primary HTML part is selected and the others are treated as related resources.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkMhtRef.html#UnpackMHTString
	 * @param ?string $mhtString
	 * @param ?string $unpackDir
	 * @param ?string $htmlFilename
	 * @param ?string $partsSubDir
	 * @return bool
	 */
	function UnpackMHTString($mhtString,$unpackDir,$htmlFilename,$partsSubDir) {
		return CkMht_UnpackMHTString($this->_cPtr,$mhtString,$unpackDir,$htmlFilename,$partsSubDir);
	}
}
