<?php

/* CkJwt.php -- Chilkat PHP extension proxy class CkJwt.
 * Generated by ChilkatApi/GenCkPhp.cs for Chilkat v11.6.1 -- do not edit.
 * Reference documentation: https://www.chilkatsoft.com/refdoc/ */

/**
 * Create, decode, verify, and time-validate compact JSON Web Tokens.
 *
 * Chilkat.Jwt is the Chilkat class for working with compact JSON Web Tokens. It can create signed JWTs, decode JWT headers and payloads, verify signatures, validate time-based claims, generate NumericDate values, and work with HMAC shared-secret tokens, RSA public/private-key tokens, ECC public/private-key tokens, and certificate-backed signing workflows.
 *
 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html
 */
class CkJwt {
	public $_cPtr=null;
	protected $_pData=array();

	function __set($var,$value) {
		if ($var === 'thisown') return swig_chilkat_alter_newobject($this->_cPtr,$value);
		$this->_pData[$var] = $value;
	}

	function __get($var) {
		if ($var === 'thisown') return swig_chilkat_get_newobject($this->_cPtr);
		return $this->_pData[$var];
	}

	function __isset($var) {
		if ($var === 'thisown') return true;
		return array_key_exists($var, $this->_pData);
	}

	function __construct($res=null) {
		if (is_resource($res) && get_resource_type($res) === '_p_CkJwt') {
			$this->_cPtr=$res;
			return;
		}
		$this->_cPtr=new_CkJwt();
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html
	 * @return bool
	 */
	function get_Utf8() {
		return CkJwt_get_Utf8($this->_cPtr);
	}

	/**
	 * When true, strings passed to and returned from the object are UTF-8. When false (the default), they are ANSI (the locale's 8-bit character encoding).
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html
	 * @param bool $b
	 */
	function put_Utf8($b) {
		CkJwt_put_Utf8($this->_cPtr,$b);
	}

	/**
	 * Controls whether Chilkat removes insignificant JSON whitespace from the header and payload passed to CreateJwt and CreateJwtPk before Base64URL encoding and signing. The default is _TRUE_. Compaction reduces token size without changing the JSON data model. Because the exact header and payload bytes are part of the signing input, changing this property changes the encoded token and its signature even when the JSON values are equivalent. Whitespace inside JSON strings is preserved: Compaction removes formatting whitespace between JSON tokens; it does not remove spaces that are part of a quoted string value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#AutoCompact
	 * @return bool
	 */
	function get_AutoCompact() {
		return CkJwt_get_AutoCompact($this->_cPtr);
	}

	/**
	 * Controls whether Chilkat removes insignificant JSON whitespace from the header and payload passed to CreateJwt and CreateJwtPk before Base64URL encoding and signing. The default is _TRUE_. Compaction reduces token size without changing the JSON data model. Because the exact header and payload bytes are part of the signing input, changing this property changes the encoded token and its signature even when the JSON values are equivalent. Whitespace inside JSON strings is preserved: Compaction removes formatting whitespace between JSON tokens; it does not remove spaces that are part of a quoted string value.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#AutoCompact
	 * @param bool $newVal
	 */
	function put_AutoCompact($newVal) {
		CkJwt_put_AutoCompact($this->_cPtr,$newVal);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#DebugLogFilePath
	 * @param CkString $str
	 */
	function get_DebugLogFilePath($str) {
		CkJwt_get_DebugLogFilePath($this->_cPtr,$str);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#DebugLogFilePath
	 * @return ?string
	 */
	function debugLogFilePath() {
		return CkJwt_debugLogFilePath($this->_cPtr);
	}

	/**
	 * If set to a file path, this property logs the LastErrorText of each Chilkat method or property call to the specified file. This logging helps identify the context and history of Chilkat calls leading up to any crash or hang, aiding in debugging.  Enabling the VerboseLogging property provides more detailed information.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#DebugLogFilePath
	 * @param ?string $newVal
	 */
	function put_DebugLogFilePath($newVal) {
		CkJwt_put_DebugLogFilePath($this->_cPtr,$newVal);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorHtml
	 * @param CkString $str
	 */
	function get_LastErrorHtml($str) {
		CkJwt_get_LastErrorHtml($this->_cPtr,$str);
	}

	/**
	 * Provides HTML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorHtml
	 * @return ?string
	 */
	function lastErrorHtml() {
		return CkJwt_lastErrorHtml($this->_cPtr);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorText
	 * @param CkString $str
	 */
	function get_LastErrorText($str) {
		CkJwt_get_LastErrorText($this->_cPtr,$str);
	}

	/**
	 * Provides plain text information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorText
	 * @return ?string
	 */
	function lastErrorText() {
		return CkJwt_lastErrorText($this->_cPtr);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorXml
	 * @param CkString $str
	 */
	function get_LastErrorXml($str) {
		CkJwt_get_LastErrorXml($this->_cPtr,$str);
	}

	/**
	 * Provides XML-formatted information about the last called method or property. If a method call fails or behaves unexpectedly, check this property for details. Note that information is available regardless of the method call's success.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastErrorXml
	 * @return ?string
	 */
	function lastErrorXml() {
		return CkJwt_lastErrorXml($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastMethodSuccess
	 * @return bool
	 */
	function get_LastMethodSuccess() {
		return CkJwt_get_LastMethodSuccess($this->_cPtr);
	}

	/**
	 * Indicates the success or failure of the most recent method call: _TRUE_ means success, _FALSE_ means failure. This property remains unchanged by property setters or getters.  This method is present to address challenges in checking for null or Nothing returns in certain programming languages.  Note: This property does not apply to methods that return integer values or to boolean-returning methods where the boolean does not indicate success or failure.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#LastMethodSuccess
	 * @param bool $newVal
	 */
	function put_LastMethodSuccess($newVal) {
		CkJwt_put_LastMethodSuccess($this->_cPtr,$newVal);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal use. Leave empty unless instructed: Set this property only when a Chilkat example, release note, or support response documents a specific option needed for an unusual interoperability case.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#UncommonOptions
	 * @param CkString $str
	 */
	function get_UncommonOptions($str) {
		CkJwt_get_UncommonOptions($this->_cPtr,$str);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal use. Leave empty unless instructed: Set this property only when a Chilkat example, release note, or support response documents a specific option needed for an unusual interoperability case.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#UncommonOptions
	 * @return ?string
	 */
	function uncommonOptions() {
		return CkJwt_uncommonOptions($this->_cPtr);
	}

	/**
	 * Provides a catch-all string for specialized compatibility options that are not exposed as ordinary properties. The default is the empty string, which is appropriate for normal use. Leave empty unless instructed: Set this property only when a Chilkat example, release note, or support response documents a specific option needed for an unusual interoperability case.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#UncommonOptions
	 * @param ?string $newVal
	 */
	function put_UncommonOptions($newVal) {
		CkJwt_put_UncommonOptions($this->_cPtr,$newVal);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#VerboseLogging
	 * @return bool
	 */
	function get_VerboseLogging() {
		return CkJwt_get_VerboseLogging($this->_cPtr);
	}

	/**
	 * If set to _TRUE_, then the contents of LastErrorText (or LastErrorXml, or LastErrorHtml) may contain more verbose information. The default value is _FALSE_.  Verbose logging should only be used for debugging.  The potentially large quantity of logged information may adversely affect peformance.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#VerboseLogging
	 * @param bool $newVal
	 */
	function put_VerboseLogging($newVal) {
		CkJwt_put_VerboseLogging($this->_cPtr,$newVal);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#Version
	 * @param CkString $str
	 */
	function get_Version($str) {
		CkJwt_get_Version($this->_cPtr,$str);
	}

	/**
	 * Version of the component/library, such as "10.1.0"
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#Version
	 * @return ?string
	 */
	function version() {
		return CkJwt_version($this->_cPtr);
	}

	/**
	 * Creates a signed JWT using an HMAC shared secret. header may be a complete JOSE header JSON object or the shorthand HS256, HS384, or HS512. A shorthand value produces a standard header containing the corresponding alg value. payload is the JSON claims set. Chilkat signs the JSON supplied by the application; it does not automatically add claims such as iss, sub, aud, iat, nbf, or exp. password is the shared secret used by both the signer and verifier. AlgorithmOperation HS256HMAC with SHA-256. HS384HMAC with SHA-384. HS512HMAC with SHA-512. On success, the result is a compact JWS in the form base64url(header).base64url(payload).base64url(signature). The Base64URL segments do not use padding. Signed, not encrypted: Anyone who receives the token can decode its header and payload. The HMAC protects integrity and authenticity only; do not place confidential information in the claims unless the token is protected separately. Shared-secret guidance: Use a high-entropy secret appropriate for the selected HMAC algorithm. A human password is usually weaker than a randomly generated key. Use CreateJwtPk when the trust model requires an asymmetric private key.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#CreateJwt
	 * @param ?string $header
	 * @param ?string $payload
	 * @param ?string $password
	 * @return ?string
	 */
	function createJwt($header,$payload,$password) {
		return CkJwt_createJwt($this->_cPtr,$header,$payload,$password);
	}

	/**
	 * Creates a signed JWT using the private key associated with cert. The certificate must provide access to an RSA or EC private key compatible with the alg value in header. header may be a complete JOSE header JSON object or the shorthand RS256, RS384, RS512, PS256, PS384, PS512, ES256, ES384, or ES512. payload is the JSON claims set. PS256/384/512 support was added in Chilkat v10.0.0. On success, the result is a compact JWS in the form base64url(header).base64url(payload).base64url(signature). Private-key access is required: A certificate containing only the public key cannot create a signature. Signing can also fail when the private key exists but is not accessible, is nonfunctional, requires unavailable user interaction, or is incompatible with the selected algorithm. Certificate trust is a verifier decision: Creating a JWT with a certificate-backed key does not by itself establish that recipients should trust that certificate. The verifier must obtain the correct public key or certificate through a trusted mechanism and apply any required certificate validation policy.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#CreateJwtCert
	 * @param ?string $header
	 * @param ?string $payload
	 * @param CkCert $cert
	 * @return ?string
	 */
	function createJwtCert($header,$payload,$cert) {
		return CkJwt_createJwtCert($this->_cPtr,$header,$payload,$cert);
	}

	/**
	 * Creates a signed JWT using an asymmetric private key. header may be a complete JOSE header JSON object or one of the documented shorthand algorithm names. payload is the JSON claims set, and key must be a private key compatible with the selected alg. JOSE algorithmRequired key and signature scheme RS256, RS384, RS512RSA private key; RSASSA-PKCS1-v1_5 with the corresponding SHA-2 digest. PS256, PS384, PS512RSA private key; RSASSA-PSS with the corresponding SHA-2 digest and MGF1. ES256, ES384, ES512EC private key compatible with the requested ECDSA algorithm. EdDSAEd25519 private key. Use a full JOSE header containing "alg":"EdDSA". The shorthand forms documented for this method are RS256, RS384, RS512, PS256, PS384, PS512, ES256, ES384, and ES512. PS256/384/512 support was added in Chilkat v10.0.0; Ed25519 signing with EdDSA requires Chilkat v9.5.0.95 or later. On success, the result is a compact JWS in the form base64url(header).base64url(payload).base64url(signature). Key compatibility: The private-key type must agree with alg. Do not select an algorithm solely from untrusted input. Choose the algorithm as part of the application protocol and use a key intended for that algorithm. Claims remain application-defined: This method creates the cryptographic signature. The application is responsible for constructing and later validating issuer, audience, subject, lifetime, nonce, and other required claims.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#CreateJwtPk
	 * @param ?string $header
	 * @param ?string $payload
	 * @param CkPrivateKey $key
	 * @return ?string
	 */
	function createJwtPk($header,$payload,$key) {
		return CkJwt_createJwtPk($this->_cPtr,$header,$payload,$key);
	}

	/**
	 * Returns a JWT NumericDate equal to the current system time plus numSecOffset. The result is the number of whole seconds elapsed since 1970-01-01T00:00:00Z, ignoring leap seconds. A positive offset produces a future time and a negative offset produces a past time. Typical uses include generating values for iat, nbf, and exp claims. The method returns the numeric value only; it does not insert the value into a JSON payload. System-clock dependency: The result is based on the computer's current clock. Token lifetimes and validation can be incorrect when the system clock is inaccurate.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#GenNumericDate
	 * @param int $numSecOffset
	 * @return int
	 */
	function GenNumericDate($numSecOffset) {
		return CkJwt_GenNumericDate($this->_cPtr,$numSecOffset);
	}

	/**
	 * Decodes and returns the JOSE header JSON from the first segment of the compact JWT. For a token in the form header.payload.signature, this method Base64URL-decodes the header segment and interprets it as JSON text. No verification is performed: Decoding a header does not prove that the token is authentic. Until VerifyJwt or VerifyJwtPk succeeds, treat every header value—including alg, kid, jku, and x5u—as attacker-controlled input. The method fails if the token does not contain a decodable compact-JWT header.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#GetHeader
	 * @param ?string $token
	 * @return ?string
	 */
	function getHeader($token) {
		return CkJwt_getHeader($this->_cPtr,$token);
	}

	/**
	 * Decodes and returns the JOSE header JSON from the first segment of the compact JWT. For a token in the form header.payload.signature, this method Base64URL-decodes the header segment and interprets it as JSON text. No verification is performed: Decoding a header does not prove that the token is authentic. Until VerifyJwt or VerifyJwtPk succeeds, treat every header value—including alg, kid, jku, and x5u—as attacker-controlled input. The method fails if the token does not contain a decodable compact-JWT header.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#GetHeader
	 * @param ?string $token
	 * @return ?string
	 */
	function header($token) {
		return CkJwt_header($this->_cPtr,$token);
	}

	/**
	 * Decodes and returns the claims-set JSON from the second segment of the compact JWT. For a token in the form header.payload.signature, this method Base64URL-decodes the payload segment and returns the resulting JSON text. Decoding is not validation: This method does not verify the signature, token lifetime, issuer, audience, nonce, or any other claim. Use the appropriate signature-verification method first, then apply the claim checks required by the application. The method fails if the token does not contain a decodable compact-JWT payload.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#GetPayload
	 * @param ?string $token
	 * @return ?string
	 */
	function getPayload($token) {
		return CkJwt_getPayload($this->_cPtr,$token);
	}

	/**
	 * Decodes and returns the claims-set JSON from the second segment of the compact JWT. For a token in the form header.payload.signature, this method Base64URL-decodes the payload segment and returns the resulting JSON text. Decoding is not validation: This method does not verify the signature, token lifetime, issuer, audience, nonce, or any other claim. Use the appropriate signature-verification method first, then apply the claim checks required by the application. The method fails if the token does not contain a decodable compact-JWT payload.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#GetPayload
	 * @param ?string $token
	 * @return ?string
	 */
	function payload($token) {
		return CkJwt_payload($this->_cPtr,$token);
	}

	/**
	 * Checks the JWT exp (expiration time) and nbf (not before) claims against the current system time. It returns _TRUE_ when every present time constraint permits the token to be used, and _FALSE_ when the token is expired, is not yet valid, or its relevant time data cannot be processed. ClaimValidity rule expThe token is not accepted on or after the expiration time. nbfThe token is not accepted before the not-before time. leeway is a number of seconds allowed for clock skew. Keep it small; a few minutes is typical. The method checks whichever of exp and nbf are present and does not require either claim to exist. Time checking is separate from signature verification: IsTimeValid does not verify the JWT signature and does not validate iss, aud, sub, iat, jti, nonce values, or application-specific claims. A normal validation workflow verifies the signature, checks time constraints, and then validates all required claims.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#IsTimeValid
	 * @param ?string $jwt
	 * @param int $leeway
	 * @return bool
	 */
	function IsTimeValid($jwt,$leeway) {
		return CkJwt_IsTimeValid($this->_cPtr,$jwt,$leeway);
	}

	/**
	 * Verifies the signature of a compact JWT signed with the shared secret in password. This method accepts only the HMAC algorithms HS256, HS384, and HS512. It rejects none and algorithms intended for asymmetric keys. Chilkat reads the token's alg value, computes the corresponding HMAC over the encoded header and payload, and compares the result with the signature segment. The method returns _TRUE_ only when the signature matches. Signature verification only: A successful result does not validate expiration, not-before time, issuer, audience, subject, or any other claim. Call IsTimeValid and perform the application's required claim checks separately. Enforce the expected algorithm: Applications should inspect the verified header and require the specific HMAC algorithm allowed by their protocol rather than accepting any algorithm merely because this method supports it.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#VerifyJwt
	 * @param ?string $token
	 * @param ?string $password
	 * @return bool
	 */
	function VerifyJwt($token,$password) {
		return CkJwt_VerifyJwt($this->_cPtr,$token,$password);
	}

	/**
	 * Verifies the signature of a compact JWT using key. The public key must match the private key that created the signature and must be compatible with the algorithm named by the token's alg header. JOSE algorithmVerification key RS256, RS384, RS512RSA public key for RSASSA-PKCS1-v1_5. PS256, PS384, PS512RSA public key for RSASSA-PSS. ES256, ES384, ES512EC public key compatible with the ECDSA algorithm. EdDSAEd25519 public key. Ed25519 verification requires Chilkat v9.5.0.84 or later. The method returns _TRUE_ only when the signature is valid. It returns _FALSE_ for a malformed token, unsupported or incompatible algorithm/key combination, incorrect key, or invalid signature. The unsecured none algorithm is rejected. Use a trusted key-selection process: Do not automatically trust a key URL or key identifier taken from an unverified JWT header. Select the verification key from trusted configuration or a securely obtained and validated key set, and enforce the expected algorithm. Claims are not checked: Signature validity proves only that the token was signed by the holder of the corresponding private key and was not modified. Validate time, issuer, audience, nonce, and all other required claims separately.
	 * @see https://www.chilkatsoft.com/refdoc/phpCkJwtRef.html#VerifyJwtPk
	 * @param ?string $token
	 * @param CkPublicKey $key
	 * @return bool
	 */
	function VerifyJwtPk($token,$key) {
		return CkJwt_VerifyJwtPk($this->_cPtr,$token,$key);
	}
}
